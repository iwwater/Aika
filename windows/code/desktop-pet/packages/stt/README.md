# Aika STT package

This optional package owns speech input. It exposes a credential-backed cloud batch adapter and a no-key local streaming adapter under the public `stt.transcribe` capability. It does not load TTS, playback, camera, or model weights automatically; local model paths are supplied by the host configuration.
