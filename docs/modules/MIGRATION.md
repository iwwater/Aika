# 旧阶段到模块的映射

2026-09-10 起，模块 SPEC 与 TESTING 取代旧阶段串行派发和全仓小阶段门禁。旧文档归档在 `docs/archive/stages/`，AC 和历史证据保留，不重新编造通过状态。

| 旧阶段 | 新归属 |
| --- | --- |
| S1 LLM 生命周期/取消/保存 | LLM-02 的既有基线；已批准的 S1 不重开 |
| S1 ASR 时间/排序/回合 | STT-01/02；真实输入归 STT-03 |
| S1 队列停止/交付状态 | TTS-01/02；真实播放归 TTS-03 |
| S1 真实开口打断、回声、端到端首音频 | INT-02，仍 DEFERRED |
| S2 Soul/Mode/Schema | LLM-01；选择与设置 UI 归 FE-02 |
| S3 Context/Runtime | LLM-02；UI Hook 桥接归 FE-01；Remote/生产入口兼容归 INT-01 |
| S4 Memory / S5 Agent / S6 RAG | LLM-03 / LLM-04 / LLM-05 |
| S7 环境感知 | 后续独立能力；当前不派发 |
| S8 Live2D | 前端未来表现阶段；不纳入 FE-01–03 |

## 历史证据复用

S1 提交 `bc9f5bb` 已完成当前 LLM 范围并推送；前端全量 287 测试、后续定向 19 测试、Rust 5 测试及打包是不同批次证据，不能相加成新的全量结果。新模块以各自新 AC 查证覆盖，没有改动的旧行为可引用旧证据。

旧 S2 已派发，现改为 LLM-01 范围；执行者保留已完成代码，对应 UI 工作如已有修改单独列 FE 待验收，不撤销用户工作，也不把它混进 LLM 模块完成声明。
