# RT-01 · v0.5 契约与单 Runtime 宿主边界

状态：READY（仅授权本地实现与自动验证；真实服务/人工 AC 未执行）。
需求来源：[v0.5 与增量需求](../../PRD_V0.5.md)；执行规则：[安全执行计划](../../GOAL_EXECUTION_PLAN.md)。

## 前置与范围

- 前置：CORE-01～09 自动审查；INT-01 可自动部分。
- 修改范围：现有 domain、runtime contracts、组合根；不搬目录。源码根为 aika-crossplatform；优先复用现有端口，不为文档目录迁移源码。
- 非目标：本份范围外功能、发布、公网部署、真实账号操作。前置自动契约通过可开发；需要真实安全属性的集成不可由 fake 放行。

## 行为与接口

分别定义 identity/conversation/thread/runtimeTurn/agentSession/deviceSession；可信 SourceEnvelope 独立于旧 text/voice/proactive。记录协议版本、消息幂等键、宿主epoch、能力矩阵。桌面关闭 WebView 不意味着 TS Runtime 常驻；MVP 要求宿主存活，离线明确不可用，不另开影子 Runtime。

所有新增公共字段需在 CONTRACTS 登记版本、兼容 adapter 与消费者；持久化使用临时数据库验证升级与失败回退，不触碰用户库。

## 验收条件

| AC | 要求 |
| --- | --- |
| RT-01-A | 旧桌面提交/取消/存储契约无回归 |
| RT-01-B | 外部入口只能调用同一 Runtime facade；UI/ACP 不直接访问互相状态 |
| RT-01-C | 旧消息来源标 unknown，不伪造账户归属；新字段显式兼容映射 |
| RT-01-D | 关闭/重启宿主可观测 offline/recovering，不宣称云端接管 |

## 验证与交付

先读当前生产实现与既有测试，列出定向命令；测试必须走本模块生产实现，fake 只替代外部依赖。记录命令、退出码、逐 AC 证据、影响消费者、未执行的真实/人工项。逻辑测试不证明 UI 可用、真机或真实服务通过。
报告路径：../reports/RT-01_ACCEPTANCE.md。仅所有可自动 AC 通过才能写 AUTO_PASS / 待人工验收；FAIL 不可改为 NOT RUN 来推进依赖。完整验收维持待人工；不自动提交或推送。


## 全文审阅：最小身份信封

定义SourceEnvelope{version:1,principalId,accountRef,conversationId,threadId?,origin,messageId,receivedAt,trust}；origin可desktop/mobile/telegram/feishu/qq/environment/agent/unknown，trust只能由认证端口生成。RT-01仅冻结类型与桌面兼容适配，RT-02负责真实会话隔离。不得增加必填外部字段使旧submit调用失效；新默认本地scope只映射本地主体，不把unknown外部输入当本地。

现有Runtime单活动轮、storage全局listMessages/latestSummary，RT-01不声称已经多会话安全。内核唯一与多会话状态是两回事；实际隔离由RT-02前置，所有外部入口在它通过前关闭。宿主在线状态用可注入心跳/租约建模，真实关窗回归列INT-01。
