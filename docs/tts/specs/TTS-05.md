# TTS-05 · 真实云 TTS 试听

状态：条件执行；真实服务/设备/预算待具备，当前 NOT RUN。
需求来源：[v0.5 与增量需求](../../PRD_V0.5.md)；执行规则：[安全执行计划](../../GOAL_EXECUTION_PLAN.md)。

## 前置与范围

- 前置：TTS-04；已批准服务、预算与设备。
- 修改范围：验收脚本与 reports，原则上无产品改动。源码根为 aika-crossplatform；优先复用现有端口，不为文档目录迁移源码。
- 非目标：本份范围外功能、发布、公网部署、真实账号操作。前置自动契约通过可开发；需要真实安全属性的集成不可由 fake 放行。

## 行为与接口

固定中文/日文/英文各三段，记录文本长度、服务模型、首音频延迟、全程时长、失败/停止、可得账单；播放指标标测点；听感人工记录。

所有新增公共字段需在 CONTRACTS 登记版本、兼容 adapter 与消费者；持久化使用临时数据库验证升级与失败回退，不触碰用户库。

## 验收条件

| AC | 要求 |
| --- | --- |
| TTS-05-A | 实际服务响应与设备播放分别留证，不以 HttpFetch fake 代替 |
| TTS-05-B | 取消后无继续播放；服务失败提示可见 |
| TTS-05-C | 音质/发音人工结论与计费实测缺失分别 NOT RUN |
| TTS-05-D | 缺预算/设备为 BLOCKED，仅用户明确后置才 DEFERRED |

## 验证与交付

先读当前生产实现与既有测试，列出定向命令；测试必须走本模块生产实现，fake 只替代外部依赖。记录命令、退出码、逐 AC 证据、影响消费者、未执行的真实/人工项。逻辑测试不证明 UI 可用、真机或真实服务通过。
报告路径：../reports/TTS-05_ACCEPTANCE.md。本份真实验收未执行时只能写准备完成/真实验收待执行，不可因自动AC集合为空而AUTO_PASS；FAIL 不可改为 NOT RUN 来推进依赖。完整验收维持待人工；不自动提交或推送。


## 全文审阅：分离证据与通过条件

本份是实际服务/播放验收，不存在仅凭脚本可用就整份AUTO_PASS的捷径。9段试听不代替TTS-03的15句和10次停止P95门槛。开始前冻结文本/后端/预算上限、首网络响应与首可闻声音的测点；音质/发音必须留逐样本人工判断，计费不可得写NOT RUN。终态可以“准备完成/真实验收待执行”，不能空集合自动AC全过而冒充试听通过。
