# N085-06 · 质量评估、迁移恢复与版本收口

状态：`NOT_STARTED`。前置：[N085-01～05](../SPEC.md#2-顺序依赖和需求映射) 的自动证据；真实正式桌面链可运行。对应 N085-R07、85-F，并复核 85-A～E。本步不重新设计各模块。

## 回放与计量

使用 N085-00 冻结的来源、问题和负例及预登记目标，保留每条样本的预期域/来源、实际候选、实际检索段、入选原因和最终回复判定。分别统计候选精确率/召回率、错误自动激活、错误归因、检索命中/漏召回、预算、延迟、模型调用数与 Token；按配对、原作/用户域和时间问题拆分，不只报总体均值。硬安全不变量有一项失败即整版 FAIL；质量目标未达则修对应 SPEC，不事后删样本或降低阈值。

真实模型回放走正式 `BackendSession → DialoguePipeline` 和活动包/正式 Context，不由脚本手工拼 Prompt。至少覆盖“用户目标/最近经历”“角色原作事实/剧情范围”“用户纠正后再问”“遗忘后再问”四类对话；记录实际模型/来源、次数、成本和脱敏结果。fixture 证明规则，真实调用证明接线与可用性，两种证据分列。没有可用真实 Provider 时记 `BLOCKED`，不标整版自动完成。

## 兼容与组合验证

1. 用脱敏旧库副本验证升级、重启、恢复、回退和再次升级；候选、supersedes、disputed、来源撤销与无正文 tombstone 不丢失。迁移失败保留旧数据和可恢复说明，不打开用户真实数据库作测试。
2. 验证基础文字、+连续性、+0.8 实际已交付事件的代表组合；缺包和未授权 Observation 零长期写入。若 0.8 感知尚未交付，只验证明确关闭及拒绝 fixture，并将真实 Observation 联调列为未完成能力，不造成功证据。
3. 复核原有 0.7/0.79 直接消费者、Character Pack 激活/回退、History/Memory 生命周期、Context 总预算、管理鉴权与页面深链。只扩展发现真实影响的回归范围。
4. 汇总 85-A～F、每步报告、实施提交、测试命令/退出码、模型/人工证据和开放问题；给出 `IMPLEMENTED/WIRED/EXPOSED` 矩阵。版本人工体验留待用户集中确认，不代签通过。

## 交付条件

- N085-00 预登记质量目标达到，所有硬安全门槛为零违例；正式消息的候选、更新、检索、纠正/遗忘在生产链闭环，真实模型与数据迁移/恢复有证据。
- 每个已启用来源的授权、保存、撤销和 UI 状态均与真实运行一致；未交付的 0.8 来源明确关闭且不算已验证。0.9 的语音/表现范围不成为本版质量证明。
- 自动门槛全部通过时状态可为 `PENDING_USER_ACCEPTANCE`；用户确认实际体验后才记整版验收完成。报告位置：`../reports/N085-06.md`。
