# N07-00 执行报告 · 实际接口基线与文字闭环探针

日期：2026-09-22；状态：**AUTO_PASS（部分验收）**。

## 已完成

- 核对 0.61 实际 `KnowledgeLibraryStore`、`SqliteLifecycleMemoryPort`、`DialogueContext` 与 0.65 `FlowRuntime`、`ProviderTransport` 的入口和缺口。
- 编写 N07-00～N07-05 执行 SPEC；没有把 Character Pack、双时间线、撤销或管理 UI 误写成已交付。
- `npm run check`：退出码 0。
- `npm run build`：退出码 0。
- 相关文字/知识库/Provider 回归：27/27 PASS。
- 受控真实探针：[N07-00-real.md](N07-00-real.md)：使用配置中的 flash 模型，单次运行 2 次调用，完成“短资料 → 结构化草稿 → 证据回源校验 → 带角色上下文的文字回复”。

## 真实探针边界

这次只使用仓库内自写的短虚构资料，没有联网抓取或用户私人文本。探针复用了现有 `ProviderTransport` 的 HTTPS、SSE、授权和模型配置，但尚未接入生产 Character Pack store；所以它证明的是 Provider 和文字协议可用，不证明 0.7 的持久化、激活/回退、双时间线、User Soul、纠正/遗忘或 Context 预算已经完成。

探针第一次试运行遇到模型输出截断，第二次遇到模型返回未登记的 evidence ID；两次均被本地校验拒绝，随后收紧输出约束后通过。这两次失败说明证据校验门槛有效，不能把模型自报的来源当作事实。整个过程总调用数为 4，未超过当前 5 RPM 限制。

## 后续入口

下一步从 [N07-01](../specs/N07-01.md) 开始，实现来源快照、稳定 block locator 和不可变 Character Pack 草稿存储；完成后再将真实探针迁移为生产 `CharacterDistiller` adapter。

