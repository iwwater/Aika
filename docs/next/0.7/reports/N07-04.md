# N07-04 执行报告 · User Soul、User Wiki、关系覆盖与撤销

日期：2026-09-22；状态：**AUTO_PASS（受控自动闭环；0.65 人工验收继续暂缓）**。

## 交付

- 在现有 companion SQLite 中新增 `continuity_pair_state`、`continuity_facts`、`continuity_tombstones` 和 `continuity_operations`。
- User Soul、User Wiki、Relationship 三层使用 `userId + characterId + characterInstanceId` 隔离；Character Soul 仍只读来自不可变 Character Pack。
- 支持 candidate→active、状态有效期、纠正 supersedes、forget tombstone、operation 幂等和来源撤销过滤。
- 助手自述不能直接建立用户事实；推断必须携带来源；后台 derived lease 以 revision/epoch 防止陈旧写回。
- Companion Timeline 事件可关联来源事实；关联事实被遗忘后，事件不会重新进入 Continuity Context。

## 自动验收

- `npm run check`：退出码 0。
- `npm run test:next07`：50/50 PASS。
- 覆盖配对隔离、助手自述拦截、无证据推断拒绝、纠正审计、状态变化、tombstone、来源撤销和 stale lease。

## 限制

本步没有实现全自动长期沉淀策略，也没有把模型推断默认提升为稳定 Soul；这些行为仍须通过后续产品策略和人工验收确认。0.65 的人工矩阵继续暂缓。
