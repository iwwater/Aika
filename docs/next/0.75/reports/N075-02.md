# N075-02 实施记录：控制台外壳与启动流程（初稿）

状态：**PARTIAL（外壳视觉初稿已获用户确认，登记顶栏切侧边栏 TODO；真实桌宠与控制台独立运行偏差在 RP75-01/05 修复）**。更新日期：2026-09-23。

本文件按 [SPEC 第 4 节](../SPEC.md) 的登记模板填写。

## 1. 界面登记

| 字段 | 内容 |
| --- | --- |
| 界面 ID | N075-02-SHELL |
| 界面名称 | 控制台外壳（框架、全局导航、启动与鉴权、运行总览） |
| 所属步骤 | N075-02（依赖 N075-01） |
| 涉及文件 | `windows/code/desktop-pet/management/ui/` 下 `app.mjs`、`style.css`、`icons.mjs`、`modern-overview.mjs`、`routes.mjs`、`server.ts` |
| 可复用基础设施 | `routes.mjs`（15 个 page 目录、legacy section 桥接、token 抹除、配对作用域解析） |
| 旧入口 → 新入口 | 顶部 6 大主模块导航；二级联动子标签；右键快捷直达链接；页面内整合现代化运行总览 |

## 2. 本轮询问与用户回答

- 询问（2026-09-22）：这次准备修改【N075-02 控制台外壳与全局导航】，希望改成什么样子？
- 用户回答并提供 4 张生成参考图：
  - 图 1：运行 / 总览（系统正常 Hero 卡片、4 项指标胶囊、最近活动与任务、快捷操作、顶部 6 模块导航）
  - 图 2：Trace / 调用链追踪（筛选栏、统计指标、列表 + 详情抽屉）
  - 图 3：扩展 / 知识库列表（多级标签、卡片网格与侧边栏）
  - 图 4：角色 / 记忆 Records（多维筛选、三列布局、标签与元数据）
- **用户本轮明确授权**：“先按照这个风格来，这是我生成的图片，所以说里面可能会有一些多余的元素，你先跑一版初稿出来，允许你一定程度自由发挥”。

## 3. 用户确认的复现范围

依据用户提供的 4 张参考图体系，确立并复现了以下全套外壳与总览要素：
1. **顶部导航栏 (`.app-navbar`)**：
   - 品牌区：Aika-Next Logo + 标题 + 副标题“AI 工作伙伴”。
   - 6 大主模块导航按钮：`运行`、`角色`、`配置`、`连接`、`扩展`、`工作`（带专属图标与高亮状态）。
   - 右侧实用控件区：作用域下拉胶囊（`作用域: 本机 ▼`）、`Q Ctrl + K` 快捷搜索条、主题切换（Sun 图标）、通知铃铛（带红点）、用户头像（带在线状态绿点）。
2. **二级子导航栏 (`.sub-header-bar`)**：
   - 根据选中的一级模块动态展示专属子页面列表，激活项带有 Royal Blue 下划线指示。
   - 右侧展示“最后更新时间”与“刷新”按钮。
3. **运行 / 总览页面 (`.main-canvas`)**：
   - 页面标题与说明（`运行 / 总览`）。
   - **Hero 运行状态卡片**：绿色大圆 checkmark 图标、`系统运行正常`、Aika 头像与名言引用（`“让每一次对话，都更接近你。” —— Aika`）。
   - **4 项指标胶囊**：运行时长（时钟图标）、CPU 使用率（芯片图标 + SVG 折线图）、内存占用（数据库图标 + 进度条）、今日消息数（气泡图标 + 迷你柱状图）。
   - **内容双列网格**：
     - 左列：最近活动列表（带绿/黄/蓝/紫状态圆点）、最近任务网格（3 个卡片，带“已完成”徽标）。
     - 右列：快捷操作卡片（蓝色闪电图标，5 个常用直达操作项带右箭头）。
4. **全局微型 Footer (`.app-footer`)**：
   - 版本号与 Slogan 信息。

## 4. 自动验证与联调证据

- 静态检查：`npm run check` 退出码 `0`。
- 构建状态：`npm run build` 退出码 `0`。
- 路由与静态资源测试：`node --test tests/management/routes.test.mjs`（4/4 PASS）。
- 连续性与后端集成测试：`npm run test:next07`（50/50 PASS）。
- 契约回归测试：`npm run test:next`（80/80 PASS）。

## 5. 本地运行与体验方式

可在项目目录下运行以下命令启动服务并在浏览器中查看初稿效果：
```powershell
cd F:/AIVoice/Aika-Next/windows/code/desktop-pet
node tools/serve-management.mjs
```
访问控制台输出的本地 URL 即可直观查看完整的顶部导航、二级联动栏与运行总览页面。

## 6. 用户审核、反馈与真实桌宠接线

- 用户审核反馈（2026-09-22）：“没啥问题，TODO里面加一个可以将顶栏放在左侧的功能（现在不实现）然后你帮我把接线做了，也就是接入真实桌宠”。
- **TODO 登记**：已在 SPEC 与本报告正式记录：`TODO: 增加控制台布局切换能力——支持将顶栏导航切换为左侧侧边栏布局（当前暂不实现，留待后续外观/布局定制项）`。
- **真实桌宠接线完成**：
  - 更新 `desktop/pointer-router.ts` 中 `PANEL_ENTRIES` 的所有深链落点，修复历史 FE75-01 / FE75-02 缺陷：
    - `knowledge` 修正为 `/#page=knowledge`
    - `settings` 修正为 `/#page=overview`
    - `memory` 修正为 `/#page=memory&section=records`
    - `timeline` 修正为 `/#page=timeline`
    - `diagnostics` 修正为 `/#page=events`
    - `status` 修正为 `/#page=health`
    - `skin` 修正为 `/#page=skins`
  - 更新 `desktop/main.mjs` 中顶部“控制台”点击与右键点击直达 `/#page=overview`。
  - 重新编译桌宠前端产物 `npm run build:desktop`，验证 11 项指针路由与桌宠交互测试全部通过。

## 7. 2026-09-23 现场复核与整改分工

依据 2026-09-23 代码 review（`investigations/20260923-review/README.md`），本报告确认：
1. **保留视觉初稿成果**：用户对顶部分组导航、二级联动栏及总览卡片风格的初稿认可真实有效，不整版推倒重写；登记的“顶栏切左侧侧边栏”TODO 保持有效。
2. **整改真实运行与数据偏差**：
   - **RV75-01**：现场启动桌宠使用了实验脚本并传 `--preview`，控制台使用独立脚本，两者未接入同一 `trial-backend` 生产实例，导致右键提示离线预览。此接入问题归入 **RP75-01** 统一解决。
   - **RV75-06 / RV75-07**：总览数据硬编码“系统运行正常”与“已加密同步”，并把当前分页拉取的 6/10 条记录误作真实总量，且错误捕获 epoch 判断写反。此问题归入 **RP75-05**，实施前将向用户确认异常与实际指标的呈现要求并重写数据层。
