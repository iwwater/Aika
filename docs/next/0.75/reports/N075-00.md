# N075-00 验收报告 · 全量入口与功能基线盘点

日期：2026-09-22；状态：**AUTO_PASS（基线盘点完成；0.65 人工验收继续暂缓）**。

## 1. 目标与执行边界

作为 0.75（全前端逐界面重写）的第 0 步，本步骤纯为技术基线与功能全量盘点：
- **无可见 UI 修改**：不新增、不自行设计任何可见组件。
- **无旧代码删除**：不删除任何旧页面、脚本或接口，保留所有既有未提交改动。
- **建立权威映射**：枚举所有旧入口，与目标重写 SPEC、API/Bridge 及验收编号建立严密映射（见 [ENTRY_CATALOG.md](../ENTRY_CATALOG.md)）。

## 2. 工作树基线与运行环境

- **工作树基线提交**：`5b2e034` (`docs(0.65): K65-01 报告 §13 补记`)。
- **构建与测试状态**：
  - `npm run check`：退出码 `0`。
  - `npm run build`：退出码 `0`。
  - `npm run test:next`：**80 / 80 PASS**（退出码 `0`）。
  - `npm run test:next07`：**39 / 39 PASS**（退出码 `0`）。
- **运行命令与端口**：
  - 控制台服务：`node dist/tools/serve-management.js`（本地 HTTP 端口随机分配，Bearer Token 保护）。
  - 桌面应用启动：`npm run dev` 或 Electron 主进程通过 `desktop/electron/main.mjs` 加载。

## 3. 全量入口盘点成果

系统当前共梳理出 **41 个功能入口与呈现点**，已完整收录于 [全量前端功能与入口映射总表 (ENTRY_CATALOG.md)](../ENTRY_CATALOG.md)，分布如下：

1. **主控制台一级页面（10 个）**：
   - `overview` (运行总览) → 归属 **N075-12**
   - `memory` (记忆与对话) → 归属 **N075-08**
   - `projects` (项目索引) → 归属 **N075-13**
   - `tasks` (任务转发) → 归属 **N075-13**
   - `wechat` (微信连接) → 归属 **N075-14**
   - `models` (模型与声音) → 归属 **N075-04**
   - `presentation` (表情与动作) → 归属 **N075-14**
   - `skins` (外观与换肤) → 归属 **N075-06**
   - `health` (模块状态) → 归属 **N075-12**
   - `events` (运行记录) → 归属 **N075-12**
2. **记忆系统子标签页（10 个）**：
   - `dynamics`, `emotion`, `import`, `fragments`, `traces`, `policy`, `maintenance`, `records`, `prompt`, `context` → 分别归属 **N075-07 / N075-08 / N075-09 / N075-14**
3. **桌宠右键功能菜单（10 个）**：
   - `chat`, `skin`, `knowledge`, `settings`, `memory`, `timeline`, `diagnostics`, `microphone`, `status`, `click_through` → 分别归属 **N075-03 / N075-05 / N075-06 / N075-08 / N075-09 / N075-10 / N075-12 / N075-15**
4. **独立单页与模态视图（5 个）**：
   - `aika.html` / `aika-view.mjs` (独立配置单页) → 归属 **N075-04 / N075-07 / N075-09**
   - `self-setup-view.mjs` (首次配置向导) → 归属 **N075-02**
   - `balances-view.mjs` (余额凭据弹窗) → 归属 **N075-12**
   - `next65-view.mjs` (插件包与 Flow 视图) → 归属 **N075-11**
   - `knowledge-view.mjs` (知识库视图) → 归属 **N075-10**
5. **桌面浮层与控件（6 个）**：
   - `drawer` (聊天抽屉)、`toast` (通知)、`work-card` (工作卡片)、`work-records` (工作记录)、`mic-test-panel` (试麦面板)、`display-controls` (模型尺寸缩放) → 分别归属 **N075-05 / N075-06 / N075-13 / N075-15**

## 4. 历史架构与路由缺陷（FE75-01～FE75-06）整改责任分配

- **FE75-01**（右键 `knowledge` 直链 JS 文件）：由 **N075-03 / N075-10** 解决，统一规范为控制台路由 `/#page=knowledge`。
- **FE75-02**（右键 `timeline/diagnostics/runtime` 落空）：由 **N075-02 / N075-03** 解决，补全对应一级路由跳转。
- **FE75-03**（前端硬编码单角色 `companion`）：由 **N075-07 / N075-08** 解决，全面支持 0.7 的角色与多实例作用域（`userId` + `characterId` + `characterInstanceId`）。
- **FE75-04**（`next65-view` 与宿主管理未接通）：由 **N075-11** 解决，接入生产宿主真实管理接口。
- **FE75-05**（`app.mjs` 与 `aika-view.mjs` 存在两套重复表单）：由 **N075-04** 解决，合并为统一的多源管理应用服务。
- **FE75-06**（缺少 0.7 连续性双时间线与草稿展示）：由 **N075-07 / N075-09** 解决，对接 0.7 已落库的 `CharacterPackStore` 与 `ContinuityReadPort`。

## 5. 0.65 人工验收口径

- 0.65 的 32 项人工验收继续按约定暂缓，不代签通过。
- 在后续 0.75 逐界面改造中，对应的界面操作（如麦克风试音、换肤渲染、窗口拖拽与穿透）将逐项记录真实的桌面交互证据。

## 6. 结论

N075-00 基线与全量功能清单已全部冻结，状态记为 **AUTO_PASS**。
下一步正式进入 **N075-02（控制台外壳与启动流程）**，严格遵守执行纪律：先征询用户对于外壳的设计与功能要求，并索取第一张参考图。
