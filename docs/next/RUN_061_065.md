# 0.61 → 0.65 续跑记录（RUN_061_065）

本文件是 [EXECUTION_061_065.md](EXECUTION_061_065.md) §3 要求的续跑账本。**上下文压缩或新 worker 接手后，先读本文件，再核对实际代码与 `git status`，然后继续。** 只保留当前状态，不堆历史。

- 更新：2026-09-22（**K65-01 完成 AUTO_PASS，下一步 K65-02**）；2026-09-21（FIX61-11 token 修复与 0.61 基线冻结轮；同日安静树复测见 §3a，integration 计数更正见 §3a/§4）
- 工作树：`F:/AIVoice/Aika-Next`（分支 `aika-next`）；构建单元 `windows/code/desktop-pet/`（npm，node v24.9.0）
- Git：**未提交、未推送、未 stash、未回滚**。HEAD = `45ee904`（K65-01 门槛所测修订 = 此 HEAD + 未提交 K65-01 产物）

## 1. 当前步骤

**0.65 K65-01 完成：AUTO_PASS（01-A/B/C/D/E 全部通过）。** 交付：四个契约模块 `contracts/{plugin,capability,provider-source,flow-profile}.ts`、五个 `plugins/*.ts`（`paths`/`manifest`/`package-build`/`boundary`/`sdk-emit`）+ `esm-graph.{mjs,d.mts}` + `secret-store.ts`、SDK 入口 `tools/emit-plugin-sdk.mjs`、8 个测试文件（`tests/next65/`）与 `fixtures/plugin-sdk/`；冻结 API v1 面 31 项（`sdk:next65` 实测 `31/31 declared`）。验收报告 `docs/next/0.65/reports/K65-01.md`（含 TEST_PLAN §2/§5 映射表）。**本轮门槛实测**（cwd `windows/code/desktop-pet/`，Node v24.9.0，HEAD `45ee904` + 未提交工作树）：`npm run check` 0；`npm run build` 0；`npm run sdk:next65` 0（14 文件）；`npm run test:next65` 0（**89/89**，duration 7151.9 ms）；`npm run test:next61` 0（**148/148**，duration 42454.9 ms）。01 的新架构测试**无旧用例前身**（0.61 无包/插件/SDK 概念），属真正新增覆盖；旧业务行为回归由 K65-00 三个基线测试（21 例）承载，断言未改。**本步记录了两个生产缺陷发现**：(1) `plugins/esm-graph.mjs` 的 `extractSpecifiers` 对 `import … from` 恒返回 `[]`，致 01-C 空洞通过（fixture 修复前 `ok:true/0 issues`、修复后 `ok:false/8 boundary_violation`），由新增 `readImportSpecifier()` 修复；(2) 全部已提交 fixture 声明了冻结词表外参数却通过（`presentation.render:['payload']` 等四项），`unsupported_parameter` 从未落到任何东西上，由把 `CAPABILITY_PARAMETER_TYPES` 接入 `validateCapabilityDeclaration` 并改正 fixture 值修复。

**下一步动作：K65-02（包注册与惰性加载宿主）。** 启动前先读 K65-01 报告 §6 的决策记录：SDK 模块范围（D1，不缩减 31 项 `PLUGIN_SDK_SURFACE`）、`contracts/index.ts` **不再导出**四个插件模块（D2，插件面与宿主 runtime 面分离，插件作者权威入口为 `dist/next65-sdk/index.ts`）、Flow Profile 只冻结 schema v1 + 结构化校验而执行/诊断/Context 组合归 07（D3）、`knownCapabilityIds` 供给则强制不供给则开放（D4）、D3 `SecretStore` 建于既有 `credentialRegistry()` 且故意不入 `SDK_MODULES`（D5）。另须注意 K65-00 §7 的 02/02A 前置：`ManagedLocalHostId` 空字符串（BASELINE H-14 起）、`transport.ts:56` loopback 放行与血缘约束、保留 `ManagementSettingsStore` 的 `historical=true` legacy catalog 路径、不得 bump `CONTRACT_VERSION`。

**FROZEN re-pin 已执行（K65-01 独立审计轮，2026-09-22）**：K65-01 修改了两个已冻结文件——`tsconfig.json`（include 增 `plugins/**/*.ts`、exclude 增 `tests/next65/fixtures/**`）与 `package.json`（增 `"sdk:next65": "npm run build && node tools/emit-plugin-sdk.mjs"`）。按 §6 约定，审计轮在**当前修订 HEAD `45ee904` + 未提交 K65-01 产物**上重跑 0.61 收口门槛全集并复算 hash，**re-pin 完成，原"re-pin 推迟"条目作废**（见下 §7 re-pin 记录）。**Git：K65-01 由审计员提交并推送（见 §7）。**

**K65-00 提交隔离核对补遗（2026-09-22，报告 §9 / BASELINE §8）**：上述 K65-00 交付已在 FIX61-10 提交 `02a6870` 中入库（8 文件 718 插入：SPEC/TESTING/TEST_PLAN/BASELINE/K65-00 报告/evidence/specs 状态行/package.json 脚本），00-A…00-E 逐项复核维持 PASS。工作树未提交项全部属 K65-01 在途产物（contracts 四文件、plugins/**、emit-plugin-sdk.mjs、tests/next65 四个新测试 + fixtures、package.json `sdk:next65` 行、tsconfig include/exclude 增量），本步未提交未改动；K65-00 范围无未提交残留。双树裁决：`windows/code/desktop-pet` 为唯一权威树，仓库根平级目录 `code/` 只读不碰（`git status --porcelain code` 零输出佐证）。本轮门槛实测：`npm run check` 0；`npm run test:next65` 0（58 用例 = 基线 21 + K65-01 在途 37，后者仅验证共存不构成 00 交付证据）。

**0.61 侧：token 缺陷已修复，基线已重跑门槛并冻结（FROZEN，安静树复测见 §3a）**，见 §3/§3a/§3b。K65-00 记录为"未验证"的重冻结流程已由本轮完成。**§3a 复测未发现任何门槛回归；integration 失败计数由并发期误读的 6 更正为安静树实测 21（构成见 §3a/§4），FROZEN 标签维持，以 §3a 所测修订为准。**

~~下一步动作：K65-01（公共插件契约与独立包格式）。~~ **已于 2026-09-22 完成并 AUTO_PASS，见上文；当前下一步为 K65-02。** K65-00 报告 §4 的 D1～D7 七项归属决策（`CONTRACTS.md` L3 与 K65-01 L11/L19 委派给 00 的 SDK 路径、五类型 schema/type 归属、`SecretStore`、`capabilityId` 词表、超时默认值）已由 K65-01 全部落实，逐项裁决见本报告 `docs/next/0.65/reports/K65-01.md` §6。

**K65-01 收编补遗（2026-09-22 第二工位）**：并行会话产物并入本棒收口。补齐三件缺口——(1) **01-F 导入原子登记**（spec §1 交付项，此前未实现）：新增 `plugins/package-import.ts`（校验→staging 复制→registry 原子 rename，拒绝零写入，不实现 loader）+ `packageImport.test.ts` 7 例；(2) **SlotBinding 冲突裁决**（BASELINE §8 登记议题）：schema 层放行 `auth:'none'`+费用可选/`unknown`、0.61 协议断言不动、legacy-slot.ts 单向桥，以 `legacySlot.test.ts` 7 例冻结（报告 §10）；(3) **冻结清单/兼容规则/错误类别 16 项**成文（报告 §11）+ probe-extract.mjs 删除收编为 `importScanner.test.mjs` 的处置记录 + 继承 NOTES 引用（报告 §9）。收编后门槛独立复跑（cwd `windows/code/desktop-pet/`）：`check` 0、`build` 0、`sdk:next65` 0（31/31）、`test:next65` **103/103**（89+7+7）、`test:next61` **148/148**、定向 `slot-registry` 9/9、`fix61-01.openConfig` 8/8，全部退出码 0；FROZEN re-pin 复核并入其中（报告 §12.4）。

## 2. 对应报告

| 步骤 | 报告 | 状态 |
| --- | --- | --- |
| FIX61-01～09 | `docs/next/0.61/reports/FIX61-01.md` … `FIX61-09.md` | AUTO_PASS（05/09 记 PASS，语义等同） |
| FIX61-10 | `docs/next/0.61/reports/FIX61-10.md` | 10-A/B/C AUTO_PASS（真实交付，已由 K65-00 逐条核实），10-D DEFERRED_TO_K65_11 |
| **FIX61-11 token 修复** | `docs/next/0.61/reports/FIX61-10.md` §7（更新）与 §3b（本文件） | **已修复并冻结**（见 §3b） |
| **K65-00** | `docs/next/0.65/reports/K65-00.md` + `BASELINE.md` | **AUTO_PASS**（02a6870 入库；2026-09-22 提交隔离核对补遗：报告 §9 / BASELINE §8，逐 AC 维持 PASS） |
| **K65-01** | `docs/next/0.65/reports/K65-01.md` | **AUTO_PASS**（01-A 14 / 01-B 6 / 01-C 16 / 01-D 10 / 01-E 19 + D3 3 例 + 收编补遗 01-F 7 / 裁决 7；`check` `build` `sdk:next65` 0、`test:next65` 103/103、`test:next61` 148/148、定向 slot-registry 9/9 + fix61-01 8/8。报告 §9–§12 收编补遗；并行会话产物并入后由本棒提交） |
| K65-02～11 | `docs/next/0.65/specs/K65-*.md` | 未开始 |

## 3. 冻结基线门槛（0.61 重冻结轮实测，2026-09-21T20:35–20:50+08:00）

运行目录：`F:/AIVoice/Aika-Next/windows/code/desktop-pet`；冻结窗口 `2026-09-21T20:52:55+08:00`。

**工作树计数为实测快照，且树上有其他 worker 并行写入**：`2026-09-21T20:52:55` 实测 `git status --porcelain` = **30 modified / 101 untracked = 131 条**；`20:56:47` 复测已变为 **39 modified / 90 untracked = 129 条**（期间其他 worker 继续增删文件）。**故门槛结论以"被冻结文件 hash"为准，不以条目计数为准**——§3b 的 6 个文件 hash 是本轮实测并已复算稳定，`desktop/electron/main.mjs` 与 `tools/management-url.mjs` 与 K65-00 独立记录一致。

| 命令 | 退出码 | 计数的通过/失败 |
| --- | --- | --- |
| `npm run check` | 0 | tsc 无错误 |
| `npm run test:next` | 0 | 80 pass / 0 fail / 0 skipped |
| `npm run test:next61` | 0 | **148 pass / 0 fail / 0 skipped**（= K65-00 的 143 + 本轮新增 5 例；见 §3b） |
| `npm run test:windows` | 0 | 22 pass / 0 fail / 0 skipped |
| `node --test dist/tests/media/*.test.js` | 0 | 63 pass / 0 fail |
| `npm run build:windows` | 0 | build + build:desktop + build:wake + build:native 四段全过 |
| `npm run test:windows:ui` | 0 | stdout 含 `WINDOWS_SMOKE_OK: Live2D renderer, isolated preload, backend round trip, panel layout.`（**判据 = 该文本 + 退出码两者**；stderr 仍有一行既有 `Renderer: script-error`，未做根因定位，不得声称窗口无错） |
| `npm run test:next65` | 0 | **21 pass / 0 fail / 0 skipped**（K65-00 交付；本轮只复跑确认，未改其文件） |

**本轮门槛未降低**：未删断言、未 skip、未放宽阈值、未改标签。上表全部为本轮实跑原始输出。**并发期数字已被安静树复测（§3a）复核：148 与 21/21 复现成立，其余计数一致；integration 计数口径以 §3a 为准（并发期 32 文件口径已被替换）。**

**0.61 侧后一轮命令（K65-00 轮实测，保留供追溯）**：`npm run build` 0；`npm run test:next:real`（无开关）2 显式 BLOCKED；`PET_NEXT_REAL=1 npm run test:next:real` 0（3 pass / 2 skipped）；~~integration 逐文件隔离（32 文件）261 tests / 247 pass / 6 fail / 3 skipped~~ —— **并发期数字，已被 §3a 替换（55 文件 / 397 tests / 368 pass / 21 fail / 8 skipped）**。

**源码修订关键 hash（0.61 冻结值，完整表见 `0.65/reports/BASELINE.md` §3 及 §3b）**：`app/trial-backend.ts` `9290601E…`、`providers/transport.ts` `D2C22116…`、`providers/slot-registry.ts` `1015D42D…`、`desktop/build/renderer.js` `D29DC83E…`；**本轮改动两处**：`desktop/electron/main.mjs` `96FD6F25…`、`tools/management-url.mjs` `8780358A…`（见 §3b）。

**本轮新增/修改（未提交，K65-00 轮）**：`tools/run-tests.mjs`（新增 `next65` 分支）、`package.json`（新增 `test:next65`）、`tests/next65/{baselineSurface.test.ts,baselineBehavior.test.ts,rendererArtifact.test.mjs}`、`docs/next/0.65/reports/{BASELINE.md,K65-00.md}`、`docs/next/0.65/{SPEC.md,specs/K65-00.md}`（状态行）。

## 3a. 安静树复测（0.61 重验证轮，2026-09-21T21:05–22:30+08:00）

**本轮性质**：在并发 worker 全部停止后的安静树上重跑全部冻结门槛与 integration 逐文件清扫，修正 §3/§4 中受并发污染的数字。期间实测一次并发闯入（FIX61-10 收口 worker 未停止，21:20–21:32 编辑 6 个测试文件并编译/跑测试，已由父会话叫停并 kill 其 2 个进程；闯入改动为合法 FIX61-10 修复，按会话决策保留）。另按授权清杀了本机一次 600 s 截杀留下的 2 个孤儿 node 进程（`pending-memory-management` 截杀残留）。复测结束前确认树上无任何非 DSH/Codex/Playwright 的 node 进程。

**所测修订**：HEAD `d7c61d6` + 当前工作树 dirty 集合（160 条 status；六冻结文件 hash 复算逐字一致，另含 FIX61-10 收口轮对 `app/evaluate-lifecycle.ts`、`management/server.ts`、`management/bootstrap.ts` 及 6 个测试文件的改动）。

### 门槛复测（每命令顺序实跑，非并发）

| 命令 | 退出码 | 计数（本轮实测） |
| --- | --- | --- |
| `npm run check` | 0 | tsc 无错误 |
| `npm run test:next` | 0 | 80 pass / 0 fail / 0 skipped |
| `npm run test:next61` | 0 | **148 pass / 0 fail / 0 skipped**（= 23 编译文件 + 9 mjs 源；`_probe.test.mjs` 等 148 例全数收集） |
| `npm run test:windows` | 0 | 22 pass / 0 fail / 0 skipped |
| `node --test dist/tests/media/*.test.js` | 0 | 63 pass / 0 fail（7 文件） |
| `npm run build:windows` | 0 | build + build:desktop + build:wake + build:native 四段全过（日志逐段核对） |
| `npm run test:windows:ui` | 0 | stdout 含 `WINDOWS_SMOKE_OK: Live2D renderer, isolated preload, backend round trip, panel layout.`（判据 = 文本 + 退出码；stderr 仍有既有 `Renderer: script-error` 一行，未做根因定位） |
| `npm run test:next65` | 0 | **21 pass / 0 fail / 0 skipped** |

**§3 表的 148 与 21/21 均被本轮复现确认**；其余计数与 §3 一致。§3 的数字因此保留为有效，仅 integration 口径由 §3a 修正。

### integration 逐文件隔离复测（真安静树，2026-09-21T21:44–21:53+08:00）

方法：自建 dist（`npm run build` exit 0）后，`node --test --test-reporter=tap --test-timeout=90000 <单文件>` 严格一次一个文件（含 150 s wall-clock SIGKILL 防挂起）；工具与原始输出留在 `windows/code/desktop-pet/tools/audit-integration-sweep.{mjs,results.json,tap.txt}`。两文件被 150 s wall 截杀：`self-setup-50`（91 s 长断言未跑完，非挂起）由单跑补测真实计数（275 s，exit 1，5 tests / 3 pass / 2 fail）；`pending-memory-management` **仍为真挂起**（单独 `--test-timeout=180000` 亦不返回，见 §4）。

**全量口径：55 文件 / 397 tests / 368 pass / 21 fail / 8 skipped**（其中 `self-setup-50` 为截杀占位，补测结果单列于下表；若计入其单跑 5/3/2，则全量为 402 tests / 371 pass / 23 fail / 8 skipped）。失败的 13 文件逐条：

| 文件 | 退出码 | tests / pass / fail | 错误（实测文本摘要） | 判定 |
| --- | --- | --- | --- | --- |
| `integration/backend-session.test.js` | 1 | 8 / 6 / 2 | `ERR_MODULE_NOT_FOUND … tests\desktop\ui-harness.mjs` | 既有（`git log --all -- "*ui-harness*"` 为空 → 从未入库） |
| `integration/capture-feedback-wire.test.js` | 1 | 1 / 0 / 1 | 同上 `ui-harness.mjs` | 既有（同上） |
| `integration/clear-reminders.test.js` | 1 | 30 / 29 / 1 | 同上 `ui-harness.mjs` | 既有（同上） |
| `integration/deepseek-text-update.test.js` | 1 | 1 / 0 / 1 | `ERR_MODULE_NOT_FOUND … tools\prepare-trial-update.mjs` | 既有（从未入库） |
| `integration/task-preflight.test.js` | 1 | 3 / 0 / 3 | `Unexpected end of JSON input`（3 例同错） | 既有（TRACKED@HEAD，状态空） |
| `integration/trial-config.test.js` | 1 | 12 / 10 / 2 | ①`This configuration targets macOS`；②`ERR_UNSUPPORTED_ESM_URL_SCHEME … protocol 'f:'` | 既有（TRACKED@HEAD） |
| `integration/trial-update.test.js` | 1 | 1 / 0 / 1 | `ERR_MODULE_NOT_FOUND`（4 个动态 import 的 `tools/*.mjs` 均不存在） | 既有；**更正 §4**：该文件**在 dist 集合内**（动态 import 不受 tsc 检查），K65-00"不在任何测试组"的表述作废 |
| `integration/wake-model-install.test.js` | 1 | 1 / 0 / 1 | `ERR_MODULE_NOT_FOUND … tools\install-wake-models.mjs`（从未入库） | 既有 |
| `integration/work-input.test.js` | 1 | 5 / 0 / 5 | `ui-harness.mjs` 缺失 | 既有 |
| `integration/work-records-entry.test.js` | 1 | 3 / 2 / 1 | `ui-harness.mjs` 缺失 | 既有 |
| `integration/work-speech-wire.test.js` | 1 | 2 / 0 / 2 | `ui-harness.mjs` 缺失 | 既有 |
| `integration/work-voice-wire.test.js` | 1 | 1 / 0 / 1 | `ui-harness.mjs` 缺失 | 既有 |
| `integration/self-setup-50.test.js` | 1 | 5 / 3 / 2 | ①`AssertionError: deepEqual`——actual 每 slot 多 `protocol: 'openai-compatible'`（FIX61-01 归一化 vs 期望式）；②一次 `fetch failed … bad port`（复跑不复现，偶发） | **会话引入**（该文件 FIX61-10 收口轮已改，仍是 M）；其余 41 文件全 pass，另 `pending-memory-management` 1 文件挂起（13 fail + 1 挂起 + 41 pass = 55） |

skip 分布：`dania-voice-clone` 1（环境 skip）+ `memory-planning-prototype` 3 + `memory-semantic-evaluation` 4 = 8。

### 55/21 与 32/6 的裁决（Task 2 结论）

- **文件数**：`tests/integration/` 恰 55 个 `.test.ts`，`dist/tests/integration/` 恰 55 个 `.test.js`（1:1 编译产物；dist 内另有 55 个 `.d.ts` 声明，110 的口径是把声明计入）。55 个全部 `git ls-tree HEAD d7c61d6` 可见——**不是后来新增**。
- **K65-00 的"32 文件 / 261 tests / 6 fail"** 是**测量窗口错误，不是虚报动机**：K65-00（≈20:02–20:35）与其他 worker 并发跑，其间 FIX61-10 收口 worker 正在修复失败测试；被停审计员（21 时段）登记 **55 文件 / 21 失败**（其逐文件清单未留档，可核对口径为计数），本轮安静树复测实测 **21 fail**，与该计数一致。真安静树 = 审计员口径。
- **21 → 6 的差值构成**：15 个失败在 K65-00 窗口未登记（`ui-harness.mjs` 系 11 例 + `task-preflight` 3 + `wake-model-install` 1）；`trial-update` 的"不在集合内"表述亦与实测不符。§4 已按实测更正。

## 3b. FIX61-11 token 缺陷：修复、RED→GREEN 证据与 0.61 基线冻结

**缺陷**：`desktop/electron/main.mjs` 的 `open_management` 用 `new URL(value.path, url)` 拼接，而 `url` 是后端发布的 `http://127.0.0.1:<port>/#token=<64hex>`。WHATWG URL 构造器会**整体替换 fragment**，故 `new URL('/#page=skins', 'http://127.0.0.1:51234/#token=ABC').href` → `http://127.0.0.1:51234/#page=skins`，**token 丢失**。后果：功能面板「外观 / 换肤」打开正确页面但处于**未授权锁定态**。

**修复（2 个生产文件，最小改动）**：

| 文件 | before → after |
| --- | --- |
| `tools/management-url.mjs` | 新增导出 `managementTarget(session, route)`：先做**同源校验**（异构 origin 直接抛错，绝不把 token 交给外部主机），再把 session fragment 与 route fragment 合并（`sessionFragment + '&' + routeFragment`），session 在前 |
| `desktop/electron/main.mjs` | `.then(url => shell.openExternal(value.path && value.path !== '/' ? new URL(value.path, url).href : url))` → `.then(url => shell.openExternal(managementTarget(url, value.path)))`；同时 import 增加 `managementTarget`，`value.path` 正则加 `(?!\/)` 拒绝协议相对路由（`//evil.com/x`），路由守卫由 `managementTarget` 的同源检查双保险 |

**fragment 顺序依据**：读 `management/ui/app.mjs:25` 与 `:32-36` 确认——token 取自 `new URLSearchParams(location.hash.slice(1)).get('token')`，`page`/`section` 取自同一 hash 的 `get('page')`/`get('section')`；`URLSearchParams.get` 返回**第一个**同名值，故把 session fragment 放在最前，使路由自带的 `token=…` 无法覆盖真实会话。`/#page=skins` → `#token=<64hex>&page=skins`，两者同时到达。

**覆盖的四种情形**（单元测试断言）：route+token（两者都保留）、token-only（`/` 与 `undefined`/`''` 均返回**逐字节相同**的 session URL）、route-without-token（保留 route，控制台选页后要求授权）、neither（原样）。

**RED→GREEN 证据**（`tests/next61/fix61-11.shellToken.ui.test.mjs`，真 Electron 进程跑真实 host）：

| 阶段 | `desktop/electron/main.mjs` SHA256 | 结果 |
| --- | --- | --- |
| **GREEN**（修复后） | `96FD6F254631375A02E906CAF2C61376B9B8F4576D864E109727224487A4E278` | 2/2 pass，退出码 0 |
| **RED**（临时改回 `new URL(value.path, url)`） | `B3A0FCB14D34EE4AE82ACDF1E9E4C19137F152404D3FDCE320AF2B9569849FBE` | **1 pass / 1 fail，退出码 1**；失败原文 `AssertionError: skin must carry the session token: http://127.0.0.1:9735/#page=skins`，`+ actual: null` vs `- expected: 'aaaa…'` —— 即复现报告的"页面对、token 没了" |
| **GREEN 还原** | `96FD6F25…`（**与修复后逐字节相同，SHA 已复算比对**） | 2/2 pass，退出码 0 |

**测试为何不是源文本正则**：测试在真实 Electron 进程里 `import` 未修改的 `desktop/electron/main.mjs`，只替换 `shell.openExternal` 为记录器，然后点击**生产 renderer 渲染出的真实功能面板按钮**（`function-skin`/`function-memory`/`function-settings`）。链路中 preload 通道、`open_management` 分支、路由守卫、`managementUrl()` 会话校验全部走生产代码。记录到的 URL 再在真实 Chromium 窗口加载**真实控制台页面**，断言 DOM：`heading` = `外观 / 换肤`、`nav-skins` 选中、`.skin-page` 存在、`loginFormPresent === false`（**未锁定**）、`authorizedRequests > 0`、`/api/skins` 被调用。旧代码含 `/#page=skins` 字符串却仍丢 token，正说明源文本断言无法覆盖此边界。

**同源检查的实测覆盖**：`tests/next61/fix61-11.managementTarget.test.mjs` 断言 `//example.com/x`、`/\example.com/x`、`https://example.com/#page=skins` 三种输入**抛错**而非改写成异构 URL。

**其他位置的同类缺陷排查**：全仓 `new URL(` 逐个核对后**无第二处** fragment 替换缺陷。`desktop/presentation-preview.mjs:15-16` 的 `route()` 显式 `throw` 当 `url.search || url.hash || url.origin !== location.origin`，是**已有防守**；`management/server.ts:56` 的 base 是 `origin`（无 fragment），仅作路径解析；`desktop/cubism-renderer.mjs:76,92` 的 base 是 `location.href`，但仅用于取资源**路径**，不构造带 token 的页面 URL；`desktop/electron/preload.cjs` 无任何 URL 构造。

**0.61 基线状态：FROZEN（已冻结）**，所测修订 = HEAD `d7c61d6` + 上述工作树改动，冻结窗口 `2026-09-21T20:52:55+08:00`。缺陷已修，§6 的重冻结触发条件**已满足**，原 `PROVISIONAL / PENDING_REFREEZE` 标签作废。

**本轮改动文件 SHA256**：

| 文件（相对 `windows/code/desktop-pet/`） | SHA256 | 字节 |
| --- | --- | --- |
| `desktop/electron/main.mjs` | `96FD6F254631375A02E906CAF2C61376B9B8F4576D864E109727224487A4E278` | 15629 |
| `tools/management-url.mjs` | `8780358A63649035C257C25AB02B2A077A5593BE63FE327FE5FCC54F46E00D0C` | 2698 |
| `tests/next61/fix61-11.managementTarget.test.mjs`（新） | `DF6AAB2956173AC6202B7C97A40265353C343D3C2687B289691EBEB9CFBD35C1` | 4909 |
| `tests/next61/fix61-11.shellToken.ui-fixture.mjs`（新） | `3D98D6CFFF6F8286F79267FF050532F39EEC61EC6BBFB85F1B89AE06CB018F35` | 4073 |
| `tests/next61/fix61-11.shellToken.ui-electron.mjs`（新） | `AB6FBE4FC4E90B3257DCA4B6BB870A600A5007DCC2640BEA83AB2C550C64A238` | 5950 |
| `tests/next61/fix61-11.shellToken.ui.test.mjs`（新） | `54B463015C9E3D3D62ADF6E0E9FCB0707DFF16502155000EA0ADF0125463A818` | 8933 |

`desktop/electron/main.mjs` 与 `tools/management-url.mjs` 两个 hash **与 K65-00 于 `20:30:43` 独立记录的"再固化值"逐字符一致**（`BASELINE.md` §3），可作为交叉印证。

**本轮未改**：`tools/run-tests.mjs`、`package.json`（K65-00 的 `next65` 分支与脚本原样保留）；`app/evaluate-lifecycle.ts`、`app/evaluate-source-budget.ts`（其他 worker 改动，非本轮范围）。

## 4. 阻塞与既有失败（2026-09-21 安静树复测后更新）

**FIX61-10 收口轮（未协调落盘）已修复 3 项既有失败，修复经本轮实测确认**：`memory-dynamics-http.test.js` 挂起已消（`Connection: close`，单跑 1 s exit 0）；`balances.test.js` POSIX mode 断言改为按平台分支（Windows 断言 `mode&0o600`，symlink 用例 Windows 显式 skip，6 pass/1 skip exit 0）；`deepseek-text.test.ts` 修复后 3/3 pass。`memory-trial.test.js` 的 Windows 路径限制已由生产修复（`app/evaluate-lifecycle.ts` 改用 `isAbsolute`）解决，本轮实测 **7/7 pass**。

**integration 既有失败（13 文件 21 失败，全部 TRACKED@HEAD 或从未入库，逐文件实测见 §3a 表）**：

| 测试 | 退出码 | 计数 | 真实原因（实测错误文本） |
| --- | --- | --- | --- |
| `integration/backend-session.test.js` | 1 | 8 pass / 2 fail | `ERR_MODULE_NOT_FOUND … tests/desktop/ui-harness.mjs`；**从未存在于 git** |
| `integration/capture-feedback-wire.test.js` | 1 | 0 pass / 1 fail | 同上 `ui-harness.mjs` 缺失 |
| `integration/clear-reminders.test.js` | 1 | 29 pass / 1 fail | 同上 `ui-harness.mjs` 缺失 |
| `integration/deepseek-text-update.test.js` | 1 | 0 pass / 1 fail | `ERR_MODULE_NOT_FOUND … tools/prepare-trial-update.mjs`；**从未存在于 git** |
| `integration/task-preflight.test.js` | 1 | 0 pass / 3 fail | `Unexpected end of JSON input`（3 例同错；TRACKED@HEAD） |
| `integration/trial-config.test.js` | 1 | 10 pass / 2 fail | ①`TrialNotReadyError: This configuration targets macOS`；②`ERR_UNSUPPORTED_ESM_URL_SCHEME ... protocol 'f:'` |
| `integration/trial-update.test.js` | 1 | 0 pass / 1 fail | 动态 import 4 个不存在的 `tools/*.mjs` → 加载期失败。**更正**：因走动态 import，该文件**在 dist/tests/integration 集合内**（K65-00"不在任何测试组"表述作废） |
| `integration/wake-model-install.test.js` | 1 | 0 pass / 1 fail | `ERR_MODULE_NOT_FOUND … tools/install-wake-models.mjs`；**从未存在于 git** |
| `integration/work-input.test.js` | 1 | 0 pass / 5 fail | `ui-harness.mjs` 缺失 |
| `integration/work-records-entry.test.js` | 1 | 2 pass / 1 fail | `ui-harness.mjs` 缺失 |
| `integration/work-speech-wire.test.js` | 1 | 0 pass / 2 fail | `ui-harness.mjs` 缺失 |
| `integration/work-voice-wire.test.js` | 1 | 0 pass / 1 fail | `ui-harness.mjs` 缺失 |
| `integration/self-setup-50.test.js` | 1 | 3 pass / 2 fail（5 tests） | **会话引入，非既有**：FIX61-10 收口轮修改本文件后仍有 1 例确定性 `deepEqual` 失败（actual 每 slot 含 `protocol: 'openai-compatible'`，期望式未含）；另有 1 例偶发 `fetch failed … bad port`（复跑不复现）。该文件是 M（会话改动），失败须由 FIX61-10 侧跟进，不得按既有登记 |

**management 既有失败（3 文件，本轮逐文件复测确认）**：

| 测试 | 退出码 | 计数 | 真实原因 |
| --- | --- | --- | --- |
| `management/settings.test.js` | 1 | 3 pass / 3 fail | POSIX mode 位（NTFS 不兑现）+ evaluation-budget 预留冲突（本轮实测；K65-00 轮记 4/2，其中 1 例为 mode 位同因） |
| `management/relay-bootstrap.test.js` | 1 | 0 pass / 1 fail | 测试路径与 `bootstrap.ts:71` 生产路径不一致 |
| `management/integration.test.js` | 1 | 0 pass / 2 fail | `EPERM` symlink（缺 `SeCreateSymbolicLinkPrivilege`）+ `EBUSY` unlink |

**仍挂起（1 文件）**：`integration/pending-memory-management.test.js` —— `--test-timeout=180000` 亦不返回（TAP 前段实测 `EBUSY … unlink … pending.sqlite` hookFailed 后进程不退出，须 wall-clock 截杀）。

**K65-00 记录的新阻塞（0.65 前置，非既有失败）**：
- **`providers/transport.ts:56` 硬拒非 https** → 即使配置校验通过，**每个本地 OpenAI-compatible 路径仍 100% 失败**；优先级高于 `credentialRef` 等字段放宽。
- 本地 LLM 服务**不存在**（`OllamaDialogueBoundary` 为 `verified:false` 占位）→ 本地 LLM 真实回放 **BLOCKED**。
- `RegisteredVoiceStore.provider` 硬编码 `dashscope` → 本地引擎音色无法登记（05 前置）。

**执行期间并发变化（K65-00 轮，均由其他 worker 落地，K65-00 未编辑）**：`2026-09-21T20:02:59` 初冻结窗口之后，构建单元内 4 个文件被改动 —— `desktop/electron/main.mjs`（20:28:42，新 hash `96FD6F25…`）与 `tools/management-url.mjs`（20:28:38，新 hash `8780358A…`）为 **FIX61-11 的 token 修复**（新增 `managementTarget()` 合并 session fragment 与 route fragment，并把 `value.path` 正则加 `(?!\/)` 拒绝协议相对路由）；另 `app/evaluate-lifecycle.ts`(20:15:21)、`app/evaluate-source-budget.ts`(20:17:00) 非本步范围。K65-00 已于 `20:30:43` **再固化**并复跑 `test:next65`（仍 21/21，退出码 0），关键文件 `app/trial-backend.ts`/`providers/transport.ts`/`providers/slot-registry.ts`/`management/settings.ts`/`desktop/build/renderer.js` 逐个复算 **UNCHANGED**。

> **上述"token 修复未经验证"的遗留已由本文件 §3b 关闭**：修复的正确性与"锁定态已解除"已由真 Electron 窗口测试 RED→GREEN 断言，且 `npm run test:windows:ui` 在含该修复的修订上退出码 0 + `WINDOWS_SMOKE_OK`（§3）。`app/evaluate-lifecycle.ts` / `app/evaluate-source-budget.ts` 仍属其他 worker 范围，本轮未审阅、未改动。

**合法后置（不阻塞 00-A）**：0.61 四类后置项与真实远程调用 NOT RUN，沿用 FIX61-10 §7。

## 5. 待人工验收项（交 K65-11）

人工验收**整体延后到 0.65 改造完成后**，由 K65-11 合并两版清单，一次性交用户。0.61 侧清单见 `0.61/reports/FIX61-10.md` §9（左右键与拖动、换肤、知识库实际影响回答、Key/模型设置、麦克风选择/试听、边说边出字、启动失败恢复）。**未获用户明确确认前不得标 0.61 ACCEPTED、不得代签。**

K65-00 本身**无新增人工项**（基线固定为自动门槛）。

## 6. 交接注意

- 0.61 状态是"**自动门槛在所测修订上通过、人工未验收、基线已冻结（FROZEN，§3b；安静树复测 §3a 维持）**"，**不是发布完成，也不是验收通过**。K65-00 冻结的是**可复现快照 + hash**，同样不是发布提交。
- ~~**重冻结触发条件**：`desktop/electron/main.mjs` 的 `open_management` token 丢失缺陷修复落地后，须重跑收口门槛并重新记录修订/hash。~~ **已满足并已执行**（2026-09-21T20:52:55+08:00 冻结，见 §3/§3b）。**安静树复测（§3a）复核了全部门槛；复测前 FIX61-10 收口 worker 未协调落盘的改动（`evaluate-lifecycle.ts`、`management/server.ts`、`bootstrap.ts` 及 6 个测试文件）一并纳入所测修订**。若此后构建单元内任何被冻结文件再次变化，须重新走一遍重冻结流程。
- ~~**FROZEN 重固定（re-pin）推迟到 K65-01 收口审计，一次执行**~~ **已于 K65-01 独立审计轮执行完毕（2026-09-22），re-pin 记录见 §7；本条到期的延期事由已消解。** 原约定的范围即"K65-01 收口时对全部冻结文件做一次 hash 复算与门槛复核"，本次按该范围一次性完成（`tsconfig.json` 增 `plugins/**` include 与 `tests/next65/fixtures` exclude；`package.json` 增 `sdk:next65` 脚本；其余冻结文件 hash 逐个复算为 UNCHANGED）。其后若构建单元内被冻结文件再次变化，仍须重新走一遍重冻结流程。
- 不修改 `F:/AIVoice/Aika`（旧树，只读）与 `F:/AIVoice/pet-shell`；Git 一律 `git -C F:\AIVoice\Aika-Next`。`desktop/electron/main.mjs` 的 token 修复已由本轮完成并冻结，后续 worker 如需再改该文件必须同时更新 §3b 的 hash 与门槛结论。
- **K65-01 启动前必读** `docs/next/0.65/reports/K65-00.md` §4 的 D1～D7 归属决策。
- **双树裁决（2026-09-22 登记）**：0.61/0.65 的唯一权威树是 `windows/code/desktop-pet`；仓库根平级目录 `code/`（与 `windows/` 并列）只读不碰、不登记为其产物，K65-01 及后续步骤同样适用。工作树中 K65-01 在途产物（contracts 四文件、plugins/**、tests/next65 四个新测试与 fixtures 等）归属 K65-01 棒续用，其他步骤不得提交、修改或删除。
- **K65-01 议题（00 已登记未裁决）**：`SlotBinding` 强制 `credentialRef`（`providers/slot-registry.ts:54`）与 TTS 强制 `characterMicros > 0`（`slot-registry.ts:87`）同 02A「无 Key 本地来源、费用 unknown」的冲突裁决点（费用豁免与 unknown 计费口径）归 K65-01 契约冻结时决策，见 `reports/BASELINE.md` §8；02A 只消费冻结结果。
- `test:next65` 已可用且缺目录/空集合均 `exit 2`；后续每步须在其中登记真实用例文件（不得删改 K65-00 三个基线特征测试的断言）。
- 跑 management 全量时务必排除或限时 `memory-dynamics-http.test.js`；`integration/pending-memory-management.test.js` 同属挂起，须隔离或加 `--test-timeout`。

## 7. FROZEN re-pin 记录（K65-01 独立审计轮，2026-09-22）

**触发**：K65-01 改动了两个已冻结文件（`tsconfig.json`、`package.json`），满足 §6 的重冻结触发条件。本次由**未参与 K65-01 实现的独立审计 worker**执行：先从零复核 K65-01 的 AUTO_PASS（含 4 次变异测试），再重跑 0.61 收口门槛全集并复算冻结 hash。

- **所测修订**：HEAD `45ee904a908303b43dba9afbcd32dd0a7ed7f78e`（分支 `aika-next`）+ K65-01 未提交产物（提交后即为 K65-01 提交本身）。
- **re-pin 窗口**：`2026-09-22`（K65-01 审计轮门槛跑毕时刻）。
- **运行目录**：`F:/AIVoice/Aika-Next/windows/code/desktop-pet`；Node v24.9.0。

### 7.1 门槛全集（本轮亲自实跑，cwd 同上）

| 命令 | 退出码 | 计数 |
| --- | --- | --- |
| `npm run check` | **0** | tsc 无错误 |
| `npm run test:next` | **0** | **80 pass / 0 fail / 0 skipped** |
| `npm run test:next61` | **0** | **148 pass / 0 fail / 0 skipped** |
| `npm run test:windows` | **0** | **22 pass / 0 fail / 0 skipped** |
| `node --test dist/tests/media/*.test.js` | **0** | **63 pass / 0 fail** |
| `npm run build:windows` | **0** | build + build:desktop + build:wake + build:native 四段全过 |
| `npm run test:windows:ui` | **0** | **文本 + 退出码双判据**：stdout 含 `WINDOWS_SMOKE_OK: Live2D renderer, isolated preload, backend round trip, panel layout.` 且退出码 0（stderr 仍有既有 `Renderer: script-error` 一行，未做根因定位，不声称窗口无错） |
| `npm run sdk:next65` | **0** | 14 文件入 `dist/next65-sdk`；`PLUGIN_SDK_SURFACE 31/31 declared` |
| `npm run test:next65` | **0** | **89 pass / 0 fail**（= K65-01 8 新文件 + K65-00 三基线 21） |

### 7.2 变化的冻结文件 hash（本次实算）

| 文件（相对 `windows/code/desktop-pet/`） | 旧 SHA256（K65-00 冻结值） | 新 SHA256（本次 re-pin） | 字节 | 变化 |
| --- | --- | --- | --- | --- |
| `tsconfig.json` | `2FB53B1B172BB71222C25F19EEE757ADAEACAD05D60986FBBD577BC27A42885F`（752 B） | **`A26899847F00B64141FBF89F8145636A82779F448467F81DF28D68D1544BCCFD`** | 809 | include 增 `plugins/**/*.ts`；exclude 增 `tests/next65/fixtures/**` |
| `package.json` | `C1FB99F6213807E3BB96DEAD60B645395EE1E86A6F65AB7B07F0D9CA948D76E7`（2711 B，HEAD 值） / K65-00 轮增 `test:next65` | **`F01B580432E0A146284220865657642BB74874BC79B591235DC978B5907B3243`** | 2853 | 增 `"sdk:next65": "npm run build && node tools/emit-plugin-sdk.mjs"`（K65-00 的 `test:next65` 与 K65-01 的 `sdk:next65` 均在库存续） |

**其余冻结文件本轮逐个复算为 UNCHANGED**（含 §3 表的 `app/trial-backend.ts`、`providers/transport.ts`、`providers/slot-registry.ts`、`management/settings.ts`、`desktop/build/renderer.js`、§3b 的 `desktop/electron/main.mjs` `96FD6F25…`、`tools/management-url.mjs` `8780358A…`；本轮 `git status` 仅见 K65-01 在途产物与上述两文件）。

### 7.3 K65-01 审计师的变异测试证据（非 K65-01 实现方提供）

审计方独立对 4 处生产行为做临时破坏，确认对应测试**确实会失败**，事后按 SHA256 逐字节还原：

| 变异 | 破坏点 | 捕获者 | 结果 |
| --- | --- | --- | --- |
| MUT1 | `plugins/esm-graph.mjs` 的 `readImportSpecifier` 提前返回 | `dependencyBoundaryPlainImport` + `importScanner` | **13 fail / exit 1**（01-C 非空洞） |
| MUT2 | 删 `plugins/manifest.ts` 中 `unsupported_parameter` 的 `issues.push` | `capabilitySchema` | **3 fail / exit 1** |
| MUT3 | `plugins/paths.ts` 的 `detectReparsePoints` 提前返回 `[]` | `packageManifest` 的 junction 用例 | **1 fail / exit 1**（普通嵌套目录真阴性用例仍 PASS，靶向正确） |
| MUT4 | `plugins/package-build.ts` 的 `epochSeconds` 相关遗传因式被篡改 | `packageBuild` 可复现性用例 | **2 fail / exit 1** |

四次还原后 SHA256 均与变异前**逐字节相同**（`esm-graph.mjs` `8C69ADB1…`、`manifest.ts` `4D4FD4CD…`、`paths.ts` `B7EE3C5E…`、`package-build.ts` `6D4F987B…`），树内无 `MUTANT` 残留，`npm run test:next65` 复跑回到 **89/89 exit 0**。

### 7.4 Git

K65-01 连同本次 re-pin 文档更新由审计 worker 提交并推送到 `origin`（`https://github.com/iwwater/Aika.git`，分支 `aika-next`），提交仅含 K65-01 范围与 0.61 re-pin 文档；0.61 + K65-00 的工作已在 `02a6870` 入库，本次未重复提交。提交前对暂存内容做了密钥/凭据/二进制/真实家目录扫描，仅有一处已知误报（`tests/next61/fix61-07.health.test.ts` 中"断言数据必须不含路径"的安全断言字面量）。
