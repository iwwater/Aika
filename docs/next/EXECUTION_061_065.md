# 0.61 → 0.65 连续开发与集中验收计划

日期：2026-09-21；状态：执行计划已准备，本文不表示 Goal 已启动或代码已完成。用户决定：0.61 完成自动收口后直接改造 0.65，人工验收统一后置。本文件调整两版的交接时机，不豁免自动测试、必需真实回放或数据安全门槛。

## 1. 执行路径

| 阶段 | AI 自动完成 | 退出条件 |
| --- | --- | --- |
| 接续现状 | 读取真实工作树、0.61 索引与报告，定位未完成或证据不足项 | 不重复已完成工作，不把旧状态说明当事实 |
| 0.61 收口 | 完成 FIX61-01～09 剩余工作及 FIX61-10 自动 AC；修复回归，构建/启动冒烟 | 10-A/B/C 通过；FIX61-10 标 AUTO_PASS，人工项 DEFERRED_TO_K65_11 |
| 冻结改造基线 | 保存 0.61 自动证据、源码修订、依赖锁、产物与数据恢复路径 | 可复现、可回退；注明“自动通过、人工未验收”，不标 ACCEPTED |
| 0.65 改造 | K65-00→01→02→02A→03→04→05→06→07→08→09→10 | 每步自动门槛通过后进入下一步；完成最小产品、包体、多源及兼容链路 |
| 集中交接 | 准备 K65-11 清单、启动说明、产物清单及已知限制 | 自动门槛全部通过后交用户一次集中验收；AI 不代签 |

0.61 基线应有本地提交或等效可复现的源码快照、文件哈希与依赖锁。不能把含其他人在途内容的工作树直接称为发布基线；不覆盖、清理或擅自提交他人的修改。无法安全冻结时记录具体冲突，继续不依赖该基线的核对工作。

## 2. 自动工作边界

架构测试按 [0.65 测试计划](0.65/TEST_PLAN.md)随步骤实施：保留业务回归、迁移旧接线、新增包体/生命周期/多源测试，不等改造完成后才补测。既有测试入口先复用，按模块影响运行，K65-10 汇总整版证据。

AI 自主完成必要的模块实现、迁移、接线、测试、构建、缺陷修复与文档更新，不为普通实现选择和逐步测试等待用户回复。先复用现有资源，不默认安装新模型、购买/启用新服务、发布、推送或提交市场申请。

0.65 中 STT/TTS 是已有能力的包体迁移与兼容接线；新引擎及情感陪伴业务由合作者按[模块规范](../development/PLUGIN_DEVELOPMENT.md)交付。主线不能为了等待未来合作者而把既有迁移门槛删掉，也不能用空包占位宣布完成。DSH 市场、0.7、OCR/VLM 新功能不进入本 Goal。

用户睡觉或未回复不是批准，也不是放弃验收。需要设备人工操作的体验项集中后置；已经规定为必需的真实自动回放不自动改成 fixture。缺资源时保存错误、已尝试方法和恢复条件，做独立可推进项；依赖未通过的步骤不能绕过，不能无限轮询或伪造 AUTO_PASS。

## 3. 续跑记录与最终交接

沿用两版 `reports/`。执行中维护本目录 `RUN_061_065.md`（开始实现时创建）：当前步骤、对应报告、最近通过的命令/修订、阻塞、下一动作和待验收项。不要每轮新建一堆重复报告。上下文压缩或恢复后先读记录并核对实际代码，再继续。

最终提供实际可启动的产物路径与命令、0.61 基线/回退方法、0.65 包与来源清单、自动结果和一份合并人工清单。清单覆盖 0.61 左右键/拖动/换肤/知识库/设置/启动恢复/实时 ASR，以及 0.65 最小包、语音组合、来源切换、包停用更新、数据兼容与取消。相同体验只测一次，但同时关联两版需求。

K65-11 在用户实际体验前标“待人工验收”。收到反馈后补回归、修复并重验；两版需求均通过且用户明确确认后，才能分别记录 0.61 ACCEPTED 与 0.65 RELEASE_ACCEPTED。

## 4. 可直接交给 Goal 模式的指令

下面的 Goal 终点是“自动开发与验收准备完成”，不是代替用户人工验收。启动时不要另设未要求的 token 预算。

```text
请开启 Goal 模式，在 F:/AIVoice/Aika-Next 连续完成 0.61 剩余开发和 0.65 插件化、包体化改造，直到所有必需自动门槛通过、可运行产物与合并人工验收清单准备完成。

先读 AGENTS.md、docs/next/EXECUTION_061_065.md、两版 SPEC 索引和实际报告，从真实未完成项接续。0.61 完成 FIX61-10 自动门槛后冻结可复现基线，人工验收延到 K65-11，不在中间要求我验收；不要把延期标成人工通过。再按 K65-00→01→02→02A→03→04→05→06→07→08→09→10 实施，最后准备 K65-11，等待我醒后统一体验。

自主完成必要代码、测试、接线、构建和缺陷修复。STT/TTS 本轮负责已有实现的迁移与接线；新引擎、情感陪伴新业务按合作者规范留给负责人，不扩大到 DSH 市场、0.7 或视觉新业务。不自动启动其他 Agent，不覆盖其他人改动，不动旧 Aika 工作树，不推送或发布。

测试按 docs/next/0.65/TEST_PLAN.md 随每个 K65 步骤完成：保留原业务断言，迁移接线测试，新增插件、多源、生命周期和独立产物测试。复用现有 test:next65，记录旧新用例映射，不能删除失败断言或用零用例冒充通过。

保留真实测试与 fixture 的区别，不跳过必需门槛。缺凭据、设备或模型时准确记录阻塞，继续不依赖它的工作；没有安全可推进项时如实交接，不假造成功。维护 docs/next/RUN_061_065.md 便于续跑。仅在自动开发目标全部达成时将 Goal 标完成，产品仍待人工验收；未完成则遵守 Goal 的阻塞/预算规则记录实际状态。
```
