# HANDOFF — 0.61→0.65 连续开发（2026-09-21 深夜会话结束点）

给下一个会话的启动文件。**先读本文件，再读 [RUN_061_065.md](RUN_061_065.md)（续跑账本），再核对实际代码，然后继续。**

- 计划原文：`docs/next/EXECUTION_061_065.md`（用户指令："照着这个逐个完成"）
- 工作树：`F:/AIVoice/Aika-Next`（分支 `aika-next`，HEAD `d7c61d6`，**全部未提交**）
- 本会话结束时的诚实状态：**0.61 自动收口完成、K65-00 自报 AUTO_PASS、K65-01 进行到约 40% 且未验证**。三个并行 worker 在本会话末尾同时崩溃/被停，留下未完成的验证义务（见 §3）。

---

## 1. 已完成且可信任的工作（有独立审计或交叉印证）

| 事项 | 证据 |
| --- | --- |
| FIX61-01..09 状态台账对齐（stale 头部已翻转，08 冲突已和解） | `docs/next/0.61/specs/FIX61-*.md` + `SPEC.md` |
| **FIX61-05/07 两个死按钮变成真功能**（SkinStore 接入路由/UI；试麦 UI 接入 desktop；`FUNCTION_CAPABILITIES={skin:true,microphone:true}`） | `docs/next/0.61/reports/FIX61-05.md`/`FIX61-07.md`；独立审计 CONFIRMED：45 个路径穿越/编码攻击输入 + raw-socket 全部 400/404、零泄漏；52/52 断言 HEAD vs now 集合相同；448 文件零 U+FFFD；mutation 测试证明新测试非空洞 |
| 换肤深链接修复（`#page=`/`#section=` 解析） | `management/ui/app.mjs`；RED→GREEN 证明 |
| **token 丢失缺陷修复**（`managementTarget()` 合并 fragment，同源校验） | `tools/management-url.mjs` + `desktop/electron/main.mjs`；真 Electron RED→GREEN（失败原文 `skin must carry the session token`）+ 同源三输入抛错测试；详见 `RUN_061_065.md` §3b |
| **K65-00**：BASELINE.md（30 文件 SHA256）、K65-00.md（00-A..E 逐 AC + 00-C/00-E 两张矩阵 + **D1–D7 归属决策**）、`test:next65`（21 用例 + 缺目录/空集合 exit 2）、**可执行 CER 门槛**（此前只有文档记录，`_metrics08.mjs` 已被删；现重建，并首次 pin `0.wav` SHA256 `668BF8DF…`）、渲染产物 `vm.SourceTextModule` 探针（`specifiers=[]`，解决 wake 动态 import 疑问） | `docs/next/0.65/reports/K65-00.md`（**下个 worker 必读 §4 D1–D7**） |

## 2. 立即要做的三件事（按顺序）

1. **干净复跑 0.61 冻结门槛并确认 `RUN_061_065.md` §3 的数字**。原因：该轮 gate 是在与其他 worker 并发时跑的（本会话发现并发会产生假失败——见 §3）。要复跑的最小集合：`npm run check`、`test:next`(80)、`test:next61`(**账本声称 148**，=143+5 token 测试)、`test:windows`(22)、`test:next65`(21)、`test:windows:ui`（文本+退出码双判据）。全部 0 才能维持 FROZEN 标签；若 148 不符，先查 `tests/next61/fix61-11.shellToken*.mjs`/`fix61-11.managementTarget.test.mjs` 再改账本。
2. **重做 K65-00 的独立审计（被中断，未完成）**。被停的审计 worker 留下未决发现：**integration 逐文件隔离测得 55 文件 / 21 失败，而 K65-00 声称 32 文件 / 6 失败**。需要解释差异（可能是并发污染，也可能是计数口径不同，也可能 K65-00 虚报）。审计要点：runner 守卫真的 exit 2（空集合/缺目录）、CER 门槛真的会 FAIL（对坏输入 mutation）、三个基线测试非空洞、D1–D7 已记录且可实现。
3. **继续 K65-01（未完成，见 §4）**。先审计继承的 6 个文件再补齐缺口。

## 3. 本会话的事故记录（避免重蹈）

- **三个重型 worker 并发跑 build/test 导致全部崩溃/被停**，且产生假失败。教训：**同一时刻只允许一个跑 build/test 的 worker**；读文件的 recon worker 可以并行。
- 并发期间树上有 6 个孤儿 node 进程（`pending-memory-management` 19:35 起挂起、`memory-dynamics-http`、昨日残留），已在本会话结束时全部 kill。**EBUSY/EPERM 类失败先查孤儿进程再下结论。**
- 挂起测试必须排除或加超时：`management/memory-dynamics-http.test.js`、`integration/pending-memory-management.test.js`。
- integration 测试**必须逐文件隔离**跑（并发产生假失败，如 `dania-voice-clone` 并发挂、单跑 2/2 过）。
- 环境：会话沙箱 workspace 是 `F:\AIVoice\Aika`，但目标树是兄弟目录 `F:\AIVoice\Aika-Next` → 需要 danger-full-access 才能写；`glob`/`grep` 只认会话 workspace，**必须传绝对路径**；中文 md 用 read 工具（PowerShell `Get-Content` 乱码）；approval 已禁用，拒绝即最终。

## 4. K65-01 继承状态（未验证，约 40% 完成）

两个 worker 先后崩溃，留下（`tsc --noEmit` exit 0，全部 untracked）：

| 文件 | 字节 | 来源 |
| --- | --- | --- |
| `contracts/capability.ts` | 10632 | 继承 worker 改写过（20:54） |
| `contracts/plugin.ts` | 21800 | 继承 worker 改写过（20:53） |
| `contracts/provider-source.ts` | 13364 | 第一 worker（20:44），继承 worker 未触碰 |
| `contracts/flow-profile.ts` | 7392 | 同上 |
| `tests/next65/fixtures/plugin-sdk/{package.json,tsconfig.json,src/index.ts,src/host-reach-through.ts}` | 457/879/6670/925 | 继承 worker 建了 01-C fixture 骨架 |

**缺口**：`contracts/index.ts` 未 re-export（无冲突需自行决策并记录）；K65-01 测试未写；`docs/next/0.65/reports/K65-01.md` 不存在；01-A..01-E 无任何证据；tsconfig 是否会误吞 fixture 目录未确认。

AC 原文与硬约束（ManagementError 38 个直接 importer 不得加 union、TurnScope 4 字段 72 文件、ProviderProtocol 双声明、CONTRACT_VERSION 不 bump、Windows reparse point 拒绝、01-B 零执行判别器、01-D 密钥/模型权重不入包）见 K65-00.md §4 与 `specs/K65-01.md`。

## 5. 已定的用户决策（不要重新问）

- 本地 LLM 真实回放：**BLOCKED**（机器上无任何本地 LLM 服务；`transport.ts:56` 硬拒非 https 是更高优先级堵点）。云端凭据真实回放：**BLOCKED**。均如实记录，不自动安装、不买服务。
- token 修复：gate 结束后修，再 refreeze（**已执行**，见 §3b）。
- 不推送/不发布/不 commit；不动 `F:/AIVoice/Aika` 与 `F:/AIVoice/pet-shell`；git 一律 `git -C F:\AIVoice\Aika-Next`。
- K65 顺序严格一步一门槛：00→01→02→02A→03→04→05→06→07→08→09→10→11；每步独立 worker 实现 + 独立 worker 审计；接口冻结（01 的 SDK/schema、02 的 loader、02A 的多源类型）不得越步重声明。

## 6. 后续步骤队列

02（loader/registry + 惰性加载，真实 ESM fixture，Windows 重解析项）→ 02A（**只消费** 01 的五类型；四段凭据链 + `transport.ts:56` https 堵点都在这一步附近解决；`settings-store.ts:26` historical=true 的 legacy catalog 不能破坏）→ 03（最小内核，`StrictTrialMemoryProvider` 迁移要同步改 4 个测试）→ 04→05（SAPI 本地 TTS 未接线：`createTrialTtsProvider` throws `'Unregistered speech adapter'`）→ 06→07→08→09→10→11（人工清单已备好：`0.61/reports/FIX61-10.md` §9 七项）。

完整矩阵/行号/blast radius：K65-00.md §00-C/§00-E；依赖与槽现状表在 `docs/next/0.65/reports/K65-00.md`。
