# NEXT-08 · 版本自动收口与候选构建

状态：AUTO_PASS（2026-09-20；管理套件三个文件为本机既有基线问题，已登记并排除，见报告 §5/§6）。需求：N06-R09。前置：NEXT-00～07 的必需自动 AC 通过。

## 边界

允许集成测试、构建配置的必要修复、候选包和证据；不借收尾增加新功能。发现缺陷回到所属 SPEC 补失败测试修复，再跑受影响范围。

## 验收矩阵

| ID | 必需自动验收 |
| --- | --- |
| 08-A | 固定候选 commit 上，上游适用完整测试、Next 全量测试、静态检查退出 0，无未解释 skip/空测试 |
| 08-B | 文本→生产 Runtime→Provider adapter→Memory/Context→回复→Timeline 自动完整链路通过，多轮/取消/跨会话覆盖 |
| 08-C | 固定录音/输入流→生产语音桥接→Runtime→TTS 队列→终态自动集成通过；假外部依赖逐项标注 |
| 08-D | TESTING 规定的真实 ASR/LLM/TTS 回放全部通过，原始结果脱敏留档；fixture 不能替代 |
| 08-E | Windows 可运行候选构建成功，自动启动、加载配置、最小文本操作、退出通过；无需用户交互 |
| 08-F | Next 数据目录隔离、配置无 Key 泄露、Timeline 删除/迟到不复活回归通过 |
| 08-G | R01～R09 逐条对应证据；附启动方法、环境依赖、模型配置要求、已知限制与 NEXT-09 清单 |

全链路确定性集成和真实服务冒烟分别记录，不声称 fixture 证明听感。真实播放设备与主观体验在 NEXT-09，不在本步反复召集用户。

运行命令与 Windows 构建格式遵循 NEXT-00 锁定平台，不默认套用旧 Aika 的 Tauri 指令。输出包的路径、hash、版本/commit 和依赖写入报告。任一必需项 BLOCKED/NOT RUN，报告为未开发完成；全部通过才置 AUTO_PASS / MANUAL_PENDING。
