# NEXT-05 · 最小 Chat Timeline

状态：NOT RUN。需求：N06-R06。前置：NEXT-04。

## 边界与接口

实现 CONTRACTS 的 TimelinePort/ChatEvent，订阅上游已接受输入和助手终态。允许最小存储表/adapter 与查询接口；不记录 token 级日志，不采集屏幕，不新增 Timeline 检索或 Memory 策略。

## TDD 步骤

先写临时真实存储的唯一性、分页、重开、redact/tombstone 测试；再写实际 Runtime 事件到 Timeline 的集成测试；最后实现订阅和有限重试。用存储错误注入覆盖失败恢复，不 mock 掉整个 Timeline。

## AC

| ID | 验收 |
| --- | --- |
| 05-A | 每轮用户事件一次、助手终态一次，取消/失败状态明确，partial 不标完整 |
| 05-B | 同 ID 重放不重复；不同 payload 同 ID 报冲突；并发写入满足唯一性 |
| 05-C | 持久库重开可查询；同时间戳分页无漏项/重复，跨会话过滤生效 |
| 05-D | 写失败不撤销已完成回复，错误可观测；最多 3 次有界重试，成功重放不重复 |
| 05-E | redact 幂等；与来源消息关联的删除/遗忘后正文不再可查；在途/重启后重放不复活 |
| 05-F | 断开订阅/关闭会话不泄漏监听器；上游 Memory/Context 行为未改变 |

验证存储与轮次消费者相关测试。查询 UI 留 NEXT-07，不能据此跳过真实查询接口验收。
