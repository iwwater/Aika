# NEXT-04 · 文本主链与上游 Memory 保真

状态：NOT RUN。需求：N06-R05。前置：NEXT-03。

## 边界与接口

以 TurnPort 薄适配运行上游真实 Dialogue/Context/Memory 流程。允许必要接线和兼容修复；不移植旧 CompanionRuntime、旧 Kernel 或另写记忆抽取/维护队列。上游缺少已约定行为时先记录差距，以最小生产修复和失败测试补齐。

## TDD 步骤

固定旧库行为用例与上游原测试；用受控 Provider、临时存储和生产 Runtime 驱动多轮、取消、跨会话、延迟写回、纠正/遗忘。对于已有行为先运行原测试，新增保证先 RED 再实现。

## AC

| ID | 验收 |
| --- | --- |
| 04-A | 文本单轮/多轮闭环，下一轮真实 Context 包含上轮有效内容，无双 Runtime |
| 04-B | 同会话快速 A→B，A 迟到 delta/终态/写回不污染 B；终态恰好一次，取消幂等 |
| 04-C | 两会话消息、上下文及检索不串线；上下文预算按固定配置实际执行 |
| 04-D | 临时持久库重开后保留有效 Memory；纠正返回新值；遗忘内容不被在途维护任务恢复 |
| 04-E | 模型错误不产生伪完成回复，后续新轮可以继续；相关上游 Memory/Context 回归通过 |

报告每个行为的实际上游入口与证据。此步保真不意味着全量旧数据库已迁移。
