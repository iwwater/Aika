# NEXT-00 源码映射（SOURCE_MAP）

状态：AUTO_PASS（2026-09-19）。基线：upstream `ba79db152c14d4a0d4b3488cfd11be7abbfecc23`，导入合并提交 `565cd80`。
所有路径相对 `windows/code/desktop-pet/`（Windows 基线目录）；符号均为已核实（在本工作树源码中直接读到），除非标注「设计接口」（仅存在于 [CONTRACTS](CONTRACTS.md) 伪码）。

语义约定：KEEP=原样作为底座；EXTEND=保留并在候选扩展点上加 Next 能力；PORT=从旧 Aika 以测试/行为/适配器形式引入（不搬旧架构）；IGNORE=代码保留但 0.6 不启用/不维护。

## 1. 域总表

| 域 | 真实路径/符号（已核实） | 决策 | 候选扩展点 | 已有测试 | 缺口 → 归属 |
| --- | --- | --- | --- | --- | --- |
| Windows 入口 | `desktop/electron/main.mjs`（Electron 主进程，`app.setPath('userData', %APPDATA%/AAAAGENT/{desktop\|preview\|smoke-test})`）、`desktop/electron/preload.cjs`（sandbox 隔离）、`app/preview-backend.ts`（离线后端）、`app/backend.ts`（评估后端，需 PET_EVALUATION_BATCH 授权环境）、`app/trial-launcher.ts`/`trial-backend.ts`/`trial-config.ts`（试用启动，校验运行指纹）、`tools/dev-desktop.mjs`（dev/冒烟启动器） | KEEP | main.mjs 的 userData 命名与 backend 选择参数（`--backend`）是 Next 独立数据目录与启动方式的现成开关位 | `tests/windows/launch.test.mjs`（trial 配置/指纹/拒绝变更）、`tests/windows/transport.test.mjs` | 无「正式用户」后端入口（仅 trial/evaluation/preview 三种）；NEXT-02 定配置，NEXT-07 接 UI |
| contracts | `contracts/index.ts`：`CONTRACT_VERSION='0.9.0'`、`TurnScope{characterId,sessionId,turnId,generation}`、`TurnInput`、`CapturedInput`、`PerceptionResult`、`DialogueContext`、`DialogueRequest/DialogueReply`、`TtsRequest/TtsResult`、`PlaybackEvent`、`MemoryOperation/MemoryChange`、`AsrProvider`、`VisualPerceptionProvider`、`RetentionPolicy`；`contracts/character.ts`、`desktop-bridge.ts`（`DESKTOP_BRIDGE_VERSION`）、`memory-lifecycle.ts`、`memory-dynamics.ts`、`presentation.ts` 等 17 个文件 | KEEP/EXTEND | `DialogueRequest.context`、`ProviderCapabilities` 是 Provider 协议扩展的自然挂点；TurnScope 的 generation 即取消/隔离语义的权威来源 | `tests/providers/adapters.test.ts` 直接消费全部契约类型 | CONTRACTS.md 的 `Scope/TurnPort/TimelinePort` 为设计接口，映射在 NEXT-01 冻结；`Scope.generation`↔上游 `generation`、`TurnInput.clientRequestId` 幂等语义需核对（NEXT-01/04） |
| Dialogue | `core/dialogue-pipeline.ts`：`DialoguePipeline`、`DialoguePorts`、`StaleTurnError`；`core/turn-controller.ts`：`TurnController`、`sameScope`；`core/desktop-runtime.ts`：`DesktopRuntime`、`RuntimePorts`；`app/backend-session.ts`：`BackendSession`、`BackendPorts`、`PersistentMemoryPort` | KEEP | DialoguePorts（capture/dialogue/playback/memory 端口注入）即 Aika Provider/表现接入点；StaleTurnError 即旧轮迟到过滤 | `tests/providers/dialogue-presentation.test.ts`、`dialogue-thinking.test.ts`、`tests/app` 回归场景（`app/dialogue-regression-scenarios.ts`、`frozen-source-scenarios.ts`） | 无 |
| Memory | `memory/sqlite-store.ts`：`SqliteMemoryStore`（实现 `CompanionProfilePort`）、`CONFIRMED_RETENTION`（transcript 30 天/300MB、deletedMemory 30 天）；`memory/sqlite-port.ts`：`SqliteMemoryPort`（实现 `MemoryPort`）；`memory/ledger.ts`、`pending-mutations.ts`、`source-graph.ts`、`scope.ts`；生命周期 `memory/sqlite-lifecycle-*.ts`、`core/memory-lifecycle-queue.ts`；纠正/遗忘 `memory/dynamics.ts`、`sqlite-dynamics.ts`、`dynamics-policy.ts`、`sqlite-management-forget.ts`、`management-forget.ts`（app） | KEEP | MemoryPort 接口不动；Provider 侧 memory-turn/lifecycle prompt（`providers/memory-*.ts`）按模型可换 | `tests/memory/*`、`tests/providers/memory-lifecycle.test.ts`、`memory-dynamics-plan.test.ts`、release 组 `import-49/context-continuity/dynamics*` | 无新增抽取器（RPD 排除项）；遗忘↔Timeline 正文联动映射在 NEXT-05 补 |
| Context | `memory/context.ts`：`assembleContext`、`ContextReader/ContextOptions/ContextSnapshot`；`app/input-budgets.ts`：`contextInputUpperBound`；`memory/sqlite-port.ts` 的 `SqliteContextOptions` | KEEP | ContextSnapshot 是 prompt 装配唯一入口；Aika 角色配置作为一份 systemPrompt 注入此处附近 | release 组 `context-continuity.test.js`、`tests/memory` | 0.6 不新增 Timeline→Context 检索（RPD 排除）；预算规则按上游复现（NEXT-04） |
| Provider | `providers/transport.ts`：`EndpointConfig{endpoint,model,apiKey,authorizer}`、`CallAuthorizer`、`ProviderTransport`、`CallOutcome`、`denyPaidCalls`、`ProviderOperation`；`providers/qwen-dialogue.ts`（`QwenDialogueProvider`/`QwenMemoryMaintenanceProvider`，DashScope compatible-mode chat/completions）、`providers/text-protocol.ts`（`deepseekFlashModel`，DeepSeek chat/completions）、`qwen-asr.ts`、`qwen-tts.ts`、`qwen-audio-tts.ts`、`minimax-tts.ts`、`registered-voices.ts`、`voice-enrollment.ts`、`emotion-inference.ts`；配置形状 `config/providers.example.json`（models.{dialogue,memory_turn,summary,perception,asr,tts}，provider+endpoint+credentialFile 外部路径） | EXTEND | chat/completions 客户端（deepseek/dashscope 同形）是 OpenAI-compatible 适配的最近基类；新增 Gemini adapter 与可配置 endpoint/model 在 providers/ 旁路新增，不动 transport 授权语义 | `tests/providers/adapters.test.ts`（流分片/SSE/字节级 UTF-8 切割已有 fixture）、`deepseek-memory.test.ts`、`minimax-tts.test.ts`、`qwen-asr.test.ts` | OpenAI-compatible 与 Gemini 独立协议 → NEXT-03；credentialRef↔credentialFile/SecretStore 映射 → NEXT-02 |
| Voice | `media/capture.ts`：`TurnCapture`、`CaptureDriver`、`CaptureSession`；`media/playback.ts`：`TurnPlayback`、`PlaybackDriver`、`PlaybackSession`、`PlaybackSample`；`media/browser-capture.ts`/`browser-playback.ts`（Electron 渲染层驱动）；`desktop/press-to-talk.ts`（`PressToTalk`、`validVoiceKey`）、`desktop/playback-controller.ts`；`media/wav.ts`（PCM WAV 编解码）、`media/store.ts`（`MemoryMediaStore`）、`media/timing.ts`、`temporal-frames.ts`；唤醒 `media/wake/`、`app/wake-manager.ts`、`core/wake-transcript.ts` | KEEP（唤醒部分 IGNORE） | CapturePort/PlaybackPort 注入点即打断桥（TurnPort.cancel+Playback.stop）的现成挂点；打断语义已由 generation 过滤承担 | `tests/media`、`tests/desktop`；旧 Aika 语音用例（`speechQueue/speechInput/speechOutput conformance` 等）作 PORT 参考语料 → NEXT-01 登记 | AsrSegment 聚合/SpeakRequest 队列（设计接口）与上游 segment 流的映射、句序与打断桥接测试 → NEXT-06；本地 ASR 路径缺失（见盘点） |
| 存储 | `memory/sqlite-store.ts`+`sqlite-backing.ts`（better-sqlite3 12.11.1）；`projects/sqlite-project-index.ts`；`wechat/store.ts`；`media/store.ts`；`management/settings-store.ts`、`credential-store.ts`；数据位置：后端 DB 由 `PET_DATABASE` 环境变量/trial 配置指定（trial 约定 `<root>/.local/data/companion.sqlite`），桌面偏好在 `%APPDATA%/AAAAGENT/<mode>` | KEEP | `PET_DATABASE`+`PET_PROJECT_ROOT`+userData 三处即 Next 独立命名空间的全部改动点（实际配置 NEXT-02 定） | `tests/memory`（DB identity/迁移）、`tests/windows/platform.test.mjs`（ACL/路径逃逸） | Next app data 命名与迁移策略 → NEXT-02；旧库导入 0.6 不做 |
| Management | `management/server.ts`：`startManagementServer`；路由 `memory-dynamics-routes.ts`、`memory-import-routes.ts`、`self-setup-routes.ts`、`task-routes.ts`、`balance-routes.ts`、`wake-routes.ts`、`wechat-routes.ts`、`emotion-routes.ts`、`project-routes.ts`；`bootstrap.ts`、`credentials.ts`、`settings.ts`、`voice-setup.ts`；UI `management/ui`；`app/self-setup.ts`（无 Key 自助配置网页，DeepSeek+百炼预设） | EXTEND | self-setup/credentials 流是 Aika Provider 配置 UI 的宿主；settings-store 可挂 Aika profile | `tests/management/*`、release 组 setup/emotion 套件 | Aika 身份/Provider/Timeline 的管理页接线 → NEXT-02/07 |
| Work route | `harness/desktop-work.ts`：`DesktopWork`（实现 `DesktopWorkPort`）；`harness/codex-windows.ts`（`codex app-server --stdio` JSONL）、`codex-app.ts`、`connection.ts`、`forwarding.ts`、`receipts.ts`、`work-catalog.ts`、`preset.ts`；`providers/work-plan.ts`/`work-intent.ts`/`work-conversation.ts`；`projects/sqlite-project-index.ts`；`contracts/desktop-work.ts`、`projects.ts` | IGNORE（代码保留，0.6 不自动启用外部动作） | 无需扩展；未来版本按独立 SPEC 评估 | `tests/harness`、`tests/windows/codex.test.mjs`、release 组 B37/B40 套件 | 0.6 不做；R09/R10 验收时不得自动触发 |

## 2. 已核实符号 vs 设计接口

- 上表路径与符号全部读自本工作树 `windows/code/desktop-pet/` 源码（grep/直接读取），属已核实。
- CONTRACTS.md 中的 `TurnPort/TimelinePort/SpeechInputPort/SpeechOutputPort/ProviderConfig/AikaProfile/ChatEvent/AsrSegment/SpeakRequest` 为设计接口，上游无同名导出；NEXT-01 起以契约测试冻结与上游符号的映射，不得假称其为上游现有符号。
- 上游 `TurnScope` 比 CONTRACTS 的 `Scope` 多 `characterId` 字段；`generation` 承担取消/隔离语义（`sameScope`、`StaleTurnError`）。

## 3. 环境盘点（AC 00-F）

| 能力 | 上游路径 | 本机现状 | 结论 |
| --- | --- | --- | --- |
| LLM | DeepSeek（chat/completions）、Qwen/DashScope compatible-mode | 旧 Aika `aika-crossplatform/.env` 存在（只记录存在性，未读内容）；无任何已验证凭据 | 契约测试用 fixture 可先行；真实回放需用户提供已授权凭据，否则 BLOCKED |
| ASR | `qwen-asr.ts`（DashScope 付费）、WeChat ASR | 旧记录 `E:/Work/toolchains/whisper-b5130`（whisper.cpp b5130 + ggml-base，见旧库 `docs/stt/reports/LOCAL_WHISPER_SETUP_20260917.md`）**当前不可用：E: 盘不存在**；`samples/jfk.wav` 未见 | 真实 ASR 回放 **BLOCKED**。可继续独立步骤：NEXT-01 用固定 ASR fixture 建 harness；恢复路径=用户重新提供 E: 工具链或下载官方 whisper.cpp b5130（记录了官方 SHA256） |
| TTS（可解码音频） | `qwen-tts.ts`/`minimax-tts.ts`（均付费 Key） | 无已验证凭据；旧 Aika Web Speech 只能播放不出音频文件；Windows SAPI/sherpa-onnx TTS 为候选免费路径（未验证） | 真实 TTS 回放 **BLOCKED（待 NEXT-06 择路）**。可继续独立步骤：契约测试、fixture 队列逻辑；确定免费本地后端或由用户提供授权凭据 |
| Live2D | Cubism SDK + 自备模型 | 已按用户指示下载官方 SDK 5-r.5 与官方 Natori 示例模型（gitignored，见 BASELINE） | 渲染链路可用（冒烟通过）；0.6 不做 Live2D 移植，仅供 UI 冒烟 |

## 4. 已知上游问题（原版行为记录，不修改）

1. 冒烟失败路径退出码不可靠：`tools/dev-desktop.mjs` + `desktop/electron/main.mjs` 的 `--smoke-test` 在断言失败时进程仍以 0 退出（实测两次：缺模型场景）。上游 CI 只跑 `test:windows`/`test:release`，未覆盖该失败路径。NEXT-08 的自动宿主验证需以输出文本（`WINDOWS_SMOKE_OK`）加退出码双判据，必要时在 Next 侧补失败退出。
2. 上游自身文档记录 0.1.2 全量回归曾有 4 项既有失败（见 `windows/WINDOWS-VALIDATION.md`），本次未复现（138/138 与 22/22 全绿）。
