# 08-03 验收报告：授权感知与动态观察注入

- **日期**：2026-09-24
- **阶段**：08-03 授权感知与动态观察注入
- **工作区**：`F:/AIVoice/Aika-Next/` (分支 `aika-next`，源码根 `windows/code/desktop-pet/`)
- **状态**：**ALL_PASS / IMPLEMENTED / READY_FOR_0804**

---

## 一、 改动要点与实现

| 模块 | 实现内容与技术细节 | 涉及文件 |
| :--- | :--- | :--- |
| **显式授权管理器** | 1. 严格默认关闭，捕获必须携带有效 `CaptureGrant`；<br>2. 支持目标窗口/区域、处理目的地（`local` / `cloud`）、生命周期（`single` / `session`）与 TTL；<br>3. 单次 Grant 消费后状态原子置为 `expired`，后续重复请求拦截拒绝；<br>4. 目的地由 `local` 升格为 `cloud` 或目标变更时强制撤回并以新 revision 重新核对；<br>5. 撤回（`revokeGrant`）联动绑定 `AbortController`，在途 OCR/VLM 处理即刻终止。 | [`core/perception-grant.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/core/perception-grant.ts) |
| **屏幕感知引擎** | 1. 适配独立 OCR 与 VLM 多模态能力，支持组合或单引擎运行；<br>2. 原始大图图像在提取后立即 Drop / 释放内存，产物 `Observation` 仅留存 `frameHash` 摘要与结构化文本/总结；<br>3. 帧哈希缓存键机制：相同图像直接复用识别结果，避免重复推理开销；<br>4. 异步处理期间如果 Grant 被撤回，严格抛出异常并拒绝产出有效 Observation。 | [`core/screen-perception.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/core/screen-perception.ts) |
| **动态上下文注入** | 1. `ObservationContextAdapter` 仅将时效性感知内容作为 Prompt 的**动态后缀（Dynamic Suffix）**拼接；<br>2. 核心系统设定与角色记忆构建的**冻结前缀快照（Frozen Prefix）100% 保持字节一致**，确保供应商端 KV Cache 击中率；<br>3. 观察失效、超时或 Grant 撤销后，Prompt 动态后缀自动剔除屏幕感知内容，杜绝迟到污染；<br>4. 严格校验租户配对（`PairingScope`），防止跨用户或跨实例信息泄漏。 | [`core/observation-context.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/core/observation-context.ts) |

---

## 二、 自动化测试验证证据

### 1. 编译构建
- `npm run check`：退出码 0，全量严格类型检查（含 `exactOptionalPropertyTypes`）通过。
- `npm run build`：退出码 0，tsc 编译与微信小程序构建脚本全部通过。

### 2. 08-03 感知测试套件（5/5 全部 PASS）
```powershell
node --test dist/tests/next08/perception.test.js
```
- ✔ `AC-0803-1: Grant validation and single-use expiration`
  - 验证无有效 Grant 捕获直接拒绝；单次 Grant 首次捕获成功后状态变为 `expired`，再次使用同一 Grant 捕获立即被拦截。
- ✔ `AC-0803-2: Destination change from local to cloud invalidates grant`
  - 验证同一窗口本地处理复用 Grant；目的地从 local 切换至 cloud 时原 Grant 立即置为 `revoked` 并重新签署新 revision。
- ✔ `AC-0803-3: Mid-flight cancellation stops observation production`
  - 验证在途慢速 VLM 提取过程中触发 `revokeGrant` 时，`AbortSignal` 触发并中止任务，Promise 拒绝且不产出 Observation。
- ✔ `AC-0803-4: Raw image buffer dropped, structured extraction preserved with caching`
  - 验证产物 Observation 中未保留原始 `imageBytes`；基于 SHA-256 `frameHash` 的缓存机制使得相同画面不再重复调用 OCR。
- ✔ `AC-0803-5: Dynamic suffix context injection preserves frozen prefix snapshot`
  - 验证感知内容作为尾部动态后缀追加，冻结前缀哈希前后严格一致（保证 KV 缓存）；Grant 撤销后提示词自动剔除感知内容。

### 3. Next 08 汇总测试套件（17/17 全部 PASS）
```powershell
npm run test:next08
```
- 08-01 多域事件与 Timeline：7/7 PASS
- 08-02 会话与桌宠交互不变量：5/5 PASS
- 08-03 授权感知与观察注入：5/5 PASS
- **总耗时**：~1.14s，0 失败，0 告警，退出码 0。

---

## 三、 结论与下一步

08-03 阶段全部授权感知与动态上下文注入不变量已通过自动化验证。
- **下一阶段**：启动 **08-04：主动陪伴**（免打扰、配额、冷却、气泡仲裁、主动问候邀请与动作解耦）。
