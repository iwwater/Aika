# 08-06 复核报告：控制台与联合收口

- 日期：2026-09-25
- 当前状态：**自动模块回归通过；正式运行时联合验收 BLOCKED**
- 工作区：`F:/AIVoice/Aika-Next/`，源码根 `windows/code/desktop-pet/`
- 修复与门槛记录：[后 0.8 代码审查修复计划](../POST_08_CODE_REVIEW_FIX_PLAN_20260924.md)

## 结论

原报告把测试中手动创建的服务组合描述为清洁安装、生产数据整合和跨域端到端验收，并把 08-A～M 标为全绿。这些结论超出了现有证据。本次更正并加入后续进程测试后，08-01 三域 Timeline 查询链已从正式 `trial-backend` 入口验证；其他阶段仍只有模块、路由处理器或组件证据，尚不能证明 0.8 已具备完整 Windows 运行链。

正式 `trial-backend` 已接通并验证活动 Canon 角色包、真实 Companion 对话 outbox 与持久化 Work 回执的 Unified Timeline 查询。08-05 现已接通认证管理 API、Work 管理页、private profile/journal 和正式组合根；同一隔离 `trial-backend` 子进程通过 ACP 与 MCP 本地 stdio fixture 验证准备/确认/回执 Timeline/重启/重复确认不重跑/遗忘不复活。该自动化不等同外部兼容 Agent/MCP 服务或真实副作用验收。08-04 连续性事实邀请链现由另一条正式自动化覆盖：真实已保存用户文本只形成待审 candidate；用户经管理 API 显式晋升后，active/evidence-eligible fact 才进入持久策略、忙闲/DND/共享配额仲裁；text 邀请通过正常 TurnScope 接受且不启麦，Timeline 来源及遗忘过滤通过。renderer 8 秒自动收起由假时钟单测覆盖。Observation/Schedule 来源、真实 Electron 气泡显示/点击、授权感知和其他视觉验收仍未完成；Electron smoke 因 GPU 子进程错误未加载 renderer，不能记为视觉通过。0.79 的 N079-08 候选写入与 N079-09 Host/Flow 回放自动化已通过；旧库迁移恢复、可启动 Electron 的六个 UI 场景及跨日/长时验证仍为发布准入 **BLOCKED / NOT RUN**。真实 Windows 捕获、OCR/VLM、外部 ACP/MCP 兼容服务、干净安装组合及用户人工验收均未完成。08-06 及 0.8 整体不能标记 `DONE`、`ALL_PASS` 或 `ACCEPTED`。

## 验收证据分层

| 范围 | 自动模块证据 | 真实入口 / 设备 / 人工证据 | 判定 |
| --- | --- | --- | --- |
| 08-A～D 授权与感知 | Grant 状态、目的地变化、处理器路由、撤销清理、缓存生命周期与 Observation 失效测试 | Windows 窗口/区域授权、真实采集、OCR/VLM 与宿主 Context 接线未运行 | 模块回归通过；生产验收 BLOCKED |
| 08-E～F 主动陪伴 | 假时钟配额、勿扰、冷却、隔离、过期状态；正式进程覆盖保存对话→候选→显式晋升→Continuity invitation 仲裁、普通 text TurnScope、Timeline 与遗忘；8 秒收起计时器模块测试 | Observation/Schedule producer 与 Electron 窗口视觉/点击/定时行为仍未验；非 Continuity 的 voice/clarify 真实动作仍需其依赖环境 | Continuity text 子链自动化通过；08-04 整体 IN_PROGRESS / BLOCKED |
| 08-G～H Work / ACP / MCP | 幂等、权限、取消竞态、未知回执、失败关闭；ACP v1/MCP 现代与旧版的 stdio wire fixture；真实 `trial-backend` 进程再用 ACP/MCP fixture 覆盖 API、明确确认、回执 Timeline、跨进程恢复、重复确认不重跑与遗忘 | 外部兼容 ACP/MCP 服务未运行；Electron Work 页面视觉验收未运行 | **正式 fixture 运行链自动化通过；外部服务与桌面体验 BLOCKED / NOT RUN** |
| 08-I 包组合 | 测试内构造不同服务组合并检查失败关闭 | 未从干净安装包及正式 Windows 产物启动验证 | 自动 fixture 通过；清洁安装验收 NOT RUN |
| 08-J 路由与安全 URL | 路由解析、旧深链兼容、token 脱敏及管理路由测试 | Electron 导航、会话/权限实际用户流程与视觉验收未运行 | 模块回归通过；桌面验收 NOT RUN |
| 08-K 时间线 | SQLite 投影、域隔离、幂等和 Outbox 模块测试；正式进程覆盖真实 `submit_text`、活动 Canon 包、持久化 Work 回执与邀请接受活动，鉴权查询/重启/来源遗忘过滤；管理端 Timeline 页面现已消费 `/api/unified-timeline`，组件覆盖领域筛选、游标与错误态 | 页面与生产 API 通过分层自动化验证；Electron 页面视觉、真实 Observation/邀请候选/外部 Work 来源尚未验 | **后端及管理端数据链自动化通过；Electron 视觉与其他生产源仍 BLOCKED/NOT RUN** |
| 08-L 桌宠交互 | TurnScope、代次、取消及 IME 等模块不变量测试 | Windows 离线 Electron smoke 因 GPU 子进程错误（`-1073741515`）导致 `pet://app/index.html` 加载失败；视觉与人工验收未运行 | 模块回归通过；Electron smoke BLOCKED |
| 08-M 管理台历史 / Trace | Timeline 页面模拟 DOM 覆盖 records/Trace 与统一三域活动 API 请求、正文脱敏、Trace 定位、领域筛选、游标分页和错误态；管理路由测试覆盖真实处理器 | Electron/browser 视觉验收未运行；页面组件与正式运行 API 尚未在同一浏览器进程做联机视觉验收 | 页面组件与 API handler 自动回归通过；视觉与联合验收 BLOCKED |

## 本轮自动验证

在 `windows/code/desktop-pet/` 执行：2026-09-25 收口复核 `npm run test:next08` 为 **76 项通过、0 失败**；`npm run check`、`npm run build:desktop`、桌面 JS 语法检查与 `git diff --check` 通过。最新 `node --test dist/tests/management/integration.test.js` **2/2 通过，约 319 秒**，覆盖 profile 配置、发现/显式确认、Work Timeline、重启、幂等回执、遗忘传播，以及保存对话→候选→显式晋升→text invitation 的 Continuity 闭环。测试使用本地协议 fixture、隔离数据库、Provider stub 与设备 ACK，不访问外部兼容服务、真实屏幕或麦克风。此前 `npm run test:windows:ui` 构建后，Electron 在当前 Windows 宿主报告 GPU 子进程 `exit_code=-1073741515`，随后 `ERR_FAILED (-2) loading 'pet://app/index.html'`。页面组件为模拟 DOM 自动回归，没有声称浏览器视觉端到端通过；上述 fixture 和模拟 DOM 不替代真实服务、设备或桌面体验。

## 解阻条件

1. 已完成 08-01 三域查询服务、`/api/unified-timeline` 正式入口和管理端统一时间线数据视图的分层自动验证，并将 Continuity candidate 经显式晋升后的首次仲裁展示及文本接受接入正式进程。下一步仅推进 Observation/Schedule 来源和可启动 Electron 后的视觉/交互验收；Electron 宿主仍阻塞。
2. 在目标 Windows 设备上验证授权预览、目标变化、撤销、锁屏和真实 OCR/VLM；确保撤销后无有效晚到 Observation。
3. 已将 ACP v1 与 MCP `2026-07-28`/`2025-11-25` stdio adapters 接入认证 API、持久化工作卡、可审计确认和 Timeline；仍需真实兼容服务验证握手、发现、执行、拒绝、取消及断连，缺少外部服务时保持 **BLOCKED / NOT RUN**。
4. 从干净安装产物验证 Core 与可选包组合、升级/恢复和资源释放。
5. 完成 Electron 实机视觉与交互核验，并由用户完成所需人工验收；记录逐项结果后再更新状态。
