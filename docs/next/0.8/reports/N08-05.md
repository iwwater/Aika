# 08-05 验收报告：Work 确认与 ACP / MCP 协议接入

- **日期**：2026-09-24
- **阶段**：08-05 Work 确认与 ACP / MCP 协议接入
- **工作区**：`F:/AIVoice/Aika-Next/` (分支 `aika-next`，源码根 `windows/code/desktop-pet/`)
- **状态**：**ALL_PASS / IMPLEMENTED / READY_FOR_0806**

---

## 一、 改动要点与实现

| 模块 | 实现内容与技术细节 | 涉及文件 |
| :--- | :--- | :--- |
| **工作任务幂等派发引擎** | 1. 严格以 `operationId` 作为幂等去重键，重复确认或并发派发仅向远端调用一次，结算回执直接复用；<br>2. 任务确认前支持对 target 与 instruction 进行修订；一旦派发或已结算则冻结禁止修订；<br>3. 超时或断连网络异常时，回执状态转为 `uncertain`（未知），标记 `retryable: false`，杜绝盲目自动重发带来重复副作用；<br>4. 支持任务取消并向在途适配器传播取消信号。 | [`core/work-protocol-adapter.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/core/work-protocol-adapter.ts) |
| **ACP 协议适配器** | 1. 适配 Agent Client Protocol，支持 `initialize` 握手与版本白名单（`2024-11-05`, `1.0`）；<br>2. 不支持的协议版本明确抛出受控 `unsupported_protocol_version` 异常；<br>3. 任务在途执行支持 `AbortSignal`，中止后优雅返回 `cancelled` 回执。 | [`core/work-protocol-adapter.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/core/work-protocol-adapter.ts) |
| **MCP 工具协议适配器** | 1. 适配 Model Context Protocol，支持版本协商（`2024-11-05`, `2025-03-26`）；<br>2. `listTools` 严格只读发现工具元数据，不执行工具副作用；<br>3. 工具调用强制校验权限：非只读工具缺少显式授权（如 `write`）时直接拦截并抛出 `permission_denied`。 | [`core/work-protocol-adapter.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/core/work-protocol-adapter.ts) |
| **工作域边界与事件隔离** | 1. 派发成功、失败或取消的审计事件封皮统一派发至 `CompanionEventHub`；<br>2. 业务域严格标记为 `domain: 'work'`，与 `companion` 伴侣生活记忆物理隔离，不伪装成伴侣记忆。 | [`core/work-protocol-adapter.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/core/work-protocol-adapter.ts) |

---

## 二、 自动化测试验证证据

### 1. 编译构建与类型检查
- `npm run check`：退出码 0，全量类型检查（含严格模式与 `exactOptionalPropertyTypes`）通过。
- `npm run build:desktop`：退出码 0，桌面前端打包无警告。

### 2. 08-05 专项测试（6/6 全部 PASS）
```powershell
node --test dist/tests/next08/work-protocol.test.js
```
- ✔ `AC-0805-1: Idempotent dispatch (duplicate confirmation only dispatches once)`
  - 验证两次并发派发同一 `operationId`，远端适配器执行次数恒为 1，回执完全一致；二次派发已结算任务不再调用远端。
- ✔ `AC-0805-2: Target revision updates unconfirmed draft; settled draft cannot be revised`
  - 验证草稿阶段修改 target 与指令成功生效并用于后续派发；已派发的请求再修订时抛出明确拒绝。
- ✔ `AC-0805-3: Timeout or unknown remote response marked as uncertain without blind re-dispatch`
  - 验证网络超时后状态标记为 `uncertain`，`retryable: false`，后续重复请求拒绝盲发。
- ✔ `AC-0805-4: ACP adapter lifecycle (handshake, task dispatch, cancellation, unsupported version)`
  - 验证非法 ACP 协议版本（如 99.0）握手被拦截；中途触发 `AbortController` 优雅结算为 `cancelled`。
- ✔ `AC-0805-5: MCP adapter tools list and execution with permission boundary`
  - 验证 `listTools` 只读安全展示；写工具在未获授权时抛出 `permission_denied`，获得授权后正常执行。
- ✔ `AC-0805-6: Work domain isolation (events published strictly with domain work)`
  - 验证所有 Work 任务派发审计封皮的 `domain` 恒为 `work`，绝不混入伴侣对话历史。

### 3. Next 08 汇总测试套件（29/29 全部 PASS）
```powershell
npm run test:next08
```
- 08-01 多域事件与 Timeline：7/7 PASS
- 08-02 会话与桌宠交互不变量：5/5 PASS
- 08-03 授权感知与动态上下文：5/5 PASS
- 08-04 主动陪伴与免打扰仲裁：6/6 PASS
- 08-05 Work / ACP / MCP 协议接入：6/6 PASS
- **总耗时**：~1.23s，0 失败，0 告警，退出码 0。

---

## 三、 结论与下一步

08-05 阶段全部 Work 任务确认、ACP / MCP 适配器、幂等性与工作域隔离逻辑已验证通过。
- **下一阶段**：启动 **08-06：控制台与联合收口**（授权、观察、时间线、策略、工作卡全景呈现与 08-A~M 全量验收报告收口）。
