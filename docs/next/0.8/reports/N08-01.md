# 08-01 验收报告：多域事件总线与时间线投影

- **日期**：2026-09-24
- **阶段**：08-01 事件与 Timeline 投影
- **工作区**：`F:/AIVoice/Aika-Next/` (分支 `aika-next`，源码根 `windows/code/desktop-pet/`)
- **状态**：**08-01 自动化验收通过（DONE）**。正式进程覆盖活动 Canon 角色包、真实 Companion 对话和持久化 Work 回执的分域查询；这不证明真实 ACP/MCP 执行，也不代表 0.8 放行。

## 运行时接线复核（2026-09-24）

- 正式 `trial-backend` 创建 `CompanionEventHub` / `UnifiedTimelineService`，并提供带管理会话鉴权的 `/api/unified-timeline` 查询。Canon 读取固定 pairing 对应的活动 `CharacterPackStore` 快照；Companion 复用同一 SQLite 中的 History 与 `character_companion_projection_outbox`；Work 从正式 `ForwardReceipts` 持久化回执存储投影，并经 Hub 写入统一查询。
- 在隔离项目副本中由真实 `trial-backend` 子进程接收 `submit_text`，走正式 `BackendSession`、对话保存回调、History/outbox 和管理 API；provider 与播放设备仅使用测试 stub，不访问外部服务或真实设备。测试确认写入后可按 `history:<turnId>:user` 回溯。
- 同一正式进程测试在隔离 SQLite 中激活一个 Canon 角色包，并预置一条已确认的持久化 Work 回执；API 返回各自域的数据，跨进程重启后 Canon 仍从活动包读取、Work 回执重放保持幂等。Work fixture 只验证既有回执来源接线，不声称真实 Harness/ACP/MCP 派发。
- 08-04 新增的邀请接受审计不属于对话正文：`companion.invitation.accepted` 写入独立活动投影，只持久化邀请 ID、动作与状态，并要求来源记忆仍 active 且版本一致。来源编辑/遗忘后该活动从查询中过滤；接受事件不会被转成虚构 user/assistant 对话。正式进程子测覆盖接受、取消、重启与遗忘。
- 新事件只在 foreground turn 完成后 drain：若在保存回调中立即写 Companion Timeline，它会递增当前 Continuity Context 的版本，使正在完成的同一轮被判为过期并取消。耐久 outbox 在本轮结束或进程重启时恢复投影。
- 进程级自动化：`actual trial backend process recovers, queries and forgets Unified Timeline across restarts` **1/1 PASS**（`node --test --test-concurrency=1 --test-timeout=180000 --test-reporter=tap --test-name-pattern="actual trial backend process recovers" dist/tests/management/integration.test.js`）。验证三域鉴权查询、真实命令、启动 drain、完整重启、实际对话来源撤销过滤、管理 forget API 过滤，以及第三次重启后无遗忘复活。
- `npm run build`、`npm run check` 通过。2026-09-25 收口复核的 `npm run test:next08` **76/76 PASS**；`node --test dist/tests/management/integration.test.js` **2/2 PASS**，最新正式 `trial-backend` 进程组约 319 秒，覆盖 Timeline、Continuity 主动邀请和 ACP/MCP Work composition root。08-01 三域 Timeline 的正式进程证据不变。Electron smoke 仍因 GPU 子进程退出码 `-1073741515`、renderer `ERR_FAILED (-2)` 未加载，视觉验收未完成。

### 当前边界

08-01 三域正式查询来源均已在生产组合根可达，并经隔离的正式进程查询和重启回归；Canon 为活动角色包只读快照，Companion 为真实轮次的耐久投影，Work 为已确认的正式回执。邀请接受活动是 08-04 的附加子链，采用单独的安全字段投影。**08-01 自动化范围标记 DONE**。本报告不覆盖 Windows/Electron 人工体验、授权屏幕采集、邀请候选生产/展示或真实 ACP/MCP 服务；这些仍按各自阶段阻塞，也不构成 0.8 发布许可。

---

## 一、 改动要点与实现

| 模块 | 实现内容与技术细节 | 涉及文件 |
| :--- | :--- | :--- |
| **多域事件分发**<br>`CompanionEventHub` | 1. 继承并实现宿主 `HostEventChannel` 契约，支持按业务领域（`canon` / `companion` / `work`）与角色配对（`PairingScope`）精确路由；<br>2. 严格校验事件外皮：`schemaVersion: 1`、`eventId`、`occurredAt`、`pairing` 与 `sourceRef`；<br>3. 提供 `subscribeDomain` 与 `publishEnvelope`，支持异步处理与 `drain()` 收集。 | [`core/companion-event-hub.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/core/companion-event-hub.ts) |
| **跨域统一时间线**<br>`UnifiedTimelineService` | 1. 自动初始化并维护 `work_timeline_events` 表，持久化 Work 域任务回执；<br>2. 统一整合 Canon（作品事实设定）、Companion（真实对话与互动）、Work（工程协作卡）跨域只读投影；<br>3. 实施幂等性保障：相同 `eventId` 重放返回 `duplicate`，分歧载荷抛出明确冲突异常；<br>4. 强时序保障：跨域查询按 `occurredAt` 严格正序排列，乱序到达自愈；<br>5. 撤销与遗忘联动：检查 `isSourceRevoked` 与文本清空标志，被遗忘/撤销的内容在时间线投影中动态剔除，杜绝历史复活；<br>6. 租户隔离：多用户/多实例配对严格隔离。 | [`memory/unified-timeline.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/memory/unified-timeline.ts) |

---

## 二、 自动化测试验证证据

### 1. 编译构建与类型推导
- `npm run check`：退出码 0，全量类型检查通过。
- `npm run build`：退出码 0，产物同步打包。

### 2. 08-01 定向测试套件（8/8 全部 PASS）
```powershell
npm run test:next08
```
- ✔ `AC-0801-1: Domain isolation - Work, Companion, and Canon maintain strict boundaries`
  - 验证 Work 任务卡只进入 Work 投影，Companion 对话只进入 Companion 投影，相互不越权、不伪造自述。
- ✔ `AC-0801-2: Idempotent projection and conflict detection`
  - 验证相同 `eventId` 幂等忽略返回 `duplicate`，相异载荷抛出冲突。
- ✔ `AC-0801-3: Out-of-order event ordering resilience`
  - 验证 12:10、12:00、12:05 逆序到达时，时间线查询结果严格按 12:00 -> 12:05 -> 12:10 排序。
- ✔ `AC-0801-4: Pairing / tenant isolation`
  - 验证 Pairing A 与 Pairing B 的私有伴侣事件完全隔离互不可见。
- ✔ `AC-0801-5: Revocation and forgetting propagation`
  - 验证 Companion 来源与 Work `sourceRef.id`（操作 ID）撤销后，两个域的正文都从统一查询剔除；关闭并重新打开同一 SQLite 文件后仍不可见。
- ✔ `AC-0801-6: Restart recovery via outbox drainage`
  - 验证模拟进程崩溃后，启动时通过 `projectPendingCompanionEvents` 成功将 Outbox 暂存轮次沉淀进时间线库并可查。
- ✔ `Work timeline refuses a receipt without an explicit execution status`
  - 验证缺少真实执行状态的 Work 回执不能写成 Timeline 成功事件。
- ✔ `CompanionEventHub: publish, subscribe by domain, and drainage`
  - 验证领域订阅过滤（仅接收 companion 与 work，过滤 canon）及异步排水。
- **结果**：**8 tests, 0 suites, 8 pass, 0 fail，退出码 0**。

---

## 三、 逐 AC 验收证据清单

| 验收标准 | 验证结果 | 证据与行为表现 |
| :--- | :---: | :--- |
| **AC-0801-1（分域隔离）** | **PASS** | `workDetails` 与 `companionDetails` 在各自域查询中互斥呈现，Work 结果不伪装为第一人称经历。 |
| **AC-0801-2（幂等与去重）** | **PASS** | 相同 `eventId` 二次执行返回 `'duplicate'`；修改标题后重放触发 conflict 抛出。 |
| **AC-0801-3（乱序自愈）** | **PASS** | 逆序注入事件按 `occurredAt` 自动在 Timeline 结果中排入正确时序位置。 |
| **AC-0801-4（配对安全隔离）** | **PASS** | Pairing A 查不到 Pairing B 的任何伴侣历史记录。 |
| **AC-0801-5（撤销与遗忘联动）** | **PASS** | `revokeSource` 对 Companion 来源和 Work 操作来源分别生效；撤销后查询不返回敏感正文，重开 SQLite 后仍过滤。 |
| **AC-0801-6（重启恢复）** | **PASS** | Outbox 中 1 项待处理项在重启 drain 后 `projected === 1`，随即可在时间线查询中呈现。 |

---

## 四、 结论与下一步

模块级八项事件/时间线测试、收口后的 76 项 `test:next08` 回归和正式进程级三域查询/重启/遗忘测试通过，08-01 自动化交付标记 **DONE**。Work 用例证明正式 Work 回执读取/投影/重放以及 ACP/MCP fixture composition root，不证明外部兼容服务的真实执行；Windows UI、授权 Observation 来源和真实 ACP/MCP 服务仍有独立阻塞，因此 0.8 维持 **IN_PROGRESS / BLOCKED**，不放行版本。
