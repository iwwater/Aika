# 08-02 验收报告：会话与桌宠核心交互（气泡与展开抽屉协同）

- **日期**：2026-09-24
- **阶段**：08-02 会话与桌宠核心交互
- **工作区**：`F:/AIVoice/Aika-Next/` (分支 `aika-next`，源码根 `windows/code/desktop-pet/`)
- **状态**：**交互不变量模块测试 PASS；Windows/Electron 视觉和真实桌面流程 BLOCKED / NOT RUN**。不可据此称桌面交互阶段完整验收。

---

## 一、 改动要点与实现

| 模块 | 实现内容与技术细节 | 涉及文件 |
| :--- | :--- | :--- |
| **气泡与抽屉协同** | 1. 明确单一 `TurnScope` 权威原则，抽屉展开与收起仅控制视图呈现，绝不新建会话、不重复提交输入、不重复触发 TTS 播放；<br>2. 气泡淡出（`dismissBubble`）纯操作 DOM 可见性，底层 `DesktopChatLog` 完整保留当前会话历史行；<br>3. 点击轻量气泡主体触发平滑展开抽屉并淡出气泡，用户可无缝阅读完整回复与上下文；单独点击气泡关闭按钮仅关闭气泡。 | [`desktop/main.mjs`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/desktop/main.mjs) |
| **代次与状态机守卫** | 1. `DesktopViewState.accepts()` 严格校验 `generation` 与 `sessionId`，拒绝已取消或旧代次的迟到 reply/presentation 事件；<br>2. 40 秒思考状态机超时后执行真实 `command({ type: 'cancel' })` 并在气泡中展示友好提示。 | [`desktop/view-state.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/desktop/view-state.ts)<br>[`desktop/main.mjs`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/desktop/main.mjs) |
| **输入法与区域穿透** | 1. 严格在拼音输入法合成态（`composing = true` 或 `keyCode === 229`）拦截回车事件，防范未确认拼音直接上屏误提交；<br>2. 抽屉开启时上报 `#drawer` 交互矩形，收起时自动清空，系统底层 `setIgnoreMouseEvents` 恢复全窗穿透。 | [`desktop/main.mjs`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/desktop/main.mjs)<br>[`desktop/interactive-region.ts`](file:///F:/AIVoice/Aika-Next/windows/code/desktop-pet/desktop/interactive-region.ts) |

---

## 二、 自动化测试验证证据

### 1. 编译构建
- `npm run check`：退出码 0，全量类型检查通过。
- `npm run build:desktop`：退出码 0，前端桌面 bundle 重新打包完成。

### 2. 08-02 交互不变量测试（5/5 全部 PASS）
```powershell
node --test dist/tests/next08/interaction-invariants.test.js
```
- ✔ `AC-0802-1: Single turn scope invariant - drawer toggle preserves scope and does not emit new turn`
  - 验证抽屉在 thinking 和 speaking 状态下开关，`view.scope` 保持不变，不产生重复轮次或多余请求。
- ✔ `AC-0802-2: Bubble dismissal never deletes chat history`
  - 验证气泡淡出关闭后，`DesktopChatLog` 的对话记录完好无损。
- ✔ `AC-0802-3: Stale generation guard rejects late arrival events`
  - 验证来自旧代次（generation 1）的迟到回复和动作表情事件被安全拦截，不污染当前界面。
- ✔ `AC-0802-4: IME composition check prevents enter key submission`
  - 验证中文输入法选词回车不触发消息发送，合成结束后回车正常提交。
- ✔ `AC-0802-5: Interactive region arbitration toggles with drawer visibility`
  - 验证抽屉打开时光标处于抽屉区域可交互，处于角色身体区域可穿透；抽屉关闭后整窗穿透。

---

## 三、 结论与下一步

TurnScope、气泡历史保留、迟到代次和 IME 等交互不变量已通过模块级自动化测试。Electron smoke 因 Chromium GPU 子进程加载问题未完成，桌面实际呈现与操作仍未验。08-03 的模块回归可通过，但生产屏幕采集链因宿主/API/真实 OCR/VLM 缺失而 BLOCKED；见 [N08-03 报告](N08-03.md)。
