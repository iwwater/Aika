# Aika Next 0.8 RPD · 感知、主动陪伴与工作协作

版本：0.2；日期：2026-09-23；状态：产品需求草案已补齐，未拆执行 SPEC、未实施本版。依赖基线更新为 [0.79 MVP/源码核对](../0.79/SOURCE_AUDIT.md)；旧架构报告保留为历史，已修项不重复当作现存缺陷。本文件描述产品需求，不授权实际采集、联网推理或外部工具执行。

目录：[目标](#1-目标与用户流程) · [基线](#2-实际工程基线与依赖) · [范围](#3-需求与范围) · [感知](#4-屏幕感知) · [主动陪伴](#5-主动陪伴) · [工作协作](#6-workacpmcp-接入) · [契约](#7-数据与模块契约) · [界面](#8-界面与失败恢复) · [验收](#9-验收矩阵) · [实施](#10-实施拆分与发布边界)

## 1. 目标与用户流程

让用户主动选择桌宠可看到的信息，获得与当前情境相关的帮助；允许受限主动邀请；将用户明确交付的工作可靠转交到选定执行器并查看结果。屏幕观察、陪伴建议和工作执行具有不同授权范围，不相互隐式升级。

三条最小路径：

1. 选择窗口/区域 → 查看采集预览与处理目的地 → 单次读取 → OCR/视觉理解 → 对当前画面提问 → 清除观察。
2. 开启主动陪伴 → 设置时间段、来源和配额 → 符合条件时出现邀请 → 接受进入正常轮次，忽略则冷却。
3. 明确工作请求 → 显示目标项目、执行器与工作卡 → 按动作权限确认 → 发送一次 → 跟踪回执 → 展示结果或未知状态。

仅安装基础包时继续文字聊天；不安装感知、主动陪伴或工作包不影响基础功能。没有 0.7 连续性时仍可做本轮画面问答；涉及长期偏好的主动邀请明确显示缺少连续性依赖。

## 2. 实际工程基线与依赖

源码根为 `windows/code/desktop-pet/`。已有 `providers/split-perception.ts`、`media/capture.ts`、`companion/invitations.ts`、`harness/desktop-work.ts`、`harness/forwarding.ts` 和窄 MCP relay。它们分别提供视觉/音频分离、采集生命周期、邀请策略、工作卡与回执机制；不代表本版 OCR 与通用协议接入已完成。

| 依赖 | 开发前必须核实 | 允许独立推进 |
| --- | --- | --- |
| 0.65 宿主/多源 | 架构 AR-03：Binding 真正解析 source/model/adapter，取消和实例租约进入执行入口 | OCR/视觉 adapter 的协议与固定图片测试 |
| 0.7 / 0.79 连续性 | N079-01/08：派生遗忘、来源边界与真实生产投影；Context 读取已接线，可选包及 Flow 仍须按实际路径验证 | 只在当前轮使用的临时 Observation |
| 桌面采集 | 真实 Windows 窗口/区域定位、缩放坐标、权限和撤销入口 | 固定截图和假时钟测试；不能替代实机采集 |
| Work/协议 | 保留确认与回执权威，明确目标协议及兼容版本 | 受控本地执行器协议测试 |

0.79 是本版之前的有界修复轮，见 [RPD](../0.79/RPD.md) 与 [SPEC](../0.79/SPEC.md)。用户允许长期观察及其他未完成 UI 后置；按能力准入，不要求全部历史项目清零，也不声明 0.7 已全面验收。依赖长期资料的主动陪伴须先通过 N079-01/08；临时 Observation 和固定输入 adapter 可独立推进，未满足依赖的能力保持关闭。

0.79 的本地时钟模板问候属于零 LLM 基础邀请，0.8 在同一冷却、勿扰、配额与展示仲裁上扩展情境邀请，不创建第二个抢占气泡的定时系统。0.79 问候不代表已授权屏幕感知、模型推理或外部动作。

所有新增或调整的界面继续遵守：先问用户要改成什么样→随后索取参考图→按确认范围实现→主动请用户审核→通过后下一页。

## 3. 需求与范围

| ID | 必需需求 | 验收 |
| --- | --- | --- |
| N08-R01 | 按窗口/区域授予单次或会话级屏幕读取，可预览、撤销 | 08-A/B |
| N08-R02 | OCR 输出文字与位置，VLM 输出有来源的观察，明确不确定性 | 08-C |
| N08-R03 | 观察结果进入本轮 Context，带预算、时效及来源失效 | 08-D |
| N08-R04 | 主动邀请开关、免打扰、配额、冷却和去重 | 08-E |
| N08-R05 | 邀请与执行分离，用户输入优先，来源撤销取消旧邀请 | 08-E/F |
| N08-R06 | Work 草稿、目标选择、确认、幂等派发和结果回执 | 08-G |
| N08-R07 | ACP 与 MCP 分别作为可替换 adapter，声明能力和版本 | 08-H |
| N08-R08 | 按需独立包、多源配置、明确数据目的地与资源释放 | 08-I |
| N08-R09 | 可理解的授权、观察、邀请、任务和诊断界面 | 08-J |

不纳入：全桌面永久录像、登录站点爬虫、自动控制任意应用、无人确认的高风险外部动作、全自动工作代理循环、跨角色共享私人画面、通用连接器市场。已有本地提醒能力不自动扩为跨端通知平台。

## 4. 屏幕感知

### 4.1 采集和授权

默认关闭。第一次采集明确窗口/区域、单次或会话时长、OCR/VLM 来源及本地/云端目的地。授权引用绑定采集目标、用途和修订；改为另一窗口或从本地切云端时重新核对范围。持续观察只在用户主动开启后发生，显示持续可见状态和停止入口。

锁屏、目标关闭、授权撤销或包停用时停止新采集并取消在途处理。目标失效不改抓整个桌面。结果回传前再次检查授权修订；已发送的云端请求无法承诺远端撤回，应如实显示已发送与本地停止的区别。

原图默认仅用于当前观察，处理结束或到期释放。保存为长期来源需要单独明确操作；日志默认仅存标识、耗时、尺寸、结果状态。敏感区域屏蔽是辅助措施，不承诺自动识别全部秘密。

### 4.2 OCR 和视觉理解

OCR 和 VLM 分别声明能力；可只安装其中之一。不强制每次同时调用，不启动未选择引擎。OCR 保留区域坐标、阅读顺序、语言及引擎置信字段；VLM 保留 observation/source 引用和 unknown/partial/failed 状态。模型输出中的操作指令不构成工具授权。

支持多显示器、缩放和移动窗口坐标验证。重复图像可复用短期缓存，缓存键必须包含目标、画面 hash、授权代次和 Provider 修订。超限图片明确缩放/拒绝，不悄悄改变采集范围。

## 5. 主动陪伴

复用 InvitationStore 的配额、最小间隔、时区与来源失效；新增情境策略作为可选包，不另建全局轮次。建议初始沿用现有每天 2 次、间隔 3 小时的上限，用户可降低或关闭；持续观察采样频率是另一项配置，不能以邀请配额代替采集限流。

触发来源限于已授权当前观察或有效连续性条目。记录候选理由、来源及过期时间；同事件重放不增加邀请。免打扰、用户正在输入/录音/工作确认、达到配额或来源失效时不打断。

默认先给短文本邀请，接受后进入正常轮次；不得自动打开麦克风、截图更多窗口或调用工作工具。忽略/拒绝应降低同类打扰，设置页可暂停和清空候选。窗口标题和单次画面不能直接升级为稳定用户画像。

## 6. Work/ACP/MCP 接入

保留现有 DesktopWork/HarnessForwarding 的草稿与确认回执。ACP 承担外部 agent 会话/任务适配；MCP 承担已配置工具/资源的能力适配，二者不能统一假设为相同任务状态机。协议版本、传输和支持子集在实施 SPEC 中查官方文档冻结；本 RPD 不承诺最新协议全覆盖。

首版范围：一个 ACP adapter、一条 MCP 工具接入路径，加保留已有窄 relay 的兼容测试。实际目标和传输待 SPEC 按用户现有环境选择。工具发现只读元数据，不执行任务、不启动非必要服务。

工作卡应展示目标项目/目录、执行器、实际任务、数据目的地和副作用。只读任务与写入/发送/删除分开授权；用户对明确动作已有授权时不重复询问。内容或目标修订使旧确认失效；请求使用 operationId 幂等提交。

发送超时必须区分明确失败与“可能已执行、回执未知”；未知不能自动重发有副作用的动作。取消本地等待不等于远端任务取消，需报告远端确认结果。工具输出按不可信数据处理，不改变权限、角色或系统设置。任务记录归工作域，不能自动沉淀为用户亲身经历。

## 7. 数据与模块契约

以下为待实施逻辑契约，不是已存在导出；通过公共 SDK 接入，字段最终由 SPEC 冻结。

| 对象 | 最小信息 | 生命周期 |
| --- | --- | --- |
| CaptureGrant | grantId、目标、用途、处理目的地、revision、到期、状态 | 撤销即时阻止后续采集/注入 |
| Observation | observationId、Scope/配对、grantRevision、frameHash、来源、时间、OCR/VLM结果、状态 | 默认短期，不自动进入 Wiki；过期排除 |
| InvitationCandidate | id、来源引用、原因、有效期、配额域、状态 | 接受/忽略/失效幂等；不持有独立轮次 |
| WorkRequest/Receipt | operationId、目标与修订、授权摘要、远端关联、状态、结果引用 | 同请求至多派发一次；未知可查询对账 |

感知 Context 放动态后缀，不改变 0.61 冻结前缀。来源与用户×角色实例隔离沿用 0.7。观察转长期条目经现有维护入口并保留用户确认/来源；遗忘要覆盖观察衍生项及短期缓存。

## 8. 界面与失败恢复

提供感知设置和采集预览、当前观察清单、主动陪伴策略、工作卡/运行记录四类入口。错误给出原因及本地可操作恢复：重新选窗口、重授权限、改已配置来源、重试只读处理或查询未知回执。不得用“绿色已连接”表示尚未验证的模型能力。

性能门槛在首个 SPEC 固定参考机器/图片集/采样上限/超时/预算，未达标如实报告。只读 OCR/VLM 可有有界重试；持久化、通知和外部动作不盲重试。实时采集撤销和取消属于必需行为，不等待 1.0 再补。

## 9. 验收矩阵

| AC | 自动验收 | 真实/人工验收 |
| --- | --- | --- |
| 08-A | 默认关闭、拒绝/到期后零采集；切目的地使旧授权失效 | Windows 权限、预览范围与指示可见 |
| 08-B | 处理中撤销后零有效晚到 Context；目标消失不扩大抓取 | 多屏/缩放/目标关闭/锁屏 |
| 08-C | 固定中英文截图，OCR定位、空图、模糊、超限、错误与取消 | 所选实际 OCR 和 VLM 各一条回放；质量阈值预先冻结 |
| 08-D | 观察去重/预算/过期/配对隔离；Prompt 注入资料不能授权工具 | 正常桌面问答确实消费目标画面 |
| 08-E | 假时钟验证配额、时区、冷却、免打扰和重复事件 | 邀请可接受/拒绝/暂停，不抢输入 |
| 08-F | 旧观察/旧角色邀请失效；接受不隐式开麦或发任务 | 真实切角色与撤销流程 |
| 08-G | 重复确认只发一次；目标修改失效；超时未知不重发 | 已授权低风险任务发送、回执和取消 |
| 08-H | ACP/MCP 各自协议、断连、版本不支持和错误转换测试 | 所选实际兼容服务各一条接入证据 |
| 08-I | 清洁安装：基础、+感知、+主动、+工作；缺包零资源初始化 | 真实 Windows 产物启动与停用 |
| 08-J | 路由/鉴权/错误/脱敏测试，所有 R01～R09 有证据 | 每页按参考逐个审核；版本末再做联合回归 |

## 10. 实施拆分与发布边界

建议按“基线与权限 → 单次采集/OCR/VLM → Context → 主动邀请 → Work/ACP/MCP → 管理与组合回归”拆 SPEC；独立 adapter 可在公共契约稳定后单独开发。不要把本表当已经派发的 SPEC。

先跑模块及直接消费者回归，版本收口再验证代表包组合。真实资源缺失记 BLOCKED，未运行 NOT RUN；人工暂缓不等于通过。0.65 32 项保留原编号，重复场景可补统一证据但不得悄悄删除。相关：[0.7](../0.7/RPD.md)、[0.65 契约](../0.65/CONTRACTS.md)、[多源](../0.65/PROVIDERS.md)、[开发规范](../../development/PLUGIN_DEVELOPMENT.md)、[路线图](../RPD_ROADMAP.md)。
