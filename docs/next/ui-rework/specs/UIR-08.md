# UIR-08 · 集成验收与交付

状态：NOT_STARTED；日期：2026-09-25。关联 [PRD](../RPD.md)、[SPEC 索引](../SPEC.md)、[工程映射](../SOURCE_MAPPING.md)。

## 1. 目标与责任边界

覆盖：全部 UIR-R。前置：UIR-01～07。

所有直接消费者、正式 app/trial-backend.ts 启动、浏览器/Electron 控制台、桌宠与旧深链；只修本次引入的回归。

本专项 UI 可直接自行设计，不等待参考图或逐页批准。不得覆盖其他未提交修改；沿唯一运行时和既有存储权威完成直接消费者适配。

## 2. 实施步骤

1. 用真实用户数据副本/隔离测试目录验证升级：原角色/Persona/配置/资料/记忆/包状态保留，新覆盖默认兼容；不得直接改用户正式数据库做实验。
2. 正式后端运行六入口，完成配置角色→Playground→查看 Trace→Knowledge 来源/纠正/遗忘→Dashboard 的完整链；真实模型/设备证据与 fixture 分列。
3. 验证 Developer 默认隐藏/重启偏好/旧深链、窄窗口/焦点/IME、两角色/并发页面/断网/鉴权过期/取消/迟到状态。
4. 检查感知控制、Work 确认、插件资源释放与基础桌宠聊天/语音可用性；缺设备只标相关未验，不伪造通过。
5. 执行定向组合回归及 build/check/build:desktop，校验实际静态资源与 Electron 启动；交付页面截图和启动步骤、变更摘要、已知阻塞。
6. 更新本专项状态和证据，保留其他版本未验项。视觉问题可后续调整，不恢复逐页参考审批；必需功能/数据问题修复后才记开发完成。

## 3. 验收标准

| AC | 必需结果 | 当前证据 |
| --- | --- | --- |
| 08-A | UIR-R01～12 均有实现和证据链接，必需核心功能无 placeholder 代交付 | NOT RUN |
| 08-B | 正式进程端到端与持久化/升级/回退兼容验证通过，旧入口业务保留 | NOT RUN |
| 08-C | 六入口页面可运行且有截图；样式无需用户预审，功能/设备未验明确 | NOT RUN |
| 08-D | 无秘密/用户正文进入提交产物；自动通过与最终 ACCEPTED 分列 | NOT RUN |

## 4. 验证与交接

npm run check、npm run build、npm run build:desktop；运行新增 tests/ui-rework 与受影响 management/next08 回归，Electron smoke 按 UIR-00 固定命令。仅有新失败/更改再扩大验证，不默认重跑所有外部模型。

新增测试路径是计划文件，不表示已存在或通过。报告写入 `reports/UIR-08.md`（相对专项根）；记录基线/改动文件、公开契约及消费者、每项 AC、命令退出码、真实/fixture 区别和未运行项。下一步只能消费已有证据的能力。

## 5. 兼容、风险与未做项

回退保留兼容路由与原数据，新增配置覆盖采用可禁用/可恢复迁移。真实资源缺失为 BLOCKED；提供可复现启动说明，不能以浏览器 preview 成功代替生产交付。

