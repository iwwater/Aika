# K65-07 · 受约束流程编排与 Context 扩展

日期：2026-09-22；状态：AUTO_PASS（07-A～07-F 已通过；外部来源使用受控 handler fixture）。

前置：K65-06 AUTO_PASS。

需求与总验收：R02、R03、R04、R06、R08；K65-B/C/D/E/F/O。先读[执行索引](../SPEC.md)、[共享契约](../CONTRACTS.md)、[测试规则](../TESTING.md)和[源码核对](../SOURCE_AUDIT.md)。源码路径相对 windows/code/desktop-pet，新的逻辑类型不代表已存在导出。

## 源码与回归定位

core/dialogue-pipeline.ts 与 03 新默认流程、04 Context 接口、01/02 的公共能力注册；不能再次把全部业务复制到通用执行器。定向测试需覆盖 production executor 与真实测试来源插件。

这些是当前可读入口与后续任务产物引用；不将旧行号当作契约。

## 1. 本步实现边界

在已跑通的默认流程上引入 JSON Flow Profile 校验和执行：串行、受限条件、只读并行、稳定汇合。节点引用注册能力和契约版本，不能引用包内部文件；宿主无业务插件 ID 特判。

结构/依赖/类型/循环/能力启用就绪在激活前检查，失败保持原配置。预览仅静态图和条件，不触发模型/设备；实际初始化失败仍走运行错误。每轮固定 Profile、包版本和配置快照，来源撤销即时校验。

Context 来源声明来源 ID/修订、预算、前缀或后缀归属；过滤、去重、整体预算及删除/停用失效贯穿。创建新的只读测试来源插件，独立依赖公开 SDK；它不得成为普通包的默认必需能力。

只读并行按声明顺序合并，可选失败有明确降级；副作用串行且不自动重试整轮。阶段诊断从真实执行产生。


### 多源 Provider 补充

Profile stage 引用 bindingId/modelProfile 修订，明确要求能力与数据去向；每轮固定所有引用快照。缺 binding 且候选多个时不按注册顺序选择；本地失败不自动切云端。Context 来源（知识证据）与模型 SourceInstance 是不同概念，撤销机制不能混淆。 统一契约见 [PROVIDERS](../PROVIDERS.md)。

## 2. TDD 与逐项验收

新增行为先 RED，再实现、GREEN 和受影响回归；被测对象走真实 Kernel FlowRuntime/公开 handler 接线，网络、设备和来源使用受控 fixture，真实产品门槛单列。

- 07-A：PASS。FlowRuntime 在执行前拒绝缺 binding、未知 handler、未知输出和循环；预览只返回静态图，不调用 handler。
- 07-B：PASS。两个 local_read 节点并行执行并按声明输入汇合；`report_partial` 不重试副作用节点。
- 07-C：PASS。每轮冻结 profile/source revision，revision 变化使当前运行失败，绝不切换到另一个 binding。
- 07-D：PASS。执行器只依赖 capability/binding handler registry，不按包 ID 分支；内核 bundle 包含 FlowRuntime，普通包仍独立。
- 07-E：PASS。已有 03/05/06 默认路径继续回归；任意 stage 失败仅产生诊断和明确 partial/fail 状态。

- 07-F：PASS。两个阶段显式选择不同 binding，旧 run 的 source revision 失效后不会竞速或 fallback；handler 调用计数保持隔离。

## 3. 回归与交接

交付 Profile 格式、执行器与第三插件证据；08 使用实际依赖关系协调停用和更新。

只执行本步及受影响契约测试，定向命令由 00 清单和本步实际路径确定；不存在的命令先创建并验证非零用例。报告写 docs/next/0.65/reports/K65-07.md，逐 AC 记录命令、退出码、源码/产物版本、实际结果和限制，遵守 TESTING。不改 0.61 SPEC，不复制旧 Runtime，不提前实现 OCR/VLM、0.7 或下一步业务。

完成当前必需自动门槛后将索引本行更新为 AUTO_PASS；真实 Profile 管理 UI、设备播放和生产来源诊断由 09/10/11 验收。11 的产品验收状态另以用户明确确认作为依据。
