# K65-06 · STT 独立插件包与四种组合

日期：2026-09-21；状态：NOT RUN（执行规格已写，未开发/测试）。

前置：K65-05 AUTO_PASS（顺序要求，运行时不依赖 TTS）。

需求与总验收：R04、R05、R10、R13、R15；K65-B/G/K/L/P。先读[执行索引](../SPEC.md)、[共享契约](../CONTRACTS.md)、[测试规则](../TESTING.md)和[源码核对](../SOURCE_AUDIT.md)。源码路径相对 windows/code/desktop-pet，新的逻辑类型不代表已存在导出。

## 源码与回归定位

providers/sherpa-streaming-asr.ts、core/live-voice-turn.ts、core/live-voice-bridge.ts、app/backend-session.ts、media/browser-capture.ts；复用 tests/next/speechBridge.test.ts、tests/next61 中实际实时语音测例以及已交付真实语料。

这些是当前可读入口与后续任务产物引用，实施前按 00 的固定基线重新定位；不将旧行号当作契约。

## 1. 本步实现边界

把实时 ASR、输入会话/worker、麦克风/PCM 适配及专用 native 依赖移到 STT 包；保留已有已验收 batch 路径并明确标识。模型资源引用由包声明，不在基础安装携带或自动下载。

显式语音入口触发加载和设备租约；partial 仅 UI，finish 只提交一次最终文字到既有轮次权威，不重新 batch 识别，不为每 chunk 开整轮。只装 STT 时默认文字输出，不解析 TTS。

给 STT 提供独立打包目标和兼容 runtime/arch 检查。完成普通、+TTS、+STT、+两者四组合，既测试最小近期历史也覆盖兼容模式必要接线。


### 多源 Provider 补充

云端 batch、本地 streaming 作为不同 adapter 注册同 STT 能力，明确流式特性和资源约束。需要 streaming 的入口不能静默选 batch。未来其他本地引擎走同一契约，不增加唯一 local Provider 分支。 统一契约见 [PROVIDERS](../PROVIDERS.md)。

## 2. TDD 与逐项验收

新增行为先 RED，再实现、GREEN 和受影响回归；原样迁移复用特征测试。被测对象必须走真实生产接线，外部网络/设备可 fake，真实门槛单列。以下当前均 NOT RUN。

- 06-A：普通包+STT、无 TTS/播放实现时，固定 WAV→真实 streaming ASR→LLM→文字 UI 跑通，未选能力调用为零。
- 06-B：partial/final/repeated finish/取消/重连不重复提交；旧输入不污染新轮，无第二次 batch 调用，worker/采集退出可查。
- 06-C：真实 ASR 回放达到 00 冻结语料标准，记录首 partial/末 final、CER 与关键词；缺模型标 BLOCKED。
- 06-D：四种真实产物组合分别启动，未选包及其依赖缺席；资源/设备计数与输出匹配选择，纯文字路径不要求麦克风。

- 06-E：云端 batch 与本地 streaming 可共存且按绑定选择，streaming 请求选择 batch 时明确拒绝；未选引擎/设备未初始化，实际本地门槛仍按 06-C 验证。

## 3. 回归与交接

交付 STT 包和组合矩阵，为 07 的可配置流程提供已验证输入/输出能力。

只执行本步及受影响契约测试，定向命令由 00 清单和本步实际路径确定；不存在的命令先创建并验证非零用例。报告写 docs/next/0.65/reports/K65-06.md，逐 AC 记录命令、退出码、源码/产物版本、实际结果和限制，遵守 TESTING。不改 0.61 SPEC，不复制旧 Runtime，不提前实现 OCR/VLM、0.7 或下一步业务。

完成当前必需自动门槛才将索引本行更新为 AUTO_PASS；有阻塞记 BLOCKED，未运行记 NOT RUN。11 的产品验收状态另以用户明确确认作为依据。
