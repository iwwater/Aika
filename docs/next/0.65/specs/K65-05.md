# K65-05 · TTS 独立插件包

日期：2026-09-21；状态：NOT RUN（执行规格已写，未开发/测试）。

前置：K65-04 AUTO_PASS。

需求与总验收：R04、R05、R10、R13、R15；K65-B/G/K/L/P。先读[执行索引](../SPEC.md)、[共享契约](../CONTRACTS.md)、[测试规则](../TESTING.md)和[源码核对](../SOURCE_AUDIT.md)。源码路径相对 windows/code/desktop-pet，新的逻辑类型不代表已存在导出。

## 源码与回归定位

providers/qwen-tts.ts、qwen-audio-tts.ts、minimax-tts.ts、sapi-tts.ts、core/dialogue-pipeline.ts 与实际 playback/renderer 接线；复用 tests/providers/tts-download.test.ts、qwen-audio-tts.test.ts、minimax-tts.test.ts 和 speechBridge 的输出预期。

这些是当前可读入口与后续任务产物引用，实施前按 00 的固定基线重新定位；不将旧行号当作契约。

## 1. 本步实现边界

把现有 TTS Provider、语音配置适配、音频交付/播放接入和专用依赖拆为 TTS 包；保持实际已支持协议，不新增音色克隆。普通包提供通用展示订阅，TTS 包承担语音输出，不能要求 STT 或麦克风。

通过默认文字/语音输出能力选择触发 TTS 惰性加载；配置页面和元数据发现不初始化。播放事件、停止、迟到回调及音频文件清理沿用 Scope 保障，失败不重发已交付文字或重写消息。

提供单包构建产物，桌面播放所需 renderer 适配随 TTS 包加载，通过受控 IPC/公开接口注册，不静态夹带在普通包。共享运行资源登记所有者。


### 多源 Provider 补充

TTS 按“公共输出接线 + 所选合成 adapter”交付，多个云端/本地 adapter 可共存。本版至少一个既有云端与一个本地实现真实生成音频；SAPI 可作已存在本地候选，但不是唯一可扩展 local 类型。voice/model/format 按来源校验，公共播放接收规范化音频描述，不混用音色或采样率。仅用云端不携带其他本地引擎依赖。 统一契约见 [PROVIDERS](../PROVIDERS.md)。

## 2. TDD 与逐项验收

新增行为先 RED，再实现、GREEN 和受影响回归；原样迁移复用特征测试。被测对象必须走真实生产接线，外部网络/设备可 fake，真实门槛单列。以下当前均 NOT RUN。

- 05-A：普通包+TTS 在无 STT/sherpa/麦克风权限条件下完成文字→LLM→语音输出，实际产物依赖检查通过。
- 05-B：未启用/未触发时 TTS 入口无初始化；触发只加载 TTS 与显式必要依赖。
- 05-C：取消、播放失败、重复/迟到回调和撤销可停止语音，消息/回复不重复，资源清理结果真实可查。
- 05-D：至少一条已有 TTS 真实合成路径产出可解码音频；fixture 验证播放事件，真实听感留 11；独立 TTS 包无需重打内核/普通包。

- 05-E：云端和本地 TTS 分别真实生成可解码音频；同源多个音色与两来源绑定隔离，跨来源误用 voice 拒绝。只启用一个来源时另一引擎无初始化；新增第二本地 adapter fixture 无需修改内核。

## 3. 回归与交接

交付 TTS 包、产物命令及真实/fixture 分列证据；06 不把它当 STT 必需依赖。

只执行本步及受影响契约测试，定向命令由 00 清单和本步实际路径确定；不存在的命令先创建并验证非零用例。报告写 docs/next/0.65/reports/K65-05.md，逐 AC 记录命令、退出码、源码/产物版本、实际结果和限制，遵守 TESTING。不改 0.61 SPEC，不复制旧 Runtime，不提前实现 OCR/VLM、0.7 或下一步业务。

完成当前必需自动门槛才将索引本行更新为 AUTO_PASS；有阻塞记 BLOCKED，未运行记 NOT RUN。11 的产品验收状态另以用户明确确认作为依据。
