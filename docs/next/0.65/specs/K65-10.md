# K65-10 · 独立分发产物与整版自动验收

日期：2026-09-21；状态：NOT RUN（执行规格已写，未开发/测试）。

前置：K65-09 AUTO_PASS，00～08（含 02A）所有必需 AC 通过。

需求与总验收：R01～R19；K65-A～T。先读[执行索引](../SPEC.md)、[共享契约](../CONTRACTS.md)、[测试规则](../TESTING.md)和[源码核对](../SOURCE_AUDIT.md)。源码路径相对 windows/code/desktop-pet，新的逻辑类型不代表已存在导出。

## 源码与回归定位

package.json、tools/run-tests.mjs、03/05/06 单包构建与离库冒烟入口、09 UI 链路；按 00 的测试清单合并覆盖，不能只跑 npm test。

这些是当前可读入口与后续任务产物引用，实施前按 00 的固定基线重新定位；不将旧行号当作契约。

## 1. 本步实现边界

生成并核验内核、普通包、TTS、STT、兼容包与第三测试插件的独立产物/依赖/hash 清单，保留来源与许可证；只做本地交付，不推送发布。单包命令不可偷偷重打全部产品。

按 TESTING 在离开发树的干净安装目录运行四种组合及升级兼容组合，实际缺失可选代码/依赖，不只通过 disabled 配置。执行单独 TTS 更新、坏包恢复、来源撤销与并发取消、第三插件导入全链路。

汇总 K65-A～T 的证据归属与命令，现有相关 suites、test:next65、类型检查、Windows 构建和真实产物 UI 冒烟全部完成。单步已充分覆盖无需重复同一真实服务成本，只有精确版本证据可复用；必要整链回放不可省。

整版自动通过后才移交 11；缺原兼容资产或模型要明确 BLOCKED，不以全 fake 或关闭旧能力过关。


### 多源 Provider 补充

增加 K65-Q～T 的多源矩阵：同 adapter 两端点、同源两模型、不同本地 adapter、不同阶段不同来源、外部服务/宿主 worker 所有权。真实云/本地 LLM 与 TTS 分别有回放，独立引擎包仍受干净产物检查；第二本地 adapter 的 fixture 不能声称真实引擎已实现。 统一契约见 [PROVIDERS](../PROVIDERS.md)。

## 2. TDD 与逐项验收

新增行为先 RED，再实现、GREEN 和受影响回归；原样迁移复用特征测试。被测对象必须走真实生产接线，外部网络/设备可 fake，真实门槛单列。以下当前均 NOT RUN。

- 10-A：K65-A～T 每项有产物/源码修订匹配的证据，所有必需用例非零且通过，真实/fixture 分列。
- 10-B：最小安装、四组合、兼容升级、第三插件、单包更新在干净目录实际成功，无开发树解析依赖。
- 10-C：check/test:next65 及受影响 next/next61/windows/management/media/memory/providers/integration/UI 等实际覆盖清单通过，原用例不因拆包消失。
- 10-D：独立产物构建/启动、异常恢复和数据备份回退可复现；无敏感数据/权重入 Git，报告只标开发完成待人工验收。

- 10-E：K65-Q～T 全部有匹配版本的证据；云端/本地真实结果与协议 stub 分列，缺必需环境 BLOCKED，未选引擎依赖未进入对应安装产物。

## 3. 回归与交接

交付 RELEASE_MANIFEST.md 与逐 AC 汇总报告、人工清单；11 才能确认产品验收。

只执行本步及受影响契约测试，定向命令由 00 清单和本步实际路径确定；不存在的命令先创建并验证非零用例。报告写 docs/next/0.65/reports/K65-10.md，逐 AC 记录命令、退出码、源码/产物版本、实际结果和限制，遵守 TESTING。不改 0.61 SPEC，不复制旧 Runtime，不提前实现 OCR/VLM、0.7 或下一步业务。

完成当前必需自动门槛才将索引本行更新为 AUTO_PASS；有阻塞记 BLOCKED，未运行记 NOT RUN。11 的产品验收状态另以用户明确确认作为依据。
