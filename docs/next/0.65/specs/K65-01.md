# K65-01 · 公共插件契约与独立包格式

日期：2026-09-21；执行：2026-09-22；状态：**AUTO_PASS**（01-A/B/C/D/E 全部通过，见[验收报告](../reports/K65-01.md)）。

前置：K65-00 AUTO_PASS。

需求与总验收：R04、R06、R11、R14、R15；K65-E/O/P。先读[执行索引](../SPEC.md)、[共享契约](../CONTRACTS.md)、[测试规则](../TESTING.md)和[源码核对](../SOURCE_AUDIT.md)。源码路径相对 windows/code/desktop-pet，新的逻辑类型不代表已存在导出。

## 源码与回归定位

contracts/index.ts、contracts/management.ts、providers/slot-registry.ts；复用现有 Scope 与配置类型，新公共 SDK 位置由 00 映射确定。回归以契约/schema/包路径验证为主。

这些是当前可读入口与后续任务产物引用，实施前按 00 的固定基线重新定位；不将旧行号当作契约。

## 1. 本步实现边界

按 CONTRACTS.md 实现版本化 manifest、插件生命周期、宿主能力接口、稳定错误类别和 schema 校验。导入格式首版为目录包，entry 是包内已编译 ESM；不在用户导入阶段跑构建或安装脚本。发布可被包工程单独引用的最小 SDK 产物，不要求 npm 公布。

实现纯元数据校验与包构建辅助：身份/版本/文件 hash/entry 路径/平台运行时要求/能力 ID/依赖范围。拒绝越界和重解析项，发现和校验不得 import entry。具体文件路径以 00 映射落地，不把逻辑字段名误称既有类型。

能力类别至少覆盖文字对话、Context 来源、输入、输出、展示和后台生命周期。HostContext 不暴露核心实例私有字段；沿用配置与 credentialRef，不复制密钥。


### 多源 Provider 补充

按 PROVIDERS.md 增加 adapter/source/modelProfile/binding 的版本化 schema；同一 capabilityId 多 adapter 合法，冲突判定改为重复 adapter/plugin 身份。声明鉴权 none/credentialRef、三种部署、按能力分开的参数/输出约束与实例资源需求，旧云端必填字段不能套到全部本地引擎。 统一契约见 [PROVIDERS](../PROVIDERS.md)。

## 2. TDD 与逐项验收

新增行为先 RED，再实现、GREEN 和受影响回归；原样迁移复用特征测试。被测对象必须走真实生产接线，外部网络/设备可 fake，真实门槛单列。

**状态（2026-09-22 实测）**：五项全部 PASS。01-A 14 例（`packageManifest.test.ts`）、01-B 6 例（`entryExecution.test.ts`）、01-C 16 例（`dependencyBoundary` 7 + `dependencyBoundaryPlainImport` 4 + `importScanner` 5）、01-D 10 例（`packageBuild.test.ts`）、01-E 19 例（`capabilitySchema.test.ts`）；另 D3 `secretStore` 3 例。门槛 `check`/`build`/`sdk:next65` 退出码 0，`test:next65` 89/89、`test:next61` 148/148。逐项证据、计入的两处生产缺陷发现与 TEST_PLAN 映射见[验收报告](../reports/K65-01.md)。K65-00 三个基线特征测试的断言本步未改。

**收编补遗（2026-09-22 第二工位）**：补齐 spec §1「导入复制到宿主管理目录、校验后原子登记」——`plugins/package-import.ts` + `tests/next65/packageImport.test.ts` 7 例（01-F）；SlotBinding 冲突裁决以 `tests/next65/legacySlot.test.ts` 7 例冻结（报告 §10）。收编后门槛独立复跑：`test:next65` **103/103**、`test:next61` 148/148、定向 slot-registry 9/9 + fix61-01 8/8、`check`/`sdk:next65` 退出码 0。见[报告](../reports/K65-01.md) §9–§12。

- 01-A：合法普通/TTS/STT/测试包 manifest 通过；未知 schema、坏 hash、重复 adapter/plugin 身份、越界入口和 Windows 重解析项拒绝。
- 01-B：校验带可观察顶层副作用的测试入口时，该入口执行次数为零。
- 01-C：独立 fixture 工程只依赖 SDK 可编译，引用宿主/其他包私有路径被依赖边界检查拒绝。
- 01-D：包构建结果含 manifest/产物/依赖清单；同内容可验证，密钥和模型权重不进入包。

- 01-E：两个 adapter 提供同一能力合法；同 adapter 身份异内容拒绝；本地无 Key/无云费率可通过对应 schema，云端缺必需鉴权和不支持的参数被拒绝。

## 3. 回归与交接

冻结 API v1 与 schema v1、兼容规则及错误类别；02 不另造一套 manifest。

只执行本步及受影响契约测试，定向命令由 00 清单和本步实际路径确定；不存在的命令先创建并验证非零用例。报告写 docs/next/0.65/reports/K65-01.md，逐 AC 记录命令、退出码、源码/产物版本、实际结果和限制，遵守 TESTING。不改 0.61 SPEC，不复制旧 Runtime，不提前实现 OCR/VLM、0.7 或下一步业务。

完成当前必需自动门槛才将索引本行更新为 AUTO_PASS；有阻塞记 BLOCKED，未运行记 NOT RUN。11 的产品验收状态另以用户明确确认作为依据。
