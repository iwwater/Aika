# K65-00 · 固定交付基线与拆包边界

日期：2026-09-21；状态：NOT RUN（执行规格已写，未开发/测试）。

前置：FIX61-10 自动 AC 10-A/B/C 通过，0.61 可复现基线与自动报告可用；人工项 DEFERRED_TO_K65_11，不要求 0.61 ACCEPTED。自动门槛未满足时只做只读盘点，后续实施阻塞。

需求与总验收：R01、R09、R10、R15；支撑全部 AC。先读[执行索引](../SPEC.md)、[共享契约](../CONTRACTS.md)、[测试规则](../TESTING.md)和[源码核对](../SOURCE_AUDIT.md)。源码路径相对 windows/code/desktop-pet，新的逻辑类型不代表已存在导出。

## 源码与回归定位

app/trial-backend.ts、core/dialogue-pipeline.ts、core/desktop-runtime.ts、core/turn-controller.ts、package.json、desktop/build-web.mjs、tools/build-native.mjs、tools/run-tests.mjs；基线回归从 tests/next、tests/next61、tests/windows 和实际 memory/providers/management/integration 清单定位。

这些是当前可读入口与后续任务产物引用，实施前按 00 的固定基线重新定位；不将旧行号当作契约。

## 1. 本步实现边界

读取 SOURCE_AUDIT 中各入口及传递依赖，固定 0.61 提交或等效可复现源码快照、依赖锁、构建环境、数据模式和实际自动报告，附后置人工清单。不能只看索引顶行或把在途工作树当发布提交。保留现有未提交内容，不 reset、清目录或修改旧 Aika。

产出 BASELINE.md：源码/构建/接口映射、原测例清单、遗留项、最小运行资源、待拆硬依赖和迁移责任。内核仅留宿主通用机制；普通包含现有桌宠/LLM/文字/近期历史；兼容包承担长期 Memory/Timeline/知识库/冻结快照；TTS/STT 各自独立。对现有 Emotion、Work、Wake、WeChat、视觉等逐项登记：已验收已启用行为迁入对应可选兼容能力，未承诺业务不自动启用或扩大范围，不能遗漏后静默删除。

确定独立构建目录与包导入存储位置，映射公共 API 实际类型，记录加载/停止时限及语料指标依据。新增 test:next65 的非零测试发现入口，最少纳入原文字/取消特征用例，不提前实现加载器。


### 多源 Provider 补充

在基线盘点中记录每个现有 Provider 的 adapter/协议、部署方式、鉴权、模型/音色、streaming/batch 与资源所有者。固定本版真实验证选型：LLM 云端及一个本地服务，TTS 云端及一个本地实现，STT 既有云端 batch/本地 streaming；本地模型/服务不存在标明阻塞，不能把 stub 当真实部署。记录资源获取前置，不在本步自动安装模型。 统一契约见 [PROVIDERS](../PROVIDERS.md)。

## 2. TDD 与逐项验收

新增行为先 RED，再实现、GREEN 和受影响回归；原样迁移复用特征测试。被测对象必须走真实生产接线，外部网络/设备可 fake，真实门槛单列。以下当前均 NOT RUN。

- 00-A：基线修订、0.61 自动收口证据、后置人工项和遗留逐项可追溯；缺交付证据标 BLOCKED，不开放 01 实施。
- 00-B：原文字/取消/语音/记忆/快照/配置相关测例按清单实际运行，失败和缺环境如实报告，不冒充无回归。
- 00-C：依赖矩阵覆盖生产组合根、renderer、IPC、构建、native 依赖和后台；每项有唯一归属及迁移策略。
- 00-D：test:next65 有非零生产特征测试；空目录/零用例必须非零退出，基线资源与语料阈值在实现前登记。

- 00-E：多源矩阵覆盖现有提供者并给出真实回放来源、资源要求和未具备项；模型/音色标识按来源归属，不把所有 local 视为同一引擎。

## 3. 回归与交接

交付 BASELINE.md、可复现测试入口和包边界；01 据此实现接口。

只执行本步及受影响契约测试，定向命令由 00 清单和本步实际路径确定；不存在的命令先创建并验证非零用例。报告写 docs/next/0.65/reports/K65-00.md，逐 AC 记录命令、退出码、源码/产物版本、实际结果和限制，遵守 TESTING。不改 0.61 SPEC，不复制旧 Runtime，不提前实现 OCR/VLM、0.7 或下一步业务。

完成当前必需自动门槛才将索引本行更新为 AUTO_PASS；有阻塞记 BLOCKED，未运行记 NOT RUN。11 的产品验收状态另以用户明确确认作为依据。
