# K65-11 · 0.61 + 0.65 集中人工验收与版本交接

日期：2026-09-22；状态：PENDING_USER_ACCEPTANCE（10 自动门槛已通过；等待用户集中体验确认）。

前置：K65-10 AUTO_PASS。

需求与总验收：R01～R19；整版产品体验。先读[执行索引](../SPEC.md)、[共享契约](../CONTRACTS.md)、[测试规则](../TESTING.md)和[源码核对](../SOURCE_AUDIT.md)。源码路径相对 windows/code/desktop-pet，新的逻辑类型不代表已存在导出。

## 源码与回归定位

10 的 RELEASE_MANIFEST.md、报告与实际可运行产物；本步不新增业务，只闭环验收缺陷和交接。

这些是当前可读入口与后续任务产物引用；不将旧行号当作契约。

## 1. 本步实现边界

先合并 FIX61-10 后置的左右键/拖动、换肤、知识库、Key/模型设置、麦克风/实时 ASR、启动失败恢复清单，与本版相同体验去重并保留两版 AC 关联。生成简短人工清单：仅普通包启动/文字体验，分别导入 TTS/STT，四组合和真实设备打断，Profile 修改，单包更新/停用与重启，兼容数据和遗忘，失败诊断。用户集中确认，开发期间不反复要求开麦。

复现缺陷后先补回归、最小修复、定向验证；触及包契约/数据/独立依赖时重跑受影响整链，不能跳过门槛或伪造用户意见。

记录实际验收人反馈和版本提交，输出给 0.7 的插件 API/包格式、数据边界、已知限制与迁移文档。没有明确发布授权不推送、不上传产物。


### 多源 Provider 补充

人工清单增加云端/本地来源切换、同功能多模型或音色选择、来源错误定位及本地服务停止边界；验收指向实际交付 adapter 列表，不宣称支持所有部署产品。 统一契约见 [PROVIDERS](../PROVIDERS.md)。

## 2. TDD 与逐项验收

本步不新增业务代码；自动回归已由 K65-10 完成，人工清单逐项记录实际产物、设备和用户反馈。外部网络/设备可用受控 fixture 先排查，但人工体验结果不能由 fixture 代填。

- 11-A：PENDING。清单已固定到 K65-10 的实际产物版本；需用户逐项填写结果。
- 11-B：PENDING。当前自动门槛 138/138；若体验发现缺陷，先补复现/回归再重新运行受影响门槛。
- 11-C：PENDING。尚未记录 0.61 ACCEPTED 或 0.65 RELEASE_ACCEPTED，也不提前宣称 0.7 交接完成。

- 11-D：PENDING。人工清单单列云端/本地切换、同能力多模型/音色、来源错误定位和本地服务停止边界；未实际验证的 adapter 继续列为限制。

## 3. 回归与交接

完成本版验收交接，0.7 从实际交付基线开始，不预先实现其业务。

只执行本步及受影响契约测试，定向命令由 00 清单和本步实际路径确定；不存在的命令先创建并验证非零用例。报告写 docs/next/0.65/reports/K65-11.md，逐 AC 记录命令、退出码、源码/产物版本、实际结果和限制，遵守 TESTING。不改 0.61 SPEC，不复制旧 Runtime，不提前实现 OCR/VLM、0.7 或下一步业务。

人工验收未完成时本步为待验收，不因生成清单标 AUTO_PASS；用户明确确认且必需缺陷关闭后，索引本行标 RELEASE_ACCEPTED。有阻塞记 BLOCKED，未运行记 NOT RUN。
