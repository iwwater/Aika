# K65-09 · 包与流程管理 UI 及诊断

日期：2026-09-21；状态：NOT RUN（执行规格已写，未开发/测试）。

前置：K65-08 AUTO_PASS。

需求与总验收：R03、R07、R11、R12；K65-H/M/N。先读[执行索引](../SPEC.md)、[共享契约](../CONTRACTS.md)、[测试规则](../TESTING.md)和[源码核对](../SOURCE_AUDIT.md)。源码路径相对 windows/code/desktop-pet，新的逻辑类型不代表已存在导出。

## 源码与回归定位

management/ui/aika-view.mjs、management/aika-routes.ts、desktop/electron/main.mjs、desktop/electron/transport.mjs 及 08 管理接口；复用既有可信来源/配置/UI 测试，新增实际 DOM 操作与 IPC 验证。

这些是当前可读入口与后续任务产物引用，实施前按 00 的固定基线重新定位；不将旧行号当作契约。

## 1. 本步实现边界

在现有管理入口提供包列表、本地导入、依赖/资源检查、启用/停用、更新/卸载和生效状态。分开展示内核/包版本、启用意图、就绪、加载和待重启；缺可选包是正常状态，缺 LLM 配置提示修复而不假绿。

提供 Profile 复制、文本 JSON 编辑、校验预览、保存、激活和回退；不做拖拽画布。清晰区分安装包、导入配置、皮肤资产、角色包。实际权限按已有可信 renderer/IPC 边界，不让普通网页自由操作宿主。

展示每轮 Profile/包版本、stage 时间/跳过/失败/降级和资源释放状态。默认无正文/凭据；诊断保留有界且可清理。健康/列表刷新只读元数据与现有观察，不为点灯激活模块。


### 多源 Provider 补充

增加来源实例/模型音色配置/阶段绑定管理，展示协议、部署方式、端点或本地资源、鉴权需求和健康修订；可同时保存多个来源，显式选择，不只有全局云端/本地开关。列表发现与引擎加载独立；无 Key 本地表单不强求 Key 或费用。 统一契约见 [PROVIDERS](../PROVIDERS.md)。

## 2. TDD 与逐项验收

新增行为先 RED，再实现、GREEN 和受影响回归；原样迁移复用特征测试。被测对象必须走真实生产接线，外部网络/设备可 fake，真实门槛单列。以下当前均 NOT RUN。

- 09-A：真实 UI/管理 API 自动完成包导入启停/更新和 Profile 编辑激活回退，界面状态与生产宿主一致。
- 09-B：缺包、缺资源、失败、待加载、待重启准确区分，错误有定位和恢复入口；草稿失败不覆盖有效配置。
- 09-C：开面板/刷新/预览无模型、网络推理或采集副作用；凭据不进入 DOM/日志，IPC 保留来源校验。
- 09-D：诊断实际关联轮次、stage 和包版本，取消/跳过/降级不误标成功；旧轮与新活动配置可区分。

- 09-E：UI 自动完成同 adapter 两实例、同源两模型/音色及跨阶段绑定；保存不加载，选定后实际请求正确；来源故障不将整个能力所有来源标红，凭据保持脱敏。

## 3. 回归与交接

交付管理操作链和脱敏诊断，10 用实际产物做整版验收。

只执行本步及受影响契约测试，定向命令由 00 清单和本步实际路径确定；不存在的命令先创建并验证非零用例。报告写 docs/next/0.65/reports/K65-09.md，逐 AC 记录命令、退出码、源码/产物版本、实际结果和限制，遵守 TESTING。不改 0.61 SPEC，不复制旧 Runtime，不提前实现 OCR/VLM、0.7 或下一步业务。

完成当前必需自动门槛才将索引本行更新为 AUTO_PASS；有阻塞记 BLOCKED，未运行记 NOT RUN。11 的产品验收状态另以用户明确确认作为依据。
