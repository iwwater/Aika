# K65-04 · 既有长期能力兼容包与迁移

日期：2026-09-21；状态：NOT RUN（执行规格已写，未开发/测试）。

前置：K65-03 AUTO_PASS。

需求与总验收：R01、R05、R09；K65-A/F/I/J。先读[执行索引](../SPEC.md)、[共享契约](../CONTRACTS.md)、[测试规则](../TESTING.md)和[源码核对](../SOURCE_AUDIT.md)。源码路径相对 windows/code/desktop-pet，新的逻辑类型不代表已存在导出。

## 源码与回归定位

memory/sqlite-port.ts、memory/sqlite-lifecycle-port.ts、memory/knowledge-library.ts、memory/prefix-snapshot.ts、management/aika-timeline.ts；复用 tests/next/memory.contract.test.ts 及现有 provenance/forget/knowledge/prefix 测例，实际文件由 00 固定。

这些是当前可读入口与后续任务产物引用，实施前按 00 的固定基线重新定位；不将旧行号当作契约。

## 1. 本步实现边界

把 00 登记的长期 Memory、Timeline、知识库、冻结快照等迁入可选兼容包，通过公开能力/后台生命周期接线。兼容包可含多个相关插件，但不夹带强制音频/视觉。实际已验收 Emotion/Work 等按 00 归属适配；无新增 OCR/VLM 功能或 0.7 Soul/Wiki。

复用原数据库与撤销权威，不复制原消息成新权威库。定义普通包近期历史与兼容历史的交接：启用后固定唯一 writer、稳定 messageId 去重，切换读取来源明确；不自动把停用期间聊天提升为长期事实。升级保留已启用组合，初次最小安装不自启兼容包。

做数据备份、版本化幂等迁移与失败恢复。兼容来源撤销和切换立即使旧 Context/快照失效，后台旧结果不可写回。尚无 08 的运行中卸载入口时，以受控关闭/重启测试生命周期，不绕过后续门槛。


### 多源 Provider 补充

旧 dialogue/memory_turn/summary/admission 等槽迁移成不同 Binding，可选择不同 source/modelProfile，沿用原严格记忆解析、数据来源与预算规则；同轮不同阶段的来源修订记录独立，不建立全局唯一当前模型。 统一契约见 [PROVIDERS](../PROVIDERS.md)。

## 2. TDD 与逐项验收

新增行为先 RED，再实现、GREEN 和受影响回归；原样迁移复用特征测试。被测对象必须走真实生产接线，外部网络/设备可 fake，真实门槛单列。以下当前均 NOT RUN。

- 04-A：启用兼容包后原 Memory/Timeline/知识库/快照与严格遗忘测例通过；同一消息只有唯一写入权威和来源关系。
- 04-B：仅普通包时不加载兼容包/专用维护依赖；从兼容模式回到最小模式不会注入旧包快照内容。
- 04-C：0.61 数据副本迁移两次结果一致，失败可恢复备份；原消息/来源/撤销标记保留，重启不复活。
- 04-D：修订、切库/遗忘与模型返回/后台写回交错无污染；已验收其他能力逐项有保留证据或明确阻塞，不静默删功能。

- 04-E：fixture 中聊天选云端 A、摘要/记忆选本地 B，生产请求各用自己的模型/凭据/参数；切来源不丢原记忆语义，重复迁移无重复来源记录。

## 3. 回归与交接

交付升级兼容配置与数据版本清单，05/06 在新宿主验证两种存储组合。

只执行本步及受影响契约测试，定向命令由 00 清单和本步实际路径确定；不存在的命令先创建并验证非零用例。报告写 docs/next/0.65/reports/K65-04.md，逐 AC 记录命令、退出码、源码/产物版本、实际结果和限制，遵守 TESTING。不改 0.61 SPEC，不复制旧 Runtime，不提前实现 OCR/VLM、0.7 或下一步业务。

完成当前必需自动门槛才将索引本行更新为 AUTO_PASS；有阻塞记 BLOCKED，未运行记 NOT RUN。11 的产品验收状态另以用户明确确认作为依据。
