# K65-02A · 多源 Provider 注册、绑定与实例生命周期

日期：2026-09-21；状态：NOT RUN（新增执行规格，未开发/测试）。

前置：K65-02 AUTO_PASS。后继：K65-03；这是插入的实施步骤，不改变其他编号，也不是第二套包加载器。

需求：K65-R16～R19；总验收 K65-Q～T。必读：[SPEC](../SPEC.md)、[CONTRACTS](../CONTRACTS.md)、[多源契约](../PROVIDERS.md)、[TESTING](../TESTING.md)。

## 1. 源码与实现边界

providers/slot-registry.ts 当前 SlotBinding 直接携端点/模型/凭据，protocol 只有有限内置值，校验强制 credentialRef 及部分计费参数；不能把它当作已具备本地无 Key、多实例管理。先核对 00 基线，再通过薄迁移适配保留已验收协议与设置。

实现 AdapterDescriptor、SourceInstance、ModelProfile、Binding 和不可变 ResolvedBinding。adapter 来自 01/02 插件注册；同能力多 adapter 合法，重名身份冲突拒绝。来源/模型/绑定各有稳定 ID 和修订，同一 adapter 多端点、同来源多模型、不同阶段多来源并存。迁移现有 SlotBinding，重复迁移不复制配置，不丢 credentialRef 与预算策略。

管理 API 先提供保存/读取/显式校验/解析和状态入口，图形操作由 09 接入。保存草稿不加载引擎；活动引用要求能力与资源匹配，列表成功不等于可推理。能力协商按 adapter 与模型特性交集，不按模型名称猜测，不静默忽略未知参数。

实现 remote-api、用户所有 local-service 和宿主管理 managed-local 的生命周期策略。测试使用真实生产 registry/loader 和可控本地服务/worker。local-service 停用只关闭自有连接；managed-local 延迟启动、租约、排空/取消、超时和关闭释放。不得构建通用模型安装器或修改用户已有服务进程。

按完整来源/模型/资源修订隔离实例缓存，连接与模型资源分别复用。固定来源策略，暂无自动 fallback 或并发竞速；切配置下一轮生效，权限撤销即时生效。健康状态按来源和模型/配置修订记录，不按 adapter 整包标绿。

## 2. TDD 与验收

- 02A-A：同 capability 两 adapter、同 adapter 两端点、同源两模型可保存/解析；结果稳定，凭据/模型/健康不串线，候选多而缺 binding 时拒绝任意选择。
- 02A-B：无 Key 本地服务成功解析并实际调用；需要 Key 的云端配置缺凭据明确拒绝，未使用来源缺配置不阻塞所选来源；费用 unknown 不伪报零且保留适用预算约束。
- 02A-C：已启用但未选来源初始化为零，所选实例并发首次调用初始化一次；实例启动失败不污染其他来源；同模型名不同实例不共用错误缓存/资源。
- 02A-D：受控本地 worker 随最后租约释放，外部本地服务在解绑/停用后仍存活；取消后迟到事件无效，超时和队列有界；测试记录 PID/资源而非仅 mock 调用。
- 02A-E：同轮固定解析修订，下一轮切换新来源；流式中途失败不混入另一个来源，local-only 路径对云端请求计数为零，无自动重试已交付结果。
- 02A-F：旧七槽迁移两次幂等，原配置可追溯恢复；adapter 能力不足/专有参数误传拒绝；新增本地 fixture adapter 只安装独立包即可接入，宿主代码不修改。

所有新增 AC 当前 NOT RUN。真实 LLM/TTS 来源门槛分别由 03/05 实施；本步协议 stub/worker 测试不能冒充实际模型验收。

## 3. 交接与报告

先 RED 再 GREEN，定向复用 slot-registry/配置/取消用例，不删除原协议断言。报告 docs/next/0.65/reports/K65-02A.md；交付 schema、迁移结果、解析接口、三类生命周期与状态错误，03/05/06 复用它们，不各建来源管理器。必需 AC 全过才 AUTO_PASS，缺环境 BLOCKED。本步不改 0.61 SPEC 或实际业务范围。
