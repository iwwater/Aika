# K65-07 自动验收报告

日期：2026-09-22；状态：AUTO_PASS（07-A～07-F 已通过；前置：K65-06 AUTO_PASS）。

## 交付

- `kernel/flow-runtime.ts` 实现受限 JSON Flow Profile 执行：能力/binding 显式匹配、输入输出引用校验、条件规则、无循环拓扑、只读并行和副作用串行。
- 执行前对 profile 做结构与 handler registry 校验；`preview()` 只生成静态节点图，不初始化 provider、设备或外部来源。
- 每轮复制并冻结 profile/source revision；来源变更在 stage 前后检查，陈旧结果失败且不会自动换源或重试整轮。
- `contracts/index.ts` 暴露冻结的 Flow Profile 公共契约；内核 bundle 入口同时导出最小轮次内核与 FlowRuntime。

## 测试证据

新增 `tests/next65/flowRuntime.test.ts`，覆盖：

1. 缺 binding、未知 handler、未知输出和非法图拒绝；preview 无 handler 调用。
2. 两个只读来源以不同完成顺序并行，按声明输入稳定汇合。
3. source revision 变化使旧 run 失效，无 fallback；partial 策略不重试副作用节点。

实际命令（工作目录 `windows/code/desktop-pet`）：

| 命令 | 结果 |
| --- | --- |
| `npm run check` | PASS，TypeScript 0 errors |
| `node --test dist/tests/next65/flowRuntime.test.js dist/tests/next65/minimalKernel.test.js` | PASS，9/9 |
| `npm run test:next65` | PASS，139/139，退出码 0 |

## 限制与交接

本步用受控 handler/source fixture 验证执行语义，尚未接入产品管理 UI、真实设备播放和生产多来源诊断。Profile 的更新/删除事务与 UI 在 K65-08/09，整版产物和人工验收在 K65-10/11。
