# K65-00 验收报告 · 固定交付基线与拆包边界

- SPEC：[K65-00](../specs/K65-00.md)。需求：R01、R09、R10、R15；支撑全部 AC。
- 基线：HEAD `d7c61d674374d760e57d18ebe2bf0b42c7e94c4b`（分支 `aika-next`）+ 未提交工作树（`30 modified + 75 untracked`）。冻结窗口 `2026-09-21T20:02:59+08:00`，**再固化窗口 `2026-09-21T20:30:43+08:00`**（见 §00-A 末段：并行 worker 在冻结后改动 4 个文件，本步重新记录 hash 并复跑门槛）。
- 日期：2026-09-21。执行：K65-00 执行 worker。
- 状态：**00-A PASS · 00-B PASS（失败项逐条核实为既有/环境限制）· 00-C PASS · 00-D PASS · 00-E PASS**。
- 基线快照：[BASELINE.md](BASELINE.md)。原始证据（TAP 汇总、守卫退出码、CER 诊断、既有失败原始错误文本、`git` 探针）：[reports/evidence/K65-00-raw.txt](evidence/K65-00-raw.txt)。

**本步只做基线固定与拆包边界盘点，不启动 K65-01 及任何下游步骤**，不实现包加载器，不改 0.61 SPEC，不复制旧 Runtime，不动 `F:/AIVoice/Aika` 与 `F:/AIVoice/pet-shell`，不 commit / push / stash / reset。

## 1. 改动

| 文件 | 改动 |
| --- | --- |
| `windows/code/desktop-pet/tools/run-tests.mjs` | 新增 `next65` 分支（现有 `next61` 守卫模式的同构实现） |
| `windows/code/desktop-pet/package.json` | 新增脚本 `"test:next65": "npm run build && node tools/run-tests.mjs next65"` |
| `windows/code/desktop-pet/tests/next65/baselineSurface.test.ts`（新） | 10 用例：组合根/凭据读取点/轮次权威/槽词表/端点两层分歧/双 IPC 版本/渲染产物自足 |
| `windows/code/desktop-pet/tests/next65/baselineBehavior.test.ts`（新） | 7 用例：生产 Runtime 的文字/取消/失败/清理特征 + **真实模型 CER 门槛** + framing 常量 |
| `windows/code/desktop-pet/tests/next65/rendererArtifact.test.mjs`（新） | 4 用例：真实产物 VM link/求值/内联判定/worklet 边车 |
| `docs/next/0.65/reports/BASELINE.md`（新） | 七节基线 |
| `docs/next/0.65/reports/K65-00.md`（新） | 本报告 |

**零生产代码改动**（`app/`、`core/`、`providers/`、`memory/`、`management/`、`contracts/`、`desktop/` 全部未编辑）。**未改动** `desktop/electron/main.mjs`（并行 worker 归属）。

## 2. 逐 AC 证据

### 00-A：基线修订、0.61 自动收口证据、后置人工项和遗留逐项可追溯 → **PASS**

**前置核实**：`docs/next/0.61/reports/FIX61-10.md` 与 `docs/next/RUN_061_065.md` **实际存在**，其 10-A/B/C 为**真实交付证据**而非声明：

| 核验点 | 结果 |
| --- | --- |
| 10-A 逐条 AC | 报告给出 01～09 的 SPEC/状态/主命令/用例数/真实-vs-fixture 五列表，并显式记录"三处合法后置"与"真实远程全轮 NOT RUN（无凭据）"，未冒充 |
| 10-B 六项门槛 | 报告给出命令/退出码/计数/判定四列，含 `test:windows:ui` 的"文本 + 退出码双判据" |
| 10-C 数据安全 | 七项检查逐条给出证据（含 `git grep` 零命中、`git ls-files` 无 `.wav/.onnx/.sqlite`） |
| 10-D 人工 | `DEFERRED_TO_K65_11`，明确"未代签、未标 ACCEPTED" |
| 收口口径 | FIX61-10 §7 自记 `PROVISIONAL / PENDING_REFREEZE`（因 token 缺陷未修），**不谎称冻结** |

按 [EXECUTION_061_065.md](../../EXECUTION_061_065.md) §1 与根 AGENTS §5（"FIX61-10 自动 AC 通过且基线可复现即可进入 K65-00；人工项记 DEFERRED_TO_K65_11，不要求 0.61 ACCEPTED"），**前置满足，不标 BLOCKED**。

**修订再固定**：`BASELINE.md` §1/§3 给出 HEAD + 冻结窗口 + **30 个文件的 SHA256 实测值**（含 4 个关键 `.mjs`、3 个构建工具、`package-lock.json`、`tsconfig.json`）。工作树 dirty 状态显式记录为"可复现快照，不是发布提交"。

**遗留项逐项登记（无静默删除）**：
- 0.61 已知未修缺陷：`desktop/electron/main.mjs` `open_management` 的 `new URL(value.path, url)` 覆盖 base 的 `#token=…` → token 丢失 → 功能面板「外观/换肤」进入未授权锁定态。本步**未修**（归并行 FIX61-11 worker）。
  - **⚠️ 执行期间状态变化（如实记录）**：该修复**在本步执行中被并行 FIX61-11 worker 落地**（`desktop/electron/main.mjs` 20:28:42、`tools/management-url.mjs` 20:28:38）。新实现新增 `managementTarget(session, route)`：先校验 `target.origin === base.origin`，再把 session fragment 与 route fragment **合并**（session token 在前，因 `management/ui/app.mjs` 读第一个 `token`），并在 `main.mjs` 的 `value.path` 正则上加了 `(?!\/)` 以拒绝协议相对路由。**本步未编辑这两个文件，未审阅该修复的正确性，也未重跑 `test:windows:ui` 验证锁定态已解除**——该验证属重冻结流程（`RUN_061_065.md` §6）。本次冻结的 hash 因此在该文件上**已过期**，`BASELINE.md` §3 同时记录初值与再固化值。
- 0.61 人工 7 项 → `DEFERRED_TO_K65_11`。
- 既有失败 9 项 → §00-B 逐条。
- 无 macOS 环境的 FIX61-03 Swift 分支、无凭据的真实远程 → 沿用 FIX61-10 §7 的 NOT RUN 记录。

**冻结后并发改动的诚实披露**：`2026-09-21T20:02:59` 冻结窗口之后，构建单元内有 4 个文件被并行 worker 改动，**均非本步所为**（本步零生产代码改动）：

| 文件 | 改动时间 | 新 SHA256 | 影响 |
| --- | --- | --- | --- |
| `desktop/electron/main.mjs` | 20:28:42 | `96FD6F254631375A02E906CAF2C61376B9B8F4576D864E109727224487A4E278` | FIX61-11 token 修复（上文） |
| `tools/management-url.mjs` | 20:28:38 | `8780358A63649035C257C25AB02B2A077A5593BE63FE327FE5FCC54F46E00D0C` | 同上，新增 `managementTarget` |
| `app/evaluate-lifecycle.ts` | 20:15:21 | `425EAA455D1E9E8CE5B36660774A8CE662F49B02E0C5FDBACD66C4D006CC118E` | 非本步范围 |
| `app/evaluate-source-budget.ts` | 20:17:00 | `F89040D2BEF7637FEAE9BE18F3900683417493C9C7E1DDC0D8610F2DEF51B97F` | 非本步范围 |

**本步关键依赖文件的 hash 截至再固化窗口仍与初值一致**（已逐个复算）：`app/trial-backend.ts`、`providers/transport.ts`、`providers/slot-registry.ts`、`management/settings.ts` **UNCHANGED**；`desktop/build/renderer.js` **UNCHANGED**（其输入 `desktop/main.mjs` 未被改动）。改动后 `npm run test:next65` 复跑仍 **21/21 pass，退出码 0**。

### 00-B：原测例按清单实际运行，失败和缺环境如实报告 → **PASS**

主套件（cwd `windows/code/desktop-pet/`，2026-09-21 实跑）：

| 命令 | 退出码 | tests | pass | fail | skipped | 耗时 |
| --- | --- | --- | --- | --- | --- | --- |
| `npm run check` | **0** | — | — | — | — | tsc 无错误 |
| `npm run build` | **0** | — | — | — | — | tsc + build-wechat |
| `npm run test:next` | **0** | 80 | **80** | 0 | 0 | 0.9 s |
| `npm run test:next61` | **0** | 143 | **143** | 0 | 0 | 41.4 s |
| `npm run test:windows` | **0** | 22 | **22** | 0 | 0 | 16.7 s |
| `node --test dist/tests/media/*.test.js` | **0** | 63 | **63** | 0 | 0 | 0.4 s |
| `npm run test:next65` | **0** | 21 | **21** | 0 | 0 | 0.7 s |

`test:next65` 是本步新增的门槛，故**不是**"无回归"的既证；它是**新证据**。回归结论只由上表前六行支撑。

**integration 逐文件隔离实跑**（`node --test --test-timeout=60000 <单文件>`，一次一个文件，不并发）：**32 文件，261 tests / 247 pass / 6 fail / 3 skipped**。

| 退出码 | tests | pass | fail | 真实原因（实测错误文本） | 既有性判定 |
| --- | --- | --- | --- | --- | --- |
| `backend-session.test` | 1 | 8 | 6 | 2 | `ERR_MODULE_NOT_FOUND: Cannot find module '…\tests\desktop\ui-harness.mjs'` | `TRACKED@HEAD` + 工作树无改动；`git log --all -- "*ui-harness*"` **为空** → 该文件**从未存在于 git** |
| `capture-feedback-wire.test` | 1 | 1 | 0 | 1 | 同上 `ui-harness.mjs` 缺失 | 同上 |
| `clear-reminders.test` | 1 | 30 | 29 | 1 | 同上 `ui-harness.mjs` 缺失 | 同上 |
| `deepseek-text-update.test` | 1 | 1 | 0 | 1 | `ERR_MODULE_NOT_FOUND: Cannot find module '…\tools\prepare-trial-update.mjs'` | `TRACKED@HEAD`；`git log --all -- "…prepare-trial-update.mjs"` **为空** → 从未存在 |
| `memory-trial.test` | 1 | 7 | 6 | 1 | `AssertionError: The input did not match /Bounded trial is stopped/. Input: 'Error: Invalid explicit evaluation paths or run ID'` | `TRACKED@HEAD`；`evaluate-lifecycle.ts` 要求 POSIX 绝对路径，Windows-only 环境限制 |
| `pending-memory-management.test` | 挂起 | — | — | — | 单跑 **>300 s 未返回**（`--test-timeout=60000` 未截杀，进程 `10032` 起于 19:35，早于本步）；隔离扫描中亦未产出 TAP 汇总 | 既有挂起，与 `memory-dynamics-http.test.js` 同类 |
| 其余 26 文件 | **0** | — | 全 pass | 0 | — | 无回归 |

**`tests/integration/trial-update.test.ts:11-13` 的 4 个缺失工具**（侦察记录项）**已复核**：该文件**不在** `dist/tests/integration/*.test.js` 集合内，即当前**不属任何测试组**，故本步扫描未覆盖它。属**登记在案的加载期失败**（`tools/install-trial-update.mjs`、`prepare-companion-update.mjs`、`prepare-trial-update.mjs`、`upgrade-trial-budget.mjs` 均不存在），**如实记录，未修**。

**真实回放（`npm run test:next:real`）—— 比"全部 BLOCKED"更细的实测**：

| 场景 | 退出码 | 结果 |
| --- | --- | --- |
| 无 `PET_NEXT_REAL` | **2** | `NEXT-REAL BLOCKED: real-service replay requires PET_NEXT_REAL=1 plus authorized credentials…`（显式 BLOCKED，无静默 fallback） |
| `PET_NEXT_REAL=1` | **0** | tests 5 / pass 3 / **skipped 2** |

`PET_NEXT_REAL=1` 下逐项：`08-D real LLM`（DeepSeek `deepseek-flash`，2 用例 **PASS**，1508/2234 ms）、`06-F real SAPI TTS`（音色 `Microsoft Huihui Desktop`，**138286 B / 16000 Hz / durationMs=4320**，与 0.6 CORPUS_MANIFEST 登记值一致，**PASS**）、`06-E real whisper ASR`（2 用例 **skip**：`whisper-server not reachable at http://127.0.0.1:8080`）。

由此修正侦察简报的一处表述：**云端 DeepSeek 真实回放与本地 SAPI 真实合成在本机是可跑的**（0.6 已配置），真正缺的是 whisper-server 进程。但**用户已明确决定：local-LLM 真实回放与 cloud 真实回放（按 0.65 多源口径）记 BLOCKED**，理由与资源名见 §00-E；上表只陈述 0.6 侧已配置事实，**不据此声称 0.65 多源门槛已通过**。

### 00-C：依赖矩阵（组合根 / renderer / IPC / 构建 / native / 后台），每项有唯一归属及迁移策略 → **PASS**

#### C-1 生产组合根 = `app/trial-backend.ts`

| 事实（实测） | 迁移策略 |
| --- | --- |
| **68 个顶层 static import**（侦察简报记 47；工作树已被并行 worker 改动，现值以本步实测 68 为准） | 唯一组合根；03 起按其实际 import 图拆产物，**不是**改动态 import |
| **`StrictTrialMemoryProvider` 定义在组合根内部**（`trial-backend.ts:153`），并被 4 个测试文件导入：`tests/management/strict-forget-integration.test.ts:16`、`tests/integration/strict-runtime-dynamics.test.ts:5`、`tests/integration/trial-launcher.test.ts:6`、`tests/integration/memory-import-49.test.ts:129` | 迁到长期 Memory 兼容包时**必须同时更新这 4 个消费者**，否则回归断链（归属 04） |
| `keyReader()` `trial-backend.ts:199-212` 是**唯一凭据读取点**：activation 哈希匹配 + `realpathSync` 项目外校验 + `isPrivateFileSync` 私有 ACL + `/^sk-[A-Za-z0-9_-]+$/` | 迁移时保持单点；本地无钥来源需改此处（见 00-E 五段链） |
| `TrialTransport` `trial-backend.ts:72-131` 是**生产计费/边界守卫**，不是普通 transport：`:104-114` 对 `MINIMAX_TTS_MODEL` 逐字段白名单；`:116` 经 `isQwenAudioTtsModel()` 分支 `instruction`/`instructions` | 任何新 TTS adapter **必须**扩展此类的 `operation === 'tts'` 分支，否则请求被拒 |
| `createTrialTtsProvider()` `:133-150`：`minimax-tts` / `qwen-audio-tts` / `qwen-tts-instruct` 三个分支，其余 `throw new Error('Unregistered speech adapter')`（`:149`） | 05 接入本地 TTS 的**唯一**注册点 |
| `:246-251` 声明 6 个可选句柄 `wechat`/`wake`/`managementForget`/`memoryImport`/`desktopWork`/`management`，各自 try/catch 或 optional | **可复用为"缺席功能包不崩溃"的现成范式**（02/03） |

#### C-2 renderer（生产薄入口）

| 事实 | 迁移策略 |
| --- | --- |
| 单一 esbuild 入口 `desktop/main.mjs` → `desktop/build/renderer.js`（722269 B，SHA256 `D29DC83E9405AB1B…`） | 03 拆最小产品时保持单入口，避免多产物解析 |
| 产物 **`node:` 0 次、`require(` 0 次** | 已有产物级证据（非源码字符串） |
| **VM realm `SourceTextModule.link()` 实测 specifiers = []** | 产物完全自足 |
| ⚠️ 侦察提出的关键疑点**已用真实产物求解**：`desktop/wake-controller.mjs` 无顶层 import、仅经 `import('../media/wake/browser-capture.ts')`；产物内部已被 esbuild 改写为 `Promise.resolve().then(() => (init_browser_capture(), browser_capture_exports))`，**产物内不存在 `import(` 调用点**，`media/wake/browser-capture.ts` 以 `init_browser_capture` 内联存在。**结论：动态 import 未存活，不存在"打包后相对 URL 丢失"风险。** | 05/06 拆包时可沿用此判定方法（真实 link，非源串断言） |
| 求值失败于 `ReferenceError: Live2DCubismCore is not defined`（Electron 注入全局），**不是模块解析错误** | 降级可见、非静默 |
| worklet 边车 `recorder-worklet.js`(3536 B) / `wake-recorder-worklet.js`(1346 B) 实际存在 | 打包清单须含它们 |
| `desktop/*.mjs`（含 `wake-controller.mjs`、`main.mjs`、`capture-feedback.mjs` 等 14 个）**不在 `tsc` 覆盖内** | 03 拆包时须为这些文件建立独立类型/构建检查，否则拆包后无编译保障 |

#### C-3 IPC = **三个边界，不是一**个

| 边界 | 契约 | 现状 | 归属 |
| --- | --- | --- | --- |
| (a) renderer ↔ Electron | `desktop/electron/preload.cjs`：`channels = Set(['desktop','shell','diagnostic'])`、`methods = Set([...7 项])` 白名单；主进程 `contextIsolation: true, nodeIntegration: false, sandbox: true`、`trusted()` 校验 `pet://app/index.html` | 唯一 renderer IPC 面，白名单收窄 | 03（普通包界面接线）；**不要新增第二通道绕过白名单** |
| (b) Electron main ↔ backend | `contracts/desktop-bridge.ts`，`DESKTOP_BRIDGE_VERSION = '0.7.0'`；`desktop/electron/transport.mjs` 走 NDJSON stdio，4 个启动窗口（`timeoutMs`/`noProgressMs` 60000、`maxStartupMs` 600000、`shutdownTimeoutMs` 10000） | 版本常量冻结；**本步未 bump** | 03 |
| (c) management HTTP API | `management/server.ts` 监听 `0.0.0.0`→实际 `listen(0,'127.0.0.1')`，Bearer token，`management/*.ts` 共 **72 个 `/api/*` 路由字面量** | **单一配置权威真正所在** | 02A/09；09 只展示不复制状态 |

⚠️ 简报称"~20 routes"，实测为 **72 个路由字面量**（去重后）。以此为准。

#### C-4 构建

| 事实 | 迁移策略（归属 03/10） |
| --- | --- |
| **无 workspaces、无 project references、单一 `tsconfig.json`**（`rootDir:"."`、`outDir:"dist"`、`include` 列 11 个目录） | 0.65 拆包的**第一道硬墙**；须先建立独立构建目录与包导入位置 |
| `npm run build` = `tsc -p tsconfig.json && node tools/build-wechat.mjs` → **wechat 恒随主构建** | 拆包时必须解除 |
| `npm run build:windows` = `build + build:desktop + build:wake + build:native` | `build:wake` 只在 windows 目标连带 |
| `tools/build-native.mjs` win32 分支**只** `access(require('electron'))` 并打印一行，**不产出任何分发包** | **不是**可分发安装器（10 的交付物） |
| `tools/build-wake.mjs` 对 `dist/media/wake/detector-worker.js` 做**字节级字符串替换**，断言 needle `require('sherpa-onnx-node')` **恰好出现 1 次**（`body.split(needle).length!==2` 即抛错）；已替换则 `exit 0` | ⚠️ **改 `media/wake/detector-worker.ts` 的 import 写法会直接打断此构建**；02/03 触及该 worker 时必须同步改 `build-wake.mjs` |
| `tools/build-wake.mjs` 另把 `sherpa-onnx-node` + 平台包 `cp` 进 `dist/media/wake/vendor/`，并**校验版本必须为 1.13.8** | 拆包须保留 vendor 版本断言 |
| `tools/build-wechat.mjs` 把 `silk-wasm/lib/silk.wasm` 复制到 `dist/wechat/silk.wasm`（因 silk-wasm 按相对 URL 解析） | wechat 包拆分须带上 wasm |

#### C-5 native 依赖（每项唯一归属）

| 依赖 | 版本 | 生产使用点 | 归属与迁移策略 |
| --- | --- | --- | --- |
| `better-sqlite3` | 12.11.1 | **24 个生产文件**引用；其中 12 个是 `import Database`（值导入、真跑 native），12 个 `import type Database`（仅类型）。值导入关键点：`memory/sqlite-store.ts:3`、`memory/emotion-state.ts:1`、`memory/import-management.ts:5`、`memory/import-source.ts:4`、`management/aika-timeline.ts:5`、`projects/sqlite-project-index.ts:1`、`companion/invitations.ts`(type)、`harness/receipts.ts`(值)、`wechat/store.ts:1`、`app/*evaluate*`+`memory-trial.ts` | **唯一持久化权威，不可做成可选包**。04 必须保留；拆包时它是内核/普通包/兼容包的**共享必需**依赖 |
| `sherpa-onnx-node` | 1.13.8 | **两处 `createRequire`**：`media/wake/detector-worker.ts:6`（唤醒）、`providers/sherpa-streaming-asr-worker.ts:8`（流式 ASR） | ⚠️ **需两个独立可选包**，不能合成一个"本地引擎包" |
| `silk-wasm` | 3.7.1 | `wechat/voice.ts:165`（+ `wechat/qr.ts` 走 qrcode） | 仅 WeChat 可选包 |
| `pinyin-pro` | 3.29.4 | `media/wake/keywords.ts:1` | 仅 Wake 可选包 |
| `qrcode` | 1.5.4 | `wechat/qr.ts:1` | 仅 WeChat 可选包 |
| `electron` | 44.4.1 | `desktop/electron/*`、`tools/dev-desktop.mjs`、`tools/build-native.mjs` | 宿主壳；10 的分发物 |
| `esbuild` | 0.28.2 | `desktop/build-web.mjs`、`tools/build-wake.mjs`、`tools/build-wechat.mjs` | 各包构建期依赖，非运行时 |
| `typescript` | 5.9.3 | 单一 tsconfig | 01 起须为每个包提供独立编译能力 |

#### C-6 后台

| 事实（实测 grep） | 记录 |
| --- | --- |
| `setLoginItemSettings` / `autostart` / `new Tray` / `createTray` → **零命中**；仅 `app.requestSingleInstanceLock` 命中 1 处 | **不存在自启动，也不存在系统托盘**。SPEC 中"后台"措辞所设想的功能**在源码中不存在**——按 AGENTS §"不能遗漏后静默删除"，此处**如实登记为"未实现"而非迁移项** |
| 长驻循环共 **9 处 `setInterval`**（全部 `unref()`）：记忆清理 60000 ms（`app/backend.ts:54`、`app/trial-backend.ts:440`）、唤醒看门狗 1000 ms（`app/wake-manager.ts:84`，无 PCM 判失败 60000/15000）、work 轮询（`harness/desktop-work.ts:135`，`intervalMs`）、用量观测（`harness/forwarding.ts:260`，`intervalMs`）、renderer 播放追踪 500 ms（`desktop/main.mjs:586`）、wake clip 定时（`media/wake/browser-capture.ts:120`） | 03 起须由**宿主**管理生命周期（启动/停止/释放），不得散落在各包 |

#### C-7 本步新增的共享接口风险登记（供 01/02A 避坑）

| 风险 | 实测 | 影响 |
| --- | --- | --- |
| `ManagementError` 爆炸半径 | **38 个生产文件直接 import**（39 个提及；`contracts/management.ts` 为声明处） | 改其语义会广泛外溢 |
| `TurnScope` 覆盖面 | **72 个生产文件**触及（含测试 121） | 轮次类型不可轻改 |
| `ProviderProtocol` **被声明两次** | `contracts/management.ts:8` **与** `providers/slot-registry.ts:8`，结构相同但**独立声明、非 re-export**。`SlotBinding.protocol` 用后者，`ProviderSelection.protocol` 用前者 | **分歧隐患**：01/02A 统一时必须同时改两处并加契约断言 |
| `PROVIDER_SLOTS` 七值数组**被镜像两次** | `management/settings.ts:9`（导出）与 `management/aika-profile.ts:13`（本地 `const`，注释"mirrored from the management contract"） | 只改一处会静默分歧 |
| `CONTRACT_VERSION = '0.9.0'`（`contracts/index.ts:2`） | 全仓**零 importer**（仅 2 处同名不同常量 `MEMORY_IMPORT_*`/`BACKGROUND_*` 无关命中） | **不要 bump 它来表示 0.65**：无消费者，bump 不证明任何事却改动冻结契约。新插件/宿主 API 版本放**新常量**（01 冻结 API v1 与 schema v1） |
| `ProviderCapabilities`（`contracts/index.ts:186-190`） | **零消费者**（仅自身声明）。定义为 `id` + 封闭 7 值 `('asr'\|'acoustic_emotion'\|'visual_emotion'\|'dialogue'\|'emotional_tts'\|'memory_maintenance'\|'embedding')` + `evidence` | **是死声明，不是正在使用的竞争词表**。见 §4 决策 D6 |
| `ManagementSettingsStore` 历史校验 | `management/settings-store.ts:26` 以 `historical=true` 校验 `state.history` 每一项；`management/settings.ts:62-72` 在 historical 模式下**即时构造 legacy adapter catalog**（改写 perception 费率、把 `TEXT_SLOTS` 全部换成 `qwen-plus-2025-12-01`、追加 legacy adapters） | ⚠️ **02A 迁移必须保持此路径可用**，否则已写盘 history 停止加载、`ManagementSettingsStore.open` 抛错 |

### 00-D：test:next65 有非零生产特征测试；空目录/零用例必须非零退出；阈值实现前登记 → **PASS**

**入口**：`"test:next65": "npm run build && node tools/run-tests.mjs next65"`。分支与 `next61` 同构：收集 `dist/tests/next65/*.test.js`（编译产物）**并**收集 `tests/next65/*.test.mjs`（未经 tsc 的源）。

**缺目录/空集合的**刻意选择与实测**：

`collect()` 内部是裸 `readdir`，目录缺失会 **reject**。`next-real` 用 `.catch(() => {})` 吞掉，`next`/`next61` 不吞。本步**选择显式捕获并给出可读消息 + `exit 2`**（而非让裸 ENOENT 栈冒出来，也不让空跑出 0）：

| 守卫场景 | 实际输出 | 退出码 |
| --- | --- | --- |
| A 两个目录都缺失 | `K65 test directories missing (dist/tests/next65, tests/next65); build first, then re-run. Refusing a passWithNoTests run.` | **2** |
| B 目录存在但零用例 | `K65 test collection is empty; refusing a passWithNoTests run.` | **2** |
| C 放入 1 个真实用例 | `✔ guard smoke` / `pass 1 / fail 0` | **0**（证明守卫不误杀非空集合） |
| D 用 **git HEAD 版本的 runner** 跑 `next65` | `Error: Unknown test group` | **1**（RED 证据：分支是本步新增的） |

**用例数与性质（21 用例 / 3 文件，`npm run test:next65` 退出码 0）**：

| 文件 | 用例 | 被测对象 |
| --- | --- | --- |
| `baselineSurface.test.ts` | 10 | **真实生产模块**：`app/trial-backend.js`（真实 import）、`core/turn-controller.js`、`providers/slot-registry.js`、`management/settings.js`、`contracts/*`、真实产物 `desktop/build/renderer.js` |
| `baselineBehavior.test.ts` | 7 | **真实 `DesktopRuntime` → `DialoguePipeline` → `TurnController`** 全链；外部 provider 边界为 double，**本地识别器为真实 `SherpaStreamingAsr`（worker 线程）** |
| `rendererArtifact.test.mjs` | 4 | **真实构建产物**在 VM realm 的 `link()`/`evaluate()`；worklet 边车文件存在性 |

**非"mock 被测物"自查**：没有任何用例 mock 包加载器/registry/pipeline/构建产物；渲染断言走**真实产物的真实 link**，不是源码字符串。

**阈值"实现前登记"**（`BASELINE.md` §4 完整表；此处为要点）：

| 指标 | 阈值（先登记，未事后放宽） | 本步实测（三轮） |
| --- | --- | --- |
| CER ≤ 0.20 | 登记自 FIX61-08 §6 | **0.0000**（0/26 字） |
| 必需关键词 3/3（介绍/研究/感兴趣） | 同上 | **3/3** |
| 首 partial ≤ 1500 ms | 同上 | **438 / 471 / 491 ms** |
| 末 final ≤ 10000 ms | 同上 | **6 / 8 / 6 ms** |
| partial 次数 ≥ 1 | 同上 | **11** |
| 语料 SHA256 == pin | **本步新增** | `668BF8DF51A10027…` 相等 |

> ⚠️ **本步修正的一处 0.61 证据缺口（如实报告）**：FIX61-08 §6 的上述阈值**此前只是文档**。度量脚本 `_metrics08.mjs` 已删除（报告 §6 自述"度量后已删除，非交付物"），且**没有任何存活测试断言它们**——`tests/next61/liveVoicePath.test.ts` 的真实模型用例只断言"有 final、提交恰好一次"，**垃圾转写同样通过**。K65-00 因此**新建了可执行的 CER 门槛**（`baselineBehavior.test.ts` 的真实 Levenshtein 实现 + 冻结参考文本 + 语料 hash pin），把文档阈值变成真实门槛。这是"实现前登记"的落地，**不是新增宽松也不取代 0.61 的任何断言**。

### 00-E：多源矩阵覆盖现有提供者，真实回放来源/资源要求/未具备项；模型音色按来源归属 → **PASS**

#### E-1 槽位与协议

| 事实 | 值 |
| --- | --- |
| 七槽 | `'asr' \| 'dialogue' \| 'memory_turn' \| 'summary' \| 'perception' \| 'tts' \| 'admission'`（`contracts/management.ts:6`） |
| `PROTOCOL_SLOTS` | `openai-compatible` → 全部 7 槽；`gemini` → 5 槽（无 asr/tts）（`providers/slot-registry.ts:38-41`） |
| 单一配置权威 | `ManagedSettings.providers`（`management-settings.json`，revision/CAS）。**每槽是单值，不是数组** → **多源共存是 0.65 的设计目标，不是既有能力** |

#### E-2 现有 adapter 清单（`providers/management-catalog.ts` + `ADAPTERS`）

| adapterId | 槽 | 协议 | provider | 类型 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `deepseek-{dialogue,summary,admission,memory_turn}` | 4 文本槽 | openai-compatible | deepseek | reviewed | `defaultManagedSettings` 按 `TEXT_SLOTS` 派生 |
| `qwen-dialogue` / `qwen-summary` / `semantic-admission` / `strict-deepseek` | 各 1 | openai-compatible | dashscope | reviewed | `management/settings.ts:10 ADAPTERS` 默认表 |
| `qwen-perception` / `qwen-visual-emotion` | perception | openai-compatible | dashscope | reviewed | 有无 asr 时二者择一 |
| `qwen-tts-instruct` | tts | openai-compatible | dashscope | reviewed | 默认 tts |
| `qwen-audio-tts` | tts | openai-compatible | dashscope | reviewed | 音色 `longanfengyue`/`longanlingxi`/`longanlingxin` |
| `minimax-tts` | tts | openai-compatible | dashscope | reviewed | 需已登记音色；走 `TrialTransport` 逐字段白名单 |
| `qwen-asr` | asr | openai-compatible | dashscope | reviewed | **batch-only** |
| **`openai-compatible-text`** | dialogue/memory_turn/summary/admission | openai-compatible | 任意 | **open（无型号白名单）** | `capabilities.temperature: true` |
| **`openai-compatible-perception`** | perception | openai-compatible | 任意 | **open** | |
| **`openai-compatible-asr`** | asr | openai-compatible | 任意 | **open** | 自身 note 明确：**"本适配器为整段批处理，实时逐块转写由本地流式识别路径提供"**——源码已承认 batch/streaming 二分 |
| **`openai-compatible-tts`** | tts | openai-compatible | 任意 | **open** | `capabilities.instructions/cloning/voice/language` |
| **`gemini-text`** | dialogue/memory_turn/summary/admission/perception | gemini | gemini | **open** | 密钥走 `x-goog-api-key`，模型名进 URL |

#### E-3 🚨 **最高优先级阻塞（SPEC/RPD 均未点名）：`providers/transport.ts:56` 硬拒非 https**

```ts
if (endpoint.protocol !== 'https:' || endpoint.username || endpoint.password || endpoint.search || endpoint.hash || !config.model)
  throw new Error('Explicit HTTPS provider endpoint and model required');
```

**后果**：`isAllowedEndpoint()`（`slot-registry.ts:114-124`）**允许** loopback `http://127.0.0.1|::1|localhost`，且 `tests/providers/slot-registry.test.ts:38-42` **断言**该允许；但**运行时被 transport 覆盖** → **即使配置校验全过，每一个本地 OpenAI-compatible 路径都会在发请求前失败**。本步以真实生产 transport 实测复现（见 `baselineSurface.test.ts`："a loopback binding passes slot validation but is refused by the real production transport"）。

**此阻塞的优先级高于字段放宽**：只放松 `credentialRef` 等校验而不动 `transport.ts:56`，本地来源仍然 100% 不可用。

**放宽时的血缘约束（必须同时保留）**：
- `transport.ts:66` 与 `:105` 的 `redirect: 'error'` 不得取消；
- 保持 `username/password/search/hash` 拒绝（防凭据经 URL 外泄）；
- **不得**把云端 `Authorization` 头发往本地端点；
- `transport.ts:101-103` `downloadAudio()` 有**第二处独立 https-only 检查**，需一并评估。

#### E-4 凭据链（**五段校验 + 运行时读取 + 传输闸门**，"只改一处"无效）

| # | 位置 | 内容 | 本地无钥来源的问题 |
| --- | --- | --- | --- |
| 1 | `providers/slot-registry.ts:54` | `if (!text(binding.credentialRef)) throw` —— `credentialRef` 必填 | 必须放宽 |
| 2 | `management/settings.ts:76` | required 数组含 `'credentialRef'` | 必须放宽 |
| 3 | `management/settings.ts:95` | `… \|\| !text(p.model) \|\| !text(p.credentialRef)) invalid();` | 必须放宽 |
| 4 | `management/settings.ts:122` | `credentials.file(p.credentialRef, …)` catch → `invalid('请选择已登记的同供应商凭据。')` | 必须放宽 |
| 5 | `app/trial-config.ts:123` | `typeof m.credentialFile !== 'string' \|\| !isAbsolute \|\| within(projectRoot)` → fail；且 `TrialModel.credentialFile` 为**必填**（`trial-config.ts:17`） | 必须放宽 |
| 6 | `app/trial-backend.ts:199-212` `keyReader()` | `/^sk-[A-Za-z0-9_-]+$/` 与项目外私有文件 | **本地无钥来源永远无法满足** |
| 7 | `providers/transport.ts:57` | `if (!apiKey) throw new Error('Provider API key is not configured')`；`EndpointConfig.apiKey` 为必填（`:10`） | 必须放宽 |

**注**：briefing 校正指出"`settings.ts:95` 而非 `:115`"——已实测确认 `:95` 为确实的 credential/text 校验点，`:116-120` 是 `underRegisteredRate`。

#### E-5 本地能力现状（逐项真实，不把 local 视为同一引擎）

| 能力 | 源码 | 真实资源 | 现状 |
| --- | --- | --- | --- |
| 本地 **LLM** | **不存在**。`providers/qwen-dialogue.ts:95-96` 仅有 `OllamaDialogueBoundary`，注释自述"Reservation only; no Ollama process, weights, ASR or TTS are installed or assumed"，且 `verified: false` | **无**（未部署任何本地 LLM 服务） | **BLOCKED** |
| 本地 **streaming STT** | `providers/sherpa-streaming-asr.ts`（真实 worker 实现，本步以真实模型跑通 CER 门槛） | 模型包存在；但 `PET_STREAMING_ASR_DIR` **未设置**（`app/backend-session.ts:26` `if (!directory) return undefined`） | 代码与模型均可跑，**生产未接线** → 语音路径按 `safeRecognizer()` 语义**静默回退到云端 batch asr** |
| 本地 **batch STT** | `providers/qwen-asr.ts` → 云端 | — | 现网实际路径 |
| 本地 **TTS** | `providers/sapi-tts.ts`（Windows `System.Speech`，真实）；0.6 真实回放 **`tests/next/real/realTts.test.ts` PASS，138286 B / 16000 Hz / durationMs=4320** | 音色 `Microsoft Huihui Desktop` | **真实可跑，但未接入槽系统**：`createTrialTtsProvider()`（`trial-backend.ts:149`）对它 `throw new Error('Unregistered speech adapter')`；且它**不在** `management/settings.ts:10 ADAPTERS`，也**不在** `providers/management-catalog.ts`（grep `sapi` 两处均零命中） |
| **音色归属** | `providers/registered-voices.ts:9` `readonly provider: 'dashscope'`（类型字面量）；`:50` `v.provider !== 'dashscope'` 校验；`:52` `/^dashscope-[a-f0-9]{12}$/` 校验 `credentialRef` | — | **`RegisteredVoiceStore` 硬编码 `dashscope`** → **本地引擎音色无法登记**。这是 PROVIDERS §3"TTS voiceId/modelId 归当前来源"的**直接阻塞** |
| 厂商分支违规 | `providers/text-protocol.ts:8` `if (config.endpoint === DEEPSEEK_ENDPOINT)` —— 对端点字符串**相等**做厂商分支 | — | 违反 AGENTS §3"内核不应有模型厂商分支"；01/02A 须改为 adapter 内协议声明 |

#### E-6 真实性验证选型与本版资源前置（按 SPEC §1 多源补充要求固定）

| 槽 | 云端 | 本地 | 本版选型判定 |
| --- | --- | --- | --- |
| LLM | DeepSeek `deepseek-flash`（openai-compatible）——0.6 已配置且 2026-09-21 本步实测 **PASS** | **无任何服务** | 云端**所需**：授权凭据 + 网络目的地声明；本地 → **BLOCKED**：缺**一个已部署的本地 OpenAI-compatible LLM 服务端点与模型**（如 llama.cpp/ollama 的 loopback `/v1`）。**不自动安装、不新增付费服务**（用户决定） |
| TTS | 需要云端 key（未授权） | **SAPI 真实可用**（Windows 内置，无凭据、无网络） | 云端 → **BLOCKED**：缺授权凭据；本地 = `providers/sapi-tts.ts` + 音色 `Microsoft Huihui Desktop`，**所需资源已具备**，缺口只是**未接入槽系统**（05 的工作） |
| STT | `qwen-asr` batch（需 key） | `SherpaStreamingAsr` 真实模型**已具备且本步实测通过** | 云端 → **BLOCKED**：缺授权凭据；本地 = 模型包 `F:/AIVoice/toolchains/sherpa-streaming-asr/sherpa-onnx-streaming-zipformer-zh-14M-2023-02-23`（int8 encoder/joiner + fp32 decoder + tokens）+ 语料 `test_wavs/0.wav`（pin 见 BASELINE §5）+ 需设 `PET_STREAMING_ASR_DIR` |

**用户明确决定（已给定，不重复询问，直接登记）**：
1. **本地 LLM 真实回放 → BLOCKED**，缺失资源已具名（上表）。不自动安装模型、不启用新付费服务。
2. **云端真实回放（按 0.65 多源口径）→ BLOCKED**，待授权凭据。
3. 0.65 不得让本地来源依赖云端凭据或计费口径。

#### E-7 未具备项汇总（不把所有 local 视为同一引擎）

- 本地 LLM 服务：**不存在**（仅保留类型占位）。
- 本地 TTS 引擎：**存在两个不同实现**（SAPI 已真实跑通；`SherpaStreamingAsr` 属 STT，不是 TTS）。二者 adapter 不同、模型/音色标识不同、资源所有者不同 → **必须分别登记为不同 SourceInstance/adapter**。
- 本地 STT：streaming 与 batch 是**不同能力**，`openai-compatible-asr` 的 note 已自认 batch-only。
- 无原生多源能力：`ManagedSettings.providers` 每槽单值。

## 3. 测试选择理由

按根 AGENTS §6：本步是"版本收口 + 基线固定"，故 (a) 运行 0.61 已交付全部门槛（`check`/`test:next`/`test:next61`/`test:windows`/media）以建立**回归对照**；(b) 新建 `test:next65` 提供本版**非零生产特征测试**；(c) integration 采**逐文件隔离**而非整套判红，因并发运行会产生假失败且部分文件挂起——先用隔离跑取得失败清单，再逐条取真实错误文本，最后用 `git ls-tree HEAD` + `git status` 证明失败文件与 HEAD 逐字节一致，从而区分"既有失败"与"本版回归"。

**未跑**：`npm run test`（全仓）、`npm run test:release`、`npm run build:windows` 完整四段、`npm run test:windows:ui`（Electron UI 冒烟）、Tauri 打包。理由：本步零生产代码改动，按 §6"文档/无行为影响改动只做适当检查"；且这些属 10 的收口门槛。**未跑即未跑，不作为通过证据。**

## 4. K65-00 归属决策（CONTRACTS/K65-01 明确委派给 00 的未决项）

以下决策的职责来源：`CONTRACTS.md` L3"实际路径由 00 记录"、`K65-01` L11/L19。**不记录它们，01 与 02A 会重复实现同一批类型。**

| # | 决策 | 结论 |
| --- | --- | --- |
| **D1** | 新 SDK / 新契约的文件路径 | **类型面放 `windows/code/desktop-pet/contracts/`**（与现有 20 个 `.ts` 一致；`contracts/index.ts` 已有 `export type * from './self-setup.js'` 的再导出先例）。**具体新增文件（K65-01 落地，本步只冻结路径约定）**：`contracts/plugin.ts`（manifest/schema/identity）、`contracts/capability.ts`（capabilityId 词表 + 能力声明）、`contracts/provider-source.ts`（五层多源类型）、`contracts/flow-profile.ts`（Profile 与阶段）。**01-C 的独立 fixture 插件项目**放 `tests/next65/fixtures/plugin-sdk/`，并可独立构建为最小 SDK 产物——**与宿主 `contracts/` 通过公共版本边界共享，不复用宿主 tsconfig** |
| **D2** | 五个多源名字的 schema-vs-TS-type 归属（**最重要**） | **01 拥有全部 TS 类型声明 + 版本化 schema + 校验器，一次冻结为 API v1；02A 只消费这些冻结类型，实现运行时解析与实例生命周期，不得重述或改写类型。** 具体：`AdapterDescriptor`、`SourceInstance`、`ModelProfile`、`Binding`、`ResolvedBinding` 五个名字的 **interface/type 声明与 schema 全归 01**；02A 交付的是**解析函数、可变配置→不可变 `ResolvedBinding` 的转换、租约与健康实现**。理由：SPEC §3"接口冻结是当前步骤的交付"；若 01 写 schema、02A 重写类型，则类型事实上被 02A 冻结，与索引顺序矛盾。**若 02A 发现冻结类型不足，必须走"改冻结语义须列受影响消费者+兼容迁移+回归"的流程回改 01，而非就地另立。** |
| **D3** | `SecretStore` 类型归属 | **归 01**（属 schema 层）。**它不是新造概念，而是既有 `management/credentials.ts credentialRegistry()`（L10）与 `management/credential-store.ts ManagedCredentialStore` 的正式化**：01 冻结一个 `SecretStore` 接口（`ref → 是否存在/解析`，不含明文读取路径），把现有 registry 适配到它。02A 只在 SourceInstance 的 `auth` 字段**引用**该接口，不实现存储。源码现状：**`SecretStore` 零命中**，确为新类型 |
| **D4** | `capabilityId` 初始词表 | **冻结为"开放命名空间 + 必需最小集"**，而非封闭 union。必需最小集须覆盖 K65-01 L21 的六类：文字对话（`llm.chat`）、Context 来源（`context.source`）、输入（`input.capture` / `stt.transcribe`）、输出（`tts.synthesize` / `audio.playback`）、展示（`presentation.render`）、后台生命周期（`background.lifecycle`）。**同时明确：`capabilityId` 不采用 `ProviderCapabilities.capabilities` 的 7 值封闭词表**（见 D6）。允许第三方 adapter 声明新 capabilityId，但**注册表不因未知 ID 静默接受**——未声明依赖的定位被拒（CONTRACTS §2） |
| **D5** | 超时默认值（依据实测关闭/启动行为） | 见 `BASELINE.md` §4 表。**01 据此固定为有上限策略**，关键值：关闭（EOF→SIGKILL）**上限 10000 ms**（`transport.mjs:148`，下限亦 10000）；启动就绪/无进度各 **60000 ms**、启动总上限 **600000 ms**；streaming ASR open **30000 ms** / call **15000 ms**；voice finish **10000 ms**；单次话语 **120000 ms**；帧拉取重试 10 ms / 超时 2000 ms；唤醒看门狗 1000 ms 轮询（60000/15000 ms 判失败）；记忆清理 60000 ms。**超时不得给副作用操作自动重试**（CONTRACTS §3） |
| **D6** | `ProviderCapabilities`（`contracts/index.ts:186`）是否被取代 | **本步实测：零消费者**（全仓仅其自身声明）。因此它是**死声明**，不是活跃的竞争词表。**决定：不改、不删、不复用**——保持 0.61 冻结契约字面不变（避免无谓改动冻结契约），但在 `K65-01` 的契约文档中**显式声明"新 capabilityId 体系不继承 `ProviderCapabilities.capabilities`，二者无语义关系；后者保留仅供参考，0.65 不新增消费者"**，以消除 02A 的歧义。**若 10 收口时仍零消费者，另开清理任务，不在本版顺手删** |
| **D7** | `test:next65` 缺目录行为 | **选择显式捕获 + 可读消息 + `exit 2`**（见 §00-D 守卫表），而非裸 ENOENT 栈（`exit 1`）或静默吞掉（`next-real` 的 `.catch(() => {})` 会被后续误读为"已验证"）。**02A/10 新增同族测试组时应沿用本分支的写法** |

## 5. 阻塞与未做项

| 项 | 类型 | 说明 |
| --- | --- | --- |
| 本地 LLM 真实回放 | **BLOCKED** | 缺已部署的本地 OpenAI-compatible LLM 服务端点与模型；`OllamaDialogueBoundary` 为 `verified: false` 占位。不自动安装 |
| 云端真实回放（0.65 多源口径） | **BLOCKED** | 待授权凭据 |
| 本地来源接入槽系统 | **未接线（非阻塞）** | SAPI TTS 真实可用但 `createTrialTtsProvider()` 拒收；`PET_STREAMING_ASR_DIR` 未设 → 语音静默回退云端 batch |
| 本地音色登记 | **阻塞（05 前置）** | `RegisteredVoiceStore.provider` 硬编码 `dashscope` |
| `transport.ts:56` https-only | **阻塞（02A 前置）** | 见 §00-E E-3，优先级高于字段放宽 |
| `build-native.mjs` 不产出分发包 | 未做 | 10 的交付物 |
| 6 个 integration 既有失败 + 1 个挂起 | 既有，未修 | `ui-harness.mjs` / `prepare-trial-update.mjs` 从未入库；`memory-trial` 为 Windows 路径限制；`pending-memory-management` 挂起（进程起于 19:35，早于本步） |
| `tests/integration/trial-update.test.ts` | 既有，未修 | 导入 4 个不存在的 `tools/*.mjs`；**不在任何测试组内**，故未被本步扫描覆盖 |
| 未跑全套 / UI 冒烟 / 打包 | **NOT RUN** | 见 §3 |

**未验证声明（诚实边界）**：本步**未**在真机 Electron 窗口内做渲染冒烟（VM link 只证明模块图自足，不证明界面可用）；**未**验证真实麦克风、真机播放、GPU；**未**在本步重跑 `build:windows` 四段与 `test:windows:ui`（沿用 FIX61-10 在**其**修订上的记录，不代表本步修订）；**未**审阅或验证并行 worker 于 20:28 落地的 `desktop/electron/main.mjs` / `tools/management-url.mjs` token 修复（本步只记录其 hash 变化，未编辑、未跑 UI 冒烟——锁定态是否已真正解除**未经验证**）；**未**验证 `pending-memory-management.test.js` 挂起的根因（只观测到 >300 s 不返回）；**未**跑 `npm run test` 全仓、`test:release`、`build:windows`、`test:windows:ui`；**未**验证 macOS 分支。

## 6. 共享接口影响

**本步零生产代码与共享契约改动**，仅新增测试入口（`tools/run-tests.mjs` 的 `next65` 分支 + `package.json` 脚本）与三个测试文件。`contracts/` 下 20 个文件、`CONTRACT_VERSION`、`DESKTOP_BRIDGE_VERSION` **均未修改**。

新增的 `test:next65` 是**共享测试入口**：01～10 每步须在其中登记真实用例文件与定向命令（TESTING.md"命令与用例清单"），不得把计划命令记作已存在。

## 7. 下一步交接（K65-01）

- **可直接消费**：`BASELINE.md` 的固定点位/hash/阈值表/超时依据表；本报告 §4 的 D1～D7 七项决策；`tests/next65/` 三个文件作为基线特征测试（01 起新增行为须先 RED，**不得删改这三个文件的断言**）。
- **01 必须交付**：`contracts/plugin.ts`、`contracts/capability.ts`、`contracts/provider-source.ts`、`contracts/flow-profile.ts`（D1）；其中 `AdapterDescriptor`/`SourceInstance`/`ModelProfile`/`Binding`/`ResolvedBinding` 的**类型与 schema 一并冻结为 API v1**（D2）；`SecretStore` 接口（D3）；`capabilityId` 词表（D4）；超时上限策略（D5）；`ProviderCapabilities` 关系声明（D6）。
- **02A 只能消费** 01 冻结的类型，实现解析与实例生命周期（D2）；并须保留 `ManagementSettingsStore` 的 `historical=true` legacy catalog 路径（§00-C C-7）。
- **未决前置须在 02A 之前落实**：`transport.ts:56` 的 loopback 放行（连同血缘约束）、`RegisteredVoiceStore.provider` 的来源化。
- **不得**由 01 或 02A 顺手 bump `CONTRACT_VERSION`（零消费者）。

相关文档：[基线](BASELINE.md) · [执行索引](../SPEC.md) · [公共契约](../CONTRACTS.md) · [测试规则](../TESTING.md) · [源码核对](../SOURCE_AUDIT.md) · [多源 Provider](../PROVIDERS.md) · [0.61 收口](../../0.61/reports/FIX61-10.md) · [连续计划](../../EXECUTION_061_065.md)。

## 8. 审计更正（2026-09-21 安静树复测轮追加，未改动上文原始记录）

**本节由后续安静树复测 worker 追加**。上文 §00-B 的 integration"32 文件 / 261 tests / 247 pass / 6 fail / 3 skipped"为**并发期数字**：K65-00 执行时树上另有 worker 并行构建/测试，且 FIX61-10 收口 worker 恰在其测量窗口内修复失败测试。安静树（无其他 worker）逐文件隔离复测**未复现该数字**，实测如下。

**安静树实测（2026-09-21T21:44–21:53+08:00，`node --test --test-reporter=tap --test-timeout=90000` 严格一次一文件）**：

- 文件数：`tests/integration/` 55 个 `.test.ts`，`dist/tests/integration/` 55 个 `.test.js`，1:1；55 个源文件**全部在 HEAD `d7c61d6`（即本报告冻结的修订）中已被 git 跟踪**（`git ls-tree -r HEAD` 计数 55），故"32 文件"口径不能归因于此后新增文件。
- 全量：**397 tests / 368 pass / 21 fail / 8 skipped，失败 13 文件**。被停审计员先前登记的"55 文件 / 21 失败"总数与本轮**完全一致**（其逐文件清单未留档，可核对的口径是计数）；**真安静树口径以 55/21 为准**。
- 6→21 的差值：K65-00 §00-B 表只登记了 6 个失败文件；实测另有 `task-preflight`(3)、`wake-model-install`(1)、`work-input`(5)、`work-records-entry`(1)、`work-speech-wire`(2)、`work-voice-wire`(1) 等 `ui-harness.mjs` 缺失系与既有失败同因，但 K65-00 窗口未登记（推定为并发期扫描未覆盖）。
- §00-B 对 `trial-update.test.ts` 的"不在 dist/tests/integration 集合内、不属任何测试组"表述**与实测不符**：该文件经动态 `import()` 4 个不存在的 `tools/*.mjs`（tsc 不检查动态 import，`check`/`build` 均通过），编译产物在集合内且实测 1 fail（加载期 `ERR_MODULE_NOT_FOUND`）。
- 既有失败处置变化（FIX61-10 收口轮在其后落盘，本轮实测确认）：`memory-dynamics-http` 挂起已修（1 s exit 0）；`balances` 6 pass/1 skip exit 0；`deepseek-text` 3/3 pass；`memory-trial` 7/7 pass（生产侧 `evaluate-lifecycle.ts` 改 `isAbsolute`）；§4"6 个 integration 既有失败 + 1 挂起"行按此理解。逐文件全表见 `RUN_061_065.md` §3a/§4（更新版）。
- 本报告其余门槛数字（`test:next` 80、`test:next61` 143→重冻结 148、`test:windows` 22、media 63、`test:next65` 21）**已在安静树复测中复现**，无需更正。
- 证据工件：`windows/code/desktop-pet/tools/audit-integration-sweep.{mjs,results.json,tap.txt}`（含每文件起止时间戳、退出码、TAP 原文）。

