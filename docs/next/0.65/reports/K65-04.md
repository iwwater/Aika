# K65-04 自动验收报告

日期：2026-09-22；状态：AUTO_PASS（04-A～04-E 已通过；前置：K65-03 AUTO_PASS）。

## 交付

- `plugins/compatibility-runtime.ts` 提供唯一 writer、稳定 messageId 去重、source snapshot、来源撤销失效、后台生命周期和启用/停用状态。
- 迁移以原子 `.next` 替换，成功后保留单份 `.bak`；迁移 fingerprint 持久化，重复执行返回相同结果，不重复备份或写入。
- 迁移失败时原文件保持不变；已撤销 source 的旧 snapshot 不能继续写回，其他 source 不受影响。
- `packages/compatibility/entry.mjs` 只注册公开的 `context.source` 与 `background.lifecycle` 能力，没有夹带音频或视觉依赖。
- `tools/build-next65.mjs` 生成并校验独立兼容包 manifest。

## 测试证据

新增 `tests/next65/compatibilityRuntime.test.ts`，覆盖：

1. 兼容包经真实 HostContext 惰性加载，能力注册与后台生命周期解析成功。
2. 单 writer、稳定 ID、双次迁移幂等和单份备份。
3. source revoke 使旧快照失效并阻止 stale writeback，同时保留其他来源。
4. 失败迁移保留原文件和可恢复备份。

实际命令（工作目录 `windows/code/desktop-pet`）：

| 命令 | 结果 |
| --- | --- |
| `npm run check` | PASS，TypeScript 0 errors |
| `node --test dist/tests/next65/compatibilityRuntime.test.js` | PASS，4/4 |
| `npm run test:next65` | PASS，127/127，退出码 0 |

## 限制与交接

本步用真实文件系统和受控迁移 fixture 验证兼容边界，未把测试数据宣称为生产 SQLite 升级证据。原 Memory/Timeline/知识库仍由既有存储权威负责；运行中卸载和更新事务由 K65-08 继续完成，真实数据升级与产品验收留 K65-10/11。
