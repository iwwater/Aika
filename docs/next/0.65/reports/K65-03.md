# K65-03 自动验收报告

日期：2026-09-22；状态：AUTO_PASS（03-A～03-E 已通过；03-E 按用户授权使用受控 mock）；前置：K65-02A AUTO_PASS。

## 交付

实现最小内核和普通产品包的独立产物：

- `kernel/minimal-kernel.ts` 提供不依赖可选功能包的文字内核，复用现有 `NextTurnPort`、`TurnController` 和 `DialoguePipeline` 的唯一轮次权威。
- `RecentHistory` 是真实有界近期历史存储；文字路径不启动音频、STT、视觉、Wake、WeChat 或长期后台维护。缺失能力使用明确的“未安装”错误，不返回伪成功。
- `packages/normal/entry.mjs` 只通过公开 `HostContext` 注册 `llm.chat`，没有宿主私有捷径；来源/模型仍由 K65-02A 的显式 binding 选择。
- `tools/build-next65.mjs` 与 `build:next65` 生成独立 kernel bundle、普通包目录、manifest、文件 hash，并用真实 manifest validator 验证。
- `test:next65` 现在先生成 K65-03 产物，再收集同一架构回归套件。

## 测试证据

新增 `tests/next65/minimalKernel.test.ts`，覆盖：

1. kernel 产物不携带 TTS/STT/视觉/Wake/WeChat/native 可选实现，普通包 manifest 独立验证。
2. 无普通包时宿主明确报告缺包；导入普通包后使用与其他插件相同的惰性 HostContext 激活路径。
3. 最小内核多轮文字回复、真实近期历史、终态和新轮次取消；旧轮迟到回复不会写入 assistant 记录。
4. 两个受控无 Key local-service 端点经普通包文字链路回放，切换后 model/source 不串线，Authorization 为空，未发生云端请求。

实际命令（工作目录 `windows/code/desktop-pet`）：

| 命令 | 结果 |
| --- | --- |
| `npm run check` | PASS，TypeScript 0 errors |
| `node --test dist/tests/next65/minimalKernel.test.js` | PASS，5/5 |
| `npm run test:next65` | PASS，121/121，退出码 0 |

## 交接

03-E 使用用户授权的受控 HTTP mock，证明生产接线的目的地、model/source 隔离、无 Key local-service 和无 fallback 语义；真实模型部署不是本步自动门槛，后续接入真实来源时复用同一 `DialogueProvider` 与 binding，不改 Kernel 或包格式。
