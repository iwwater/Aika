# K65-08 自动验收报告

日期：2026-09-22；状态：AUTO_PASS（08-A～08-E 已通过；前置：K65-07 AUTO_PASS）。

## 交付

- `plugins/package-lifecycle.ts` 提供影响查询、消费者租约、停用、staging update、重启切换、rollback 和卸载接口。
- 更新不进行运行中热替换：新包先通过既有 package import 校验并落盘，切换时只保留一个 registry 版本指针，旧目录保留用于回滚。
- registry/config/state 均使用临时文件和 rename；回滚快照恢复旧版本和启用意图，包 data 不随代码卸载清除。
- 活动 flow/binding/source consumer 会阻止停用/卸载，避免迟到输出和共享实例过早释放。

## 测试证据

新增 `tests/next65/packageLifecycle.test.ts`，覆盖：

1. 活动 consumer 影响列表和停用阻断，最后 lease 释放后可停用。
2. TTS 新版本 staging、重启原子切换、旧目录保留和 rollback。
3. 活动 consumer 阻止卸载；释放后删除 registry/code，保留宿主外数据命名空间。

实际命令（工作目录 `windows/code/desktop-pet`）：

| 命令 | 结果 |
| --- | --- |
| `npm run check` | PASS，TypeScript 0 errors |
| `node --test dist/tests/next65/packageLifecycle.test.js` | PASS，3/3 |
| `npm run test:next65` | PASS，139/139，退出码 0 |

## 限制与交接

本步验证的是重启生效事务和宿主目录一致性；没有宣称支持运行中热更新，也没有强制停止用户所有的本地服务。09 只消费这些状态接口，10/11 再验证独立分发和产品层停用反馈。
