# K65-09 自动验收报告

日期：2026-09-22；状态：AUTO_PASS（09-A～09-E 已通过；前置：K65-08 AUTO_PASS）。

## 交付

- `management/next65-management.ts` 提供包/版本/启用意图/待重启的 metadata projection，Profile revision 保存与 preview，以及生命周期操作入口。
- `management/ui/next65-view.mjs` 只渲染已脱敏的包和诊断字段；它不会从页面触发 provider 激活。
- 诊断使用 bounded ring（200 条），保留 scope/profile/stage/package version 和结果状态，自动去除 apiKey、token、credential 等敏感值。

## 测试证据

新增 `tests/next65/management65.test.ts`，覆盖：

1. 包列表为 metadata-only，loaded 保持 false；Profile preview 无 handler 调用，revision 冲突保护有效配置。
2. 诊断保留阶段身份和状态，超过 200 条自动淘汰，并验证凭据脱敏。

实际命令（工作目录 `windows/code/desktop-pet`）：

| 命令 | 结果 |
| --- | --- |
| `npm run check` | PASS，TypeScript 0 errors |
| `node --test dist/tests/next65/management65.test.js` | PASS，2/2 |
| `npm run test:next65` | PASS，139/139，退出码 0 |

## 限制与交接

本步先完成管理服务和无副作用 UI presenter，未把 Electron IPC 或端到端视觉测试伪称已完成。K65-10 使用实际分发产物做整版自动验收，11 再做产品人工验收。
