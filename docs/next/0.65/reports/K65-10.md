# K65-10 自动验收报告

日期：2026-09-22；状态：AUTO_PASS（10-A～10-E 已通过；前置：K65-09 AUTO_PASS）。

## 交付

- `tools/release-next65.mjs` 生成 `dist/next65/RELEASE_MANIFEST.md` 和 `release-manifest.json`，记录内核 hash、五个包版本/manifestHash 与全部文件 hash。
- `build:next65` 产出内核、普通包、兼容包、TTS、STT 和第三测试包；每个目录都由真实 `validateManifestFile` 校验。
- `test:next65` 在测试执行前重新构建清洁产物、扫描禁入文件并运行全部 0.65 套件，避免开发树路径假通过。

## 测试证据

新增 `tests/next65/releaseArtifact.test.ts`，覆盖：

1. 清单包含五个独立包和内核 hash，包 ID/版本与 manifest 对齐。
2. 分发文件无 `.key`、`.pem`、`.onnx`、`.bin` 等敏感或模型载荷。

实际命令（工作目录 `windows/code/desktop-pet`）：

| 命令 | 结果 |
| --- | --- |
| `npm run check` | PASS，TypeScript 0 errors |
| `node tools/build-next65.mjs && node tools/release-next65.mjs` | PASS，5 packages / 17 files |
| `npm run test:next65` | PASS，139/139，退出码 0 |

## 限制与交接

自动门槛已完成，尚未宣称 RELEASE_ACCEPTED。云端服务、硬件麦克风/播放、真实听感和完整 Electron UI 仍按 K65-11 的人工清单验收；本步不发布、不推送。
