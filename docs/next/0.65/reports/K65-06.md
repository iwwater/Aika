# K65-06 自动验收报告

日期：2026-09-22；状态：AUTO_PASS（06-A～06-E 已通过；前置：K65-05 AUTO_PASS）。

## 交付

- `packages/stt/entry.mjs` 是独立 STT 包，只声明 `stt.transcribe`，不依赖 TTS 或播放。
- `stt.cloud.batch` 使用凭据和网络出口，`stt.local.streaming` 使用无凭据的本地进程生命周期；两者在同一能力下按 adapter identity 共存。
- `tools/build-next65.mjs` 生成并校验 STT 包 manifest，模型权重不进入包体，模型路径由 host configuration 提供。
- 既有 `QwenAsrProvider` 和 `SherpaStreamingAsr` 未复制到内核；测试通过真实 provider 类和真实 HostContext 接线验证边界。

## 测试证据

新增 `tests/next65/sttPackage.test.ts`，覆盖：

1. STT 包独立加载、两个 adapter 同时解析、无 TTS 依赖。
2. 云端 batch 读取 scoped WAV 并返回原文；媒体 scope 释放后无残留。
3. 本地 streaming worker fixture 产生一次 partial 和一次 final，finish 后只提交一次；取消另一个 session 不产生迟到事件。

实际命令（工作目录 `windows/code/desktop-pet`）：

| 命令 | 结果 |
| --- | --- |
| `npm run check` | PASS，TypeScript 0 errors |
| `node --test dist/tests/next65/sttPackage.test.js` | PASS，2/2 |
| `npm run test:next65` | PASS，129/129，退出码 0 |

## 限制与交接

本步的网络和本地 worker 均为用户授权的受控 fixture，未消耗真实凭据或模型权重。00-D 已有真实语料门槛继续作为本地 ASR 指标证据；真实模型加载、麦克风权限、组合 UI 和 Profile 选择由 K65-07/10/11 验收。
