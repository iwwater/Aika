# K65-11 集中人工验收清单

日期：2026-09-22；状态：PENDING_USER_ACCEPTANCE；前置：K65-10 AUTO_PASS，自动套件 139/139。

本报告不把自动测试或文档生成当成人工确认。用户集中验收时使用 `dist/next65/RELEASE_MANIFEST.md` 对应产物，逐项记录“通过 / 失败（复现步骤）/ 未验证”。

## 人工前置准备（2026-09-22）

以下是进入用户集中验收前已经完成的环境和预检，不替代下面 11-A1～11-A9 的人工结论：

- `.env` 已配置为完整 OpenAI-compatible 路径 `https://hanbaoyu.ggff.net/v1/chat/completions`；`PET_NEXT_REAL` 仍保持安全默认值 `0`，真实回放仅在单次进程中临时设为 `1`。
- Gemini Flash key 最小鉴权请求返回 HTTP 200；当前模型为 `gemini-3.1-flash-lite-preview`，未使用 `agycli` 模型。
- 真实本地前置回放：ASR 冻结样本与静音负例 **2/2 PASS**；Windows SAPI TTS **1/1 PASS**。
- 真实 LLM 行为回放在修正端点路径后 **2/2 PASS**：单轮回复、两轮上下文连续性；生产 `OpenAiCompatibleDialogueProvider` 结算为 `success`。
- Windows renderer smoke **PASS**：`WINDOWS_SMOKE_OK`（Live2D renderer、isolated preload、离线后端 round-trip、panel layout）。同一 smoke 进程还记录了一条脱敏的 `Renderer: script-error`；生产诊断不会暴露原始错误，已保留为人工验收前的跟进项，不能据此宣称真实桌宠窗口全量通过。
- Computer Use 前端预检 **PASS**：本机管理控制台连接成功；模型与声音、模块状态、运行记录页面可渲染，Provider 卡片和诚实空态可见。临时 loopback 服务已关闭。
- 首次 LLM 回放失败的原因已定位并修正：`.env` 只填了 `/v1` 根路径，而 `ProviderTransport` 要求完整 `/chat/completions` 路径；未修改生产传输策略。

## 清单

详细的逐项操作、准备条件、通过标准和记录模板见：[K65-11 人工验收待测矩阵](./K65-11-MANUAL-MATRIX.md)。截图中 preview 窗口的控制台不可用是设计限制；preview 只能完成矩阵 A 区的前端预检，不能替代真实包、Provider、管理台和设备验收。

| 编号 | 操作 | 结果 |
| --- | --- | --- |
| 11-A1 | 仅内核 + 普通包启动，完成多轮文字对话，确认近期历史和取消 | 待用户填写 |
| 11-A2 | 只导入 TTS 包，确认未导入 STT 时文字仍可用、语音输出可切换和停止 | 待用户填写 |
| 11-A3 | 只导入 STT 包，确认 partial/final、取消、重连和无 TTS 文字输出 | 待用户填写 |
| 11-A4 | 普通、+TTS、+STT、+TTS+STT 四组合分别启动，检查未选包不初始化 | 待用户填写 |
| 11-A5 | 修改 Flow Profile、预览、激活；确认下一轮生效、来源切换不串 binding | 待用户填写 |
| 11-A6 | staging TTS 更新、重启切换、停用、rollback；确认旧版本和数据边界 | 待用户填写 |
| 11-A7 | 兼容数据、来源撤销、严格遗忘和失败恢复 | 待用户填写 |
| 11-A8 | 云端/本地来源切换、同能力多模型/音色、错误定位和 local-service 停止边界 | 待用户填写 |
| 11-A9 | 缺包、缺凭据、设备拒绝、stage 失败/跳过/partial 的诊断展示 | 待用户填写 |

## 交接规则

本轮用户反馈已登记到 [0.65 TOFIX 台账](../TOFIX.md)：功能入口静默失败、鼠标穿透恢复、左键轻点/拖拽误判均已完成代码修复。修复证据为 pointer-router 10/10、Windows smoke 通过；真实失焦热键和物理鼠标轻点仍保留人工复验项。

- 发现问题时记录复现步骤、产物版本、scope/profile/stage 和预期行为；修复后补自动测试并重跑受影响套件。
- 在用户明确确认前，0.65 保持 `PENDING_USER_ACCEPTANCE`，不标 `RELEASE_ACCEPTED`，不发布或推送。
- 用户确认后再把 0.61 标为 `ACCEPTED`、0.65 标为 `RELEASE_ACCEPTED`，并把实际限制交接给 0.7。
