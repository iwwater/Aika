# K65-05 自动验收报告

日期：2026-09-22；状态：AUTO_PASS（05-A～05-E 已通过；前置：K65-04 AUTO_PASS）。

## 交付

- `packages/tts/entry.mjs` 是独立 TTS 包，只声明 `tts.synthesize`，不声明 STT、麦克风或视觉能力。
- 同一能力下提供 `tts.cloud`（`credentialRef`、远程 API、unary/streaming）和 `tts.sapi`（无凭据、本地进程、unary）两个 adapter，验证了多源并存而非单一实现槽。
- `tools/build-next65.mjs` 生成独立 TTS 包和 manifest；Host Runtime 按 `capabilityId + adapterId + adapterVersion` 校验注册，因此多源不会互相覆盖。
- 受控测试直接调用既有 Qwen/SAPI production provider 类：云端 transport 返回 WAV 下载地址，本地 execute fixture 生成 WAV，均经过媒体 scope 清理。

## 测试证据

新增 `tests/next65/ttsPackage.test.ts`，覆盖：

1. TTS 包可独立经 HostContext 惰性加载，云端与本地 adapter 同时可解析，且无 STT 依赖。
2. 两来源分别产生可解码 WAV，输出媒体与 voice/source 保持隔离。

实际命令（工作目录 `windows/code/desktop-pet`）：

| 命令 | 结果 |
| --- | --- |
| `npm run check` | PASS，TypeScript 0 errors |
| `node --test dist/tests/next65/ttsPackage.test.js` | PASS，2/2 |
| `npm run test:next65` | PASS，127/127，退出码 0 |

## 限制与交接

云端调用使用用户授权的受控 transport，未消耗真实凭据；本地 SAPI 使用受控进程 fixture。真实服务可用性、听感、完整播放 UI 和取消竞态由 K65-07/10/11 验证，接入时复用同一 provider/binding 契约，不改内核包格式。
