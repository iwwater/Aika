# K65-02 自动验收报告

日期：2026-09-22；状态：AUTO_PASS；前置：K65-01 AUTO_PASS。

## 交付

实现 `windows/code/desktop-pet/plugins/host-config.ts` 与 `plugins/host-runtime.ts`：

- `host-config.json` 独立记录安装后的启用意图，不因导入自动执行入口。
- `discoverInstalledPackages()` 对已登记包做真实 manifest/完整性复核，仅读元数据。
- `createPackageHost()` 按请求惰性导入安装副本；依赖和包内插件依赖先加载。
- 相同请求使用 single-flight；禁用包、依赖缺失、循环、版本不满足和重复身份明确拒绝。
- HostContext 提供作用域数据、事件、能力注册/解析、资源登记和释放；插件失败时回收本次资源，关闭时逆序释放。
- 入口模块使用真实 ESM `import()`，不把生产包加载器替换成 mock。

没有把 Provider 来源解析提前并入宿主；K65-02A 继续负责多源注册、绑定和实例租约。

## 测试证据

新增 `tests/next65/hostRuntime.test.ts`，使用真实目录包导入、真实安装副本和真实 ESM 入口，覆盖：

1. 发现/启用不执行入口，首次请求才激活。
2. 并发首次请求只执行一次激活，关闭释放资源。
3. 已安装未启用的包被拒绝且入口执行计数为零。
4. 激活异常释放已登记资源，宿主无残留 active/resource 状态。

实际命令（工作目录 `windows/code/desktop-pet`）：

| 命令 | 结果 |
| --- | --- |
| `npm run check` | PASS，TypeScript 0 errors |
| `npm run test:next65` | PASS，107/107，退出码 0 |

其中旧 K65-01、0.61 回归和新 K65-02 用例均由同一 runner 收集；真实外部模型/设备门槛仍按 TESTING 分列，未用 fixture 冒充。

## 限制与交接

K65-02 只交付包宿主、安装配置和生命周期基础；没有实现运行中停用/更新事务、Provider 来源实例管理、普通产品包接入或管理 UI。这些分别留给 K65-02A、K65-03、K65-08 和 K65-09。
