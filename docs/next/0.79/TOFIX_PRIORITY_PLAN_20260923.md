# TOFIX/TODO 当前优先级与执行计划

日期：2026-09-23。状态：**提案，尚未实施**。范围：`windows/code/desktop-pet/` 的 Windows 正式入口及直接消费者。依据为当前工作树源码、[现场 TOFIX](../0.75/TOFIX_20260923.md)、[TODO](../TODO.md)、[0.79 SPEC](SPEC.md) 与 [N079-09 报告](reports/N079-09.md)；旧台账的“待修”或旧行号不能单独作为当前实现状态。执行细节见 [本轮 SPEC](TOFIX_PRIORITY_SPEC_20260923.md)。

## 1. 选择结论

| 顺序 | 条目 | 现在做什么 | 源码依据与选择理由 |
| --- | --- | --- | --- |
| P0 | ACCEPT-11 / FIX-01：Trace 存量隐私 | 核对正式进程是否加载读取兜底；在隔离副本完整演练现有脱敏工具；形成正式库变更前可审阅的影响、备份与恢复记录。正式库清理单独执行。 | `core/trace-store.ts` 已在写、`list`、`get` 脱敏；`management/server.ts` 已强制默认脱敏。报告称正式库仍有旧明文，运行进程曾是旧构建。优先收口的是部署和存量，不重复写一套 Trace UI。 |
| P1 | UI-MAN-01：穿透时聊天/设置不可点 | 修正 Electron 命中策略并做真实 Windows 输入验收。 | `desktop/electron/main.mjs` 目前对唯一 `BrowserWindow` 整窗调用 `setIgnoreMouseEvents(true)`；`forward: true` 只转发移动，不能让 drawer 按钮重新获得左键。现有右键钩子只解决恢复入口。直接阻断日常交互。 |
| P2 | ACCEPT-02 / FEAT-01 / R-TODO-19：开启后立即可验问候 | 增加一次显式开启后的即时预览，保留日常 45 分钟空闲、6 小时冷却与忙碌抑制。 | `desktop/local-greeting.ts` 的 `tick()` 必须先满足空闲；`desktop/main.mjs` 仅每 30 秒轮询。现场要求“开启后马上看到一条”，当前路径无法满足。范围小，能与桌面输入回归一并验。 |
| P3 | FIX-02 / N079-03：长期记忆遗忘 | 做正式构建和页面路径复核、合成数据端到端回归；仅有新复现再修。 | `management/ui/views.mjs` 已有确认按钮，`app.mjs` 已调用 `/api/memory/forget`；N079-03 报告有 SQLite/HTTP/Electron 自动证据。旧 TOFIX 的“入口缺失”现已过时，不能重复造删除路径。 |

执行单位为 P0、P1、P2 三个独立改动；P3 是同轮准入验证。P0 的正式用户库写操作需要单独记录实际影响与恢复路径，不能把隔离副本 PASS 写作正式库已清理。大量未提交改动在同一工作树中；实施时先复核目标文件差异，保留现有改动。

## 2. 现在不并入的条目

| 条目 | 处理位置与原因 |
| --- | --- |
| FIX-03/04/05/06/07、PERF-01、0.65 TOFIX | 已有对应修复/定向证据；按正式运行实例做回归，出现新复现才重开。 |
| ACCEPT-08 模块健康 | `core/health-snapshot.ts` 的 registry 是观察证据，空表不等于故障。另立模块可见性 SPEC，先定义“已配置/已观察/可用”三态和无付费探测边界。 |
| ACCEPT-09/10/12、FIX-08、FEAT-03、DEBT-001/002 | Wiki/Soul 候选沉淀、Timeline 管理页、Knowledge 分域、Packages/Flow 和自然语言批量维护跨越正式来源、审阅、撤销与 Host/Flow 接线。N079-08/09 尚未过门槛；不能在本轮小修中将占位页伪装成闭环。继续按 0.79 剩余门槛与 0.8/0.85 RPD 分别拆 SPEC。 |
| FEAT-02/04、R-TODO-02/16 | `management/settings.ts` 与 `app/trial-config.ts` 已支持开放模型/协议绑定，`self-setup-view.mjs` 仍有固定 provider 展示和凭据 UI。要先核对实际多源管理契约，再立配置专题；不照旧 TODO 的“完全硬编码”结论改写核心。 |
| R-TODO-03/12/13/14、R-BUG-15 | 分别涉及本地模型安装、流式语音、屏幕授权主动陪伴、完整 Trace、真实播放竞态；依赖引擎/权限或稳定复现，留在后续版本。 |
| R-TODO-17/18 | 模块测试脚本与 `DistillationScheduler` 已存在，按当前交付证据核对剩余差距，不重复实现。 |

## 3. 执行顺序与停点

1. 固定当前工作树、构建和正式进程修订；只用聚合计数检查 Trace，先在隔离副本演练清理与恢复，再核对正式进程默认 API/UI 的 16/16 摘要结果。正式库的写入在完成影响清单后单独决策和记录。
2. 对 UI-MAN-01 写真实 Electron 输入失败用例，修正窗口级穿透与交互区域的仲裁；检查右键恢复、快捷键、拖拽、抽屉关闭/发送、面板控件及 DPI/窗口缩放。
3. 对 ACCEPT-02 写固定时钟行为测试及桌面页面交互测试；仅在用户显式由关到开时触发一次预览，不将其计入自动问候配额或写入记忆/Trace。
4. 用合成记忆完成 P3 正式路径验收；对仍未完成的旧库迁移、真实 Flow/Host 消费、User Wiki/Soul 候选和长时运行保留 `IN_PROGRESS/NOT RUN`，不因本轮小修升级 0.79 版本状态。

完成定义：每项具备代码/运行实例/自动/人工四列状态与直接证据；报告沿用 `docs/next/0.79/reports/`。本计划只确定优先级和边界，不修改既有 N079-00～09 的状态。
