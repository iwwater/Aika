# N079-01 执行报告：遗忘闭包与提炼边界

首轮执行日期：2026-09-23；2026-09-24 扩展复验：IMPLEMENTED，核心自动安全矩阵 PASS；真实旧库升级未验证。

修复 Continuity 根事实失效传播：派生 Soul 的活动判断会递归核对来源链；遗忘或纠正根事实后，依赖该根的下游派生项不可见，旧派生 ID 不能再次激活。提炼成功时本地保留请求的 `cutoffPoint`/`workTitle`；未知截止点不能退化成允许全部区块。

测试证据：

- `npm run test:next079`：14/14 通过；覆盖 A→B→C 闭包失效/拒绝复活、根事实纠正、提炼本地元数据保留及未知 cutoff 拒绝。
- `npm run test:next07`：50/50 通过。
- `npm run test:next075`：9/9 通过。

首轮执行时尚未覆盖完整重启、陈旧 lease、跨实例和复杂多块/Unicode 组合；这些自动化边界已在下方 2026-09-24 补测。未修改真实用户数据库。

## 2026-09-24 自动化扩展复验

- 新增旧幂等回执测试：先遗忘事实，再以原 `operationId` 重放写入；现会报版本冲突，不返回回执中保存的旧正文。关闭并重开 SQLite 后仍成立；重复 forget 返回已擦除的空正文。
- 新增陈旧派生 lease 重启/跨配对测试：来源撤销后的 lease 重启后仍拒绝；另一配对的有效 lease 不受影响。
- 新增多来源、长章节和 Emoji 区块截止点用例，先复现到截止章节只允许第一个切分块，随后修复为纳入截止章节所有块、排除后续章节；未知截止点仍拒绝。
- `npm run test:next079`：42/45；上述安全测试通过。3 项失败均为 Electron 页面未能启动，不是该模块断言失败。
- `npm run test:next07`：51/51 PASS。
- 未拿到真实旧库副本，本轮未做旧库升级或真实用户库迁移。
