# N079-07 执行报告：本地时间问候与超时取消

日期：2026-09-23；状态：IMPLEMENTED/WIRED；策略自动测试和本机桌宠冒烟通过，时钟/长时边界未全验证。

新增只依赖本地时钟和应用内交互的问候 scheduler：默认开启，可在桌宠功能面板持久关闭；本地时段固定模板，45 分钟无应用内交互后可触发，6 小时冷却，8 秒气泡。忙碌时消费掉本时段提示而不排队；不调用 LLM、STT、TTS、Memory，也不读取全局键鼠/屏幕。Electron user-data 保存开关及最近展示时间，重启后恢复冷却。

40 秒“思考超时”现在匹配当前 request ID/scope 后发真实 `cancel`，使迟到结果失效；旧 timer 不能覆盖新轮。

测试证据：`npm run test:next079`：14/14；scheduler 覆盖时段、空闲、忙碌不排队、冷却、关闭及模拟重启后的冷却。`npm run test:windows:ui` 桌宠 Live2D/backend 冒烟通过，但没有强制等待真实 45 分钟来触发问候，也未执行 40 秒超时到达的完整设备 E2E、夏令时/时钟回拨/休眠长时场景。

页面沿用原气泡与功能菜单外观；无逐页人工审核记录（按用户本轮豁免）。

## 2026-09-24 自动化扩展复验

- 将 renderer 的 40 秒 thinking timer 提取为 `ThinkingTimeout`，桌宠仍以原 request ID/scope 和当前气泡状态作所有权检查；测试注入 timer，不等待真实 40 秒。
- 新增测试验证当前轮 40 秒只取消一次、已排队的旧 timer 不能取消新轮、当前 scope 不再持有 thinking 状态时不做取消。
- scheduler 假时钟新增 30 小时休眠恢复场景：只对唤醒时当前时段触发一次，不补发错过时段；回拨、持久化冷却、忙碌不排队场景通过。
- 新逻辑/调度自动化均在 `npm run test:next079` 中通过。`npm run test:next079:electron` 当前为 0/2：点击穿透及本地问候预览页面均报 `ERR_FAILED`，GPU 子进程同样以 `-1073741515` 退出。
