# N079-09 执行报告：兼容回放与 0.8 准入

日期：2026-09-24 自动复验；状态：IN_PROGRESS；N079-08/09 的自动开发门槛已完成，纯逻辑/API/SQLite/跨进程回放通过；Electron、真实旧库与长时运行门槛未清，不能标记完整验收。

## 已执行自动证据

| 验证 | 结果 |
| --- | --- |
| `npm run test:next079`（build、desktop bundle、API/SQLite/Electron） | 2026-09-24：42/45 PASS。3 项 Electron Memory/Trace/WebGL 页面因 Chromium GPU 子进程退出 `-1073741515`（`STATUS_DLL_NOT_FOUND`）而页面报 `ERR_FAILED`；串行运行，随机本地端口固定在安全高位范围 |
| `npm run test:next079:electron` | desktop bundle PASS；0/2。点击穿透与本地问候预览的 `data:` 页面均报 `ERR_FAILED`，GPU 子进程启动失败 |
| `npm run test:emotion:ui` | 0/1。Electron 情绪管理页面同样在 GPU 子进程缺失 DLL 后报 `ERR_FAILED`；对应逻辑/API 为 27/27 PASS |
| `npm run test:next07` | 51/51，通过 |
| `npm run test:next075` | 9/9，通过 |
| `npm run test:emotion` | 27/27，通过 |
| `npm run test:next08` | 42/42，通过（包括管理端会话页 Timeline 组件测试） |
| `npm run check` | PASS，TypeScript 无诊断 |
| `conversation-candidates.test.ts` | 2/2，通过；候选来源、过滤、重启、幂等、撤销及不自动晋升 |
| `n079-flow-conversation.test.ts` | 1/1，通过；认证管理 API 保存/激活 Flow，真实 Host/BackendSession 消费，80 轮 + 进程重启后 20 轮 |

本轮重复运行了完整 N079 测试脚本。单独串行运行后，非 Electron 测试均通过；Electron UI 场景仍稳定复现 GPU 子进程 `-1073741515`（`STATUS_DLL_NOT_FOUND`），禁用硬件加速也未使该主机上的 Chromium 启动。受限执行环境拒绝读取 Win32 显示适配器信息，且本会话未提供另一台可执行 Windows 主机；所以无法在本会话切换到正常 Electron 环境。具体缺失的 DLL 尚未定位。WebGL 预览及 3 个 Electron UI 场景仍须在可启动 Electron 的 Windows 主机复验。

本轮还单独重跑了 `test:next079:electron`（0/2）与 `test:emotion:ui`（0/1）。当前 Windows 总计有 6 个独立 Electron UI 场景未能进入页面（3 个 next079 主套件、2 个桌宠穿透/本地问候、1 个情绪页）；这表示自动 UI 门槛仍未通过，不代表页面断言已通过或发现了 UI 产品缺陷。缺失的具体 DLL 未定位，且没有替代 Windows 主机可供本任务切换。

N079-09 真实对话回放测试在两个独立 Node 进程中运行 80 + 20 轮；Flow 来自认证管理 API 保存并激活的 profile，实际调用临时安装的有效 package/Host，并逐轮验证其输出进入唯一 dialogue 请求。该测试是合成 package 与 100 轮自动回放，不能外推为真实用户 Host 配置、跨日持续运行或长期运行验收。旧 PET1 schema 1/2/3 合成副本“启动前拒绝且文件哈希不变”、WAL sidecar 保持不变、同产品数据库重开保留数据的保护测试仍可作为拒绝策略证据，不是迁移/恢复成功证据。

## 未完成门槛

- 未复制真实旧数据库做升级前后、备份恢复/重启演练；所有 SQLite 测试使用合成/隔离库。结论限制为新安装自动化可测，旧库升级/发布未放行。
- 管理端创建并激活的 Host/Flow 已证明由真实对话路径消费；限 `context.source` 本地只读能力，Network egress 与伪报本地 side effect 均拒绝。
- 100 轮跨两个 Node 进程的对话回放通过；跨日休眠/时钟回拨、长时间或数周运行尚未验证，短回放不代表长期质量。
- N079-08 候选事实路径已接入并通过自动化安全验证；候选只等待显式审核，测试不证明语义准确率或真实用户体验。
- 当前 Electron UI/WebGL 自动验收 3 项被 Chromium GPU 子进程缺失 DLL 阻断；需要在另一台可正常启动 Electron 的 Windows 环境复跑。当前会话没有可用的替代 Windows 主机。
- UI 人工逐页审核按用户明确要求不做；它既不是阻塞项，也不等于人工版本验收通过。

可继续独立开发不依赖长期记忆的 0.8 感知/临时观察接口；在获得真实旧库演练副本、完成 Electron 自动场景并补齐所需长期运行门槛前，不进行 0.79 发布验收。人工界面验收仍按用户要求保持未审核。
