# N079-04 · 情绪页读取与历史兼容

状态：EXISTING；逻辑/API 自动化 27/27 PASS；2026-09-24 Windows Electron UI 复验 0/1，GPU 子进程缺失 DLL，页面未加载。

入口：[RPD](../RPD.md) · [SPEC](../SPEC.md) · [源码核对](../SOURCE_AUDIT.md) · [执行报告](../reports/N079-04.md)。

## 改造范围

emotion-view、emotion API 和数据投影的必要适配；不重写情绪推理或伪造情绪。

## 使用与提供的接口

现有 instanceId/characterId/revision、messages/background、nullable observation、分页及 AbortSignal。

## 实施步骤

先复现：采集脱敏 API shape、请求终态、active/visible/epoch/reading 变化。严格校验失败应出现错误而不是假定卡死；如历史缺字段确为原因，仅对明确旧 schema 转换为 unknown，保留作用域/来源校验。补请求超时或明确取消终态，避免自动无限重试。

## 验收

空数据、null observation、旧 schema、损坏行、错实例、401、服务失败、永不返回、离页/重入/恢复可见性；全部终态可解释，不保留永久 loading。

## 完成与交接

保留现有校验和请求生命周期；本轮复用既有视觉并通过自动 UI 验证，未做单页人工体验审核。

执行证据见 [报告](../reports/N079-04.md)，其中记录基线、改动、测试/真实证据、用户审核与遗留项。
