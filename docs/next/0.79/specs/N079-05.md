# N079-05 · Live2D 独立预览排障

状态：NOT_STARTED；日期：2026-09-23；前置：00。

入口：[RPD](../RPD.md) · [SPEC](../SPEC.md) · [源码核对](../SOURCE_AUDIT.md)。本文件是实施规格，不是已完成报告。

## 改造范围

presentation-view、presentationAssetRoutes、管理静态资源/CSP、Canvas 挂载与预览生命周期；不修改桌宠表现逻辑。

## 使用与提供的接口

现有 /api/presentation、/presentation-runtime/core.js、/presentation-assets/*、/presentation-shaders/*、createPresentationPreview/dispose/resize。

## 实施步骤

按请求顺序验证 core/global、动态模块、model/MOC/texture/shader、WebGL、Canvas 非零尺寸、动画循环、离页 dispose。根因定位后最小修补；禁止开放任意文件路由/降低同源策略。需同时检查旧请求完成时是否释放了新预览。

## 验收

真实安装资产可渲染，404/缺核心/WebGL 不可用/容器 resize 有具体错误；快速切页与重试不泄漏上下文；预览不改变聊天模型状态、自动策略或真实桌宠。

## 完成与交接

独立预览界面先问要求和参考图；保存实际可见画面与资源加载证据，测试不取代用户审核。

报告记入 `reports/N079-05.md`，实际执行后再创建，记录基线、改动、测试/真实证据、用户审核与遗留项。

