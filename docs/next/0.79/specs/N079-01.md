# N079-01 · 连续性遗忘与提炼边界修复

状态：NOT_STARTED；日期：2026-09-23；前置：00。

入口：[RPD](../RPD.md) · [SPEC](../SPEC.md) · [源码核对](../SOURCE_AUDIT.md)。本文件是实施规格，不是已完成报告。

## 改造范围

continuity-memory-store、character-distiller、character-pack-validator/store、生产连续性失效消费者；不改界面，不新建存储权威。

## 使用与提供的接口

sourceIds、tombstone、pair revision/epoch、issued Context、cutoffPoint/workTitle、证据块定位。

## 实施步骤

修复根事实遗忘/纠正后的派生闭包；拒绝已撤来源的新提交，重建不得复活。保留幂等且不得从旧 operation 回执重新暴露已遗忘正文。提炼成功后本地附加请求元数据；未知 cutoff 必须明确失败，章节多区块/多来源按固定边界处理。检查活动 Pack 依赖来源撤销后新旧 Context 均不继续使用旧 Soul。

## 验收

review 两条复现先失败再通过；A→B→C 遗忘、纠正、重启、陈旧/新 lease、重复提交、跨实例隔离；截止点不存在/多段/Unicode/模型不回显元数据；原 0.7 用例回归。

## 完成与交接

安全路径必须通过；不接受仅 UI 隐藏，未完成时关闭依赖该数据的 0.8 路径。

报告记入 `reports/N079-01.md`，实际执行后再创建，记录基线、改动、测试/真实证据、用户审核与遗留项。

