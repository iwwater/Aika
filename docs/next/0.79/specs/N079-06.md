# N079-06 · TTS 长 URL 表单布局

状态：CSS PATCHED/BUILD PASS；窄窗、缩放、键盘编辑及实际配置页计算样式矩阵未运行。当前 Electron UI 环境故障，未以源码断言冒充视觉验收。

入口：[RPD](../RPD.md) · [SPEC](../SPEC.md) · [源码核对](../SOURCE_AUDIT.md) · [执行报告](../reports/N079-06.md)。

## 改造范围

实际 Provider/TTS 表单容器、标签/输入/凭据选择及 CSS；不改模型协议、凭据存储或 TTS 引擎。

## 使用与提供的接口

复用已有设置字段和版本保存；URL 原值、光标、复制、键盘焦点不因显示裁剪改变。

## 实施步骤

先根据实际 DOM/CSS 定位真实撑宽节点。对 grid/flex 子项施加恰当的最小宽度和可收缩列，输入保持单行水平滚动；只在适用位置截断显示，不能给整页 overflow:hidden 裁掉菜单/焦点/错误。用户本轮已豁免逐页参考图与审核。

## 验收

长 URL、窄窗口、125%/150%/200% 缩放、长凭据名称、错误提示、键盘导航/复制和保存原值；对比前后截图。

## 完成与交接

沿用已有配置表单风格修正 `.aika-slot` 的两列最小宽度和 endpoint 折行；未做逐页人工截图审核，不调用语音模型。

执行证据见 [报告](../reports/N079-06.md)，其中记录基线、改动、测试/真实证据、用户审核与遗留项。
