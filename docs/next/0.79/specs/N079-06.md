# N079-06 · TTS 长 URL 表单布局

状态：NOT_STARTED；日期：2026-09-23；前置：00。

入口：[RPD](../RPD.md) · [SPEC](../SPEC.md) · [源码核对](../SOURCE_AUDIT.md)。本文件是实施规格，不是已完成报告。

## 改造范围

实际 Provider/TTS 表单容器、标签/输入/凭据选择及 CSS；不改模型协议、凭据存储或 TTS 引擎。

## 使用与提供的接口

复用已有设置字段和版本保存；URL 原值、光标、复制、键盘焦点不因显示裁剪改变。

## 实施步骤

先通过参考图和计算样式定位真实撑宽节点。对 grid/flex 子项施加恰当的最小宽度和可收缩列，输入保持单行水平滚动；只在适用位置截断显示，不能给整页 overflow:hidden 裁掉菜单/焦点/错误。

## 验收

长 URL、窄窗口、125%/150%/200% 缩放、长凭据名称、错误提示、键盘导航/复制和保存原值；对比前后截图。

## 完成与交接

先问当前表单想怎样调整，再取参考图；仅定向 CSS/可访问性和相关构建检查，不调用语音模型。

报告记入 `reports/N079-06.md`，实际执行后再创建，记录基线、改动、测试/真实证据、用户审核与遗留项。

