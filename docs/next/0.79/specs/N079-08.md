# N079-08 · 0.7 最小生产连续性写读闭环

状态：PARTIAL；BackendSession→History ID outbox→Companion Timeline→重启恢复→ProductionContinuityContext 路径 PASS；User Wiki/Soul 候选事实自动沉淀尚未接线。

入口：[RPD](../RPD.md) · [SPEC](../SPEC.md) · [源码核对](../SOURCE_AUDIT.md) · [执行报告](../reports/N079-08.md)。

## 改造范围

正式消息提交和既有后台维护扩展、Companion Timeline/连续性候选投影、重启恢复与消费接线；完整 UI 和长期策略优化后置。

## 使用与提供的接口

唯一提交事件、pairing、session/turn/messageId、来源版本、operationId、现有 queue/lease、Pack 版本。

## 实施步骤

成功提交的对话通过 ID-only outbox 幂等投影 Companion Timeline；主事务无法原子提交时保留可恢复待投影记录。当前实现未把用户证据提炼成 User Wiki/Soul 候选，因此不得宣称候选沉淀完成；助手自述也不会被直接晋升为用户事实。

## 验收

正式 BackendSession→提交→投影→下一轮 Context，重启恢复、重复事件、故障中断重放、撤销后后台提交、配对隔离；单轮仅一份 Dialogue 请求，后台不阻塞文字回复。

## 完成与交接

Timeline 子路径有真实 BackendSession/SQLite/恢复/Context 证据；但候选事实子路径仍未接通，依赖 User Wiki/Soul 的 0.8 主动陪伴保持禁用，不用手工 seed 冒充生产验收。

执行证据见 [报告](../reports/N079-08.md)，其中记录基线、改动、测试/真实证据、用户审核与遗留项。
