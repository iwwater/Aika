# N079-08 · 0.7 最小生产连续性写读闭环

状态：NOT_STARTED；日期：2026-09-23；前置：01。

入口：[RPD](../RPD.md) · [SPEC](../SPEC.md) · [源码核对](../SOURCE_AUDIT.md)。本文件是实施规格，不是已完成报告。

## 改造范围

正式消息提交和既有后台维护扩展、Companion Timeline/连续性候选投影、重启恢复与消费接线；完整 UI 和长期策略优化后置。

## 使用与提供的接口

唯一提交事件、pairing、session/turn/messageId、来源版本、operationId、现有 queue/lease、Pack 版本。

## 实施步骤

给成功提交的对话做幂等事件投影；明确用户证据生成候选，助手自述/引用/假设不变事实；不每轮自动升 Soul。与主事务无法原子提交时采用可恢复待投影记录而非竞争队列；管理写入与正式写入共享生命周期。明确固定实例的 MVP 支持范围，多实例未支持时拒绝而非串线。

## 验收

正式 BackendSession→提交→投影→下一轮 Context，重启恢复、重复事件、故障中断重放、撤销后后台提交、配对隔离；单轮仅一份 Dialogue 请求，后台不阻塞文字回复。

## 完成与交接

没有真实组合根证据不记 WIRED；若延期此项，0.8 长期画像主动陪伴保持禁用，不用手工 seed 冒充生产验收。

报告记入 `reports/N079-08.md`，实际执行后再创建，记录基线、改动、测试/真实证据、用户审核与遗留项。

