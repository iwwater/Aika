# N079-09 · 兼容回放与 0.8 条件化准入

状态：IN_PROGRESS / RELEASE GATE NOT CLEARED；管理端保存/激活的 Flow 已接入真实 BackendSession 对话请求，100 轮跨进程重启回放自动通过。真实旧库恢复未验证；当前自动复验共 6 项独立 Electron UI 场景被 GPU 子进程缺失 DLL 阻断，100 轮回放不等于跨日或长期运行。

入口：[RPD](../RPD.md) · [SPEC](../SPEC.md) · [源码核对](../SOURCE_AUDIT.md) · [执行报告](../reports/N079-09.md)。

## 改造范围

数据副本、真实启动器/管理实例、0.7/0.75 直接回归、缺能力降级；更新路线图与验收矩阵，不删未验项。

## 使用与提供的接口

版本/迁移、scope/取消、Source/Binding、实际 Host/Flow、History/Trace 权限、0.8 Observation/邀请/Work 消费契约。

## 实施步骤

认证管理 API 保存与激活的 profile 由正式对话 provider 装饰器调用；Flow 仅可执行安装清单中声明的 `context.source` 本地读取能力，输出作为不可信上下文交给唯一一次原对话请求。合成有效 package、Network egress 拒绝、API 鉴权、Host 实际调用、100 轮对话和两次独立 Node 进程重启回放均自动验证通过。旧库副本升级/失败恢复/重启，以及跨日或长时运行仍未验证；Timeline/连续性独立 SQLite 测试不等于全库兼容。

## 验收

测试范围覆盖发生的改动，保留 next07/next075，跑关联集成与 check/build；对话消费通过真实 `BackendSession`/`PackageHost` 入口留证。Electron 自动 UI 与人工体验分别记录；人工逐页体验按用户要求不做，不改变自动化结论。0.65 原 32 项仍保留。

## 完成与交接

产出能力准入表：可进入 0.8 的路径、禁用依赖、延期项/负责人/复验触发条件；不承诺未来零破坏，不宣布全部 0.7/0.75 完成。

执行证据见 [报告](../reports/N079-09.md)，其中记录基线、改动、测试/真实证据、用户审核与遗留项。
