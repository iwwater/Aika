# N079-07 · 零模型时间问候与气泡仲裁

状态：NOT_STARTED；日期：2026-09-23；前置：00；先确认现有聊天取消语义。

入口：[RPD](../RPD.md) · [SPEC](../SPEC.md) · [源码核对](../SOURCE_AUDIT.md)。本文件是实施规格，不是已完成报告。

## 改造范围

小型本地时间/空闲调度器、邀请策略适配、气泡展示仲裁、必要开关与偏好；desktop/main 只接线，不堆第二套 Runtime。

## 使用与提供的接口

本地时钟、应用内最近交互、既有邀请冷却/勿扰/配额、scope/requestId、当前聊天/播放/错误状态、持久化偏好。

## 实施步骤

用固定模板，绝不调用 LLM/STT/TTS 或后台提炼；开启/关闭、时段/频次/停留/文案先确认。忙碌让位、不排队补发过期问候；处理午夜/时区变化/时钟回拨/休眠恢复/重启去重。修 40s 提示：必须选择真实取消当前轮或仅提示仍等待，旧计时器不能改新轮状态。

## 验收

假时钟、连续忙碌、勿扰、禁用、休眠恢复不刷屏、重启冷却、点击穿透、手动关闭、长回复；计数断言所有模型调用为 0、无自动记忆写入、无全局活动监控。

## 完成与交接

先问本页/气泡交互、索取参考图并确认开关参数，审核后再做下一界面；不默认实现文档提案的 6～8 秒或漫画风。

报告记入 `reports/N079-07.md`，实际执行后再创建，记录基线、改动、测试/真实证据、用户审核与遗留项。

