# N082-07 · Observation 情境陪伴与共享仲裁

状态：**NOT_STARTED**。前置：01、05、06；现有持久 Invitation 配额真实入口可用。权威：[RPD](../RPD.md)、[SPEC](../SPEC.md)、[共用契约](../CONTRACTS.md)、[测试规则](../TESTING.md)。源码路径以下均相对 Windows 工程根，标“新增”的是目标文件，不声称已存在。

## 范围与修改路径

将有效屏幕 OCR 观察接入情境候选和轻量气泡，用户回应复用现有文本轮次；被动收集不触发情境输出，旧普通问候服从模式开关。显式提醒保持原用途。不新增自动工具执行、开麦、长期 Wiki 晋升或独立聊天循环。

现有：`companion/proactive-invitation-runtime.ts`、`companion/invitations.ts`、`core/proactive-companion.ts`、`core/observation-context.ts`、`app/backend-session.ts` 与桌面气泡直接消费者；目标新增最薄 `companion/observation-candidate-source.ts`。00 确认哪个 runtime 真正持有配额，只扩展该权威。

## 本步接口、消费者与生命周期

提供 ObservationCompanionPort.offer/withdraw，输入 observationId/sourceVersion/generation，不从事件包拿永久正文；消费当前模式、持续/上下文授权、现有忙闲/勿扰与共享每日投递总账。08 组合根装配、桌面 Presentation/TurnPort 消费。

候选先做来源、新颖性、预算、忙闲检查；仅必要时经已选 Binding 有界生成，最多一个生成在途。展示前再次复验，重复帧/文本不重复开口；忽略/拒绝冷却，配额和 0.79 问候共用。云模型使用 OCR 文本必须有 ContextUseGrant；本地 OCR 可继续而生成不可用。生成输出只作候选，不越过仲裁。

sourceRef 增加 Observation 类型时保持旧 continuity 候选兼容；TTL 不长于观察，删除/过期/切被动撤回未展示候选并清上下文。接受时再查有效性，走原文本 Scope 与 Inbox，不以伪 memory_record 或永久拷贝绕过旧外键。需要迁移 invitation schema 时提交源类型/引用兼容测试；Observation 在进程重启不复活。

## 验收与交接

- AC-08207-1：真实 OCR Observation→生产候选→真实配额账本→桌面事件完整回放；fixture 模型协议与真实配置模型结果分列。
- AC-08207-2：忙闲、勿扰、冷却、全局配额和重复事件不额外展示/无界调用；被动禁止普通自动问候与屏幕候选，显式提醒不受误伤。
- AC-08207-3：生成中切模式/撤销/过期、展示前失效、接受前失效均不使用旧正文；切角色不串上下文。
- AC-08207-4：接受进入唯一正常文本轮次，无自动开麦/工具/长期晋升；无 ContextUseGrant 时云 transport 零请求。

报告写 `reports/N082-07.md`，记录源码/产物版本、接口/消费者变化、命令/退出码、真实与 fixture 区分及未跑项。本步通过只代表自身范围，不代签最终人工验收。

