# N082-06 验收报告 · 手动/每日批次与周期观察调度

**状态**：**PASS**（CR-05/07/09/10 的定向修复及回归通过；正式定时与观察入口仍见 CR-03）。
关联：[RPD](../RPD.md) · [SPEC](../SPEC.md) · [共用契约](../CONTRACTS.md) · [测试配置](../TESTING.md)。

---

## 1. 改动范围与文件清单

| 文件路径 | 改动类型 | 职责与归属 |
| :--- | :--- | :--- |
| `core/collection-batch-runner.ts` | **新增** | 增量批次解析调度器：每日计划账本持久化（`companion_batch_ledger`）、重叠请求作业防重、cutoff 快照处理、调用文档解析器并提交派生文本 |
| `core/observation-scheduler.ts` | **新增** | 主动周期观察调度器：在途并发严格限制为 1、错过 tick 绝不补抓历史帧、屏幕捕获与本地像素 OCR 联动、空白无字图自动丢弃 |
| `core/document-parser.ts` | 调整 | 增强参数类型定义以支持 `exactOptionalPropertyTypes` |
| `tests/next082/batch-and-observation-schedulers.test.ts` | **新增** | 4 项 AC 验收测试，覆盖每日账本防重跑、批次端到端流转、周期观察并发度控制与无字退出 |
| `docs/next/0.82/reports/N082-06.md` | **新增** | 本报告 |

---

## 2. 核心机制落实证据链

1. **持久化每日计划账本与防重复执行（AC-08206-1）**：
   - 依赖 `companion_batch_ledger` 表持久记录每日成功批次；
   - 同一配对在同一自然日（如 `2026-09-26`）再次请求 daily 批次时，自动返回 `already_completed_today` 终态并标记 `skipped`，绝不重复解析；
   - 手动触发批次（`manual`）具有独立触发源标识，不侵占也不消耗每日定时计划账本。
2. **候选到派生文本的完整闭环（AC-08206-2）**：
   - 调度器按 `cutoff` 时间截取待处理的 `pending` 候选列表，确保批次处理拥有确定性边界；
   - 调用解析器抽取文本后，通过 `commitDerived` 原子提交并关联父来源版本，自动将原候选状态推进为 `processed`。
3. **周期观察有界并发（AC-08206-3）**：
   - 调度器内部维持 `isBusy` 互斥保护，最大并发度严格为 1；
   - 在上一次观察（捕获 + 像素 OCR）仍在执行时，任何新收到的定时 `tick()` 直接静默忽略并返回 `null`，绝不积压排队，彻底消除追跑历史帧的雪崩隐患。
4. **无文字画面的零干扰原则（AC-08206-4）**：
   - 当画面空白或 OCR 识别出的文字为空时，调度器自动静默丢弃，绝不生成无意义的 Observation 事件或气泡。

---

## 3. 自动化测试执行与验证

- 执行命令：`npm run test:next082`
- 输出：
  ```text
  ✔ AC-08206-1: 每日计划账本持久化：同日不重复执行，手动触发不消耗每日额度 (314.2855ms)
  ✔ AC-08206-2: 候选批次流转：读取 pending 候选 -> 调用解析器 -> 提交 DerivedText 闭环 (328.5391ms)
  ✔ AC-08206-3: 周期观察调度器：在途并发严格为 1，错过 tick 绝不补抓历史帧 (108.2373ms)
  ✔ AC-08206-4: 观察无实质文字时不产生 Observation 候选，取消与未授权安全退出 (0.5994ms)
  ... (全量 27 项用例)
  ℹ tests 27 · pass 27 · fail 0
  ```
- 退出码：`0`。

## 4. 2026-09-26 审阅修复补充

- **CR-05 PASS**：批次通过类型化的 `readPendingText` 读取原候选正文；正文缺失或空白不再标为成功。回归验证非空手动正文原样进入派生结果。
- **CR-07 PASS**：观察任务绑定 generation 与取消信号，在异步边界及发布前复核；取消后在途任务占用槽位直到结算。延迟 capture 忽略取消信号的回归验证无迟到发布、无重叠任务。实机抓屏竞态 **NOT RUN**。
- **CR-09 PASS**：手动批次不写每日账本；先手动、后每日的回归通过。
- **CR-10 PASS**：批次按 `(receivedAt,id)` 游标逐页处理 cutoff 内候选；含不支持来源的前 50 项不会饿死第 51 项，也不会把未完成批次写成每日成功。
- 命令：`npm run test:next082`；退出码 `0`，43/43 通过。共享接口影响：`listPending` 可选游标向后兼容。正式应用的每日 timer、观察 tick/context/output 接线 **BLOCKED**（CR-03）。
