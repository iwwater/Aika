# FIX61-10 · 版本自动收口与后置验收交接

版本：Aika Next 0.61；日期：2026-09-20；状态：NOT RUN（仅完成源码定位与设计）。

需求：全部本轮条目。前置：FIX61-01～09。代码路径均相对 `windows/code/desktop-pet/`。

目录：[源码定位](#1-源码定位) · [修复与接口](#2-修复与接口边界) · [TDD](#3-tdd-与自动验收) · [回归与交接](#4-回归与交接)

## 1. 源码定位

npm test 当前仅收集 memory/providers；test:next 仅 dist/tests/next；Windows 与 management/integration/UI 另有入口。单跑 npm test 不能证明整版回归覆盖。

## 2. 修复与接口边界

冻结最终源码提交、模型/语料 hash、数据库/设置迁移版本，按 SPEC AC 汇总 evidence。新增 tests/next61 集合及 test:next61 脚本（当前不存在），显式发现非零测试；列出受影响原测试目录，实际执行而非声明“全量”。

自动场景：启动进展→端点/Key 与模型设置→真实生产对话；导入两知识库并切换→换肤→快照生效/撤销；fake microphone PCM→实时 ASR partial→单次提交→回复/TTS→快速取消重试。真实模型/本地音频回放与 fixture 分栏。

报告 docs/next/0.61/reports/FIX61-xx.md：RED 命令/断言、GREEN 命令/退出码、生产路径、AC 对照、未跑原因、上游差异。语音/文本复用既有 CORPUS_MANIFEST，可新增针对回归的最小固定语料。

所有自动门槛通过后冻结可复现基线并直接进入 K65-00，不等待人工确认。人工项标 DEFERRED_TO_K65_11，统一在 0.65 改造后验收；人工覆盖左右键与拖动、换肤、知识库实际影响回答、Key/模型设置、麦克风选择/试听、边说边出字、启动失败恢复。发现问题先补失败测试再修再定向复验。

以上新接口名是设计建议，不声称已经存在；复用上游 Scope、取消、存储与鉴权边界。只修改本 SPEC 所需代码，共享接口变动同步记录消费者。

## 3. TDD 与自动验收

每个 AC 先建立生产路径失败测试，保存 RED 证据，再最小实现、GREEN 和重构；现有已通过行为保留回归，不要求用户中途操作。状态均为 NOT RUN。

- 10-A：01～09 每条必需 AC AUTO_PASS；必要真实回放不是 fixture 冒充；任一 BLOCKED/NOT RUN 则版本未开发完成。

- 10-B：npm run check、npm run test:next、npm run test:next61（新增后）、npm run test:windows、受影响 management/integration/media/UI 测试、Windows 构建和自动启动冒烟通过。

- 10-C：0.6 数据备份迁移、重启恢复、回退方案验证；新配置未回写旧 Aika 工作树，无 Key/个人音频/模型权重入 Git。

- 10-D（人工，后置）：把本版体验清单交 K65-11 合并，记 DEFERRED_TO_K65_11。10-A/B/C 全通过即可标 FIX61-10 AUTO_PASS 并进入 0.65；本项不阻塞自动交接，但用户明确验收后才能标版本 ACCEPTED。不自动推送、发布或分发。

## 4. 回归与交接

构建采用 npm run build:windows，桌面冒烟采用 npm run test:windows:ui；环境/资产缺失如实 BLOCKED。真实远程请求仅用已授权服务，不在文档任务中执行。

完成报告写 `docs/next/0.61/reports/FIX61-10.md`，记录实际基线、改动文件、受影响契约、AC/命令/退出码、真实与 fixture 证据。不把设计通过写成开发通过。

相关文档：[执行索引](../SPEC.md) · [版本 RPD](../RPD.md) · [源码核对](../SOURCE_AUDIT.md)。

