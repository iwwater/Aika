# FIX61-03 验收报告 · 启动进度、停滞监视与重试

- SPEC：[FIX61-03](../specs/FIX61-03.md)。需求：R-TODO-10。前置：无。
- 基线提交：`046ee27c47032fad80620ea183b889b8b0dea134`（aika-next）。工作树含 79 个未提交的 0.61 改动（FIX61-03 的 transport 实现、测试与本轮新增改动均在其中），本报告在该工作树上验证；未回滚、未 stash、未提交。
- 状态：**AUTO_PASS**。03-A/03-B/03-C 由既有 7 用例（transport-startup）+ windows transport 4 用例真实跑通；03-D 由本轮 RED→GREEN 补齐（connection-state 2 用例、真实后端进展 1 用例、真实 Chromium UI 1 用例 13 项检查）；受影响回归（test:next 80/80、windows 22/22、Electron 冒烟）通过。
- 日期：2026-09-21。执行：goal worker（FIX61-03 执行轮）。

## 1. 改动与设计

上轮结论"实现已存在"经本轮实测确认：`desktop/electron/transport.mjs` 的 backend_startup 协议、停滞窗口（默认 60s 可配）、generation 串行化与关闭序列均已在工作树中实现且全部真实跑通。本轮实测发现 03-D（UI 实际显示）只有底层支撑、没有渲染端消费，后端也没有任何进展发射点；按 TDD 补齐了这一层。另有一处本 SPEC 范围外的语法错误阻塞渲染端构建，做了最小修复（见 §6）。

| 文件 | 改动 |
| --- | --- |
| `desktop/view-state.ts` | `DesktopConnectionState` 新增 phase/sequence/completed/total/elapsedMs 字段：connecting 记录刷新显示（同代合并刷新、新代全新开始），终态一律清空；被拒记录不留残留 |
| `desktop/main.mjs` | 状态行显示启动阶段（正在校验/正在装配 + completed/total + 秒数）；新增"取消启动"按钮（仅 connecting 可见可用）；失败原因区分 ready_timeout/stalled/launch/exit 的可见文案；启动相关字段纳入 renderUI 的 diff key |
| `desktop/electron/main.mjs` | 新增 `cancel_startup` shell 分支：connecting 时 `connection.cancel()`（EOF→shutdownTimeout→SIGKILL 序列） |
| `desktop/native/main.swift` | 同一 `cancel_startup` 分支的 macOS 宿主对应实现（`connection.close()`） |
| `app/trial-backend.ts` | 生产端进展发射：`readActiveTrialConfiguration` 后、`verifyTrialRuntime` 前后、presentation policy 前、微信恢复后各发一条 `{channel:'backend_startup', sequence, phase, completed, total, elapsedMs}`；stdout 仅计数与冻结 phase 名，无路径/凭据/诊断 |
| `desktop/cubism-renderer.mjs` | （越界最小修复，见 §6）`loadRig` 内 `let binding` 与参数 `binding` 重复声明导致的 SyntaxError，重命名为 `digestInput` |
| 测试 | 新增 `tests/desktop/connection-state.test.ts`（2）、`tests/next61/fix61-03.startupProgress.test.ts`（1）、`tests/next61/fix61-03.ui.test.mjs` + `ui-scenario.mjs` + `ui-electron.mjs`（1，13 项检查）；更新 `tests/management/integration.test.ts` 的 stdout 首条契约断言 |

## 2. 逐 AC 证据

运行目录均为 `F:/AIVoice/Aika-Next/windows/code/desktop-pet`。

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `npm run build` | 0 | tsc + wechat 构建（两轮：验收前、改动后） |
| `node --test tests/next61/transport-startup.test.mjs` | 0 | 7/7（改动前后各跑一次，见 §3） |
| `node --test tests/windows/transport.test.mjs` | 0 | 4/4 |
| `node --test tests/windows/{codex,launch,platform,transport}.test.mjs` | 0 | 22/22（windows 组全量） |
| `node --test dist/tests/desktop/connection-state.test.js` | 0 | 2/2（RED→GREEN） |
| `node --test dist/tests/next61/fix61-03.startupProgress.test.js` | 0 | 1/1（真实后端进程） |
| `node --test tests/next61/fix61-03.ui.test.mjs` | 0 | 1/1（Electron 44.4.1 真实 Chromium，13 项检查） |
| `npm run test:next` | 0 | 80/80 |
| `node tools/dev-desktop.mjs --smoke-test` | 0 | WINDOWS_SMOKE_OK（真实 Electron + Live2D + 离线后端往返） |

| AC | 状态 | 证据 |
| --- | --- | --- |
| 03-A 累计超过 60 秒但持续真实进展可 ready；完全无输出或仅重复心跳进入可见失败 | PASS | transport-startup 3 条 03-A 用例 7/7 之中。假时钟（`t.mock.timers`）+ 真实子进程/真实管道：18 次 `progress` 记录间各 tick 5s（累计 90s 假时钟）后仍 connecting 并最终 ready，terminal 状态为空、total=100 原样转发；完全无输出的进程 60001ms 后 `failed`/`ready-timeout`/canRetry；20 次相同心跳（sequence/completed 不前进）后 `failed`/`stalled` 且一条进展都没有发布过；无进展窗口可配（500ms 起始窗 + 2000ms 停滞窗用例），停滞恰好发布一次终态。阶段/计数 UI 可见性由 03-D 的 Chromium 用例覆盖（"正在校验对话服务 20/80 · 2 秒"实际渲染） |
| 03-B 启动取消、退出、spawn 错误、损坏 JSON、hash 不匹配可诊断；失败不能绕过完整性检查进入 ready | PASS | transport-startup 03-B 两条：取消发布 `disconnected`/`cancelled`/canRetry、子进程以 exit 0 退出（EOF 先行）、closing 集合清空、取消后 send 返回 false；ready 前 exit(3) 发布 `exit`+exitCode=3；不可执行文件发布 `launch`，均可重试。损坏 JSON→`invalid-message` 且允许新 generation ready（windows/transport 2/4）。hash 不匹配：`verifyTrialRuntime` 变更文件后 `assert.rejects`（windows/launch 22/22 之中），真实后端进程在 backend_ready 前也走同一校验（startupProgress 用例的 verifying 进展点即在 hash 校验处） |
| 03-C 快速连续重试只存在一个活跃后端；旧 generation 的 ready/exit 不改变新状态 | PASS | transport-startup 03-C 两条：3 次 `start()` 并发后 generation=+3、被取代的子进程已退出（exitCode 非 null）、锁文件仍恰好被一个后端持有、无任何 backend_error（没有后端见过忙锁）、被取代 generation 在其替换启动后零状态发布、旧 generation 的 send=false 新 generation send=true、close 后锁释放；迟到的旧代 ready 逐字节检查不复活/不影响新代，新代恰好发布 connecting+ready 各一条 |
| 03-D UI 实际显示阶段、原因、取消和重试；重试成功后恢复文字与语音入口 | PASS | connection-state 2/2：phase/sequence/completed/total/elapsedMs 在 connecting 中保持可见、重复心跳不改变计数、新代重置 total 保留 phase、终态清空、被拒记录零残留。Chromium 用例 13/13 检查：真实 `index.html`+`build/renderer.js` 渲染"正在校验对话服务 20/80 · 2 秒"→"正在装配对话服务 45/80 · 3 秒"、心跳不改计数、"取消启动"按钮在 connecting 可见、点击发出 `cancel_startup`、stalled 后显示"对话服务启动停滞，无进展"、取消按钮隐藏、重新连接按钮出现且点击发出 `reconnect`、generation 2 ready+backend_ready(bridge 0.7.0) 后显示"我在这里"、send/voice 按钮恢复可用 |

### RED 证据

本轮 03-D 为真实 TDD：先写测试、在未改实现时运行并保存 RED，再最小实现转 GREEN。RED 记录（退出码均为 1）：

- `node --test dist/tests/desktop/connection-state.test.js`：2/2 fail，`actual: undefined, expected: 'verifying'`（phase 字段不存在）与后续字段断言失败。
- 后端进展用例首跑：无 backend_startup 记录（初始 RED 由真实后端 stdout 捕获验证；随后两轮 RED 属 fixture 环境问题——symlink EPERM 与 macOS bundle 缺失，均已改为 junction 与 electron 必需集解决，非实现问题）。
- Chromium 用例首跑（无渲染端实现时）：10 项检查失败（按钮缺失、状态文案恒为"正在连接…"、无 cancel_startup），实现后转 13/13 通过。

03-A/03-B/03-C 的实现与测试由前序轮次落盘、本轮未观测到 RED 阶段，属"实现已存在、验收未跑"的补验情形；本轮在其真实路径上复核通过。

## 3. 上轮"transport 假红"的最终判定

**确认环境假红，本轮真实环境全绿。** 上轮沙箱观测的 `Transport timeout` 与 `'launch' !== 'exit'` 与 piped spawn 被沙箱拒绝的形态吻合；本轮在真实环境同一工作树、同一测试文件 7/7 通过（首跑 3.8s 总时长），逐用例：

1. `03-A: sustained real progress past 60s of accumulated time still reaches ready` ✔ 99ms
2. `03-A: no output terminates on the startup window, repeated heartbeats on the stall window` ✔ 89ms
3. `03-A: progress renews a configurable no-progress window and a later stall is visible` ✔ 80ms
4. `03-B: cancelling startup is diagnosable, ends the child and releases the shutdown bookkeeping` ✔ 79ms
5. `03-B: exit before ready and an unlaunchable executable stay visible and retryable` ✔ 82ms
6. `03-C: rapid retries keep exactly one live backend and a single released lock` ✔ 1648ms
7. `03-C: a superseded generation cannot revive or fail the current one` ✔ 1650ms

改动完成后重跑仍 7/7。windows 组（`npm run test:windows` 等价：codex 7 + launch 1 + platform 14 + transport 4）22/22，上轮"21/22 失败"状态已不复存在（恢复）。

## 4. 回归

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `npm run check`（验收早期一次） | 0 | 无类型错误 |
| `npm run build`（收尾） | 0 | 编译通过 |
| `npm run test:next` | 0 | 80/80 |
| windows 组 4 文件 | 0 | 22/22 |
| `tests/desktop/view-state.test.ts` + 新 connection-state | 0 | 12/12（10+2） |
| `dist/tests/integration/trial-launcher.test.js` | 0 | 2/2 |
| `tools/dev-desktop.mjs --smoke-test` | 0 | 真实 Electron 冒烟（覆盖本轮 electron/main.mjs 改动） |
| `dist/tests/management/integration.test.js` | 1 | 2/2 fail——**环境性既有失败**，与本轮改动无关（证据：还原本轮断言改动后同样 2/2 fail；失败点均在 fixture 的 symlink EPERM / sqlite-wal EBUSY 清理竞态，先于断言执行）。本轮仍按新契约更新了其 stdout 断言（`initial.at(-1)` 为 backend_ready 且之前只有 backend_startup） |
| `dist/tests/integration/trial-config.test.js` | 1 | 14 用例 2 fail——**既有 Windows 环境缺陷，未修（非 03 范围）**：① launch 用例 fixture 无 `desktopHost:'electron'`，win32 上必抛 "targets macOS"（该用例只能在 macOS 过）；② "independent ASR" 用例 `await import(resolve(...))` 传入盘符路径触发 `ERR_UNSUPPORTED_ESM_URL_SCHEME`（仓内同类代码均用 `pathToFileURL(...).href`） |

## 5. 证据性质分列（fixture / 真实）

- **真实生产路径**：backend_startup 协议/停滞窗口/generation 串行化（真实子进程+真实管道，仅假时钟）；进展发射（真实后端进程：真实 hash 校验、真实 SQLite、真实锁文件，真实 EOF 关闭与锁释放）；UI 渲染（真实 Electron 44.4.1 Chromium + 生产 index.html + 生产 renderer bundle）；Electron 冒烟（真实窗口 + 离线后端完整往返）。
- **fixture**：UI 用例的"shell 桥"与后端进展记录由测试注入（这证明渲染端消费与宿主协议编排，不证明真实后端在真实硬件上的时序）；startupProgress 用例的 provider/budget 面为 fixture。
- **真实远程供应商**：未调用（无授权凭据），NOT RUN。
- **人工验收**：真人桌面环境中观察启动阶段文案、点按取消/重试 NOT RUN，属版本末人工项（FIX61-10）。
- **macOS 宿主**：Swift 侧 `cancel_startup` 分支已同步实现但 NOT RUN（本机无 macOS 编译/运行环境），属待人工/待联调项。

## 6. 共享契约影响（03 拥有：backend_startup / generation / 关闭序列）

| 契约 | 变化 | 消费者与影响 |
| --- | --- | --- |
| `backend_startup`（stdout 协议） | 生产端 trial-backend 现在会在 backend_ready 前发出 3~4 条进展记录 | 消费者 04/07：通过 shell 的 connectionChanged 转发读取 phase/sequence/completed/total/elapsedMs——两宿主均原样透传（electron main.mjs `deliver('connectionChanged', state)`、Swift `deliver("connectionChanged", ...)`），渲染端 `DesktopConnectionState` 已加字段；旧消费者忽略未知字段不受影响。Windows 宿主对进展做窗口续期，进度停止驱动"stalled"；macOS Swift 宿主不解析该 channel（记录在 `state != "ready"` 时被丢弃），仍走 15s 兼容超时，行为未回退 |
| `tests/management/integration.test.ts` stdout 契约 | 首条消息从 backend_ready 变为 backend_startup（ready 仍在） | 已同步更新断言；任何直接解析 stdout 首条消息的外部脚本需要同样调整（仓内已无其他此类消费者） |
| generation / 关闭序列 | 本轮无变化（前序轮已实现：串行 spawn、EOF→超时→SIGKILL、旧代消息隔离） | 04/07 依赖的"ready 前进度可读、旧代消息不可用"语义已由 03-C 用例实证 |
| shell 消息 `cancel_startup` | 新增（electron + Swift 双宿主） | 渲染端"取消启动"按钮；宿主仅在 connecting 态接受，非连接态忽略 |

## 7. 已知限制与待人工

- **越界最小修复（需审阅确认）**：`desktop/cubism-renderer.mjs` 的 `loadRig` 存在 `let binding` 与参数 `binding` 重复声明的 SyntaxError（FIX61-05 未完成的未提交改动），导致 esbuild 无法产出 renderer bundle，阻塞一切渲染端验证。本轮将循环内遮蔽变量重命名为 `digestInput`（语义中性、无行为变化）。该修复不属于 03 范围，仅因不修无法完成任何 03-D 验证；FIX61-05 执行轮应复核其原意（此变量仅为拼指纹字符串）。
- **真实桌面人工验收 NOT RUN**：真实屏幕上的阶段文案可读性、取消/重试的真人操作路径、真实慢启动（大数据库/多模型）下的观感，属 FIX61-10 版本末人工项。
- **macOS 宿主 NOT RUN**：Swift `cancel_startup` 分支与其 15s 兼容超时的组合行为需 macOS 环境验证。
- **生产端进展粒度较粗**：SPEC §2 设想的"读配置、hash、数据库、Provider 装配"逐阶段上报，本轮落地为 hash 校验前后与装配完成共 3 个锚点（启动很快时足够驱动窗口续期与 UI 阶段显示）；若未来启动耗时迁移到数据库打开阶段，可在对应位置增加发射点而不改协议。
- **其他 SPEC 观察（未顺手修）**：① `trial-config.test.ts` 两个用例在 Windows 上的环境缺陷（macOS-only fixture、`import(resolve(...))`），建议其归属轮或 FIX61-10 修复；② `management/integration.test.ts` 在 Windows 上 fixture 建删层的 symlink/EBUSY 既有失败；③ SPEC.md §1 仍写"全部开发 NOT RUN"，与 §2 索引表实际状态不一致（延续 FIX61-02 报告的观察）。

相关文档：[执行索引](../SPEC.md) · [版本 RPD](../RPD.md) · [源码核对](../SOURCE_AUDIT.md)。
