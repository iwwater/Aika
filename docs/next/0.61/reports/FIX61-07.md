# FIX61-07 验收报告 · 模块健康状态与麦克风诊断

- SPEC：[FIX61-07](../specs/FIX61-07.md)。需求：R-TODO-08、R-TODO-11。
- 基线提交：`046ee27c47032fad80620ea183b889b8b0dea134`（aika-next，其上叠加 FIX61-01/04/06）。
- 状态：**AUTO_PASS**（核心自动部分）。8 条自动用例通过。真实硬件听感与 Electron 窗口内试麦属版本末人工验收。
- 日期：2026-09-21。执行：goal worker。

## 1. 解决的两个用户诉求

**R-TODO-08 状态灯**：用户要的是「绿=已连接可用」，不是「端点能访问」。核心修正是把三种不同强度的断言分开：

| 证据 | 含义 | 能否点亮绿灯 |
| --- | --- | --- |
| `configured` | 该槽位有凭据/地址/模型绑定 | 否 |
| `reachable` | 端点应答了非计费请求（如模型列表） | 否 |
| `operational` | **本次配置下真实调用成功** | **是** |
| `selfCheck` | 本地能力自检通过（如 SQLite 完整性） | **是** |

这正是 SOURCE_AUDIT 指出的缺陷：「模型列表可达就可以绿灯」——只能证明 API 可访问，不证明模型权限、语音能力、推理成功。

**R-TODO-11 麦克风诊断**：用户当前语音不可用且原因不明。关键设计是**试麦与后端 ready 解耦**——后端失败时用户仍必须能检查设备。

## 2. 改动文件

| 文件 | 改动 |
| --- | --- |
| `core/health-snapshot.ts`（新） | `ModuleHealthRegistry`、`HealthEvidence`、`HEALTH_GREEN_EVIDENCE`、`evidenceFromCall()`、`healthStalenessMs()` |
| `media/microphone-test.ts`（新） | `MicrophonePreferenceStore`、`MicrophoneTestLease`、`describeTrackSettings()`、`TEST_RECORD_MAX_MS` |
| `management/health-routes.ts`（新） | `/api/health`、`/api/health/:module`（含修复入口）、`/api/microphone/preference` |
| `management/runtime.ts` | 新增 `health` 字段与 `configRevision` 参数；`observeCall` 在成功/失败路径写入真实证据（**取消不写证据**） |
| `management/server.ts` | 挂载 `/api/health*` 与 `/api/microphone*` |
| `management/bootstrap.ts` | 透传 `health: healthManagement(runtime.health)` 与麦克风偏好 |
| `app/trial-backend.ts` | 打开真实 `MicrophonePreferenceStore` |

## 3. 逐 AC 证据

运行目录：`F:/AIVoice/Aika-Next/windows/code/desktop-pet`
主命令：`node --test dist/tests/next61/fix61-07.health.test.js`，退出码 **0**，tests 8 / pass 8 / fail 0。

| AC | 状态 | 证据 |
| --- | --- | --- |
| 07-A 缺 Key、列表可达但推理失败、语音配置存在但模型未加载、SQLite schema 错误均不会误绿；每个灯能到原因与可用修复入口 | PASS | 四则子断言：`configured:false`（缺 Key）→ `unknown`；`configured+reachable` 但无 operational（列表可达）→ `degraded`，**断言 `notEqual ready`**；语音引擎未加载 → 非绿；`schema_mismatch` → `failed` 且 `repairAction` 非空。另断言**每一条非绿灯都必须有 `reasonCode` 或 `repairAction`**（无解释的灯在结构上不成立），并提供 `defaultReason`/`defaultRepair` 兜底 |
| 07-A 只有 operational 证据才点亮绿灯；切配置后旧结果失效 | PASS | `operational:true` → `ready`；`HEALTH_GREEN_EVIDENCE` 恰为 `['operational','selfCheck']`。陈旧判定：超过 30 秒（`healthStalenessMs()`）后不再显示 `ready`，但 `evidence.operational` 仍保留可见；`reconfigure(8)` 后 `snapshot().configRevision === 8`、观测仍记 `configRevision === 7`、`stale === true`、`reasonCode === 'config_changed'` |
| 07-B 后端 failed 时可独立试麦；明确动作前不申请权限，试麦没有摄像头/网络/数据库写入；结束与异常释放全部媒体资源 | PASS | 用例用一个只实现 `enumerateDevices`/`getUserMedia` 的 fake 驱动真实 `MicrophoneTestLease`：`listDevices()` 后 `permissionRequests === 0`（**列设备不申请权限**）且**只返回 audioinput、绝不列出摄像头**；`start()` 时断言 `constraints.video === undefined`（**永不请求摄像头**）；`stop()` 后 fake 记录到 `stopped`，`active() === false`，`level() === null`；`start()` 通过注入的 media 完成，不经过任何后端连接状态——即后端 failed 时仍可用 |
| 07-B 枚举输入设备，选择 default 或 deviceId，偏好只存本机 app userData；设备变更/拔出显示原因，可明确选择回默认，不静默换麦 | PASS | 偏好 store 以 `deviceId: null` 表示「系统默认设备」（**显式选择，不是缺失值**）；`save('mic-2')` 后重开文件读回 `'mic-2'`；`save(null)` 读回 `null`；落盘内容不含任何文件系统路径 |
| 07-C 设备拔出、标签隐藏、拒绝权限、无声/全零 PCM 的诊断不混淆 | PASS | `diagnose()` 四种输入分别得到 `device_missing`/`label_hidden`/`permission_denied`/`no_signal`，**断言四者 code 互不相同**，且各自 message 指向不同用户动作（重新选设备 / 先授权 / 检查系统隐私设置 / 检查静音与输入源） |
| 07-D 5 秒上限、电平值域、实际采样率显示、关闭面板清理 | PASS | `TEST_RECORD_MAX_MS === 5000` 且 lease 返回 `maxDurationMs` 并挂 5 秒自动停止定时器；`validateLevel` 拒绝 `rms<0`、`peak>1`、`peak<rms`，接受 `0..1`；`describeTrackSettings({})` → 三项全 `null`（**缺失即 unknown，不编造**），有值时如实读取 `sampleRate`/`channelCount`/`label` |

### RED 证据

实现前 `npm run check` 失败原文：

```
tests/next61/fix61-07.health.test.ts(9,80): error TS2307: Cannot find module '../../core/health-snapshot.js' or its corresponding type declarations.
tests/next61/fix61-07.health.test.ts(10,107): error TS2307: Cannot find module '../../media/microphone-test.js' or its corresponding type declarations.
```

## 4. 回归

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `node --test dist/tests/next61/{fix61-07.health,fix61-06.knowledge,fix61-04.pointer,fix61-01.openConfig}.test.js` | 0 | **31/31** |
| `npx tsc -p tsconfig.json`（仅本 SPEC 文件） | 0 | 无本 SPEC 相关错误（同批并行 SPEC 的在途错误不计） |

## 5. 已知限制与待联调

- **真实硬件听感与 Electron 窗口内实际试麦**属版本末人工验收（FIX61-10），此处标 **NOT RUN**。
- 「不为定时点灯生成付费请求」由设计保证：健康只由 `observeCall` 的真实结果与显式 `selfCheck` 驱动，**没有任何定时器发起探测**；但也因此首轮对话前多数灯为 `unknown`/`degraded`——这是刻意的诚实状态，不是缺陷。
- 桌面端试麦 UI（设备下拉、电平条、5 秒本地回放与 object URL 销毁）尚未接入 `desktop/main.mjs`：本 SPEC 交付的是可直接驱动的生产 lease 与偏好 store，UI 接线属 FIX61-10 收口前的人工验收准备项，**标 NOT RUN**。
- 前端 `media/browser-capture.ts` 尚未接收 `deviceId` 参数（正式采集仍用系统默认设备）。这是 R-TODO-11 的下一步接线，本轮未完成，如实登记。

相关文档：[执行索引](../SPEC.md) · [版本 RPD](../RPD.md) · [源码核对](../SOURCE_AUDIT.md)。
