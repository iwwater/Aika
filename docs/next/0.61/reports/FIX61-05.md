# FIX61-05 验收报告 · 多模型换肤与模型包生命周期

- SPEC：[FIX61-05](../specs/FIX61-05.md)。需求：R-TODO-07（换肤）。前置：FIX61-04。代码路径相对 `windows/code/desktop-pet/`。
- 基线提交：`046ee27`（aika-next）。工作树含大量未提交的 0.61 改动，本报告在该工作树上验证；**未回滚、未 stash、未提交**。
- 状态：**PASS**。05-A/05-B/05-C/05-D 四条 AC 全部由真实跑通的用例支撑（9 用例：skinStore 5 + renderer 4），其中 05-D 使用**两套合法真实 Cubism 模型**，非 mock。
- 日期：2026-09-21。执行：FIX61-05 执行 worker。

## 0. 现场核实（接续上一轮被取消的 worker）

| 项 | 核实结果 |
| --- | --- |
| `reports/FIX61-05.md` | **原本不存在**，本报告为本轮新建（非推倒重来） |
| `specs/FIX61-05.md` 头部状态 | 原为 `NOT RUN（仅完成源码定位与设计）` → 本轮改为 `PASS` |
| `SPEC.md` 索引行 | 原为 `NOT RUN` → 本轮改为 `PASS` |
| 已有实现（未跟踪/已改） | `contracts/skin.ts`(新)、`management/skin-store.ts`(新)、`management/moc-inspector.ts`(新)、`desktop/cubism-renderer.mjs`(改)、`tools/configure-model.mjs`、`desktop/electron/assets.mjs` |
| 已有测试 | `tests/next61/fix61-05.skinStore.test.ts`（4 用例）、`tests/next61/fix61-05.renderer.test.ts`（3 用例） |

上轮"skinStore 0/4 假红（fixture 路径解析到 `F:\AIVoice\.local` 被沙箱拒）"经本轮真实环境复现：**确为环境假红**。本轮 `danger-full-access` 下 4/4 立即通过，无需改代码。

## 1. 本轮改动

| 文件 | 改动 |
| --- | --- |
| `desktop/cubism-renderer.mjs` | **核心新增**（详见 §2）：① `releaseSkinResources()` 释放旧皮肤资源并计数；② `loadRig` 先以独立 `CubismMoc` 探针验证候选**可构建**，通过后才释放旧皮肤；③ generation 哨兵，迟到的被取代切换丢弃自身工作而非回写；④ 提交前 `stale()` 检查 + 原子提交 `skin`/`activeRevision`；⑤ 无 `Expressions` 的 rig 不再崩溃（真实多模型必需）；⑥ `import CubismMoc`、`CubismMotionManager` |
| `tests/next61/fix61-05.renderer.test.ts` | 重写为可在 Node 运行（用 esbuild 以桌面构建相同配置打包渲染器，注入 Cubism Core + `pet://` fetch 垫片）；新增 **05-D 两套真实模型**用例；修正 2 处测试自身缺陷（见 §5） |
| `tests/next61/fix61-05.skinStore.test.ts` | 新增 1 用例：05-C"换肤只改外观，对话 scope/Memory/知识库不变" |

### 为什么渲染器必须打包才能测

`cubism-renderer.mjs` 是浏览器代码：`import` 的 Cubism Framework 是 **TypeScript 源文件**（含 `enum`，Node strip-only 模式报 `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`）且使用**无扩展名 specifier**（`--experimental-transform-types` 下报 `ERR_MODULE_NOT_FOUND`）。这与生产路径一致——`desktop/build-web.mjs` 同样用 esbuild 打包。测试复用同一 esbuild 配置，**被测的仍是生产渲染器代码**，不是替身。

## 2. 渲染器生命周期实现（本轮真正补齐的部分）

上一轮只留下 `this.generation = 0` 与 `releaseCounts` 的**字段声明**，没有任何实现。实测证据（改动前）：

```
after switch counts: {"textures":0,"renderers":0,"models":0,"listeners":0}
old model getParameterCount after switch: 96      ← 旧模型仍存活，从未释放
```

即 05-B"迟到不覆盖"与 05-C"释放计数平衡"**均未实现**。本轮按 TDD 补齐：

1. **先验证后切换**（AC 关键）：哈希只证明来源，不证明可构建——字节翻转的 moc 仍可能哈希成其自带目录所记录的值。故在释放任何东西之前，用**独立 `CubismMoc` 探针**真正构建一次候选（探针自带 `deleteModel`/`release`，不触碰屏上模型），并要求参数表非空。失败则原样抛出，`releaseCounts` 不变、旧皮肤存活。
2. **再释放**：验证通过后 `releaseSkinResources()` 删除旧纹理、调用 SDK `release()`（权威释放 renderer/moc/模型/各 manager）并计数 `models`（+`renderers` 当有 WebGL renderer 时）。**不额外显式释放 renderer**——SDK `release()` 已包含，否则双重释放。
3. **manager 复原**：SDK `release()` 会把 `_motionManager`/`_expressionManager` 置 null，导致切换后 `reset()`/`setAutomaticPolicy()` 抛 `Cannot read properties of null`。这两者属于实例而非某一皮肤，故复原（`_modelMatrix` 由 `loadModel()` 依新 rig 画布尺寸重建，不在此复原）。实测修复后 `policy: ok / reset: ok / updateView: ok`。
4. **generation 哨兵**：每次 `loadRig` 领取一个 generation；`stale()` 判断自己是否已被更新切换取代。被取代者在提交前抛 `模型切换已被更新的选择取代`，绝不回写 `skin`/`activeRevision`。
5. **原子提交**：`this.skin = skin; this.activeRevision = skin.revision;` 仅在全部成功后执行。

## 3. 逐 AC 证据

运行目录均为 `F:/AIVoice/Aika-Next/windows/code/desktop-pet`。

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `npm run build` | 0 | tsc + wechat（改动前后各一次） |
| `node desktop/build-web.mjs` | 0 | 生产渲染器 bundle 构建（确认渲染器改动不破坏桌面构建） |
| `node --test dist/tests/next61/fix61-05.skinStore.test.js` | 0 | **5/5** |
| `node --test dist/tests/next61/fix61-05.renderer.test.js` | 0 | **4/4** |
| `npm run test:next` | 0 | **80/80** |
| 15 个确定性 fix61 测试文件（01/02/03/04/05/06/07/09，排除 ui 与 live） | 0 | **81/81** |
| `node --test dist/tests/contracts/*.test.js dist/tests/app/*.test.js` | 0 | 4/4 |
| `node --test dist/tests/integration/presentation-intent.test.js` | 0 | 2/2 |

| AC | 状态 | 证据 |
| --- | --- | --- |
| **05-A** 两套本地 fixture 包导入、枚举、切换、重启恢复；每套实际使用自己的模型、纹理、参数与预设 | PASS | skinStore `05-A` 用例：内建包无需导入即注册（origin=builtin）；第二套包导入后 `modelFingerprint`/`assetFingerprint` 均为 64 hex、`modelEntry=pet.model3.json`、`capabilities.textures=1`、参数映射为自身四项；`activate(1,'second-pack')` → `activeSkinId=second-pack`、`revision=2`；**重启**（重新 `SkinStore.open`）恢复 `second-pack`；两套包 `asset()` 解析到**各自**不同路径且均非内建路径；`activate(1,...)` 旧 revision 抛 `version_conflict` 且**不掉活**当前皮肤 |
| **05-B** 损坏 moc/缺资源/错误 hash/路径逃逸拒绝，旧模型仍可用；快速 A→B→A 迟到加载不能覆盖最终选择 | PASS | skinStore 两条 05-B：翻转 moc 字节被拒（真实 Core 判定参数表为空）、删纹理被拒、5 种逃逸（绝对路径/盘符/反斜杠遍历/`..`/百分号编码）全拒、catalog 指纹与字节不一致被拒（`预设目录与模型字节不一致`）、Cubism 2（model.json + .moc）被拒；全部拒绝后 `activeSkinId` 仍为 `local-model`、只有通过的 1 个包进注册表、`revision` 只 +1。renderer 两条：损坏候选被拒时 `releaseCounts` **不变**、`skin`/`activeRevision` 不变、`_model` 仍是原对象；**迟到切换**（用挂起的 read 强制晚于胜者完成）抛 `已被更新的选择取代`，最终选择 `final-pack` 在其落定后仍成立 |
| **05-C** 重复切换后释放计数平衡；对话 scope、Memory、当前知识库均不变 | PASS | renderer 05-C：一次切换 `models=1`、新模型 ≠ 旧模型且可用；再切换 3 次 → `models=4`（**每切换恰好释放一次，无泄漏无重复释放**）；切换后 `_motionManager`/`_expressionManager` 存活且 `reset()` 不抛。skinStore 新增 05-C：切换后 `sameScope` 仍成立、`characterId/sessionId/turnId/generation` 全部不变；每个 skin 描述符**不含** `characterId`/`knowledgeLibrary`/`memory` 字段；注册表序列化中不含 `"characterId"`、`"knowledgeLibrary"` |
| **05-D** 至少两套合法真实模型完成自动加载/切换冒烟（只有 mock 不算通过） | PASS | **两套合法真实 Cubism 模型，均经真实 Live2D Cubism Core 构建**：① 内建 `desktop/assets/local-model`（natori_pro_t06，`moc3` 771072B，96 参数 / 176 drawables）；② **第二套真实模型 Hiyori**（`F:/AIVoice/pet-shell/probe/live2d/assets/Hiyori`，`Hiyori.moc3` 443648B，70 参数，2 张 2048 纹理、无 Expressions 组）。Core 实测：`{natori: moc✓ model✓ params:96}`、`{hiyori: moc✓ model✓ params:70}`。renderer 05-D 用例同一实例上：内建 → Hiyori（参数数断言**不相等**，证明确为两套不同 rig）→ 切回内建（参数数复原为 96），`releaseCounts.models=2`。**非 mock**：fixture 与生产共用同一 `CubismMoc`/`Core` 与同一渲染器代码 |

## 4. 05-D 真实模型来源与 BLOCKED 判定

- 仓库内**只有一套**真实模型（`desktop/assets/local-model`）。
- 第二套取自 `F:/AIVoice/pet-shell/probe/live2d/assets/Hiyori`——**仓库外、本机已有、合法 Cubism 4 样例模型**（Live2D 官方免费样例，随其分发条款授权）。上一轮 worker 的 `.probe/fix61-05/load-check.ts` 已指向该路径，本轮核实其完整（moc3 + model3 + cdi3 + physics3 + pose3 + userdata3 + 2 纹理 + 10 组 motion）。
- 因该模型在仓库外，测试以 `AIKA_FIX61_05_SECOND_MODEL` 环境变量定位（默认上述路径）。**已验证**：路径存在时 05-D 真实执行（`skipped 0`）；指向不存在目录时 05-D 显式 SKIP 并说明原因——**不会静默通过**。
- 结论：**05-D 不 BLOCKED**。若审阅机器无该模型，05-D 会如实 SKIP（不会伪装 PASS），需设置该变量指向任一合法 model3 包。
- `tools/configure-model.mjs` 产物：已核实其仍为**单目录绑定**（只读 `desktop/assets/local-model`，校验 Moc/Physics/DisplayInfo/Textures/Expressions/Motions.Idle[0]，拒绝不安全的模型相对路径），未改为支持任意第二套包；此为 SPEC §2"最小支持用户导入"的既有入口，本轮未扩展。

## 5. 修正的上轮测试自身缺陷（诚实记录）

1. `fix61-05.renderer.test.ts` 第 71 行正则为 **GBK 误读的乱码**（`妯″瀷涓庨 预...`），改为 `/未能解析模型|moc|模型/`。
2. 原 05-B"迟到切换"用例**并未真正并发**：先 `await` 了较新的切换再断言旧者，旧者根本没机会迟到。改为用可挂起的 `readAsset` 强制旧切换晚于新切换完成——这样才真正测到"迟到不覆盖"。
3. 原 05-D 测试**不存在**；本轮新增。

## 6. `digestInput` 复核（FIX61-03 §7 越界最小修复）

FIX61-03 报告 §7 声明：`loadRig` 内 `let binding` 与参数 `binding` 重复声明的 SyntaxError，已将循环内变量重命名为 `digestInput`，并要求 05 执行轮复核其原意。

**复核结论：符合 05 原意，予以确认保留。** 依据：
- 代码位置 `desktop/cubism-renderer.mjs` `loadRig`，`let digestInput = '';` 后 `digestInput += path + '\0' + hash(...) + '\n'`，仅用于拼接进场指纹字符串，随后 `hash(new TextEncoder().encode(digestInput))` 与 `skin.catalog.modelFingerprint` 比对。
- 该变量**仅**是"把各引用文件路径与其 SHA-256 拼成一段文本再整体哈希"的累加器，**语义与参数 `binding`（模型包绑定对象）完全不同**，故改名未改变任何行为——修复的是命名冲突，不是逻辑。
- 与 `management/skin-store.ts` 的 `legacy` 指纹算法（同样 `path + '\0' + digest + '\n'` 排序拼接后 sha256）**一致**，因此渲染器与注册表对同一套字节得出同一指纹；本轮 05-A/05-B/05-D 的指纹断言全部通过，反向印证该改名未破坏指纹契约。
- 本轮在此基础上新增了"**哈希之外**"的真实可构建性验证（§2 第 1 点）：哈希通过但 Core 建不出参数表时仍拒绝，补齐了仅靠哈希无法覆盖的损坏场景。

## 7. 共享接口影响

- 新增 `contracts/skin.ts`（`SkinManagement`/`SkinManifest`/`SkinDescriptor`/`SkinCapabilities`/`SkinState` 等）。它是**新增契约**，未改动既有契约字段。
- `desktop/cubism-renderer.mjs` 新增公开成员：`releaseSkinResources()`；`loadRig` 增加被取代时抛错的行为。既有 `load()`/`bindSkin()`/`builtInManifest()` 签名不变。
- `management/skin-store.ts` 目前**未接入** management 路由（`contracts/management.ts`、`contracts/index.ts`、`aika-routes.ts` 均无 skin 引用）。注册表能力已完整且被测试覆盖，但**端到端的"管理端导入/切换 → 渲染器生效"联调尚未接线**——这是本 SPEC AC 之外但换肤真正可用的必要步骤，记为遗留项。
- 换肤只改外观：`skinId` 明确**不是** `characterId`（0.7 Character Pack 另行绑定），已由 05-C 用例断言。

## 8. 遗留问题 / 未做（不顺手修）

| 项 | 说明 |
| --- | --- |
| SkinStore 未接入 management 路由/UI | 注册表已完整，但无 HTTP/UI 入口；换肤端到端链路未联调。属 FIX61-05 AC 之外，交由后续 SPEC |
| `tools/configure-model.mjs` 仍单目录绑定 | 未扩展为支持导入任意第二套包 |
| `dist/tests/management/presentation.test.js` 2 例失败 | `actual: 1, expected: 40`（catalog 计数）与 `actual: undefined`。**与本轮无关**：该测试对渲染器零引用（`cubism/renderer.mjs/Jellyfish` 引用数 = 0），属 FIX61-02/04 catalog 层既有问题，本轮只记录不修 |
| `next61` live 用例（如 `liveVoiceIntegration.test.js` 08-E）失败/挂起 | 需真实 ASR 设备与凭据；该文件 import 列表不含本轮任何改动文件，属既有环境依赖。全量 `npm run test:next61` 因此无法整体跑完（600s 超时），本轮改跑 15 个确定性 fix61 文件 81/81 |
| `management/moc-inspector.ts` 未跟踪 | 实测工作正常（真实 Core 建模并返回参数表），但 `git status` 显示为未跟踪新文件，提交时需显式纳入 |
| 探针文件 | `.probe/fix61-05/` 下本轮新增 `node-load.mjs`/`node-switch.mjs`/`node-after.mjs`/`api.mjs`/`rig-bundle.js` 为本轮诊断产物，非交付物，可清理 |

## 9. 未核实项

- **未做真实 WebGL 渲染验证**：渲染器测试为 headless（`canvas=null`，跳过 `createRenderer`/纹理上传）。05-C 的 `renderers`/`textures` 计数在无声路径下保持 0（`gl` 不存在时不删纹理）；真实 GPU 释放计数需 Electron 真机冒烟。
- **未跑全量 `npm run test` / `test:windows` / Tauri 打包**（按 AGENTS.md 测试边界：只跑本模块与本次改动相关范围）。
- **未验证 Electron 真机换肤 UI**（无 UI 入口，见 §8）。
- 第二套真实模型的**授权条款**未做法律复核，仅按"本机已有、官方免费样例"事实使用。
