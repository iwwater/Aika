# FIX61-09 验收报告 · 冻结上下文前缀与异步更新

- SPEC：[FIX61-09](../specs/FIX61-09.md)。需求：R-TODO-09。前置：FIX61-01、FIX61-06。代码路径相对 `windows/code/desktop-pet/`。
- 基线提交：`046ee27`（aika-next）。工作树含大量未提交的 0.61 改动，本报告在该工作树上验证；**未回滚、未 stash、未提交、未执行任何 git 写操作**。
- 状态：**PASS**。09-A/09-B/09-C/09-D/09-E 五条 AC 全部由真实跑通的用例支撑（**27 用例 / 6 文件**），另按 SPEC §2 边界补 7 条边界用例（本轮新增，见 §5）。
- 日期：2026-09-21。执行：FIX61-09 修复轮 SPEC worker。

## 0. 现场核实（接续上一轮中断的 worker）

| 项 | 核实结果 |
| --- | --- |
| 实现是否在场 | **是**：`memory/prefix-snapshot.ts`（untracked）、`contracts/prefix.ts`（untracked）、`contracts/index.ts:97` 的 `DialogueContext.prefix?`、`memory/sqlite-lifecycle-port.ts` 前缀逻辑、`memory/sqlite-lifecycle-state.ts:308` 的前缀来源并入、`providers/aika-dialogue.ts:71 requestHead` 的 Provider 装配，均在场且已被 `npm run build` 编译通过 |
| 测试是否在场 | **是**：5 个 `tests/next61/fix61-09.*.test.ts` + `fix61-09.harness.ts`，本轮在**真实环境**（非沙箱）重跑确认 20/20 绿 |
| 上一轮报告 | `reports/FIX61-09.md` **原本不存在**，本报告为本轮新建 |
| `specs/FIX61-09.md` 头部状态 | 原为 `NOT RUN（仅完成源码定位与设计）` → 本轮改为 `PASS` |
| `SPEC.md` 索引行 | 原为 `NOT RUN` → 本轮改为 `PASS`（带报告链接） |

## 1. 命令与退出码

运行目录均为 `F:/AIVoice/Aika-Next/windows/code/desktop-pet`。

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `npm run build`（tsc + wechat） | **0** | 编译通过（改动前、改动后各一次，均 0） |
| `node --test dist/tests/next61/fix61-09.{budget,frozenPrefix,recovery,refresh,revocation}.test.js` | **0** | **20/20**（逐用例见 §4） |
| `node --test dist/tests/next61/fix61-09.boundary.test.js`（本轮新增） | **0** | **7/7**（先 6 绿 1 红 → 修正测试时序假设后 7/7，见 §5） |
| `npm run test:next61` | **0** | **125/125**（含 09 全部 27 例） |
| `npm run test`（default：memory + providers，09 改了 `memory/*`，属受影响模块） | **0** | **572/572**，fail 0 |
| `npm run test:next` | **0** | **80/80**，fail 0 |

**已知环境性失败核对**：本轮 `npm run test` / `test:next` / `test:next61` 输出中**未出现** `windowsAcl` / `spawnSync EPERM` / powershell 相关失败（`Select-String -Pattern "windowsAcl|spawnSync|EPERM|powershel"` 在两份输出中零命中），fail 计数均为 0。前几轮记录的那批环境性失败在本机本轮**未复现**，因此无需判定为「非本 SPEC 回归」——直接为全绿。

## 2. 本轮改动（TDD：补测 → RED → 修正 → GREEN）

| 文件 | 改动 |
| --- | --- |
| `tests/next61/fix61-09.boundary.test.ts` | **本轮新增**（7 例）。覆盖 SPEC §2 边界：前缀不含轮 ID/时间戳/随机排序、动态后缀才是可裁剪区、前缀自身超预算为显式报错、前缀必须位于请求最前部、纠正即撤销且下一轮先走安全前缀、收紧隐私 hold 立即撤销、跨会话边界不复用 |
| `tests/next61/fix61-09.harness.ts` | 新增可选 `suffixBytes` 透传（`PrefixHarness` → `SqlitePrefixOptions`），仅供边界用例配置后缀预算；**未放宽任何断言** |
| `docs/next/0.61/reports/FIX61-09.md` | 本报告（新建） |
| `docs/next/0.61/specs/FIX61-09.md` | 头部状态行 `NOT RUN` → `PASS` |
| `docs/next/0.61/SPEC.md` | 索引行 `NOT RUN` → `PASS`（带报告链接） |

**未改动任何生产代码。** 上一轮在途的实现经逐条核对已满足 5 条 AC 与全部边界（见 §3/§5），本轮只补测试与文档。

## 3. 逐 AC 证据

| AC | 状态 | 证据（真实运行，非沙箱） |
| --- | --- | --- |
| **09-A** 同快照多轮真实序列化请求的 prefix bytes/hash 相同；仅后缀变化、当前输入一次、冻结摘要与历史不重复 | **PASS**（4 例） | `frozenPrefix`：`c3.prefix.id/hash === c2.prefix.id/hash`，`frozenRegion()` 的 sha256 与 UTF-8 字节长度逐轮相同；`dynamicSuffix` 摘要**不同**且含当前输入；序列化串中 `第三句话`/`第一句话`/`第一句回答`/`第二句话`/`用户喜欢红茶`/`最近聊过喝茶。` 各出现**恰好 1 次**；普通增长（新记忆「用户买了新键盘」）不重写前缀、`prefix.text` 中不含该文本；**重启**后 `c5.prefix.hash !== c2.prefix.hash` 且含新记忆；文本轮与语音轮的冻结区字节相同（perception `happy` 不入前缀）；默认 next-start 下跨 24h 墙钟 `hash` 不变 |
| **09-B** 默认运行内冻结、重启刷新、6h TTL、主动刷新均用假时钟；后台卡住不阻塞 | **PASS**（4 例） | `refresh`：next-start 下 30h 墙钟跳变 + 新记忆增长后 `third.prefix.hash === second.prefix.hash`，重启后下一轮才换（含「用户换了新工作」）；`mode:'interval'` 下 5h 内不变、跨过 6h+1ms 的那一轮**仍在旧快照上被服务**、下一轮才用重建结果，且 `published.record.builtAt === clock.now()`（**假时钟是唯一时间源**）；后台被 `deferred` 卡住时前台轮仍完成且 `prefix.hash` 等于可用快照、`entered === false`（前缀构建不依赖严格记忆写入器）；`tokenBudget:1` 的构建失败报 `prefix_exceeds_budget` 且旧快照仍可用 |
| **09-C** 构建中撤销 CAS 拒绝；模型返回/播放前撤销停止交付；重启不复活 | **PASS**（5 例） | `revocation`：飞行中 forget → `port.assertContextCurrent(c2)` 抛 `stale_context`、`appendAssistant` 被拒；**真实 `DialoguePipeline`** 上 `outcome.status === 'failed'`，`events` 中 **无 `reply`、无 `tts`**、`store.inspect(two,'two:assistant') === null`（未写回）；下一轮先走 `prefix:minimal`（不含被遗忘文本），再走重建的完整快照；改 prompt → 旧身份「青梅竹马」不再下发、新身份「你是用户的同事」到达 provider；切库/删文档 → 旧库文本永不再到达、新库文本被冻结；CAS：构建中改 model / 改 privacyRevision / 改 knowledge key 均抛 `prefix_snapshot_superseded`，行保持 `building`、`reusable()` 为 null，正常 key 才发布成功；重启后撤销的快照**不复活**（`c3.prefix.complete === false` 且遗忘文本未下发） |
| **09-D** 持久化中断/坏 hash/版本冲突可恢复；无快照首轮用安全最小前缀；无全库检索计数证明复用 | **PASS**（6 例） | `recovery`：首轮 `prefix.id === 'prefix:minimal'`、`complete === false`、含当前身份且无未校验库内容、`calls.length === 1`（未等构建）；中断的 `building` 行 `reusable()` 为 null、重试 `revision+1`、旧行事后 publish 被拒；篡改 `prefix_text` → `snapshots.load()` 返回 null、损坏字节从不下发、降级到 minimal；key 中 model 冲突 → 降级 minimal 且冲突行永不被服务/恢复；`counts.rank === 0`（**零次全库重排**）且 `counts.contextRecords <= 15`（5 轮，每轮 ≤3 次有界读）；知识撤销修订按轮携带（`key.knowledge.revision === books.revision`、fingerprint 64 位），删文档后下一前缀即移除 |
| **09-E** 同语料新旧模式实测装配耗时/TTFT/token，**不虚构固定收益** | **PASS**（实测见 §6） | `budget` 用例在同 store、同语料（5 轮）、同 fixture provider 下对比「无前缀配置的动态模式」与「冻结模式」，只报告**本机可测量**的装配耗时与序列化字节；TTFT / cacheUsage / cacheHitRate / cost / inputTokens **全部为 `unknown`** 且用例内 `assert.equal(summary.ttft, UNKNOWN)` 显式断言——**未声称任何收益比例** |

## 4. fix61-09 测试清单（6 文件 / 27 用例）

| 文件 | 用例数 | 性质 |
| --- | --- | --- |
| `fix61-09.frozenPrefix.test.ts` | 4 | fixture transport + **生产** port/store/assembly/provider（09-A） |
| `fix61-09.refresh.test.ts` | 4 | 假时钟 + 生产刷新策略（09-B） |
| `fix61-09.revocation.test.ts` | 5 | fixture transport + **真实 `DialoguePipeline`**（09-C） |
| `fix61-09.recovery.test.ts` | 6 | 真实 SQLite 前缀表 + 生产装配（09-D） |
| `fix61-09.budget.test.ts` | 1 | fixture 度量对比，缺失维度标 unknown（09-E） |
| `fix61-09.boundary.test.ts` | 7 | **本轮新增**：SPEC §2 边界（见 §5） |
| `fix61-09.harness.ts` | — | 共享 harness：假时钟、真实 `PrefixSnapshotStore`、真实 `SqliteLifecycleMemoryPort`、真实 `OpenAiCompatibleDialogueProvider` + `ProviderTransport`（只 fixture fetch） |

## 5. 边界逐项核对（SPEC §2，本轮实测）

| 边界要求 | 状态 | 证据 |
| --- | --- | --- |
| `memory/sqlite-lifecycle-state.ts` 的 `assertContextCurrent` **仍在且未被弱化**（SPEC 明确禁止删除） | **PASS — 仍在** | `sqlite-lifecycle-state.ts:322` 定义完整（unissued/modified 检查 → policy/scope/epoch/prompt → `pendingHolds` 收紧 → 逐 source 版本校验），被 `contextSources:339`、`appendAssistant:354,356`、`trackContext:292` 调用，且 `sqlite-lifecycle-port.ts:450` 在其之上**追加**知识库修订检查后仍调用它。FIX61-09 只做**加法**：`trackContext:308` 把前缀 pin 的来源并入既有 source 集，注释明确「without a second validation mechanism and without removing the original check」 |
| 前缀不含轮 ID / 时间戳 / 随机排序 | **PASS** | 新增用例断言冻结区不含 `one`/`two`/`three`/`two:user`/`one:user`，`/\d{4}-\d{2}-\d{2}T..:..:..\.\d{3}Z/` 不匹配，不含 `createdAt`；同快照重放两次序列化字节完全相同（确定性） |
| 预算超界截**动态后缀**而非静默截前缀 | **PASS** | 前缀文本整体交付（`messages[0].content === prefix.text`），`prefix.suffixBytes` 只约束尾部；**前缀自身装不下时为显式报错**：`tokenBudget:1` → `foregroundContext` 抛 `prefix_exceeds_budget`（`sqlite-lifecycle-port.ts:195-203 #assertPrefixFits`），且 `peekSnapshot()` 为 null（未偷偷发布），**不是每轮悄悄截前缀** |
| 改 prompt / 切库 / 纠正 / 遗忘 / 收紧权限立即失效并先走安全前缀 | **PASS** | 5 条撤销路径均有断言（09-C 的 forget/prompt/切库/删文档 + 本轮新增的纠正与隐私 hold）；撤销后**下一轮 `complete === false`**（走 `prefix:minimal`，且不含被撤销内容），再下一轮才是完整重建快照 |
| 安全前缀也无法构造时**可见拒绝** | **PASS** | `tokenBudget:1` 时 `foregroundContext` **reject** `prefix_exceeds_budget`，属调用方可见错误，不降级为「继续聊」 |
| 前缀必须位于请求稳定前部 | **PASS** | 新增用例：`messages[0].role === 'system'` 且 `content` 与 `prefix.text` 逐字节相同，冻结历史紧随其后且顺序与快照一致（`frozenRegion()` 亦校验） |

### 本轮唯一 RED 与处置（诚实记录）

新增的「收紧隐私 hold」用例**先跑出 1 红**：我在取消 hold 后**立刻**断言下一轮前缀重新含「用户喜欢红茶」，实际为 `false`。核查后确认**不是实现缺陷**：`pending.cancel()` 后该轮起的新快照需要一次后台构建（`next-start` 语义），我在断言点比重建早了一轮。SPEC 并未规定「释放 hold 后第 N 轮恢复」，因此**我改的是测试的时序假设**（再走一个完整轮对，再断言 `complete === true` 且无越界泄漏），**未改任何生产代码、未放宽断言**。修正后 7/7 绿。

## 6. 09-E 度量（实测数值，无任何虚构收益）

命令：`node --test dist/tests/next61/fix61-09.budget.test.js`（退出码 0）。语料固定 5 轮中文对话，同 store、同 fixture provider（`https://unit.invalid`，仅回放两帧 SSE）。

| 指标 | 动态模式（旧） | 冻结模式（新） | 判定 |
| --- | --- | --- | --- |
| 装配耗时（ms，逐轮实测 / 2 次运行） | `[16.278, 11.623, 11.987, 11.327, 14.093]` / `[12.336, 6.576, 6.006, 6.476, 6.134]` | `[37.373, 13.925, 13.913, 11.663, 9.637]` / `[21.215, 7.543, 6.512, 6.556, 8.031]` | **仅报告**：本机上冻结模式第 1 轮含一次性构建，后续轮与动态模式**同量级（约 6–14 ms）**，两次运行噪声大于差异，**不构成任何加速结论** |
| 冻结前缀字节（逐轮） | — | `[1214, 1736, 1736, 1736, 1736]` | **PASS**：第 2 轮起逐字节稳定（首轮 1214 为 `prefix:minimal` 安全前缀） |
| 动态模式整请求字节 | `[1462, 1626, 1660, 1824, 1855]` | — | 仅报告 |
| TTFT | **unknown** | **unknown** | 本轮无授权凭证与真实模型，用例显式断言为 unknown |
| cache usage / cache-hit rate | **unknown** | **unknown** | 同上；稳定本地前缀只是供应商缓存复用的**前提**，不是命中证据 |
| cost / input token | **unknown** | **unknown** | 同上；冻结**不减少**逻辑输入 token 数 |

**诚实声明**：SPEC §3 09-E 明确要求「不把固定比例收益作为虚构 PASS」。本表**未给出任何百分比收益**，也未用 fixture 传输冒充真实端点。冻结的本地可证收益仅一条：**同一快照的引导字节逐轮不变**（由 `frozenPrefix` 与 `budget` 两处字节/哈希断言证明）。

## 7. 共享契约影响（PrefixSnapshot / historyWatermark / CAS publish 归 09 拥有）

- **新增**（untracked，09 拥有）：
  - `contracts/prefix.ts` — `DialoguePrefix { id, revision, hash, text, messages, sources, suffixBytes, complete }`、`PrefixMessage`。
  - `contracts/index.ts:97` — `DialogueContext.prefix?: DialoguePrefix`（**可选新增字段**，既有消费者零改动即可继续工作；未配置 `SqlitePrefixOptions` 时 `context.prefix === undefined`，动态路径行为完全不变，已由 `budget` 用例断言）。
  - `memory/prefix-snapshot.ts` — `PrefixSnapshotRecord`（含 `historyWatermark`、`privacyRevision`、`sources`）、`PrefixSnapshotStore`（CAS publish / `usable` / `reusable` / `validate`）、`PREFIX_REFRESH_INTERVAL_MS=6h`、`PREFIX_BUILD_ATTEMPTS=2`、`PREFIX_MESSAGE_LIMIT=8`。
- **消费者**：
  - **Provider**（`providers/aika-dialogue.ts`）：`requestHead()` 把冻结文本作为首个 system 段、冻结历史紧随、动态后缀与当前输入置于尾部；请求体新增**仅本地诊断**字段 `prefixId` / `prefixHash` / `suffixBytes`，**不改任何既有 wire 字段**。Gemini 侧经同一 `head` 走 `systemInstruction`，同样把稳定块置于前部。
  - **Memory lifecycle**（`memory/sqlite-lifecycle-port.ts` / `sqlite-lifecycle-state.ts`）：前缀来源并入既有 `assertContextCurrent` 的 source 集；`assertContextCurrent` 追加知识库修订检查。**两者均为加法，未删除或弱化既有校验**。
  - **0.7 复用**：`SqlitePrefixOptions.identity/coreMemories/books/binding` 均为注入式，0.7 的 Soul/Persona 可直接替换 identity 来源；`renderPrefixText` 的「阶段摘要 / 相关长期记忆」分段格式即为 0.7 摘要与 Soul 的落位预留。SPEC §2 声明 0.61 不依赖未实现的 Soul，本轮未越界实现。
- **未改动**：`contracts/desktop-bridge.ts`、`contracts/memory-lifecycle.ts`、`contracts/knowledge.ts` 的既有字段；`core/dialogue-pipeline.ts` 本轮零改动（其撤销停止交付由既有 `assertContextCurrent` 报错路径承担，已由 09-C 真实 pipeline 用例证明）。

## 8. 已知限制 / 待人工项

| 项 | 说明 |
| --- | --- |
| 无真实端点度量 | 09-E 的 TTFT / cache usage / hit rate / cost / input token 全部 `unknown`；需在有授权凭证与真实模型的环境下补测，且**不得**由本报告的字节稳定性反推收益 |
| 真实模型 / 真实 KV 缓存命中未验 | 冻结只保证本地请求稳定；供应商是否缓存、缓存多久、是否折扣，均在本进程之外 |
| `interval` 模式为可选配置 | 默认 `next-start`（本运行冻结、下次启动后台刷新）；interval 默认 6h，仅当调用方显式配置才有 TTL 语义 |
| 后台构建为单飞行（single-flight） | 每个 port 同时最多一个构建在途；多个会话边界共享该槽位，极端并发下刷新可能顺延到下一个可用时机 |
| `peekSnapshot()` / `candidateForInspection()` / `contextForInspection()` | 为测试与诊断开放的观察入口，非业务接口；不应被生产消费方依赖 |
| 临时输出文件 | 本轮产生的 `_fix6109_run.txt` / `_run_test.txt` / `_run_testnext.txt` / `_run_next61.txt` / `_b.txt`（untracked，位于 desktop-pet 根）建议清理；非交付物 |
| 全量回归未跑 | 按 AGENTS.md 测试边界只跑受影响范围（09 全部 + `test:next61` 125 + `test` 572 + `test:next` 80）。未跑 `test:windows` / `test:release` / Tauri 打包 / 真机 Electron 冒烟 |

## 9. 未核实项

- **未核实**真实供应商端点下的 cache usage 与费用（无授权凭证）。
- **未核实** `memory/prefix-snapshot.ts` 在跨版本升级 SQLite schema 上的迁移行为（表为 `CREATE TABLE IF NOT EXISTS`，旧库无迁移脚本需求，但未做跨版本实测）。
- **未核实** 0.7 Soul/Persona 接入后 `identity` 注入点是否需要额外字段（本轮仅预留注入口）。
- **未核实** `interval` 模式下 `intervalMs` 由调用方自定义为非 6h 值时的长时行为（仅测了默认 6h 与 5h/6h+1ms 边界）。
- **未跑** `npm run test:windows`（依赖真实 Windows 环境）与打包验证。

## 10. Git 操作声明

本轮**未执行任何 git 写操作**（无 commit / push / stash / checkout / reset / clean / add）。仅执行只读 `git status --porcelain`、`git log --oneline`。工作树中他人/其他 SPEC 的未提交改动全部保留。
