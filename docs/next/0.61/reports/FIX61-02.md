# FIX61-02 验收报告 · 模型发现与配置表单

- SPEC：[FIX61-02](../specs/FIX61-02.md)。需求：R-TODO-01、R-TODO-02、R-TODO-16。前置：FIX61-01。
- 基线提交：`046ee27c47032fad80620ea183b889b8b0dea134`（aika-next）。工作树含 78 个未提交的 0.61 改动（FIX61-02 的实现与测试文件均在其中），本报告在该工作树上验证；未回滚、未 stash、未提交。
- 状态：**AUTO_PASS**。FIX61-02 全部 21 条自动用例（11 + 7 + 2 + 1）通过；受影响回归（next 契约 80/80、`npm run check`、`npm run build`）通过。
- 日期：2026-09-21。执行：goal worker（FIX61-02 执行轮）。

## 1. 改动与设计

本轮是**真实环境执行验收轮**：实现与测试由前序轮次落盘在工作树中，本轮没有修改任何生产代码或测试文件；本轮完成的是把上一轮被沙箱阻塞的用例（wiring 的 Windows ACL 路径、UI 的 Electron 真实 Chromium 路径）全部真实跑通。下表记录被测实现现状（工作树中的未提交改动）：

| 文件 | 职责 |
| --- | --- |
| `management/model-discovery.ts` | 纯逻辑 `ModelDiscovery`：`resolveModelsUrl` 由推理 URL 推导 apiBase（`/v1/chat/completions`→`/v1/models`、`api.deepseek.com/chat/completions`→`/models`、apiBase 追加 `/models`），无法无歧义推导时拒绝并提示 `modelsEndpoint`；两协议分页（20 页/1000 条上界）、10 秒超时（`MODEL_DISCOVERY_TIMEOUT_MS`，可注入）、AbortSignal 取消、错误分类（401/403→unauthorized、404→not_found、429→rate_limited、非 JSON/坏形状→invalid_response、超时/取消独立 code）；错误消息经 `CREDENTIAL_ECHO` 正则脱敏，密钥永不进 URL（Gemini 走 `x-goog-api-key` 头），`redirect: 'error'` 防凭据跟随跳转 |
| `management/model-discovery-draft.ts` | `AikaDiscoveryDraftStore`：持久化发现来源与最近列表（revision CAS），只存凭据引用不存密钥，不持有槽位绑定（`models`/`slots` 字段不落盘） |
| `management/aika-routes.ts` | `ModelDiscoveryService`（串行保护：慢端点迟到答案不覆盖新来源）+ `aikaManagement` 路由：`GET/POST /api/aika/discovery`、`GET/PUT /api/aika/discovery/source`，走既有管理错误平面，拒绝 raw key 正文 |
| `management/bootstrap.ts` | 生产组合根接线：发现服务读受限凭据文件（与槽位同一 `credentialRegistry`），draft 落 `.local/data/aika-model-discovery.json`，随 `startManagementServer` 挂载 |
| `management/aika-console.ts` | presenter 侧 `discoveryToken` 单调令牌：迟到答案丢弃、失败保留旧列表并提示"手工填写型号名" |
| `management/ui/aika.html`、`management/ui/aika-view.mjs` | 表单化 Aika 页：端点/协议（select）/凭据引用（select）/modelsEndpoint/获取按钮/搜索框/发现列表（能力 declared 原样、unknown 明示）/逐槽绑定下拉/手工型号输入；失败不清空已选模型，key 不进 DOM |
| 测试 | `tests/next61/fix61-02.discovery.test.ts`（11）、`.configure.test.ts`（7）、`.wiring.test.ts`（2）、`.ui.test.mjs` + `ui-fixture.mjs` + `ui-electron.mjs` + `ui-scenario.mjs`（1） |

## 2. 逐 AC 证据

运行目录均为 `F:/AIVoice/Aika-Next/windows/code/desktop-pet`。

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `npm run build` | 0 | tsc + wechat 构建通过（stderr 仅 npm config 警告） |
| `node --test dist/tests/next61/fix61-02.discovery.test.js` | 0 | tests 11 / pass 11 / fail 0 |
| `node --test dist/tests/next61/fix61-02.configure.test.js` | 0 | tests 7 / pass 7 / fail 0 |
| `node --test dist/tests/next61/fix61-02.wiring.test.js` | 0 | tests 2 / pass 2 / fail 0 |
| `node --test tests/next61/fix61-02.ui.test.mjs` | 0 | tests 1 / pass 1 / fail 0（Electron 44.4.1 真实 Chromium） |
| `npm run check` | 0 | 无类型错误 |
| `npm run test:next` | 0 | 80/80 |

| AC | 状态 | 证据 |
| --- | --- | --- |
| 02-A 两种协议的分页、空列表、重复 ID、循环 token、401/403/404/429、非 JSON、超时可恢复且脱敏 | PASS | discovery 11/11。分页：Gemini `pageToken=next-1` 两次请求、key 仅在 `x-goog-api-key` 头且任意 URL 不含 key；OpenAI 兼容一页一请求、`redirect: 'error'`。空列表为正常结果（`items: []`，`truncated: false`）。重复 ID 跨页只报一次（`['dup','first','second']`）。循环 token 第二页检出、2 次请求后停止且 `truncated: true`。上界：20 页封顶、1000 条封顶。错误：401/403→`unauthorized`、404（HTML 正文）→`not_found`、429→`rate_limited`、200 HTML→`invalid_response`、`data` 非数组→`invalid_response`；全部消息不含 key 与 `sk-fix6102` 且含"型号"提示。超时：mock 计时器 10s 触发 `timeout`；`AbortController` 取消→`cancelled`；`timeoutMs: 25` 注入证明同一生产超时分支。未知凭据引用/不支持协议/非 HTTPS modelsEndpoint 均在联网前拒绝（`calls.length === 0`）。`resolveModelsUrl` 拒绝对 `.../custom/infer` 追加 `/v1/models`，显式 `modelsEndpoint` 获胜 |
| 02-B A 端点慢请求在改成 B 后返回不能覆盖 B；手填自定义型号无需成功拉列表 | PASS | configure 7/7。慢端点：第一请求挂起→切 Gemini 端点完成发现→慢响应迟到时路由返回**当前**状态（`endpoint === GEMINI_ENDPOINT`、`items` 为 `new-endpoint-model`），draft 不被覆盖，慢端点不再被询问。手填：`PUT /api/aika/discovery/source` 是 revision 写（0→1）、零供应商请求、不持有 `slots`；持久化文件有 `endpoint`、`observedAt: null`、无 `models`/`slots`；`validateManagedSettings` 接受手填 `hand-typed-model-2026` + 自定义推理 URL。失败保留旧列表：429 与 404 两次刷新失败后 draft 仍为 `['kept-model']`。错误平面：raw key 正文→`invalid_request`（0 次凭据读取、0 次网络）、未知引用→`invalid_request`、不存在路径→`not_found`、`anthropic` 协议→`invalid_request`；draft 文件无 `sk-`、有 `ref-a`；页面 note 含"不代表"。presenter：迟到答案不覆盖新列表且不产生错误，失败后旧列表保留、`discovering: false`、手工型号照常写入 |
| 02-C UI 保存并重启后，实际请求使用选中型号；鉴权头不进入 DOM、普通配置和日志 | PASS | wiring 2/2 + UI 1/1。重启与请求：保存 `selected-model-2026`/`https://my-llm.example.com/v1/chat/completions`（revision 1）→全新 `ManagementSettingsStore.open` 重启→`effectiveTrialConfiguration` 读回同模型/端点/协议/凭据文件→真实 `createAikaDialogueProvider` 发出的请求 URL、`body.model`、`Authorization: Bearer` 头逐一断言，key 不进请求体；`management-settings.json` 含模型名、不含 `sk-`。鉴权挂载：无 token→401；带 token GET→200 空列表；POST→后端（非浏览器）发起供应商请求；供应商 403→HTTP 403 错误包络（`code: 'forbidden'`、消息含"手工填写"、无 key）。真实 DOM：Electron 加载生产 `aika.html` + `aika-view.mjs`，fake 传输断言页面只用会话 token 鉴权、key 不进 document；慢端点被替换后迟到列表不显示；搜索过滤生效、unknown 能力不编造；选中模型绑定 dialogue 槽并经 `PUT /api/settings`（expectedRevision 3）保存、其余槽不动；失败发现后已选模型不被清空、手填可保存；身份保存保留 legacy providers 数组不被清空；模型 id 以文本渲染（无 `<img` 注入）。UI 场景 6/6 检查通过、`errors: []` |

### RED 证据（诚实说明）

- 本轮工作树中实现与测试同在，**无法在本轮重放"实现前 RED"**；测试文件头注释声明 RED→GREEN 流程，但本轮未观测到 RED 阶段，不编造 RED 记录。FIX61-02 属"实现已存在、验收未跑"的补验情形。
- 上一轮（沙箱受限只读会话）观测：wiring 1 红、判为环境假红（测试自建凭证文件触发 `windowsAcl`→powershell 被沙箱拒）；UI 用例因无法 spawn electron 未跑。本轮在真实环境 wiring 2/2 绿、UI 1/1 绿，证实该判断为环境假红。
- 02-A 超时用例本轮实测耗时 10.0s（真实等待完整 deadline 的那条），非跳过。

## 3. 回归

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `npm run check` | 0 | 无类型错误 |
| `npm run build` | 0 | 编译通过 |
| `npm run test:next`（next 契约套件） | 0 | 80/80 |
| FIX61-02 四个测试文件 | 0 | 21/21 |

本轮未修改任何共享实现文件，故未扩大到 default（572）/windows（22）全量组；这两个组上一次由 FIX61-01 报告覆盖（当时全绿），其后工作树又叠加了 FIX61-04/06/07 等其他 SPEC 的改动，其回归由各自报告负责。工作树现已存在 `npm run test:next61` 脚本（`tools/run-tests.mjs next61`），本轮按任务边界未使用它，FIX61-10 收口时应核对。

## 4. 证据性质分列（fixture / 真实）

- **fixture（fake 供应商面）**：所有 `fetch` 供应商响应为测试内 fake——这只能证明发现协议、缓存、路由、presenter 编排与脱敏逻辑，不证明任何真实供应商返回该形状。
- **真实生产路径**：真实 `ManagementSettingsStore`/`validateManagedSettings` 持久化与重启、真实 SQLite（`AikaTimelineStore`）、真实受限凭据文件（`restrictPrivatePathSync`，项目根外）、真实 HTTP 服务器（`startManagementServer` 会话 token 401/200/403 包络）、真实 Electron 44.4.1 Chromium + 生产 DOM/视图模块、生产组合根 `bootstrap.ts` 的发现服务接线（源码在 `management/bootstrap.ts` §94–101，wiring 测试经 `startManagementServer` 走真实路由挂载）。
- **真实远程供应商**：未调用（无授权凭据），**NOT RUN**。发现成功只证明 models 资源可访问，不证明指定模型能完成推理——页面 note 已固定该语义。
- **人工验收**：真人浏览器点按（视觉布局、真实会话流）**NOT RUN**，属版本末人工项。

## 5. 共享契约影响

| 契约/边界 | 变化 | 消费者与兼容方式 |
| --- | --- | --- |
| `aikaManagement(profileStore, timelineStore, discovery?)` | 第三参新增可选 `AikaDiscoveryPort` | 未传时发现路由不可用但控制台其余部分不受影响；既有两参调用（`tests/next/aikaConsole.test.ts`）仍绿（本轮 next 80/80 实证） |
| `startManagementServer` 选项 `aika` | 携带发现端口后新增 `GET/POST /api/aika/discovery`、`GET/PUT /api/aika/discovery/source` 路由 | 挂在既有会话 token 鉴权与错误平面之下；无新增鉴权机制 |
| 新增文件（`model-discovery.ts`、`model-discovery-draft.ts`、`management/ui/aika.html`） | 模块内新增，无既有导出变更 | 消费者为 bootstrap/aika-routes/aika-view；不改 FIX61-01 的 `ProviderSelection`/槽位契约 |
| 凭据面 | 发现只读凭据引用对应的受限文件，不复用、不缓存、不落盘密钥 | 与 FIX61-01 的 `credentialRegistry`/`readSetupKey` 同一边界 |

## 6. 已知限制与待人工

- 真实远程发现与推理 **NOT RUN**（无授权凭据）；对非阿里系/非 Google 真实网关的线协议兼容性属**待联调**。
- UI 真人点按、视觉与真实会话流**待人工**；自动证据为真实 Chromium 内的 DOM 断言。
- 测试装置外观缺陷（不影响判定）：`fix61-02.ui-electron.mjs` 以 `result.includes('"error"')` 判断失败，而结果 JSON 恒含 `"errors":[]`，故用例通过时也会打印一条 `AIKA_UI_DEBUG` 页面快照。属诊断噪音，不改变 pass/fail。
- 其他 SPEC 观察（未顺手修）：`docs/next/0.61/specs/FIX61-01/04/06/07.md` 文件头状态行仍为 NOT RUN，而执行索引已标 AUTO_PASS——与本 SPEC 同类，建议由各执行轮或 FIX61-10 统一回填；SPEC.md §1 仍写"全部开发 NOT RUN"，与索引表实际状态不一致。
- FIX61-07 依赖本 SPEC（索引表），本报告的 21/21 证据即其前置依据。

相关文档：[执行索引](../SPEC.md) · [版本 RPD](../RPD.md) · [源码核对](../SOURCE_AUDIT.md)。
