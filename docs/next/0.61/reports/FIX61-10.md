# FIX61-10 验收报告 · 版本自动收口与后置验收交接

- SPEC：[FIX61-10](../specs/FIX61-10.md)。范围：FIX61-01～09 全轮 + 本版自动收口。
- 基线：HEAD `d7c61d674374d760e57d18ebe2bf0b42c7e94c4b`（分支 `aika-next`）+ 大量未提交工作树改动。**基线状态：FROZEN（已冻结，2026-09-21T20:52:55+08:00）**，见 §7；本报告 §2 的原始门槛证据记录于较早的 PROVISIONAL 修订，重冻结后的门槛见 §7b。
- 日期：2026-09-21。执行：FIX61-10 执行 worker（§7b 由 FIX61-11 token 修复轮补记）。
- 状态：**10-A AUTO_PASS（所测修订）· 10-B AUTO_PASS（所测修订）· 10-C PASS · 10-D DEFERRED_TO_K65_11**。**token 缺陷已修复并通过 RED→GREEN 验证，基线已冻结（§7b）；但人工验收仍未进行，本版不计"已验收"。**

## 1. 改动

本 SPEC 主要是收口与文档核对，生产代码零改动。改动文件如下。

| 文件 | 改动 |
| --- | --- |
| `docs/next/0.61/SPEC.md` | §4 第 47 行：把"新增 test:next61 及其测试目录…**当前并不存在**"改为**已存在并可用**的真实状态（命令、runner 分支、集合为空的 `exit 2` 保护、源文件数），并**保留**规则"命令存在不等于通过，不要提前把未运行/未通过的命令写成 PASS" |
| `docs/next/0.61/specs/FIX61-10.md` | §2 "（当前不存在）" → 已落地；§1 新增"test:next61 现状核实"段落 |
| `docs/next/0.61/specs/FIX61-01.md` | 头部状态 `NOT RUN` → `AUTO_PASS`（报告已逐条核实） |
| `docs/next/0.61/specs/FIX61-04.md` | 头部状态 `NOT RUN` → `AUTO_PASS`（标注 04-C 屏幕边缘/键盘为收口项） |
| `docs/next/0.61/specs/FIX61-06.md` | 头部状态 `NOT RUN` → `AUTO_PASS` |
| `docs/next/0.61/specs/FIX61-07.md` | 头部状态 `NOT RUN` → `AUTO_PASS`（核心自动部分） |
| `docs/next/0.61/specs/FIX61-08.md` | 头部 `PASS` → `AUTO_PASS`，并加"状态口径统一说明"（解决与 SPEC 索引的冲突，见 §6） |
| `docs/next/RUN_061_065.md`（新） | EXECUTION_061_065.md §3 要求的续跑账本 |
| `docs/next/0.61/reports/FIX61-10.md`（新） | 本报告 |

**未改动**任何生产代码、测试代码或共享契约；**未提交、未推送、未 stash、未回滚**。

## 2. 逐 AC 证据

### 10-A：01～09 每条必需 AC AUTO_PASS，必要真实回放不是 fixture 冒充

**PASS（所测修订）**。逐条核实各报告主命令与结论：

| SPEC | 报告状态 | 主命令/用例数 | 真实 vs fixture |
| --- | --- | --- | --- |
| 01 开放模型配置 | AUTO_PASS | `fix61-01.openConfig.test.js` 8/8 | fixture/本地 SQLite；真实远程 NOT RUN（无凭据） |
| 02 模型发现与配置表单 | AUTO_PASS | 21 用例 | fixture 传输 + 真实 Chromium UI |
| 03 启动进度与重试 | AUTO_PASS | 11 + 3 项检查 | 真实子进程 stdout |
| 04 左右键分流 | AUTO_PASS | `fix61-04.pointer.test.js` 8/8 | 真实生产 router/renderer |
| 05 多模型换肤 | PASS | 报告用例 | **真实 Cubism rig**（`Live2D Cubism SDK Core 6.0.1`） |
| 06 知识库 | AUTO_PASS | `fix61-06.knowledge.test.js` 7/7 | 真实 SQLite + 真实 Provider 序列化 |
| 07 健康与麦克风 | AUTO_PASS（核心） | `fix61-07.health.test.js` 8/8 | 真实 SQLite/设备诊断逻辑 |
| 08 实时 ASR | AUTO_PASS | 27 用例 / 6 文件 | **真实本地 sherpa-onnx 流式模型**（08-D/08-E），非 batch fallback |
| 09 冻结前缀 | PASS | 27 用例 / 6 文件 | fixture 传输；09-E 的 TTFT/cache/cost **如实记 unknown**，未从 fixture 推断 |

**"必要真实回放不是 fixture 冒充"核实**：08-D/08-E 用真实本地 sherpa-onnx 流式模型跑通（本轮 `test:next61` 输出可见 `[real-streaming] partials=11 firstPartialMs=1456 ... text="对我做了介绍那么我想说的是大家如果对我的研究感兴趣"` 与 `✔ 08-D/08-E real local streaming model`）；05 加载真实 Cubism 授权 rig。**真实远程模型调用全轮 NOT RUN**（无授权凭据），各报告均如实标注、未冒充。09-E 明确写 `"ttft":"unknown: no authorized credential or model in this run; nothing is inferred from a fixture"`——这正是"不夸大"的合规写法。

**任一 BLOCKED/NOT RUN 则版本未开发完成**：三处合法后置（04 屏幕边缘/键盘 UI、07 Electron 窗口内试麦接线、03 macOS Swift 分支）与真实远程调用 NOT RUN 均**不属 01～09 的必需 AC**，故不阻塞 10-A。§7 的 token 缺陷曾据此阻止宣布收口完成，**该缺陷已修复并冻结（§7b）**；仍未闭的是 10-D 人工验收。

### 10-B：六项自动门槛

运行目录：`F:/AIVoice/Aika-Next/windows/code/desktop-pet`（node v24.9.0 / npm 11.16.0）

| 命令 | 退出码 | 计数 | 判定 |
| --- | --- | --- | --- |
| `npm run check` | **0** | tsc 无错误 | PASS |
| `npm run test:next` | **0** | **80 pass / 0 fail** | PASS |
| `npm run test:next61` | **0** | **143 pass / 0 fail / 0 skipped** | PASS |
| `npm run test:windows` | **0** | **22 pass / 0 fail** | PASS |
| `node --test dist/tests/media/*.test.js` | **0** | **63 pass / 0 fail** | PASS |
| `npm run build:windows` | **0** | build + build:desktop + build:wake + build:native 四段全过 | PASS |
| `npm run test:windows:ui` | **0** | stdout 含 `WINDOWS_SMOKE_OK: Live2D renderer, isolated preload, backend round trip, panel layout.` | PASS |

**UI 冒烟判据**：按 0.6 BASELINE 的提醒，**同时**看 `WINDOWS_SMOKE_OK` 文本**与**退出码——两者均满足。stderr 恒有一行既有 `Renderer: script-error`（实测 HEAD 纯净副本同样复现），**未做根因定位，也不声称窗口无错**。

**未降低任何门槛**：本轮未删断言、未 skip、未放宽阈值、未改标签。`test:windows:ui` 的退出码与成功文本均为工具原始输出。

`test:next61` 组成（本轮实测）：`dist/tests/next61/*.test.js`（编译产物）+ `tests/next61/*.test.mjs` 共 42 个源文件、143 用例，`fail 0 / skipped 0`。

### 10-C：数据安全与迁移

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 0.6 数据迁移/重启恢复 | **PASS** | `test:next61` 内 09-D（快照重启恢复、半写快照丢弃、篡改行不复活）、09-B（next-start 冻结、重启拾取增长）、06-B（关库重开后库与文档仍在）全绿；09-C「revoked snapshot is not resurrected by a restart」通过 |
| 回退方案 | **PASS** | 知识库为 3 张 `CREATE TABLE IF NOT EXISTS` 附加表，未改 0.6 既有表含义（FIX61-06 §5）；`test:next61` 内 `09-D a tampered snapshot row is discarded on load` 证明异常数据不恢复；`management/settings` 的 revision/CAS 保证陈旧写入被拒且旧配置保留 |
| 新配置未回写旧 Aika 工作树 | **PASS** | ①`tests/next/nextNamespace.contract.test.ts` 断言 Next 与 `AAAAGENT` 数据根**不同且非嵌套**、legacy sentinel 字节不变、`userData` 行不含 `AAAAGENT`（`test:next` 80/80 内全绿）；②实查旧树 `F:/AIVoice/Aika` 的改动 mtime 为 **9/16 21:04** 与 **9/20 22:43–23:10**，**全部早于本轮**（本轮 9/21 18:xx），且 `verify61/probe-08c.mjs` 是 FIX61-08 worker 已知探针。**本轮对旧树零写入。** |
| 无 Key 入 Git | **PASS** | `git grep -l -E "sk-[A-Za-z0-9]{16,}"` 对全部 tracked 文件**零命中** |
| 无个人音频入 Git | **PASS** | `git ls-files` 无任何 `.wav/.mp3/.m4a/.flac`；`.gitignore` 含 `*.wav` |
| 无模型权重入 Git | **PASS** | `git ls-files` 无任何 `.onnx/.bin/.pt/.safetensors/.gguf`；`.gitignore` 含 `*.onnx` |
| 无用户数据库入 Git | **PASS** | 无 tracked `.sqlite/.db`；`.gitignore` 含 `*.sqlite*` |

命中的 `credential-store.ts` / `credentials.ts` / `balance-credentials.ts` 是**凭据处理源码**（按文件名匹配），非密钥内容。

### 10-D（人工，后置）

**DEFERRED_TO_K65_11**。按 K65-11 §1 line 17，人工清单与 0.65 相同体验去重合并，用户集中确认。**未代签、未标 ACCEPTED。**

## 3. 既有失败逐条核实（非 0.61 回归）

**判定方法**：下列测试文件全部 `TRACKED@HEAD` 且工作树 `git status` 为空（与 HEAD 逐字节一致），即在**未修改的基线上同样失败**；且相关被引用的生产/工具文件在 git 全历史中从未存在。

| 测试 | 退出码 | 计数 | 真实原因（实测） |
| --- | --- | --- | --- |
| `integration/trial-config.test.js` | 1 | 10 pass / 2 fail | ①`TrialNotReadyError: This configuration targets macOS` — fixture 缺 `desktopHost:'electron'`，仅 macOS 可过；②`ERR_UNSUPPORTED_ESM_URL_SCHEME … protocol 'f:'` — `:214` `await import(resolve(...))` 传盘符路径，仓内同类代码用 `pathToFileURL(...).href` |
| `integration/capture-feedback-wire.test.js` | 1 | 0 pass / 1 fail | `ERR_MODULE_NOT_FOUND … tests/desktop/ui-harness.mjs`；**该文件从未存在于 git**（`git log --all -- "*ui-harness*"` 为空），HEAD 中有 **7 个** tracked 测试引用它 |
| `integration/backend-session.test.js` | 1 | 6 pass / 2 fail | 同上 `ui-harness.mjs` 缺失（非 0.61 改动） |
| `integration/deepseek-text-update.test.js` | 1 | 0 pass / 1 fail | `ERR_MODULE_NOT_FOUND … tools/prepare-trial-update.mjs`；**从未存在于 git** |
| `integration/memory-trial.test.js` | 1 | 6 pass / 1 fail | `evaluate-lifecycle.ts:25` 要求 POSIX 绝对路径（`root.startsWith('/')`）→ Windows 上抛 `Invalid explicit evaluation paths or run ID`；macOS-only 假设 |
| `management/settings.test.js` | 1 | 4 pass / 2 fail | ①`:23` `assert.equal((await stat(file)).mode & 0o777, 0o600)` → `438 !== 384`（438=0o666）；NTFS/Node 不兑现 POSIX mode 位，`balances.test.ts:38-43` 已记载该限制并做了 win32 分支，此处**缺分支**；②`Background reservation would block foreground`（evaluation-budget 预留冲突）。0.6 `NEXT-08:79`/`NEXT-07:40` 已登记 |
| `management/relay-bootstrap.test.js` | 1 | 0 pass / 1 fail | `ENOENT …\data\desktop-pet\project-index.sqlite` vs `bootstrap.ts:71` 实际打开 `resolve(base.projectRoot, '.local/data/project-index.sqlite')` — **测试与生产路径不一致**（不是 EBUSY） |
| `management/integration.test.js` | 1 | 0 pass / 2 fail | ①`EPERM … symlink … node_modules`（Windows 缺 `SeCreateSymbolicLinkPrivilege`；本机实测 `New-Item -ItemType SymbolicLink` 同样失败）；②`EBUSY … unlink companion.sqlite`。0.6 `USER_ACCEPTANCE.md:36` 已登记 |
| `management/memory-dynamics-http.test.js` | — | **挂起** | 需超时截杀；0.6 三份报告均登记 |

**关于 `management/balances.test.js`**：单跑 **6 pass / 1 skip / 退出码 0**，symlink 用例显式 `SKIPPED on Windows: symlink privilege unavailable`。`438 !== 384` 出自 **`settings.test.js`**，**不是** balances。

**关于并发运行假失败**：`dania-voice-clone.test.js` 在与其它文件并发跑时出现 2 个失败，**单跑 2/2 通过**——属并发资源竞争假阳性，非真实缺陷。该文件的 `protocol: 'openai-compatible'` 断言改动由 0.61 轮其他 worker 完成（适配 FIX61-01 的附加字段），当前工作树状态正确。

**结论**：以上 9 项**全部为既有失败/环境限制**，无一由 0.61 引入。本轮**一律未修**（属上游 macOS→Windows 移植产物或环境能力限制，不在 FIX61-10 交付范围，且多为共享测试文件）。0.6 `USER_ACCEPTANCE.md:36` 与 `NEXT-07/08/09` 用 stash 对照实验已确认同一组问题与本轮无关。

## 4. 共享接口影响

**本轮零生产代码改动，无共享接口变化。** 收口核实到的既有契约变化（由 01～09 引入，本轮仅核对未改）：`ProviderSelection.protocol`（可选、读取时归一化）、`TrialModel.provider` 放宽为 `string`、`DialogueContext.knowledge`（可选）、`SqliteMemoryPort.contextSnapshot()`（新增）、`/api/health*`、`/api/knowledge*`、`/api/microphone*`（新路由，复用既有 Bearer 鉴权）、知识库 3 张附加表。均为**向后兼容或纯新增**，消费者适配已由各 SPEC 报告记录。

## 5. 测试选择理由

按 AGENTS.md §6「版本收口」行：本版收口需跑本版所有必需 AC、相关全量回归、真实回放、构建与自动集成，故执行 SPEC 明确列举的全部六项门槛（check / test:next / test:next61 / test:windows / build:windows / test:windows:ui）加受影响 management/media/integration 套件。对 management/integration 采用**逐文件分类**而非"整套判红"：先用整轮取得失败清单，再逐文件单跑取真实错误文本，最后用 `git ls-tree HEAD` + `git status` 证明失败文件与 HEAD 逐字节一致，从而区分"既有失败"与"本版回归"。未跑 Tauri 打包（非本 SPEC 门槛）。

## 6. 文档一致性核对（本轮 reconciliation）

| 项 | 原状 | 处置 |
| --- | --- | --- |
| `SPEC.md:47` / `FIX61-10.md:15` "test:next61 当前不存在" | **STALE** | 改为真实状态，**保留**"不要提前写成 PASS"的规则 |
| `FIX61-01/04/06/07.md:3` 头部 `NOT RUN` | **STALE**（报告均为 AUTO_PASS） | 逐条读报告核实后改为 `AUTO_PASS` |
| `FIX61-08` 索引 `AUTO_PASS` vs 头部/报告 `PASS` | **冲突** | 核对报告 §0/§4：08-D/08-E 由真实本地 sherpa-onnx 流式模型跑通，满足 10-A 门槛，故统一为 `AUTO_PASS`，并在 spec 头部写明统一理由（**未下调索引**） |
| `FIX61-05/09` 索引 `PASS`、报告 `PASS` | 一致 | 不改（语义与 AUTO_PASS 等同，非冲突） |

## 7. 已知缺陷与收口结论

**已知缺陷（曾阻止宣布冻结）：`desktop/electron/main.mjs` 的 `open_management` 用 `new URL(value.path, url)` 拼接，而 base 已带 `#token=…`，拼接替换 hash 导致 token 丢失**，从功能面板点「外观 / 换肤」会打开正确页面但处于**未授权锁定态**。

- 该缺陷**本轮由 FIX61-11 token 修复轮修复**，并已用真 Electron 窗口测试 RED→GREEN 验证；见 §7b。
- 因此 §7 原记的 **PROVISIONAL / PENDING_REFREEZE 已作废**，基线转为 **FROZEN**。
- 本报告 §2 表格记录的是**修复前修订**上实跑的计数（`test:next61` 143）；修复后同一集合增加 5 例，重冻结门槛见 §7b。**§2 的 143 未被改写，仍是那个修订上的真实值。**

其他：真实远程 provider 调用 **NOT RUN**（无授权凭据）；FIX61-03 macOS Swift `cancel_startup` 分支 **NOT RUN**（无 macOS 环境）；`memory-dynamics-http.test.js` 与 `pending-memory-management.test.js` 挂起（既有）。

## 7b. 重冻结（FIX61-11 token 修复轮，2026-09-21）

**修复**（改动 2 个生产文件，均为最小改动）：

| 文件 | before → after |
| --- | --- |
| `tools/management-url.mjs` | 新增导出 `managementTarget(session, route)`：先同源校验（异构 origin 与协议相对路由 `//host/x` 直接抛错），再合并 fragment —— `sessionFragment + '&' + routeFragment`，**session 在前** |
| `desktop/electron/main.mjs` | `shell.openExternal(value.path && value.path !== '/' ? new URL(value.path, url).href : url)` → `shell.openExternal(managementTarget(url, value.path))`；`value.path` 正则加 `(?!\/)` |

**fragment 顺序依据（读源码而非猜测）**：`management/ui/app.mjs:25` 用 `new URLSearchParams(location.hash.slice(1)).get('token')` 取 token，`:32-36` 用同一个 hash 的 `get('page')`/`get('section')` 取路由，且**在 `history.replaceState` 清 hash 之前**解析。`URLSearchParams.get` 返回**第一个**同名值，故 session fragment 置于最前，使路由自带的 `token=…` 不能覆盖真实会话。合成结果 `#token=<64hex>&page=skins`，两者同时到达。

**RED→GREEN（真 Electron 进程，跑未修改的生产 host）**：

| 阶段 | `desktop/electron/main.mjs` SHA256 | 结果 |
| --- | --- | --- |
| GREEN（修复后） | `96FD6F254631375A02E906CAF2C61376B9B8F4576D864E109727224487A4E278` | 2/2 pass，退出码 0 |
| **RED**（临时还原为 `new URL(value.path, url)`） | `B3A0FCB14D34EE4AE82ACDF1E9E4C19137F152404D3FDCE320AF2B9569849FBE` | **1 pass / 1 fail，退出码 1**：`skin must carry the session token: http://127.0.0.1:9735/#page=skins`（token 为 `null`）—— 复现所报缺陷 |
| GREEN（还原后） | `96FD6F25…`（**SHA 复算与修复后逐字节一致**） | 2/2 pass，退出码 0 |

**测试不是源文本正则**：测试 `import` 未修改的 `desktop/electron/main.mjs`，只把 `shell.openExternal` 换成记录器，点击**生产 renderer 渲染的真实功能面板按钮**，记录到的 URL 再于真实 Chromium 窗口加载**真实控制台页面**并断言 DOM：标题 `外观 / 换肤`、`nav-skins` 选中、`.skin-page` 存在、**`loginFormPresent === false`（未锁定）**、`authorizedRequests > 0`、`/api/skins` 被调用。

**重冻结门槛（所测修订 = HEAD `d7c61d6` + 当前工作树，冻结窗口 `2026-09-21T20:52:55+08:00`）**：

| 命令 | 退出码 | 计数 |
| --- | --- | --- |
| `npm run check` | **0** | tsc 无错误 |
| `npm run test:next` | **0** | **80 pass / 0 fail / 0 skipped** |
| `npm run test:next61` | **0** | **148 pass / 0 fail / 0 skipped**（143 + 本轮新增 5 例） |
| `npm run test:windows` | **0** | **22 pass / 0 fail / 0 skipped** |
| `node --test dist/tests/media/*.test.js` | **0** | **63 pass / 0 fail** |
| `npm run build:windows` | **0** | build + build:desktop + build:wake + build:native |
| `npm run test:windows:ui` | **0** | stdout 含 `WINDOWS_SMOKE_OK: Live2D renderer, isolated preload, backend round trip, panel layout.` |
| 新增用例单跑 `node --test tests/next61/fix61-11.shellToken.ui.test.mjs tests/next61/fix61-11.managementTarget.test.mjs` | **0** | **5 pass / 0 fail** |

**UI 冒烟判据**：**同时**看 `WINDOWS_SMOKE_OK` 文本**与**退出码——两者均满足。stderr 仍有一行既有 `Renderer: script-error`，**未做根因定位，也不声称窗口无错**（K65-00 亦未跑该门槛，本轮补跑）。

**未降低门槛**：未删断言、未 skip、未放宽阈值、未改标签。

**本轮改动文件 SHA256**：`desktop/electron/main.mjs` `96FD6F25…`（15629 B）、`tools/management-url.mjs` `8780358A…`（2698 B）、`tests/next61/fix61-11.managementTarget.test.mjs` `DF6AAB29…`、`tests/next61/fix61-11.shellToken.ui-fixture.mjs` `3D98D6CF…`、`tests/next61/fix61-11.shellToken.ui-electron.mjs` `AB6FBE4F…`、`tests/next61/fix61-11.shellToken.ui.test.mjs` `54B46301…`（完整值见 `docs/next/RUN_061_065.md` §3b）。

**同类缺陷排查**：全仓 `new URL(` 逐个核对，**无第二处** fragment 替换缺陷 —— `desktop/presentation-preview.mjs:15-16` 已显式拒绝带 `hash/search` 的地址；`management/server.ts:56` 的 base 是无 fragment 的 origin；`desktop/cubism-renderer.mjs:76,92` 只取资源路径，不构造带 token 的页面 URL；`desktop/electron/preload.cjs` 无 URL 构造。

**结论**：0.61 基线 **FROZEN**（所测修订 `d7c61d6` + 上表工作树改动）。**10-D 人工验收仍为 DEFERRED_TO_K65_11，未代签、未标 ACCEPTED。**

## 8. 待联调项

- 远程自定义端点/多源真实调用（无凭据）。
- 能力型适配器对非阿里系服务的字段兼容性（FIX61-01 已记）。
- FIX61-06 生成中/播放中切库的端到端交错依赖 09 的 `privacyRevision` 校验闭环。

## 9. 0.61 人工体验清单（交 K65-11，DEFERRED_TO_K65_11）

K65-11 按 §1 line 17 与 0.65 相同体验**去重合并**。以下为 0.61 侧条目，均**未经人工验证**。

| # | 体验项 | 关联 AC | 怎么试 | 期望 |
| --- | --- | --- | --- | --- |
| 1 | **左右键与拖动** | 04-A/04-B/04-C | 左键短点角色、左键拖动、右键开面板、Escape 关面板 | 短点=抚摸一次且不开聊天；拖动=移动桌宠不触发抚摸/面板；右键开/关同一功能面板一次；九个入口（chat/skin/knowledge/settings/memory/timeline/diagnostics/microphone/status）无死链 |
| 2 | **换肤** | 05-A/05-B/05-C | 导入两个本地模型包，来回切换后重启 | 切换原子生效、失败候选保留旧皮肤；重启后恢复所选皮肤；换肤**不改**对话作用域/Memory/知识库 |
| 3 | **知识库实际影响回答** | 06-A/06-E | 建 A/B 两库各导入同名不同内容文件，切库后提问 | 回答只体现所选库内容、切「无」后不体现；**回答质量需人工判断**（自动仅证明正文进入请求） |
| 4 | **Key/模型设置** | 01-A/02-A/02-C | 填自定义端点+模型、手动填模型名、保存后重启 | 保存并读回、重启仍在；密钥不明文落盘；无效 URL 被拒且旧配置保留 |
| 5 | **麦克风选择/试听** | 07-B/07-C/07-D | 在诊断页选设备并试听，拔/插设备、拒绝权限 | 试听与后端 ready 解耦；拔设备/隐藏标签/拒权限/全零采集四种诊断可区分；5 秒上限 |
| 6 | **边说边出字** | 08-A/08-C/08-D | 对着麦克风连续说话 | 说话过程中有 partial 逐段出现；松开后**只提交一次**轮次；快速取消不残留旧文本 |
| 7 | **启动失败恢复** | 03-A/03-B/03-C | 断网/改坏端点启动，观察进度与重试 | 停滞可见并可取消/重试；快速重试只留一个后端；旧代消息不复活 |

**附加已知限制（体验时请注意）**：~~第 2 项从功能面板点「外观/换肤」当前会因 §7 的 token 缺陷进入**未授权锁定态**~~ → **该缺陷已修复并冻结（§7b），本条限制已解除**：功能面板「外观 / 换肤」现在合成的 URL 同时携带 `#token=<64hex>` 与 `&page=skins`，控制台在该页处于**已授权（未锁定）**状态，已由真 Electron 窗口 DOM 断言（`loginFormPresent === false`）。**第 2 项仍属人工体验项**（视觉结果、切换后重启恢复、换肤不影响对话作用域等需人工确认），只是不再带 token 前提条件。

相关文档：[执行索引](../SPEC.md) · [版本 RPD](../RPD.md) · [源码核对](../SOURCE_AUDIT.md) · [续跑账本](../../RUN_061_065.md)。
