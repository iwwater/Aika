# MVP-07 · Fork 基线、可行性与剪枝（DRAFT）

依据：[RPD](../../RPD_MVP_0.6.md) MVP-R07、[统一规则](../SPEC_MVP_0.6.md)。仅文档；AC 全部 NOT RUN。前置：独立实施授权、仓库命名/位置与用途决定。

范围：独立 shell 仓库的基线、构建配置、实际存在的待删功能及依赖；Aika 仅写本阶段报告。不修改 Aika 业务源码。

先完成 A～C，再执行剪枝 D；Live2D 核查结果限制对应路径，不能将有条件可行写成整体通过。

| AC | 验收与证据 |
| --- | --- |
| A | 记录上游 tag、完整 commit、原始资产哈希、本地工具链和实际构建命令；原版 Windows 构建可启动。来源与工作目录可复现，不依赖未知本机残留 |
| B | 对照 PET-01 和生产 fixtures 固定四端点请求/响应/错误及状态通道；逐项盘点功能“存在/不存在/保留/删除”，给源码依据。不存在的 Agent/Memory 不列为剪枝成果 |
| C | 用最小样例核查候选 Cubism Core、显示库、WebView 与现成授权模型的组合；记录版本、技术结果和目标用途/分发条件。未解决许可条件阻塞对应分发路径，技术失败阻塞 MVP-11；不推迟到发布阶段才评估 |
| D | 仅删除清单中实际存在且不需要的功能与引用/依赖；保留 HTTP、窗口、托盘、必要本地设置与模型选择、版权及来源信息。每项有前后对照，原版 sprite 与四端点定向回归通过 |
| E | 给出可推进范围、阻塞项及后续实际路径清单；不存在的上游能力作为待新增需求登记，不声称已交付 |

报告：`docs/integration/reports/MVP-07_ACCEPTANCE.md`。保留上游基线与可回退提交；本阶段不发布、不引入第三方插件市场、不复制 Aika 的 Agent 后端。
