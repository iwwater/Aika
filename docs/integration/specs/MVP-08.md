# MVP-08 · 品牌、菜单与生命周期（DRAFT）

依据：[RPD](../../RPD_MVP_0.6.md) MVP-R08、[统一规则](../SPEC_MVP_0.6.md)。AC 全部 NOT RUN。前置：MVP-10、用户决定正式命名。

范围：shell 品牌资源、窗口/托盘/设置菜单、单实例与新增退出接口及配置；Aika 消费新能力的代码归 MVP-09。不改 renderer 内部，不改四端点既有语义。

| AC | 验收与证据 |
| --- | --- |
| A | 产品名、图标、托盘、安装器、菜单、关于页与示例文案按决定替换；保留所需版权、来源与许可展示，不把必要声明视为漏删品牌 |
| B | 菜单基于 MVP-10 槽；至少保留本地模型选择、已支持的表现设置、状态与明确退出入口；无通往已删功能的死入口 |
| C | 冻结单实例作用域（Windows 用户会话、安装身份、配置/profile 的关系）；第二实例不生成重复窗口/HTTP 服务。端口占用、已有其他应用、状态未就绪分别处理，不凭端口推断所有权 |
| D | 区分关窗隐藏与用户显式退出；显式退出释放 HTTP、托盘、渲染与任务；重复退出有确定结果 |
| E | 新增协议退出为可检测能力；仅接受当前受管实例的已验证调用者与实例凭据，拒绝缺失/错误/过期凭据。attach 不获得退出权；本机地址不等于已认证。凭据不进入日志 |
| F | 四端点回归通过；真实进程复验第二次启动、用户退出、占端口、错误退出请求；记录新增端点版本、认证及错误契约供 MVP-09 消费 |

报告：`docs/integration/reports/MVP-08_ACCEPTANCE.md`。不声称 Aika 已自动消费新退出端点，不启动安装/卸载回归。
