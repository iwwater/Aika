# MVP-13 · 真机与发行验收（DRAFT）

依据：[RPD](../../RPD_MVP_0.6.md) MVP-R13、[统一规则](../SPEC_MVP_0.6.md)。AC 全部 NOT RUN。前置：MVP-08～11 适用项通过；MVP-12 仅在纳入时加入。

范围：shell 与 Aiki 集成的验收、必要构建/打包配置及发行材料；发现源码缺陷回对应 SPEC 修复，不在验收中绕过。安装/卸载按明确授权的环境与范围执行，不默认操作用户日用系统。

| AC | 验收与证据 |
| --- | --- |
| A | 固定双仓 commit、依赖、Windows/WebView、显示器/DPI、角色与安装包哈希；sprite 与 Live2D 均复验四端点和真实可见表现；按 PET-07 适用 AC 逐项记录 |
| B | 验证断连恢复、owned/attach、单实例、显式退出、sprite↔Live2D、成功/失败换装；无双重输出、越权关闭或旧任务复活 |
| C | 分别记录 Aiki/shell 的 10 分钟待机、30 次交互与多次切换的 CPU/RAM/FPS/冷启动、资源计数；沿用 PET 目标 HTTP 受理 P95≤300ms、气泡可见 P95≤1000ms。其他性能预算须在采样前明确，未定/未达不得笼统性能 PASS |
| D | 检查默认开关、发往 shell 的数据与诊断日志；仅允许的可见文本/动作/短状态出站，凭据与完整 OCR/记忆不泄露 |
| E | Windows MSI 构建、启动/重开验证；按目标用途登记包内容、来源、许可证、模型授权与所需源码提供材料；未解决的组合许可条件阻塞相关对外发行 |
| F | 在获授权环境完成适用安装/升级或重装/卸载验证，确认数据保留策略与进程资源释放；只打包不等于安装通过。与 INT-03 仅复用同一产物和环境适用证据 |
| G | 分别裁决模块完成、0.6 产品 DoD、发行就绪；列出所有 FAIL/BLOCKED/NOT RUN。0.5 Voice/FE-33/宿主稳定性欠账仍单独登记，不能随 0.6 通过自动关闭 |

报告：`docs/integration/reports/MVP-13_ACCEPTANCE.md`。只执行本里程碑明确需要的组合检查；不自动发布、上传发行包、购买服务或启动素材流水线。
