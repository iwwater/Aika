# 合批后执行计划 · 2026-09-26

基线：`codex/aika-local-cloud-merge`，`1aefe44`（制定计划时的已推送提交）。本计划安排下一轮工作，不改变各 SPEC 的验收状态；开始具体实现时仍先读对应 PRD、SPEC 和共享契约，一次只执行一份 SPEC。工期是单人净工作量估计，不含外部授权、实机预约和等待 CI 的时间。

## 审阅结论

1. Git 历史已合批，两个产品运行端仍独立：`aika-crossplatform/` 是 Tauri/React 主工程，`windows/code/desktop-pet/` 是 Next 的 Electron 工程。不能从合并提交推断运行时或数据已互通。合批分支已推送到 `origin`，尚未并回 `master` 或 `aika-next`。
2. 本地主工程的类型检查、任务调度与正式组合根定向测试、前端构建通过；浏览器启动被 `llm.contextSources` 声明 `knowledge.wiki`、但 localStorage 宿主未提供它阻断，故定时任务界面仍未目视验证。[合批记录](LOCAL_CLOUD_MERGE_20260926.md)。
3. Next 0.82 的 43/43 定向自动测试通过，CR-01、04～10 有定向修复；CR-02/03 的正式能力及依赖它们的实机验收按用户决定 **DEFERRED**，版本整体 **BLOCKED**。[审阅记录](../next/0.82/reports/CODE_REVIEW_20260926.md)。
4. `docs/STATUS.md` 对 0.81 仍写“NOT_STARTED”，与其 [SPEC](../next/0.81/SPEC.md) 的已实现及 G1～G3 实机证据冲突，也未列 0.82。0.82 SPEC 内“当前所有步骤 NOT_STARTED”与本轮报告冲突。应以逐项报告重建导航状态，不用批量替换成 PASS。
5. Next UI [修复方案](../next/ui-rework/MVP_REPAIR_PLAN_20260925.md) 已确认正式 Playground 端口缺失、角色预设未真正按角色生效、取消轮次 ID 不可信；卡顿原因待浏览器复现。0.79 仍有旧库恢复、Electron UI 环境和长时准入缺口；0.81 G4/G5、性能阈值和自然试运行未完成。SER-06～10 有实现和报告，但日语研究结论仍取决于授权数据及独立评测。

## 顺序与计划表

| 顺序 | 工作包与预计净工时 | 具体动作与文件边界 | 验证和完成条件 | 前置/交付 |
| --- | --- | --- | --- | --- |
| P0-1 · 现在 | 合批准入核对，0.5～1 天 | 检查分支相对 `origin/master`、`origin/aika-next` 的提交与文件清单；确认无生成产物、密钥或未经许可数据；核对根 `LICENSE` 对两套来源和素材的适用范围；修正 `docs/STATUS.md`、0.82 SPEC 的过时导航文字。只改文档和必要的提交范围问题。 | `git diff --check`；列出每个版本的“已实现/自动通过/正式接线/实机/未运行”证据；许可证适用范围有明确结论或维持发布阻断。 | 先于并回主线或发布；产出合批准入记录。许可判断需要权利人确认时保持 BLOCKED。 |
| P0-2 · 现在 | 本地主工程启动修复，0.5～1.5 天 | 在 `aika-crossplatform/src/app/plugins/contextSourcesPlugin.ts` 核对 `KnowledgeWikiToken` 的条件提供与插件声明；按浏览器 localStorage 和桌面 SQL 两种宿主分别修复能力声明/解析。只触及插件契约及其定向测试。 | 两宿主内核启动；浏览器真实打开，任务入口可见、可创建/取消一项测试任务；SQL 宿主 Wiki 管理仍可用；对应 `composition`/插件测试与 TypeScript 检查通过。 | P0-1 的分支范围确认；产出对应模块 SPEC 报告及浏览器证据。 |
| P0-3 · 现在 | 两端架构与交付目标定版，约 0.5～1 天 | 画出 Tauri 与 Electron 的启动入口、Runtime、存储、授权、角色和任务边界；决定下一交付版本主运行端、另一端的维护边界与可能共享的纯契约。只产一份 `docs/architecture/` 决策记录，不搬业务代码。 | 决策列明当前两端各自可运行的功能、不能互通的数据和是否需要迁移；后续每个改动有明确归属。 | P0-1；若产品主端尚未确定，先按两端独立维护，不启动跨端移植。 |
| P1-1 · 随后 | Next UI MVP 修复，约 2～4 天，按原方案逐项实施 | 先在真实浏览器复现 Playground 卡顿，再修正式端口、服务端轮次与取消、角色预设持久化/生效，最后收敛六入口和假数据。按 [UI 修复方案](../next/ui-rework/MVP_REPAIR_PLAN_20260925.md) 控制范围，不碰 0.82 后置采集。 | 正式组合根发送、回复、取消同一轮；双角色配置隔离且重启恢复；慢/失败请求仍可导航；无虚构成功或就绪状态。跑 UI 定向测试并在真实浏览器验证。 | P0-3 确认 Next 仍在下一交付范围；每一小步留报告，失败保持 REPAIR_REQUIRED。 |
| P1-2 · 随后 | Next 0.79/0.81 剩余准入，约 2～5 天主动工作，另计自然试运行 | 先解决 Electron UI 测试环境和 0.79 旧库副本恢复；再按 [0.81 验收计划](../next/0.81/ACCEPTANCE_PLAN.md) 核实 G1～G3、冻结性能阈值，按需安排 G4/G5；全部放行后才进入 N081-07 正常配置试运行。 | 0.79 UI 与迁移记录可复验；0.81 G1～G5 分项有正式/实机证据，安全门槛零失败；三日、累计八小时试运行和标注另行记录，未完成不得标 ACCEPTED。 | P1-1 保证真实管理入口可用；设备/用户参与不可用时准确记 BLOCKED 或 NOT RUN。 |
| P1-3 · 随后 | SER 研究证据收口，约 1～3 天准备工作，数据到位后另估 | 审核 SER-06～10 报告、数据清单和 split 复现性；对 RAVDESS 与现有获授权小样本分别报告结果。获授权日语语料后，按 [SER 路线图](../ser/ROADMAP_V2.md) 冻结 speaker-independent split，再复评 eGeMAPS/SSL/校准/鲁棒性；不提交原始受限语料或生成产物。 | 相同清单、标签、split 可重算；UAR、macro-F1、失败/拒识计入分母；无授权数据时只报告英文 sanity check 和日语探索，SER-11 不启动。 | 与 P1-2 独立；申请邮件的外发另需用户明确授权。 |
| P2 · 后置 | 0.82 CR-02/03 与整版验收，重新排期 | 仅在用户明确恢复该范围时，实现真实抓屏/像素 OCR、正文来源、自动调度和陪伴输出，再做正式组合根及实机验收。 | 原 [0.82 TESTING](../next/0.82/TESTING.md) 的全部必需 AC 有真实证据后才可进入人工验收；不能以 43/43 自动测试代替。 | **DEFERRED**。TTS M1 重训、旧语音/声学、环境感知/Live2D 的其他后置事项也不随本计划自动启动。 |

## 执行门槛与分支策略

- 先在当前合批分支做 P0 的定向修复与文档收口，保持原 `master` 工作区原样；每个工作包独立提交，规范/状态文档与业务代码分开提交。提交前核对文件清单，推送前核对分支和远端，不强推。
- 代码合批后的下一次合入目标需由 P0-1/P0-3 的准入结论决定。若根许可证范围、敏感内容或两端交付边界未清楚，保留审阅分支，不宣布可发布。创建 PR 可以承载审阅，但 PR 通过不自动代表 0.79、0.81、0.82 的版本验收。
- 每个实现工作包开始前选对应模块现有 SPEC；若缺 SPEC，先补一份最小规格及 AC。仅运行受影响模块的测试；到明确的集成或发布里程碑才扩到全量构建与设备矩阵。
- 本计划的净工时合计不含等待：P0 约 1.5～3.5 天，P1 准备与主动工作约 5～12 天。它是排序参考，不是承诺日期。P2 不计入当前排期。
