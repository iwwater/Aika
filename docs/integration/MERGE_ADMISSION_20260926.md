# 合批分支准入核对 · 2026-09-26

基线：`codex/aika-local-cloud-merge`，核对时 HEAD `cd529b7`；当前文档提交后续另记。此记录只覆盖 Git 合批准入，不代表产品发布或 0.79/0.81/0.82 验收。

| 项 | 证据与结论 |
| --- | --- |
| 分支与历史 | 分支跟踪 `origin/codex/aika-local-cloud-merge`；`6a73780` 保留本地和 `origin/aika-next` 两个父历史，后续本地增量和 CR-01、04～10 修复分提交。原 `master` 工作区未改。 |
| 本轮增量产物 | `git diff --name-only 6a73780..cd529b7` 为 82 个路径；该增量中没有 `research/ser/output/`、`research/ser/manifests/`、`.env`、私钥或数据库文件。另有 14 个 `research/ser/output/` 文件来自此前已跟踪的本地历史，不属于本次新增未跟踪产物；不能将“未引入新生成产物”误写为“分支中没有历史生成产物”。 |
| 敏感内容快速筛查 | 对源码/文档做常见明文密钥格式检索，唯一命中是 UI 中的 `sk-xxxxxxxxxxxxxxxxxxxx` 占位提示；未据此发现真实明文密钥。此检查不是完整的凭据审计。JTES 文件是尚未填写收件地址及个人身份的申请模板，未发送邮件或取得数据授权。 |
| 许可证范围 | 根 `LICENSE` 来自 Next 历史，文本明确仅覆盖权利人有权许可的原创内容；`THIRD_PARTY_NOTICES.md` 明确不为 Aika 自有代码指定整体许可证。两套历史合并后，不能由根文件位置推断其覆盖 Aika 全部代码、素材或移动端。公开分发/发布前须由相应权利人确认适用范围和展示方式；在此之前发布准入 **BLOCKED**。不修改现有许可证正文。 |
| 工程验证 | 以[合批记录](LOCAL_CLOUD_MERGE_20260926.md)中的定向命令和退出码为准。本轮仅修正文档状态，未重跑工程测试。Tauri 浏览器启动仍 BLOCKED；Next 0.82 正式链路 DEFERRED，不能以 43/43 自动测试替代。 |

下一步：先修复 Tauri 插件启动缺口并验证浏览器任务入口；随后给两运行端的交付主次写入架构决策。合入主线的目标、许可证结论和版本发布门槛分别确认，不能混作一次 Git 合批验收。

后续进展（2026-09-26）：`4e723e3` 修复浏览器插件契约和任务时钟回调；浏览器聊天/设置/任务面板可见，后台 tick 告警消失；浏览器表单创建/取消及真实 Tauri 仍未运行。双端边界另见[架构记录](../architecture/DUAL_RUNTIME_BOUNDARY_20260926.md)。本表的原核对基线与当时阻断记录保留，不回写为此前已通过。
