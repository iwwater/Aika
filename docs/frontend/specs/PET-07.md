# PET-07 · Windows OpenPet 闭环验收

状态：DRAFT / 实现 NOT RUN。需求 DPI-07；前置 PET-01～06适用模块AC通过。
这是0.5桌宠跨模块里程碑；不等于完整Aiki发布或真实语音验收。

## 范围与证据

正常只输出 `reports/PET-07_ACCEPTANCE.md` 与非私人截图/录屏、测量记录。发现缺陷退回对应PET定向修复。真实Windows宿主+未修改OpenPet+生产Adapter必须连通；上游Aiki业务事件允许受控fixture触发，但须记录这一边界，不宣称真实LLM/感知质量。

记录Windows版本、DPI/显示器、Aiki提交与脏文件、OpenPet tag/哈希、角色profile、HTTP地址、启动方式、构建/测试命令及退出码。健康与播放可见性分开取证。

## 验收矩阵

| AC | 场景 | 通过条件 |
| --- | --- | --- |
| PET-07-A | 外部安装并启动OpenPet→Aiki启用attach | ready、角色可见；Aiki真实宿主传输成功，不靠浏览器CORS |
| PET-07-B | 生产展示事件触发thinking→最终happy/action+say | 四类命令与实际表现对应；中文气泡一次；不得仅用curl代替生产链路 |
| PET-07-C | 当前角色缺少情绪动作，随后切换角色 | 文本仍显示；能力更新；旧映射不被继续当已验证 |
| PET-07-D | A轮取消/B轮开始、网络超时、突发状态、断线后恢复 | 旧轮无新增发送；POST不自动重试；恢复不重播；气泡/event按已验证TTL消退，已受理action的持续行为如实记录，不承诺强制取消 |
| PET-07-E | 关闭OpenPet、错误端口、协议错误服务 | Aiki文字聊天可继续；TTS调度不被桌宠错误阻塞（fake输出证明调度，真人音频另列） |
| PET-07-F | managed冷启动、重复启动、正常关闭、退出Aiki | 仅启动一次；ready需探测；用户关闭不反复拉起；attach实例永不终止 |
| PET-07-G | stopOwnedOnExit开/关、启动中退出、端口被占 | 只处理拥有进程；超时有界；不误杀占用者；默认桌宠可独立保留 |
| PET-07-H | 启用集成后最小化/恢复主窗、重启Aiki | 无旧自研pet窗口/重复订阅；设置恢复正确，主窗业务入口保留 |
| PET-07-I | 30次公开短句交互与10分钟待机 | 受理P95≤300ms、可见P95≤1000ms；分别记录Aiki/OpenPet CPU、RSS/工作集口径与冷启动；不凭感受给PASS |
| PET-07-J | 正文/内部内容/错误哨兵检查及交付材料 | 出站仅允许字段；日志无正文/密钥；安装器与runtime路径清楚，协议/素材权利记录完整 |

性能测量注明时钟来源、UI观测方式及误差。资源消耗只记录实测，不套用旧自研CPU/RAM/FPS承诺；异常资源增长登记缺陷并阻止无条件宣称稳定。

## 完成判定

逐AC区分fixture、真实HTTP、device；任何真实表现项缺证据不得整份PASS。窗口拖动/托盘属于上游，只做基本可用性检查，不为其缺陷重写窗口系统。

必要时构建实际Aiki桌面宿主以验证原生装配；运行受影响回归。完整发布仍执行INT-03，安装后发布回归不可被这份局部验收替代。最终报告明确0.5已完成范围与未支持的Live2D/点击回传/口型。
