# FE-33 · 环境链路真机装配验收（不含 Live2D）

> **部分依赖调整 · 2026-09-14**：本文件环境/读屏/权限验收与报告保留。依赖旧自研pet的交互/窗口场景按[Integration RPD迁移表](../RPD_DESKTOP_PET_INTEGRATION.md)单列，0.5桌宠链路改由[PET-07](PET-07.md)验收；不覆盖或重写本文件已有真机证据。

状态：PARTIAL，2026-09-15 按 Legacy Pet 删除后的生产范围修订；历史真机证据保留，适用项仍需分项复验。依据：[专项 PRD](../PRD_ENVIRONMENT_AND_LIVE2D.md)、[FE-31](FE-31.md)/[FE-32](FE-32.md) 验收报告里的遗留设备项、[共享契约](../../modules/CONTRACTS.md)「FE-32/FE-31 追加」节。本次仅改规格，不运行设备验收。

**为什么单立一份**：本规格最初用于补足 2026-09-14 生产装配后的设备证据。如今已有部分真实 Tauri/Provider 链路记录，不能继续称为“从未真机运行”；仍需按下表逐项验证当前环境、权限与陪伴行为。模块内 PASS 不自动覆盖这些设备项，也不要求恢复已被替代的 FE-27～29 才能完成本份。

## 进入条件与范围

前置：Windows 真机 + 可运行的 Tauri 桌面构建（`cargo build --release --features custom-protocol`，缺该 feature 会白屏，见 A6 记录）。需要用户在场操作与目视；需要一份可用的 Provider 凭据才能做 E。

范围**只有**「装配是否真的接通、真机传感器是否真的按 SPEC 行为」。**不含**：Live2D 渲染与口型、旧 FE-30 角色组合负载、FE-32-A 的冻结集 CER（缺真实素材，另行解决）。FE-30 的当前适用范围遵循桌宠迁移表，不在此复活旧角色线；外部桌宠验收归 PET-07，未来 shell 归 MVP-13。

不新增业务架构。发现缺陷退回对应 SPEC 定向修复，不在验收脚本里绕过。截图与录屏只用非私人画面。

## 场景矩阵

| ID | 操作与证据 | 通过条件 | 收口的遗留项 |
| --- | --- | --- | --- |
| FE-33-A | 真实进程冷启动，打开设置页 | 环境感知分组 `available=true`；两个 source 初始为「已关闭」；**应用启动本身零采集**（无 hook 线程、无 WGC 会话）；开关状态与库里持久化值一致；宿主不支持时显示「权限被拒/错误」而不是假装运行中 | 新装配本身；FE-19 启动恢复 |
| FE-33-B | 开启「前台应用感知」，在 VSCode↔浏览器↔游戏间切换；连续开关 20 次；锁屏与 150% DPI 各一次 | 切换延迟 <1s 且进程名正确；20 次开关后无线程/句柄泄漏（任务管理器或 handle 计数取证）；锁屏/最小化下 busy 观测为 `unknown` 而非 false | FE-19 真机三项 |
| FE-33-C | 开启「屏幕感知」，用非私人画面触发变化；分别让前台是普通窗口 / Aiki 主窗 / 被自身窗口遮挡的窗口 | `environment_capture_window` 真机返回 `ok` 时携带正确进程名与客户区矩形（**返回体无窗口标题**）；前台是主窗时回退到「最后一个有效外部窗口」并重新验证；被自身窗口遮挡返回 `obscured` 且提示调整窗口。外部 OpenPet 不沿用已删除自研 pet 的身份判断，按当前窗口策略另留证 | FE-32-D 当前适用部分 |
| FE-33-D | 在真实窗口上读中文文章、英文文档、中英混排 IDE 各若干次（手动「看屏幕聊聊」与自动变化两条路径都走） | 双语资源离线加载成功（断网下仍可用）；热读屏 **P95 ≤3000ms**，FE-21 固定词区域仍 ≤2000ms；摘录 ≤20 段/2000 字符且截断可见；限流到上限时显示下一次可用时间，不静默挂起 | FE-32-F 性能项；FE-21 真机 WGC |
| FE-33-E | 关着「将环境摘要用于对话」与「允许屏幕文字用于对话」各发一轮；逐个打开再各发一轮（**需真实 Provider**） | 两个开关都关时请求里**零环境内容、零摘录**；只开摘要时只有应用名+时长；开摘录后摘录带来源与时间出现在「参考资料」区块且标「（未确认）」；关掉开关后的下一轮立刻为空。原图与完整 OCR 原文不出现在日志、Trace、数据库任何一处（逐处检查并留证） | FE-32-C 的真机出口复核 |
| FE-33-F | OpenPet OFF 时，主窗逐项点验主动 / 安静 / 暂停读屏 / 看屏幕聊聊 / 结束陪伴；另验最小化再恢复后的状态 | 行为符合 FE-31 且状态显示真实；quiet 下自动发送为 0；暂停后普通聊天可用；结束陪伴**不中断**主窗已有用户轮与 TTS。旧 pet 输入、拖动、双击不再作为本项要求；外部桌宠行为归 PET-07 或未来明确纳入的 MVP-12 | FE-31-G 当前主窗业务；MVP-03-B 可复用同构建证据 |
| FE-33-G | 采集/识别/装配/预约各阶段分别触发「暂停读屏」「结束陪伴」「停止全部感知」与锁屏 | 迟到结果一律不外发；停止全部感知后会话读屏标为「已暂停」而不是冒充运行中；资源（WGC 会话、OCR worker、hook 线程）确实释放 | FE-31-E 锁屏项；FE-19 stopAll 真机 |

## 证据要求

每项记录：真机配置（Windows 版本、显示器分辨率与 DPI 缩放、构建号）、操作步骤、观测值、截图或录屏（非私人画面）。性能项给测量方法与时间序列，不只给一个数。

**不许**：把逻辑轨的测试结果抄进本报告充当设备证据；把「没观察到问题」写成通过；用 fixture 图代替真实窗口截图。做不到的项如实记 NOT RUN / BLOCKED 并说明缺什么。

## 文件范围

正常路径下**不改生产代码**，只产出 `docs/frontend/reports/FE-33_ACCEPTANCE.md`。若真机暴露缺陷：定向修复落在对应 SPEC 的文件范围内（例如窗口抓取问题改 `src-tauri/src/screen.rs`、会话状态问题改 `src/presentation/companionSessionController.ts`），并在本报告与对应 SPEC 报告两处登记。

## 已知会卡住的地方（提前说，省得现场返工）

1. `chi_sim.traineddata` 已随包（tessdata_fast 4.1.0，Apache-2.0），生产装配声明 `eng+chi_sim`；但**安装包是否真的把 `public/tessdata` 带进去**没验过——D 项要先确认这一步，否则 worker 创建会直接失败。
2. 独占全屏游戏下 WGC 可能给黑帧，FE-21 已按无效帧丢弃。C/D 遇到时**如实记录差异**，不要解释成识别失败。
3. 外部 OpenPet 的置顶、拖动及全屏行为归 PET-07，不恢复 FE-20 自研实现。FE-33_DEFECTS 中旧 pet BUG-01 已失去当前对象；BUG-02/03 仍须真机复验，不随迁移自动关闭。
4. E 项会产生真实 Provider 调用费用，按已有凭据与授权执行，不新增购买。
