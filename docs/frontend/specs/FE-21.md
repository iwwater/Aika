# FE-21 · Screen Event：帧 diff + ROI 截图 + OCR 关键词

> 最新用户要求：本SPEC仍为英文固定词表基础，[FE-32](FE-32.md)新增前台可见中英文文本与manual读屏，属于完整首版必需。该扩展允许手动绕过diff、首次基线OCR、独立授权受限摘录出口，不能用本SPEC旧非目标阻止实现；两种模式共用调度/worker上限。

> 2026-09-14：按 [环境与 Live2D PRD](../PRD_ENVIRONMENT_AND_LIVE2D.md) 补齐质量门槛、出口与预算；文末修订优先。新增内容待审阅，执行仍 DEFERRED。

状态：已全文审阅；保持后置。前置FE-18，不能用截图fixture替代生产diff/OCR算法。

## 架构与接口依据

本 SPEC 必须实现 [模块架构与接口](../ARCHITECTURE.md) 中对应阶段的端口、事件、状态与错误语义；通过 adapter 兼容现有实现。下方 AC 同时验证这些契约，无需启动其他真实模块。

需求与边界见 [模块 PRD](../PRD.md)「感知成本阶梯」与「隐私与默认」，依赖遵循 [共享契约](../../modules/CONTRACTS.md)。事件契约见 [FE-18](FE-18.md)。

核心次序：先判断「画面变没变」（低成本），变了才对固定 ROI 截图 + OCR（贵），OCR 命中关键词规则才产生事件。**绝不做定时全屏 OCR**。

## 设计

Rust（`src-tauri/src/screen.rs`，`cfg(windows)`）：

- invoke `environment_screen_enable(enabled: bool)`：开启后专用线程每 500ms 经 Windows.Graphics.Capture 抓主显示器帧 → 缩到 96×54 灰度 → 与上一帧平均绝对差；超阈值 emit `environment://screen-change` `{ magnitude }`；false 停线程。WGC crate（`windows` / `windows-capture`）实现时按成熟度选，emit/invoke 形状不变。
- invoke `environment_capture_region(roi)`：当前帧裁剪 ROI → PNG base64 返回。只在 screen-change 之后被调用。
- V1 只做主显示器；exclusive fullscreen 下 WGC 可能黑帧，如实记录到真机清单。

TS：

- `src/services/environment/screenSource.ts`：实现 `EnvironmentSource`。screen-change（节流 ≥ 2s）→ `capture_region`（V1 固定 ROI：屏幕中央横带，宽 80%、高 20%）→ OCR → 关键词规则 → emit `EnvironmentEvent`。
- `src/services/environment/ocrText.ts`：tesseract.js（WASM）**懒加载**——仅屏幕感知开启后创建 worker；`eng` traineddata 与 wasm 打包进 Tauri resources（安装包离线，不运行时外联）；`chi_sim` 体积大，后选。OCR 引擎不进契约：产出口径只有 `EnvironmentEvent`，换引擎不动消费方。
- `src/services/environment/keywordRules.ts`：默认词表 `{ pattern, kind, event }`（VICTORY/胜利/DEFEAT/失败/PENTAKILL/五杀 → `game_event`；Error/Failed/错误 → `screen_keyword`）；OCR 词置信度按具体引擎输出区间归一为0..1再作为事件confidence，不是原值直接透传。

隐私：设置键 `SETTING_KEYS.environmentScreenEnabled` 默认 `"false"`。关闭时无 capture 调用、不建 OCR worker；开启后原图只在内存中走 base64 → OCR → 丢弃，不落盘、不进日志、不进 Trace；OCR 文本只出现在 `EnvironmentEvent` 与 policy 输入，不自动写长期记忆。

## 范围

| 文件 | 改动 |
| --- | --- |
| `src-tauri/src/screen.rs` | 新增（diff 线程 + 2 命令） |
| `src-tauri/src/lib.rs`、`capabilities/default.json`、`Cargo.toml` | 注册命令、权限、WGC/tesseract 资源依赖 |
| `src/services/environment/screenSource.ts` | 新增 |
| `src/services/environment/ocrText.ts` | 新增 |
| `src/services/environment/keywordRules.ts` | 新增 |
| `src/app/plugins/environmentPlugin.ts` | screen source 按能力注册 |
| `src/services/storage/contracts.ts` | `SETTING_KEYS` 加 1 键 |
| `src/App.tsx` | 设置区屏幕感知开关（默认关） |
| `docs/modules/CONTRACTS.md` | 追加登记（新设置键） |

## 取舍

**diff 在 Rust 做、OCR 在 TS 做**：帧抓取与缩放比较在采集侧完成才省得下来——不把全帧扔过 IPC；OCR 放 TS 是因为 tesseract.js 生态在 WASM 侧、且规则匹配本来就是 TS 域逻辑。边界是「Rust 只说画面变了 + 按需给 PNG」。

**WGC 而不是 GDI BitBlt**：WGC 是微软当前官方采集 API（窗口/显示器/帧流），BitBlt 在硬件加速窗口上不可靠。crate 成熟度留实现时定，接口（emit/invoke 形状）已在本 SPEC 冻结。

**tesseract.js 而不是 PaddleOCR-onnx**：V1 目标是固定词表命中（大字号英文为主），不追求全文档识别；tesseract.js WASM 免训练依赖、可离线打包。中文/小字/多区域留给 V2（RapidOCR/PaddleOCR ONNX，走 onnxruntime-web），由那时的效果数据决定。

**固定 ROI、不做区域配置 UI**：游戏提示区高度可预测（中央横带）；配置 UI 的复杂度留给真实误报数据说话。

## AC

| AC | 模块内验收 |
| --- | --- |
| FE-21-A | fake 帧序列：无变化 → 无事件；显著变化 + OCR 命中 → 正确 kind 的事件（schemaVersion/timestamp正确，confidence归一为0..1）；变化但未命中 → 无事件 |
| FE-21-B | 频控：screen-change 节流 ≥ 2s、OCR 每分钟上限、同一关键词 dedupe 窗口（FE-18 monitor 语义复用）各自生效，丢弃计数可见 |
| FE-21-C | 隐私边界：关闭时 worker 不创建、无 `capture_region` 调用；注入 fake 断言全链路无文件写入与网络请求；OCR 文本不出现在任何持久化调用的参数里 |
| FE-21-D | 能力降级：非 Windows 宿主 screen source 不注册、设置项隐藏、启动无错误 |
| FE-21-E | 真机清单（记入验收报告）：LOL 结算画面（VICTORY/DEFEAT）、视频网站、IDE 报错三场景的命中/漏报/误报样本与 OCR 耗时；只记录不调参 |

## 不在本 SPEC

- VLM、持续全屏 OCR、多显示器与自定义 ROI 配置 UI、中文 `chi_sim` 包。
- 用户自定义词表编辑、识别结果自动进 Memory。
- UIA Reader（另行评估）。

只执行本模块测试；Rust对生产diff算法用固定像素矩阵做定向测试并cargo check；生产OCR用打包英文模型识别固定图像验证；编排测试另用fake capture/OCR；真机部分单列。验收报告放 `../reports/FE-21_ACCEPTANCE.md`。

## 2026-09-13 审阅补充

状态：保留后置，未授权采集。英文OCR模型不能作为中文“胜利/五杀”识别证据；V1只承诺英文OCR，中文词表可保留但中文能力不宣称支持；中文扩展需另行范围确认及本地chi_sim资源验证。OCR权限/ROI、busy、错误清理、worker终止明确；关闭递增epoch，在途截图/OCR完成不得广播。新增AC：关闭后迟到结果0事件、无采集权限可见拒绝、恶意OCR指令不执行且不写User Soul。真实屏幕识别与固定图fixture分列。

## 算法/资源与性能口径

采集仍需获取帧才能做diff，不能宣传“画面没变零采集”；主机原始帧仅内存处理，跨IPC只传受限ROI。diff阈值为预冻结配置；默认OCR并发1、最多10次/分钟、重复关键词2秒去重，忙时合并最新帧不积压；ROI越界/分辨率变化需拒绝或重算并标诊断。记录配置但不在同一验收集上反复调参。

Windows资源与Tesseract JS依赖分开归Cargo、package/lock、Tauri resources；WASM/worker/traineddata必须离线可解析，使用生产ocrText和固定英文图真实跑一次，不允许只mock OCR返回词。仅cargo check不足以证明生产diff和OCR可工作。

## 2026-09-14 · 可复现识别验收与权限出口

需求映射：SET-02/04/05、ENV-03～09、PRD AC-03/04/05。算法前置 FE-18；设置页和停止全部复用 FE-19 的 Presenter，独立算法测试可注入该端口，不依赖 FE-22 或 pet。

- 默认仅英文词表 VICTORY/DEFEAT/PENTAKILL/Error/Failed，按独立词边界、大小写无关匹配，记录 ruleId；中文不列支持。game_event 和 screen_keyword 均由 OCR 词级置信度归一化，词级不可用时不造 1.0。
- 原图不出主窗授权内存；OCR text 可供当次本地规则匹配，但 recent/context/policy reason 只引用 ruleId 对应的固定摘要。禁止 OCR 原文混入 prompt、Notifier、pet、远程、Trace 或持久化；FE-22 消费事件前投影。
- 首版 diff 参数拟冻结：96×54 灰度像素值归一到 0～1，平均绝对差 ≥0.03 才触发；500ms 采样，首个有效帧只建基线，分辨率改变重建基线。全黑帧按无效帧处理。该阈值为本次设计值，须在开发集验证，变更记录理由并在验收前冻结。
- ROI 为当前主显示器中央 80%×20%，坐标校验后裁剪；OCR worker 懒加载，离线资源加载超时 15000ms，单次识别超时 5000ms，超时终止并清理 worker，本次无事件；不无限重试。用户再次启用可重新初始化。
- OCR 每分钟 10 次按单调时钟滚动窗口计数，并发 1，pending 至多 1；限流只记无正文计数。关闭先撤销 generation，再中止 capture/worker；忙时合并不积压图片。

### 样本与质量门槛（本版目标，非实测结果）

执行时先准备开发集 30 张和独立验收集 120 张，均为可使用的公开 / 自制非私人画面，登记来源和哈希。验收集 60 张正例，每个英文词 12 张；60 张负例，游戏、视频、IDE 各 20 张，含近似拼写 / 非目标文字。五词正例应覆盖至少两种字号或背景。测试集禁止调参，重复画面及其缩放版本不能跨开发 / 验收集。

事件级 TP 为图片预标目标词及 kind 均正确；多报 / 错报计 FP，漏掉预标词计 FN。冻结集精确率 TP/(TP+FP) ≥95%、召回率 TP/(TP+FN) ≥85%；分母为零不得记 100%。同时报告逐词与逐场景结果，不以总体指标掩盖完全不支持某词。

固定图证明 OCR 算法，不证明 WGC。真机另采游戏结算、视频、IDE 各 10 分钟，至少 20 次人工标注 ROI 内目标出现；仅保存计数、时间、词表 ID 和硬件信息，不保存私人屏幕。真实事件精确率 ≥95%、召回率 ≥85%；样本不足为 NOT RUN，不在该集调参。目标在 ROI 外单列覆盖限制，不混入 ROI 内召回率。

性能验收在报告预登记 CPU/GPU/内存、分辨率与包版本：热启动 OCR 单次 P95 ≤2000ms；首轮含资源初始化单列且 ≤15000ms。组合资源预算归 FE-30；未达标记 FAIL，不能靠增加限流或缩减验收集过关。

| ID | 追加 AC |
| --- | --- |
| FE-21-F | 生产 diff 验 0.03 边界、首帧、黑帧、重建基线；生产 OCR 跑冻结 120 张，提交逐项计数与质量指标 |
| FE-21-G | fake 计时 / bridge 验超时、并发1、pending1、滚动窗口、关闭迟到零事件；生产离线资源在断网条件下加载成功 |
| FE-21-H | 恶意 OCR 指令与隐私文本穿过生产规则 / 摘要出口：请求、数据库、日志、pet/远程收不到原文，固定摘要仍正确 |
| FE-21-I | 真机 3×10 分钟质量及热 OCR P95 达到上述门槛；与固定图片证据分表，不以原 AC-E 的“只记录”代替新质量门禁 |

追加范围：`src/services/environment/fixtures/` 下仅放许可明确的非私人固定图与 manifest、定向质量测试 / 评估脚本；生产路径仍原范围。测试命令在应用目录为 `npm test -- src/services/environment`，Rust diff 生命周期定向测试及 cargo check；真机报告附配置和实际评估命令到原 FE-21_ACCEPTANCE.md，不在本次拆文档时生成假报告。
