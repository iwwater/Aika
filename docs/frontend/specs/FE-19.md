# FE-19 · Windows 前台进程传感器与 Context 注入

> 最新陪伴扩展：[FE-31](FE-31.md)在已授权范围内提供统一开启入口和会话所有权；[FE-32](FE-32.md)追加独立屏幕文字授权/摘录出口，保留本SPEC标题不采集与最终撤销校验。锁屏为强制停止采集与发送，不能仅返回busy=true后让结算候选继续。

> 2026-09-14 PRD 拆分修订：需求见 [环境与 Live2D PRD](../PRD_ENVIRONMENT_AND_LIVE2D.md)。文末修订替代旧标题采集、授权和 busy 语义；新增内容待审阅，执行仍 DEFERRED。

状态：已全文审阅；保持后置。前置FE-18，恢复执行后遵循异步start/stop契约。

## 架构与接口依据

本 SPEC 必须实现 [模块架构与接口](../ARCHITECTURE.md) 中对应阶段的端口、事件、状态与错误语义；通过 adapter 兼容现有实现。下方 AC 同时验证这些契约，无需启动其他真实模块。

需求与边界见 [模块 PRD](../PRD.md)「感知成本阶梯」，依赖遵循 [共享契约](../../modules/CONTRACTS.md)。事件契约见 [FE-18](FE-18.md)。

成本阶梯第一层：不截图就知道「在用哪个应用」。Rust 薄壳沿 `remote.rs` / `bridge.ts` 先例（invoke 命令 + 事件），TS 侧只认 FE-18 的 `EnvironmentEvent` 契约。

## 设计

Rust（`src-tauri/src/foreground.rs`，`cfg(windows)`）：

- invoke `environment_foreground_enable(enabled: bool)`：true 时在专用线程 `SetWinEventHook(EVENT_SYSTEM_FOREGROUND, WINEVENT_OUTOFCONTEXT)`；回调内 `GetWindowThreadProcessId` + `QueryFullProcessImageNameW`（取文件名）+ `GetWindowTextW`，只向获授权主窗口推送 `environment://foreground`，包含sourceId/eventId/epoch/seq/process/title及测点时间，禁止全WebView广播；false 时卸载 hook。
- invoke `environment_foreground_current()`：返回当前前台（hook 触发前的初始状态）。
- 非 Windows 编译目标：模块整体 `cfg(windows)`，命令不存在——TS 探测即知能力缺失。

TS：

- `src/services/environment/foregroundSource.ts`：`createForegroundSource(bridge)` 实现 `EnvironmentSource`；bridge 形状同 `services/remote/bridge.ts`（invoke/listen 注入，便于 fake）。`start(emit, signal)` 先await listen，再enable，再current兜底；按epoch/seq去重，停止同时unlisten/disable。
- `src/services/environment/contextSource.ts`：`createEnvironmentContextSource(monitor)` 实现 LLM 已发布的 `ContextSource`（`id: "environment"`，`section: "environment"`——`domain/context.ts` 已预留该 section）。`load` 把 snapshot 转成一条摘要 snippet：应用名 + 持续时长 + 最近事件 kind 计数；**窗口标题原文不进上下文**。
- 装配：tauri 宿主插件组通过不读取窗口内容的能力探测成功才把 foreground source 放进 `EnvironmentSourcesToken`；环境插件提供 `EnvironmentContextSourceToken`；`ContextSourcesToken` 的提供点追加 `tryResolve` 环境源（在 memoryPlugin 现 provider 内追加或独立装配插件，实现时定；registry 每 token 单提供者，禁止两处 provide 同一 token）。
- 设置：`SETTING_KEYS.environmentForegroundEnabled`（`"environment.foregroundEnabled"`，默认 `"false"`）；App.tsx 设置区新增环境感知分组，开关即 enable/disable。

## 范围

| 文件 | 改动 |
| --- | --- |
| `src-tauri/src/foreground.rs` | 新增（hook 线程 + 2 命令） |
| `src-tauri/src/lib.rs` | 注册命令 |
| `src-tauri/capabilities/default.json` | 新命令权限声明 |
| `src/services/environment/foregroundSource.ts` | 新增 |
| `src/services/environment/contextSource.ts` | 新增 |
| `src/app/plugins/environmentPlugin.ts` | foreground source 按能力注册；提供环境 context source |
| `src/app/plugins/memoryPlugin.ts`（或独立装配点） | `ContextSourcesToken` provider 追加 tryResolve 环境源（跨边界，见下） |
| `src/services/storage/contracts.ts` | `SETTING_KEYS` 加 1 键 |
| `src/App.tsx` | 设置区环境感知开关 |
| `docs/modules/CONTRACTS.md` | 追加登记（新设置键；装配点触碰 LLM 侧文件、`ContextSource` 接口未变） |

## 取舍

**hook 事件式，不做常驻轮询**：`EVENT_SYSTEM_FOREGROUND` 由系统推送，空载成本≈0。探测失败就如实降级为「无此能力」，不写兜底轮询去掩盖。

**默认关 = Rust 根本没装 hook**：开关关闭时 TS 不调用 enable，Rust 侧不存在 hook；不是「装了但不转发」。隐私边界可在代码路径上验证，而非靠过滤。

**Context 注入复用 `ContextSource`，不动 Assembler**：`environment` section 与 `ContextSource` 都是 LLM 侧早已发布的接口（注释里本来就写了环境由后续实现），本 SPEC 只新增一个源实现与注册；Assembler「缺源不失败」的降级语义原样生效。

**只取进程名与窗口标题**：标题可能含隐私文本（文件名等），所以标题只存在于 snapshot 与调试显示；进 LLM 上下文的摘要由 contextSource 控制为「应用名 + 时长」。

## AC

| AC | 模块内验收 |
| --- | --- |
| FE-19-A | fake bridge：listen → enable后收到 `current` 兜底，延迟query不覆盖更新事件；Rust 消息转成 `foreground_changed` 事件且 schemaVersion/timestamp 正确；停止函数调用 disable |
| FE-19-B | 默认关：设置键缺省时无 enable 调用、无事件流出；打开后 snapshot 立即可更新；再关闭后事件停止且 disable 被调用 |
| FE-19-C | 能力降级：浏览器/测试宿主（命令探测失败）foreground source 不注册；若 monitor 因此无源则 token 不注册，设置项隐藏，启动无错误 |
| FE-19-D | Context 注入：环境源注册时，assemble 后 `AgentContext.environment` 含「应用名 + 时长」摘要且 section 为 `environment`；未注册时该 section 为空、回复链路无错 |
| FE-19-E | 隐私：contextSource 产出的 snippet 不含窗口标题原文 |
| FE-19-F | 真机清单（记入验收报告）：VSCode ↔ 浏览器 ↔ 游戏切换，进程名/标题正确、事件延迟 < 1s；开关连续 20 次无泄漏迹象 |

## 不在本 SPEC

- UIA Reader（FE-22 后视效果再立项）、屏幕截图/OCR（FE-21）。
- 策略接线：本 SPEC 事件只更新 snapshot 与 context，不触发主动消息（FE-22）。
- 应用使用时长统计/日报（未立项）。

只执行本模块测试；Rust执行定向生命周期/关闭清理测试及cargo check；编排层用fake bridge；真机部分单列。验收报告放 `../reports/FE-19_ACCEPTANCE.md`。

## 2026-09-13 审阅补充

状态：保留后置，未授权启动。start应先listen再enable并query，按epoch/序号合并去重，dispose同时unlisten/disable，处理enable失败与迟到current。窗口标题和进程信息为敏感资料，默认不进Trace正文、User Soul或远程输出。补AC：stop后迟到事件不更新snapshot、初始化竞态不回退、无权限可见unsupported。真实Win32采集独立NOT RUN。

## Win32可执行性与隐私出口

SetWinEventHook所在专用线程必须有消息循环；停止投递退出消息并在该线程Unhook，回调快速入有界队列，防重入/关闭竞争。能力探测只返回支持标记，不在默认关闭时调用current读取标题。完整窗口标题只驻留获授权本地主窗内存，日志/Trace使用受控摘要。

依据：[Microsoft SetWinEventHook](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setwineventhook)。补Rust生命周期测试：初始化失败/停止前事件/重复开关资源释放；真实<1s与20次切换仍需设备。

## 2026-09-14 · 最小采集、独立授权与可信 busy

需求映射：SET-01/03～06、ENV-01/02/08、PRO-04、PRD AC-01/02/05。前置 FE-18；不依赖真实 OCR、桌宠或 LLM 服务。

### 实施边界与接口

1. 删除原设计的 `GetWindowTextW` 调用；事件、current、snapshot 与真机 AC 都只验 process，不读或展示标题。标题诊断首版不提供，未来另立，不以空字符串掩盖实际采集。
2. 新增 `environment.contextEnabled` 默认 false；原 `environment.foregroundEnabled` 仍默认 false。设置页分开展示采集与“将环境摘要用于对话”，说明当前 Provider 可能收到摘要。设置写入失败反馈错误；关闭动作先执行内存撤销，再尝试持久化，存储失败不能继续外发。
3. 新增生产 `environmentPresenter.ts`，依赖 monitor 与注入设置端口，提供每源开关、状态订阅和 stopAll；Hook 不 import 服务实现。停止全部先撤销 monitor generation，关闭环境主动与摘要使用，清空缓存，等待资源释放。失败明确显示；下次启动设置读取失败按关闭处理。
4. `contextSource.load` 只返回 FE-18 DTO 的受控摘要，读取时检查授权及 source 活跃状态；请求提交前再次验证授权 generation/TTL，撤销期间装配出的旧摘要不得发送。利用已有 Runtime 请求构建边界追加可选校验，不另建发送路径；具体接入点在报告记录，受影响 LLM 消费者跑契约测试。
5. 为 FE-22 提供 `BusyObservation { value: boolean | null; observedMonotonicMs; hostEpoch; reasonCode }`，通过可替换宿主 adapter 注入。拟在前台 source 启用期间查询前台窗口客户区与所属显示器边界、最小化与会话锁定状态；全屏或锁定为 true，成功观测普通可见窗口为 false，查询失败为 null。它仅表示可观测打扰状态，不声称知道用户心理或工作强度。
6. busy 查询只在传感器开启时进行：前台切换更新，主动候选提交前按需刷新，观测有效期 2000ms；无有效观测视 unknown。禁止按进程名判断全屏。Rust 实现时核实使用的 Win32 API 与 DPI 坐标语义；宿主锁定 / 无前台句柄不返回 false。

### 文件范围与共享影响

沿用原范围，追加 `src/presentation/environmentPresenter.ts`、`src/services/environment/busySource.ts` 与对应测试、宿主 busy 命令及权限；实际源码根为 `aika-crossplatform/`。请求出口校验仅限现有请求构建处，不重写 ContextAssembler。新设置、BusyObservation、可选出口校验登记共享契约，注明 FE-21/22 和 Runtime 为消费者。

### 追加 / 修订 AC

| ID | 方法与通过条件 |
| --- | --- |
| FE-19-G | fake bridge + Rust 采集负例：默认无 hook/query；任何启用路径均无标题读取，DTO / 日志无 title；原 AC-F 改验进程名与时延，不再要求标题 |
| FE-19-H | 采集开、摘要关→可本地更新但零模型摘要；摘要开→只出规范化摘要；load 后关闭 / TTL 过期→最终请求无旧摘要；设置写失败仍撤销 |
| FE-19-I | stopAll 同时处理多个 fake source、持久化失败和在途请求装配：停止状态真实，缓存清空，尚未提交的候选不发送 |
| FE-19-J | busy 生产判定用固定矩形 / 锁定 / 错误 fixture；普通窗口 false、全屏或锁定 true、过期 / 失败 null；实际 Win32+DPI 真机另列，不用 fake 冒充 |

运行 environment 与新 Presenter 定向测试、受影响请求构建契约测试；Rust 执行 foreground/busy 生命周期定向测试及 cargo check。真实切换 <1s、20 次开关、锁屏与不同 DPI 清单分列到原验收报告。无真机证据对应项 NOT RUN，不能整 SPEC PASS。
