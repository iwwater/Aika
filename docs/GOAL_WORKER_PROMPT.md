# 给另一位 worker 的完整提示词

请在 F:\AIVoice\Aika 开启 goal 模式，目标是：按 docs/GOAL_EXECUTION_PLAN.md 的依赖顺序，尽可能完成本轮已授权的本地实现、旧SPEC自动审查、必要修复和自动验收，将需要人工/真实设备/外部授权的部分整理为可复现验收队列。不要把整个产品发布或全人工验收设为本轮完成条件。

先读 AGENTS.md、docs/PRD_V0.5.md、docs/REVIEW_V0.5_AND_BACKLOG.md、docs/GOAL_EXECUTION_PLAN.md，再读当前模块PRD/SPEC/共享契约。把执行计划中波次0～8转成持久清单 docs/GOAL_RUN_LEDGER.md，一次执行一份SPEC，完成后自主挑选下一个前置满足的节点，不要每份结束询问是否继续。

你可以直接修改本轮范围内代码/配置/文档，修复相关缺陷，写定向测试和验收报告。先保留git status/diff，保护全部现有未提交改动；不覆盖其他worker更改。同文件并发冲突先停该文件并记录，继续不冲突任务。不要自动提交、推送、合并、发布、reset --hard、clean、stash、批量回滚或删除用户数据。不安装全局依赖，不自动升级无关包；必要项目依赖固定版本并审查lock diff。禁止读取私人语料、真实数据库或输出凭证。

真实发消息、远程账户动作、新费用、公网开放、生产库迁移、运行外部Agent写改/命令都需要该动作已有明确授权；没有就用外部端口fixture完成本地逻辑，把真实部分标BLOCKED/NOT RUN并继续。不要关闭宿主审批或安全沙箱来“跑通”；不要把worktree或prompt限制当沙箱。用户原来后置的语音、环境传感器、桌宠/Live2D、Stage3、云Relay继续后置。

执行状态严格遵循计划：AUTO_PASS仅表示所有可自动AC通过且待人工；不得写全部完成、人工通过或发布可用。既有待审阅项先审代码和证据，不重写已实现功能。FAIL先定向修；两种有依据方案无效或约30分钟无新证据，记录具体阻塞并跳转无依赖节点，不能把FAIL改成PASS或让依赖消费失败保证。缺真机不阻塞独立逻辑，但安全负例不过绝不能启动真实外部执行。

测试走本模块生产代码，mock只替换外部依赖；每份只测影响范围。到明确里程碑再做一次对应回归，不循环全仓build/test。报告实际命令/退出码、逐AC证据类型、共享契约消费者、未测项与人工步骤；不复制历史通过数字，不改AC、不skip失败、不编造真实样本。每份结束写模块报告/索引和GOAL_RUN_LEDGER。

持续推进直到所有当前可执行节点完成，或剩余全部依赖外部条件。届时汇总AUTO_PASS、PARTIAL、FAIL、BLOCKED、NOT RUN、DEFERRED和下一步人工清单。goal状态必须服从工具规则：只有本轮可执行范围及完整交接目标实际达成才能complete；否则保留active或达到工具规定阈值才blocked，不为了结束虚报完成。上下文压缩后从账本继续，勿重做已经证实的节点。

全文审阅更新：当前未执行及早期缺验收的SPEC已完成设计全文审阅，但不代表实现验收。新增LLM-12是FE-26成本页的数据前置，按更新执行计划推进；FE-17分pre/tauri/dev-relay/public，GW-04不等待无关Node/public分支。TTS-05、INT-04真实轨不能因无本地自动AC而空集通过；AGT-05必须从实际任务入口验收，RT-02必须验证生产Runtime的历史/摘要/取消隔离。以后只需重新审阅改变过的设计和真实实现证据，不重复本次全文扫描。
