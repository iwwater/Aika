# agents 模块需求

上位需求：[v0.5](../PRD_V0.5.md)。范围与验收：[SPEC索引](SPEC.md)。

目标：管理外部Agent多轮生命周期，通过ACP适配并验证权限可执行性。

不为目录图搬代码；token在接口旁，平台逻辑在宿主，组合根装配。无能力明确不可用；安全能力缺失必须拒绝执行。验收分自动契约、真实服务及人工，缺一不宣称完整交付。持久数据按身份隔离、版本迁移并用临时库验证；所有外部输入都不能自行授予权限。
