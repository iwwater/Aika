# AGT-04 · Claude ACP 适配

状态：READY（仅授权本地实现与自动验证；真实服务/人工 AC 未执行）。
需求来源：[v0.5 与增量需求](../../PRD_V0.5.md)；执行规则：[安全执行计划](../../GOAL_EXECUTION_PLAN.md)。

## 前置与范围

- 前置：AGT-02。
- 修改范围：独立adapter manifest/配置、相同契约包。源码根为 aika-crossplatform；优先复用现有端口，不为文档目录迁移源码。
- 非目标：本份范围外功能、发布、公网部署、真实账号操作。前置自动契约通过可开发；需要真实安全属性的集成不可由 fake 放行。

## 行为与接口

核实当前官方适配器身份与能力；复用AGT-02，认证/配置隔离于Codex；不得复制Runtime。

所有新增公共字段需在 CONTRACTS 登记版本、兼容 adapter 与消费者；持久化使用临时数据库验证升级与失败回退，不触碰用户库。

## 验收条件

| AC | 要求 |
| --- | --- |
| AGT-04-A | 同AGT-03的只读/拒绝写/取消证据要求 |
| AGT-04-B | 不可把Anthropic普通API配置当适配器已登录 |
| AGT-04-C | 选择适配器不改变会话/权限语义 |
| AGT-04-D | 一适配器不可用不影响另一个；真实运行缺口独立列出 |

## 验证与交付

先读当前生产实现与既有测试，列出定向命令；测试必须走本模块生产实现，fake 只替代外部依赖。记录命令、退出码、逐 AC 证据、影响消费者、未执行的真实/人工项。逻辑测试不证明 UI 可用、真机或真实服务通过。
报告路径：../reports/AGT-04_ACCEPTANCE.md。仅所有可自动 AC 通过才能写 AUTO_PASS / 待人工验收；FAIL 不可改为 NOT RUN 来推进依赖。完整验收维持待人工；不自动提交或推送。


## 全文审阅：独立适配

沿用AGT-03相同manifest与负例矩阵但使用独立配置/认证；AGT-03实现并不是本份硬前置，公共用例包来自AGT-01/02。协议版本或权限能力不匹配列unsupported，不把认证成功当文件/进程约束已通过；实例失败不触发自动切换另一收费Agent。
