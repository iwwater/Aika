# GW-04 · Device Gateway 配对与能力会话

状态：READY（仅授权本地实现与自动验证；真实服务/人工 AC 未执行）。
需求来源：[v0.5 与增量需求](../../PRD_V0.5.md)；执行规则：[安全执行计划](../../GOAL_EXECUTION_PLAN.md)。

## 前置与范围

- 前置：RT-03、FE-14、FE-17-pre、FE-15、FE-17-host/tauri；FE-16可选。
- 修改范围：已有 outbound/remote adapter、device repository。源码根为 aika-crossplatform；优先复用现有端口，不为文档目录迁移源码。
- 非目标：本份范围外功能、发布、公网部署、真实账号操作。前置自动契约通过可开发；需要真实安全属性的集成不可由 fake 放行。

## 行为与接口

复用 AikaLink，先保持 Tauri HTTP 长轮询，不为架构图另造 WS Runtime。一次性配对码交换 per-device 可撤销会话；capability 为服务端授权与设备声明交集。LAN discovery 可先手填地址，自动发现单列未交付。

所有新增公共字段需在 CONTRACTS 登记版本、兼容 adapter 与消费者；持久化使用临时数据库验证升级与失败回退，不触碰用户库。

## 验收条件

| AC | 要求 |
| --- | --- |
| GW-04-A | 配对码短期单用、逐设备撤销不影响其他设备 |
| GW-04-B | 租约/心跳/断线/重连gap与宿主epoch明确 |
| GW-04-C | 能力不支持可见降级；未经授权无法订阅Trace/发审批 |
| GW-04-D | Android/Web为终端；PC离线显示不可用，不生成第二份回复 |

## 验证与交付

先读当前生产实现与既有测试，列出定向命令；测试必须走本模块生产实现，fake 只替代外部依赖。记录命令、退出码、逐 AC 证据、影响消费者、未执行的真实/人工项。逻辑测试不证明 UI 可用、真机或真实服务通过。
报告路径：../reports/GW-04_ACCEPTANCE.md。仅所有可自动 AC 通过才能写 AUTO_PASS / 待人工验收；FAIL 不可改为 NOT RUN 来推进依赖。完整验收维持待人工；不自动提交或推送。


## 全文审阅：解除无关传输依赖

Tauri设备链依赖FE-14、FE-17-pre、FE-15与FE-17-host/tauri；FE-16及public未完成不阻塞Tauri LAN本地模块。配对码/会话/撤销服务归FE-17，本份只加设备记录/列表/能力协商/心跳与重连体验。默认手填地址，自动Discovery明确未交付。

capabilities声明chat/voice_input/voice_output/notification时逐项探测并服务端取交集；浏览器缺音频上传/播放能力就隐藏并说明，不能因为协议枚举存在宣称手机语音已完成。真实语音链仍后置。不用设备名或IP当身份；离线期间不本地调用LLM，重连不自动重放状态未知的指令。
