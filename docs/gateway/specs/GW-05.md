# GW-05 · Feishu/Lark 适配

状态：READY（仅授权本地实现与自动验证；真实服务/人工 AC 未执行）。
需求来源：[v0.5 与增量需求](../../PRD_V0.5.md)；执行规则：[安全执行计划](../../GOAL_EXECUTION_PLAN.md)。

## 前置与范围

- 前置：GW-02契约稳定。
- 修改范围：Feishu platform adapter。源码根为 aika-crossplatform；优先复用现有端口，不为文档目录迁移源码。
- 非目标：本份范围外功能、发布、公网部署、真实账号操作。前置自动契约通过可开发；需要真实安全属性的集成不可由 fake 放行。

## 行为与接口

仅新增平台传输、认证、能力映射；明确 tenant、事件验签/重放防御、私聊绑定；接入前查官方文档。

所有新增公共字段需在 CONTRACTS 登记版本、兼容 adapter 与消费者；持久化使用临时数据库验证升级与失败回退，不触碰用户库。

## 验收条件

| AC | 要求 |
| --- | --- |
| GW-05-A | 复用GW-01契约包 |
| GW-05-B | 跨tenant隔离及伪造事件拒绝 |
| GW-05-C | token刷新失败可见且无泄漏 |
| GW-05-D | 真实账户验收独立NOT RUN |

## 验证与交付

先读当前生产实现与既有测试，列出定向命令；测试必须走本模块生产实现，fake 只替代外部依赖。记录命令、退出码、逐 AC 证据、影响消费者、未执行的真实/人工项。逻辑测试不证明 UI 可用、真机或真实服务通过。
报告路径：../reports/GW-05_ACCEPTANCE.md。仅所有可自动 AC 通过才能写 AUTO_PASS / 待人工验收；FAIL 不可改为 NOT RUN 来推进依赖。完整验收维持待人工；不自动提交或推送。


## 全文审阅：传输选择与验收范围

首次实现先形成官方能力快照（Feishu/Lark域名、tenant/user ID范围、文本DM、事件来源、token刷新、限流），依据该版本选择出站连接/事件订阅，默认不开公网webhook。若选择webhook必须实现平台要求的验签、解密和重放窗口并列入真实部署门禁。最小交付仅已绑定私聊文本，语音/文件/群功能未支持则明确拒绝。官方fixture测试与真实发送各自记录，不能自动调整他人订阅配置。
