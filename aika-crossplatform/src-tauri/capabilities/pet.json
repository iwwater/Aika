{
  "$schema": "./gen/schemas/desktop-schema.json",
  "identifier": "pet",
  "description": "Capability for the pet window: minimal display-only permissions (FE-20)",
  "windows": ["pet"],
  "permissions": [
    "core:default"
  ]
}
