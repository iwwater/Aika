# Aika Next worker 规则

本分支是 Aika Next 的独立主线，初始化只有文档。默认中文。唯一文档根为 `docs/`。

1. 先读 `docs/README.md`、总 RPD、0.6 RPD、共享契约和当前 SPEC。默认一次执行一个 SPEC，按依赖推进，不自行扩大版本范围。
2. 当前初始化任务仅创建文档；本文是后续开发的执行规范，不代表已经实现、测试或验收。
3. AAAAGENT 是新运行时底座；旧 Aika 仅供只读参考。禁止修改 `F:/AIVoice/Aika`、`F:/AIVoice/pet-shell` 或参考副本，禁止把旧 Aika 全套 Runtime/Kernel 搬入新主线。
4. 本分支为 orphan 独立历史，不得直接合并到旧 `master`。上游引入方式见 NEXT-00；不以清空目录、覆盖文档、重置现有分支的方式初始化。
5. 新行为先写失败测试，再实现、重构和定向回归；原样行为沿用特征测试。优先复用旧语料及预期，不用删除断言、跳过测试或降低标准换取通过。
6. 小阶段只测相关模块及受影响契约。被测模块走生产代码，外部模块可 fake；版本收尾统一全量回归、自动集成和必要构建。开发期间不要求用户开麦、试听或操作 UI。
7. 真实模型回放与 fixture 编排测试分别报告；缺少必需环境为 BLOCKED，未运行是 NOT RUN，不得标 PASS。版本末尾人工验收由用户执行。
8. 设计接口是逻辑契约，未经 NEXT-00 源码核对，不假称其为上游现有符号。优先薄适配；公共契约变更记录影响和兼容方式。
9. 报告写入 `docs/next/0.6/reports/`，逐 AC 附命令、退出码、实际结果和证据。日志脱敏，不提交密钥、私人录音、模型权重、用户数据库或 node_modules。
10. 不默认发送外部消息、调用付费服务、推送远端或发布。保留上游来源、许可证和必要说明；实际分发另行核查。
