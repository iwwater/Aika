# Aika Next 开发与 Agent 协作规范

默认中文。工作树 `F:/AIVoice/Aika-Next`，文档唯一根 `docs/`。本文件约束本项目，不修改用户全局开发偏好。版本状态以对应 SPEC 索引和实际报告为准，不把规划、历史说明或文件存在当成实现证据。

## 1. 工作单位与责任范围

默认一次处理一个明确模块、插件、Provider adapter 或当前 SPEC。开发者对**本次变更的模块、公共契约和直接受影响的接入链路**负责；影响确实继续传播时再扩展，不默认接管全应用。

- 开始前确定：目标版本/SPEC、负责模块、预期改动路径、使用/提供的契约、直接消费者、验收范围。简单任务在进度说明中写清即可，不为每次小改动新建计划文件。
- 同一任务内自主完成必要的最小消费者适配、测试和文档更新，不因文件跨目录反复请求确认。不得借机重构无关模块、升级无关依赖或实现未来版本。
- 外部依赖用稳定公共契约与 fixture/stub 隔离；被测模块、被测宿主机制和本次接入链路必须是真实生产代码。
- 无关已知故障记录来源、复现与影响，不顺手扩大修复；由本次变更引起的回归必须处理，不能以“属于别的模块”为由遗留。
- 缺上游已交付契约时先定位真实缺口。可以完成独立部分，但不伪造上游已完成、不绕过 SPEC 前置，不用空实现换取链路成功。
- 普通非论文脚本、配置、代码和文档修改在既有授权范围内直接完成；需要用户补充的仅是无法推断且影响目标的决定。不要把内部实施选择都变成审批项。

## 2. 按任务读取上下文

先读本文件、[文档入口](docs/README.md)、目标版本的 RPD/SPEC 索引及当前 SPEC；再按影响范围读取公共契约、来源核对、测试规则和直接依赖的报告。不要每次强制重读全部历史需求、全库或固定的 0.6 文档。

| 任务 | 重点入口 |
| --- | --- |
| 0.61 修复 | `docs/next/0.61/SPEC.md`、当前 FIX61、该版源码核对与相关报告 |
| 0.65 内核/插件包 | [SPEC](docs/next/0.65/SPEC.md)、[CONTRACTS](docs/next/0.65/CONTRACTS.md)、[TESTING](docs/next/0.65/TESTING.md)及当前 K65 |
| 多源 adapter / 模型绑定 | 另读 [PROVIDERS](docs/next/0.65/PROVIDERS.md)，核对实际 adapter/来源与 SDK 版本 |
| 独立能力包开发 | [插件开发规范](docs/development/PLUGIN_DEVELOPMENT.md)，结合当前版本契约和直接消费者 |
| 0.61 → 0.65 连续开发 | [连续执行计划](docs/next/EXECUTION_061_065.md)：0.61 自动收口后进入 0.65，人工验收集中后置 |
| 兼容迁移 | 另读实际源版本的数据/生命周期契约和验收证据；按需读 0.6，不假设当前工作树等于固定基线 |

设计接口不是已有源码符号。首次实施按基线映射核实；符号变化做最薄适配并记录，不盲从旧行号。使用 `rg` 定位相关入口与消费者，先看目标路径的 Git 状态和现有改动。

## 3. 架构与模块边界

0.65 目标是**独立内核 + 普通产品包 + 按需功能插件包**；Flow Profile 是组合机制。RPD/SPEC 定义的目标不表示已落地，尚未拆出的包不要假装存在。

| 归属 | 负责 | 不应承担 |
| --- | --- | --- |
| 内核宿主 | 包发现/依赖/加载、公共契约、唯一轮次与取消、资源生命周期、编排与必要诊断 | 模型厂商分支、桌宠业务、每种引擎依赖、另一套 Memory |
| 普通包 | 桌宠、文字交互、基本角色/近期上下文、LLM 产品入口和选定 adapter | 强装 TTS/STT/视觉/长期维护 |
| 功能包 | 本功能接线、数据处理、所需界面/资源与单包产物 | 复制宿主状态机、越过公开接口操作其他包私有对象 |
| Provider adapter | 对应协议/引擎、输入输出转换、能力声明及专有参数 | 全局当前模型、用户记忆写入、任意控制外部服务 |
| 多源管理 | 来源/模型/音色/阶段绑定、修订、解析、实例租约与健康 | 把全部 local 视为一个来源，或每个功能另造注册中心 |

- 内置与导入插件使用同一公开契约；包间不得引用私有路径。能力 ID 可有多个提供者，adapter/plugin 身份不得冲突覆盖。
- 五层分离：Capability → Adapter → SourceInstance → ModelProfile → Binding。云端、本地服务、托管本地引擎的认证/参数/资源与所有权分别处理。
- 只初始化所选来源与必要依赖。发现、读配置、健康展示不得启动所有引擎；云端路径不强装本地专用环境。
- 用户自部署服务只断开本应用连接，不终止其进程；宿主管理进程按租约释放。停止采集即时生效；不以代码卸载待重启延迟停止。
- 默认显式选源，不静默 fallback；不混用不同来源的流式 token/音频或音色标识。本地失败不能自动把数据发云端。
- Scope、取消、来源撤销和幂等提交是公共保障，不能为了模块独立测试而移除。兼容包不得恢复已遗忘数据。
- STT、TTS、情感陪伴的新模块开发由合作者负责；主线负责公共契约、已有功能包体迁移和整体接线。遵守[合作者规范](docs/development/PLUGIN_DEVELOPMENT.md)，不把 DSH 市场分发追加为当前任务。

## 4. 公共接口变更与共享文件

模块内重构保持契约即可自主完成；新增兼容能力按任务同步文档和定向契约测试，不默认扩大为全库改造。

公共 schema/API/事件/manifest/数据模式发生变化时：

1. 说明变更字段或语义、版本/兼容方式，检索真实消费者，记录直接影响路径。
2. 在同一任务完成必要的最小消费者适配，或提供可验证的兼容层；不能单边改接口后把构建失败留给下个 worker。
3. 更新契约和相应测试；行为破坏/迁移需有旧→新与恢复方案。若需求范围实质改变，先报告与用户目标的差异，再处理依赖该决定的部分。
4. 契约、根依赖锁、共享注册表、构建入口和版本状态索引是共享改动。只改需要的项；不顺带重排、整文件格式化、全量升级或覆盖已有修改。

不因多人/多 Agent 能力可用就自动启动并行。用户明确安排多人协作时，按模块/文件划分写入责任，公共契约指定单一修改者，先统一契约再分工；读取可以并行，共享文件不得互相覆盖。已交付稳定接口上的独立模块可分别开发，具体前置仍以任务授权和 SPEC 为准。

## 5. 渐进实施与独立模块开发

0.65 首次架构迁移继续遵守 K65 索引的前置顺序；它用于建立可用基线，不代表未来每改一个 TTS adapter 都要重新执行 00～11，更不代表 STT 在运行时依赖 TTS。

公共 SDK/加载器/来源管理交付后，后续独立模块任务只需确认兼容基线、直接依赖和该模块验收。不要重新跑已完成基础步骤，不自行跳过尚未完成的迁移前置或提前实现未来版本。

接线测试若依赖尚未交付模块，可先验证自身契约并明确“集成待验”；不能以全 fake 证明集成已通过。任务目标包含正式接线时，必须完成或报告该部分阻塞，不能只交一个孤立模块。

用户已确认 0.61 人工验收延到 0.65 改造完成后。FIX61-10 自动 AC 通过且基线可复现即可进入 K65-00；人工项记 DEFERRED_TO_K65_11，不要求 0.61 ACCEPTED，不降低自动门槛。K65-11 合并两版人工清单，未获用户确认不代签。执行与续跑见[连续计划](docs/next/EXECUTION_061_065.md)。

## 6. 按影响选择测试

新行为或缺陷先失败测试，再最小实现、重构和定向回归；纯搬迁/重构复用特征测试。文档和无行为影响的小改动只做适当检查，不为可逆低影响变更写镜像测试。禁止删断言、skip、降低指标换 PASS。

| 改动类型 | 默认必需验证 | 扩大条件 |
| --- | --- | --- |
| 文档/导航 | 链接、格式、编号和语义一致性 | 涉及规则时核对相关规范，不跑模型回放 |
| 模块内部行为 | 本模块测试、相关类型检查 | 接口/行为外溢时加消费者回归 |
| 单个 Provider adapter | 该 adapter 的协议/参数/取消/错误测试，来源解析及一条生产接入链；真实门槛按当前 SPEC | 不测全部厂商组合；共享来源管理变化才扩展相关来源矩阵 |
| 公共契约 | 契约测试、直接消费者类型/接入测试 | 核心语义影响更多链路时按证据递归扩展 |
| 包体/依赖/加载更新 | 目标包构建、独立安装/加载/停止、受影响最小组合及未选依赖缺席检查 | 修改宿主构建/解析时扩展基础产品和代表组合 |
| 核心轮次/取消/数据迁移 | 受影响文字/语音/后台/撤销/恢复链路 | 影响跨全局或覆盖无法界定时做广泛回归 |
| 版本收口 | 本版所有必需 AC、相关全量回归、真实回放、构建与自动集成 | 按版本 SPEC；最后集中人工验收 |

每次报告为何选择这些测试。确无可用定向类型检查/构建时，可运行共享编译检查；模块化不是拒绝必要共享检查的理由。测试已通过且无新变化，不重复扩大验证。

真实 Provider/设备结果、协议 stub、fixture 与人工体验分列。被测模块走生产代码，外部模块可 fake；不得 fake 被测 loader/registry/adapter 证明自己通过。必要真实资源缺失为 BLOCKED，未运行 NOT RUN，未知不是成功。不要求用户每步开麦、试听或操作 UI；人工体验集中版本末。

## 7. 状态、报告与交接

报告默认位于 `docs/next/<目标版本>/reports/`，按当前 SPEC ID 命名；独立模块任务用清楚的模块/任务名。必要报告复用同一文件，不重复生成形式文档。

报告最少包括：

- 目标模块与源码/产物修订、改动路径及责任边界。
- 公共接口是否改变、实际受影响消费者及兼容策略。
- AC 与测试命令、退出码、用例数、结果；新增行为的 RED/GREEN 证据。
- fixture/真实服务/人工验证的区分；阻塞、无关已知故障和未做项。
- 下一步需要消费的接口/包版本及尚待集成项。

模块通过只代表模块范围通过。完整 SPEC 有必需真实回放或接线未完成时，模块结果可记通过，但 SPEC 仍 BLOCKED/IN_PROGRESS；不得把它升级成 AUTO_PASS。所有必需自动门槛完成才是“开发完成，待人工验收”，用户明确确认才是版本验收完成。

只更新本任务负责的状态与证据，不替其他模块修改通过状态。报告摘要先说明结果和影响，不倾倒全部日志，不重复既定决策。

## 8. 工作区、历史与外部操作

- AAAAGENT 是运行时底座，旧 Aika 只读参考；禁止修改 `F:/AIVoice/Aika`、`F:/AIVoice/pet-shell` 及参考副本，禁止搬入旧 Aika 全套 Kernel。Git 操作从 Next 工作树进行，不以共享 Git 元数据位置为由修改旧工作树。
- `aika-next` 为独立历史，不直接合并旧 master。不用 reset、清目录、覆盖文档或撤销他人改动解决问题；必要修复基于当前文件做最小补丁。
- Windows 默认范围是 `windows/code/desktop-pet/` 的实际入口，不因同名文件自动同步 macOS 副本；跨平台变更按本任务实际影响处理。
- 保留来源、许可证和资产说明；日志脱敏，不提交密钥、私人录音、模型权重、用户数据库、node_modules。
- 不默认发外部消息、调用新付费服务、安装模型、推送或发布。已明确授权的操作不重复征求许可；资源/权限不足如实说明，完成不受阻部分。
